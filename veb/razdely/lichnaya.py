#!/usr/bin/env python3
# TOOL-CONTRACT: called-by-code — will be rendered by
# `tools/sobrat_stranicu.sobrat_html` once this section exists.
"""The personal page: what one named person sees about themselves.

🔴 THIS MODULE IS EMPTY ON PURPOSE, AND EMPTY IS ITS CORRECT STATE TODAY. It is
the address the next position of the wave is written against; it exists so that
position can be started without touching any other file, which is the entire
reason the page was cut apart. Nothing on the site links here yet.

The shape to fill in is the one every other section already has:

    def razdel(kt) -> str:
        ...

`kt` is `veb.obshchee.karkas.Kontekst` — the role, the database handle, the days,
the rooms, the groups, the teachers, the pupils. Read `veb/razdely/listki.py` for
the smallest complete example of a section.
"""
from __future__ import annotations
