ЗАЯВКА: 2026-09-02T18:27 · автор: host · арка: не названа
СРОЧНОСТЬ: blokiruet
РОД: pravka-koda
АДРЕСАТ: infra/llm.py core/services/raspoznavanie.py tests/photo/

СНЯТАЯ ЗАДАЧА ПРИЕЗЖАЕТ ПРЕПОДАВАТЕЛЮ КАК СДАННАЯ: в схеме ответа модели нет поля для «снято», и прочерк кладётся в solved. Замер 02.09 18:4x, ПЕРВОЕ живое распознавание настоящего бланка за волну (P7 в отчёте признаёт, что живого прогона не было). Синтетический бланк tools/blank.py с ИЗВЕСТНЫМИ отметками: u1 крестики 1а° и 1в°, u3 крестик 3°, u7 крестики 1б° и 2, u5 ПРОЧЕРК на 1а°. Ответ google/gemini-2.5-flash: u1 solved [1а°,1в°] ВЕРНО, u3 solved [3°] ВЕРНО, u7 solved [1б°,2] ВЕРНО, u5 solved [1а°] НЕВЕРНО — это прочерк. В raw_text модель видит его правильно, строкой «u5  -». ВИНОВАТА НЕ МОДЕЛЬ, А СХЕМА: infra/llm.py, required [student_code, solved], поля свойств только solved и alternatives; поля для снятия нет вовсе. Промпт при этом велит вернуть строку, «в клетках которой есть ПОМЕТКА» — прочерк пометка, и единственное место, куда его можно положить, это solved. Модель сделала единственное, что схема позволяла. ПОЧЕМУ ЭТО ХУЖЕ ОБРЕЗКИ (заявка про crop рядом): обрезка даёт ПУСТОЙ разбор, то есть видимый отказ, а это даёт НЕВЕРНОЕ ЗНАЧЕНИЕ в таблице подтверждения — человек, подтверждающий двадцать строк, пропустит одну лишнюю галочку. Понятие в предметной области ЕСТЬ: журнал append-only знает retract, в прошлом году их 735, и легенда самого бланка написана владельцем: «Сдал — крестик в клетке. Снято — прочерк. Пусто — не сдавал». Схема зрения потеряла ровно ту треть, которую бланк называет. ПОЧИНКА: добавить в схему поле retracted (enum тех же меток), в промпт — три состояния клетки дословно по легенде бланка, а в разбор ответа — раскладку retracted в события retract, а не assert. ПРОВЕРКА, КОТОРАЯ ОБЯЗАНА КРАСНЕТЬ: тест с бланком, где есть и крестик, и прочерк, требует, чтобы прочерк НЕ попал в solved; на нынешнем коде он красный. ЗОНА: infra/llm.py core/services/raspoznavanie.py tests/photo/. НЕ ЧИНИТЬ, ПОКА ИДЁТ P18 (её зона включает core/services/).
