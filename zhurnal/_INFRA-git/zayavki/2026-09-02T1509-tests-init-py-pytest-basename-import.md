ЗАЯВКА: 2026-09-02T15:09 · автор: host · арка: не названа
СРОЧНОСТЬ: obychnaya
РОД: pravka-koda
АДРЕСАТ: tests/

КЛАСС ХРУПКОСТИ ТЕСТОВ, второй случай за сутки, останавливает ВЕСЬ прогон: одинаковые БАЗОВЫЕ ИМЕНА тест-модулей в разных папках. Под tests/ нет ни одного __init__.py, поэтому pytest именует модуль по basename, и два файла с одним именем дают 'import file mismatch' на СБОРЕ — падает не пара, а весь прогон целиком. Замер 02.09: tests/views/test_privacy.py (P5) столкнулся с tests/photo/test_privacy.py (P7) в момент влития main; 0 собрано, 1 error. Обойдено переименованием своего в tests/views/test_views_privacy.py — но КЛАСС не закрыт: любая следующая позиция, назвавшая файл частым именем (test_privacy, test_flow, test_layout, test_service), снова уронит сбор у всех. Это тот же корень, что у уже открытой заявки 2026-09-02T1301 про 'from conftest import' — папки тестов не пакеты. Верная починка одна и закрывает оба: положить __init__.py в tests/ и в каждую её подпапку, тогда имя модуля становится полным путём. Проверка: завести временный tests/views/test_flow.py рядом с tests/photo/test_flow.py и убедиться, что python3 -m pytest -q собирает зелёным.
