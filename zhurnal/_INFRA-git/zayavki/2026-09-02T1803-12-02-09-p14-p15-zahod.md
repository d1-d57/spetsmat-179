ЗАЯВКА: 2026-09-02T18:03 · автор: host · арка: не названа
СРОЧНОСТЬ: obychnaya
РОД: pravka-koda
АДРЕСАТ: _generator/tools/priyomka.py

ГЕЙТ Г12 СУДИТ ВХОДНУЮ ГИГИЕНУ ПО СОСТОЯНИЮ НА ВЫХОД И КРАСНЕЕТ НА ЧУЖИХ ВЕТКАХ. Замер 02.09: приёмка P14 и приёмка P15 покраснели обе, и обе не по вине заходов. Механизм: заход на ВХОДЕ проверяет «невлитых zahod/*-веток» и пишет в ## ГИГИЕНА ВХОДА галочку «да»; на входе это ВЕРНО. Приёмка идёт часом позже, гейт Г12 перепроверяет ту же галочку СВЕЖИМ прогоном и видит ветки, заведённые ПОСЛЕ того, как заход закончил: у P14 это была живая P15-tekst, у P15 — живая P18-teksty, заведённая через 15 минут после её закрытия. Гейт предлагает два исхода — «влить» или «поставить нет со списком»; влить живую чужую ветку нельзя, значит доступен ровно один, и оркестратору приходится править ЧУЖУЮ секцию, объясняя в ней, почему это не подлог. При волне из восемнадцати позиций это происходит на каждой приёмке, идущей внахлёст с работой, то есть ПОСТОЯННО. ПОЧЕМУ ЭТО ХУЖЕ, ЧЕМ ШУМ: гейт учит оркестратора ПРАВИТЬ ГАЛОЧКУ, чтобы стало зелено, — то есть тренирует ровно ту привычку, ради запрета которой гейт и заведён. ПОЧИНКА: сравнивать не с текущим прогоном, а с СОСТОЯНИЕМ НА МОМЕНТ ВХОДА — либо гейт берёт снимок, который заход обязан приложить (он и так требует «секция, снимок и галочка»), и сверяет галочку СО СНИМКОМ, а свежий прогон печатает отдельной строкой как справку; либо исключает из свежего прогона ветки, созданные позже коммита захода (git log --format=%ct -1 <ветка> против времени последнего коммита зоны). Первое проще и честнее: снимок уже есть в файле. ПРОВЕРКА: приёмка захода, закрытого час назад, при живой чужой ветке даёт Г12 ЗЕЛЁНЫЙ без правки чужой секции, а заход, соврaвший в галочке НА ВХОДЕ, по-прежнему краснеет. Найдено приёмками P14 и P15 волны sborka-bota.
