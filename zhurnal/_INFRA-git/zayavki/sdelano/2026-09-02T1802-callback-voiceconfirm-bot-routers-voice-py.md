ЗАЯВКА: 2026-09-02T18:02 · автор: host · арка: не названа
СРОЧНОСТЬ: blokiruet
РОД: pravka-koda
АДРЕСАТ: bot/routers/voice.py bot/keyboards/views.py tests/klyuchi/

ГОЛОСОВОЙ ЧЕРНОВИК НЕ МОЖЕТ ЗАПИСАТЬ НИКТО: два разных экрана делят один префикс callback, и «Записать» под диктовкой уходит не туда. VoiceConfirm (bot/routers/voice.py:119) и ViewYear (bot/keyboards/views.py:108) упакованы ОБА как «vy», и VoiceConfirm().pack() == ViewYear(student_id=1).pack() == vy:1 ПОБАЙТОВО. Кто из роутеров включён раньше, тот и съедает тап; сейчас раньше идёт views (bot/app.py:195) — значит нажатие уходит в экран УЧЕНИКА, чей require_role отказывает преподавателю. Порядком включения это НЕ лечится: кого ни поставь первым, второй экран умирает. СТЕПЕНЬ: голос был одним из двух каналов, которые владелец собирался проверять 03.09 (фото он смотрит в школе, значит остаются текст и голос); канал текста закрыт P15, канал голоса не работает вовсе на последнем шаге — распознавание отработает, черновик покажется, а кнопка подтверждения не приведёт никуда. ПОЧЕМУ ЭТО ДОЖИЛО ДО ВЕЧЕРА: дефект НАЙДЕН гейтом классов P20 и ИМ ЖЕ ПРОПУЩЕН — tests/klyuchi печатает его абзацем «ИЗВЕСТНЫЙ ДЕФЕКТ callback:vy» и даёт 6 passed. Проверка, которая знает о поломке и не краснеет, это надежда, а не проверка (skills/disciplina-kachestvo). Заявки на дефект не было заведено ни одной — проверено грепом по всей очереди. ПОЧИНКА В ОДНО СЛОВО, её называет сам гейт: дать VoiceConfirm собственный префикс (например prefix='vgo'). ВТОРАЯ ПОЛОВИНА ПОЧИНКИ, БЕЗ КОТОРОЙ КЛАСС ОСТАЁТСЯ ОТКРЫТЫМ: tests/klyuchi обязан КРАСНЕТЬ на столкновении префиксов, а не печатать его — тест, перебирающий все классы CallbackData и требующий, чтобы pack() двух разных экранов не совпадал ни на одном наборе полей. Доказать красноту возвратом prefix='vy' обратно. ЗОНА: bot/routers/voice.py bot/keyboards/views.py tests/klyuchi/. 🔴 НЕ ЧИНИТЬ, ПОКА ИДЁТ P18-teksty: её зона включает bot/routers/ и bot/keyboards/, а двух писателей в один файл волна себе не позволяет. Найдено приёмкой 02.09 18:0x при разборе заявки P15 о выходе за зону.
ЗАКРЫТО: 2026-09-02T19:39 · префикс VoiceConfirm сменён на vgo; сторож tests/klyuchi/test_callback_prefixes.py перебирает все 30 классов CallbackData и доказан КРАСНЫМ до починки, теперь «классов 30, различных упаковок 30, столкновений 0»; восемь литералов «vy:1» в тестах голоса заменены вызовом класса — литерал пережил бы смену префикса молча; callback:vy снят из реестра известных дефектов как починенный. main 916 passed
