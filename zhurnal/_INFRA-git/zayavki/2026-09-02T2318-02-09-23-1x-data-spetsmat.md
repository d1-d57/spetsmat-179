ЗАЯВКА: 2026-09-02T23:18 · автор: host · арка: не названа
СРОЧНОСТЬ: blokiruet
РОД: pravka-koda
АДРЕСАТ: tools/import_konduit.py bot/handlers/owner.py ops/

ТАБЛИЦА ЗАКРЕПЛЕНИЙ ПУСТА В БОЮ, И ЗАПОЛНИТЬ ЕЁ НЕЧЕМ: экран аудитории завтра покажет ПУСТУЮ аудиторию. Замер приёмки 02.09 23:1x на боевой базе data/spetsmat.db: select count(*) from enrollment даёт 0 при 56 учениках и 15847 отметках. Единственный писатель — infra/enrollment_repo.py:174 (insert into enrollment), и вызывается он ТОЛЬКО из tests/: греп по bot core infra tools ops seed на assign/move/insert into enrollment вне тестов не даёт ни одного вызова. Ни экрана владельца, ни импортёра (tools/import_konduit.py), ни команды в ops/ для закрепления НЕТ. ЭТО РОВНО ТОТ КЛАСС, КОТОРЫЙ ВОЛНА ЛОВИТ ВЕСЬ ДЕНЬ: тест сам зовёт assign и потому зелёный, а в бою assign не зовёт никто — как пустой BOT_TOKEN при 486 зелёных тестах и как vision, не проведённый в workflow_data при 18 гейтах из 18. Позиция P12 при этом НЕ ВИНОВАТА: её собственные требования выполнены, судья проверил восемь из восьми и нарушений не нашёл; дефект в отсутствии боевой точки входа. ЦЕНА НА ЗАНЯТИИ, поимённо: (1) resolve_many вернёт None на всех 56, экран аудитории P13 покажет room_students пустым (core/services/room.py:486) — старший не увидит своих учеников кнопками, ради чего экран и делался; (2) сводка после занятия уйдёт ВСЕМ 19 преподавателям вместо работавших в этот день, потому что запасной путь svodka.py при пустом закреплении берёт всех; (3) «сводка по группе» не имеет истории вовсе. ПОЧИНКА: заход, который (а) заполняет enrollment из владельческого файла распределения, (б) даёт владельцу команду перевести ученика, (в) проверяет ЖИВЫМ прогоном, что экран аудитории показывает непустой список на боевой базе. ПРОВЕРКА, ОБЯЗАННАЯ КРАСНЕТЬ: тест требует count(*) > 0 в enrollment на боевой базе ПОСЛЕ прогона импортёра и падает на сегодняшнем состоянии. Найдено судьёй P12 (модель fable) в веере 02.09, подтверждено приёмкой командой.
