ЗАЯВКА: 2026-09-02T12:36 · автор: host · арка: zhurnal/2026-09-02_spetsmat-bot
СРОЧНОСТЬ: obychnaya
РОД: git-operaciya

ДОЛГ ВОЛНЫ: дописать rapidfuzz в цель deps файла Makefile. Позиция P7 считает СВОЮ уверенность через rapidfuzz.fuzz.ratio, потому что уверенности модели не существует (схлопывается в 0,9 и 1,0 при падающей точности). На этой машине пакета не было, и tests/photo/test_confidence.py пропускался — то есть второй независимый канал сверки не проверялся вовсе. Оркестратор поставил rapidfuzz 3.13.0 вручную (pip install --user) и прогнал: 13 passed, пропусков 0. Но Makefile этого не знает: цель deps проверяет только pytest, yoyo и aiogram, и на чистой машине пропуск вернётся молча. Развилку вынес сам заход P7 в ## ВОПРОСЫ пунктом 4. Зона Makefile ни у одной живой позиции не занята.
ПЕРЕАДРЕСОВАНА: 2026-09-04T02:34 · требует содержательной правки кода/конфига (Makefile: цель deps) — вне прав git-контура: этому проходу дано только merge именованных веток в main, вывоз, коммит хвоста Cowork (индекс/автологи) и гашение веток; правку кода делает заход-разработчик
