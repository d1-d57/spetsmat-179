ЗАЯВКА: 2026-09-02T18:27 · автор: host · арка: не названа
СРОЧНОСТЬ: blokiruet
РОД: pravka-koda
АДРЕСАТ: core/services/raspoznavanie.py tests/photo/

ОБРЕЗКА СНИМКА ОТРЕЗАЕТ КОДЫ УЧЕНИКОВ И НОМЕРА ЗАДАЧ, КОГДА ЛИСТ ЗАНИМАЕТ ВЕСЬ КАДР — разбор возвращает rows пустым всегда. Замер 02.09 18:3x на синтетическом бланке tools/blank.py с пятью известными отметками. core/services/raspoznavanie.py:_crop_to_sheet ищет четырёхугольник ЛИСТА; если края листа в кадре нет, крупнейшим прямоугольником оказывается САМА ТАБЛИЦА (её линии), и обрезка выдаёт crop:bounding-rect(1354x745) ровно по сетке. Колонка кодов u1..u18 и строка подписей 1а°..10и° — ТЕКСТ, стоящий СНАРУЖИ линий, и они уходят за границу. Модель получает голую сетку: она видит все пять отметок и честно отказывается угадывать, чьи они, — rows пустой, а в raw_text пишет «в последней ВИДИМОЙ строке, в последнем ВИДИМОМ столбце». То есть отказ корректен, а канал не работает. КОНТРОЛЬНЫЙ ЗАМЕР, доказывающий причину: тот же бланк, положенный на серый фон с видимым краем, даёт crop:skipped-sheet-not-found — обрезка честно отключается, снимок идёт целиком, и та же модель возвращает rows с верными кодами и метками (u1 [1а°,1в°], u3 [3°], u7 [1б°,2]). Разница ровно в обрезке. ПОЧЕМУ ЭТО ВАЖНО ИМЕННО ЗАВТРА: преподаватель, желающий разборчивый снимок, подносит камеру так, чтобы лист занял кадр — то есть попадает в сломанный случай ПО УМОЛЧАНИЮ. ОБХОД ДЛЯ ЧЕЛОВЕКА, работает без починки: снимать так, чтобы в кадр попал край листа и немного стола. ПОЧИНКА: обрезать не по крупнейшему прямоугольнику, а только когда найденный четырёхугольник ЗАМЕТНО больше внутренней сетки (например, площадь листа не меньше 1.2 площади крупнейшей внутренней рамки), либо расширять найденный прямоугольник на поле в 6-8% с каждой стороны — коды и подписи стоят вплотную снаружи. ПРОВЕРКА, КОТОРАЯ ОБЯЗАНА КРАСНЕТЬ: прогнать prepare() на бланке без полей и потребовать, чтобы на выходе ОСТАЛСЯ текст кодов — например, что ширина результата не меньше ширины исходника минус поля. Сейчас такой тест красный. ЗОНА: core/services/raspoznavanie.py tests/photo/. НЕ ЧИНИТЬ, ПОКА ИДЁТ P18.
ПЕРЕАДРЕСОВАНА: 2026-09-04T02:34 · требует содержательной правки кода/конфига (core/services/raspoznavanie.py, tests/photo/) — вне прав git-контура: этому проходу дано только merge именованных веток в main, вывоз, коммит хвоста Cowork (индекс/автологи) и гашение веток; правку кода делает заход-разработчик
