ЗАЯВКА: 2026-09-02T13:33 · автор: host · арка: не названа
СРОЧНОСТЬ: obychnaya
РОД: pravka-koda
АДРЕСАТ: core/services/sessions.py infra/sessions_repo.py

ДЕФЕКТЫ ЗОНЫ P6, найденные верификатором §3 ПОСЛЕ того, как ПРАВКА 2 запретила заходу трогать код («ни строки кода, ни теста, ни рефактора») — поэтому они названы, а не починены. Все двенадцать проверок критерия готовности при этом ЗЕЛЁНЫЕ (провалов 0, проверено 12 из 12): дефекты лежат ВНЕ фиксированной выборки, и это ровно тот случай, ради которого верификатор ходит другим методом. (1) record_marks_for_lesson НЕ атомарен, а его docstring обещает обратное: батч [легальный, с будущей датой] поднимает InvalidLessonDate, но в marks остаётся 1 записанная строка — валидация и запись чередуются в цикле, каждый set_state открывает свою транзакцию. Отказ корректен, обещание «журнал не тронут» ложно. (2) САМЫЙ ЗЛОЙ: повторный тап посещаемости с дефолтным teacher_id=None ЗАТИРАЕТ атрибуцию учителя в NULL и рапортует changed=True, хотя статус не менялся; причина — changed = existing[status] != status or existing[teacher_id] != teacher_id в infra/sessions_repo.py, что нарушает записанный тут же контракт «changed is True iff the new status differs». Позиция существует ради различения «был, не сдавал» от «не был», и молчаливая потеря того, КТО с ним работал, бьёт по той же цели. (3) Внешние ключи не валидируются: сырой sqlite3.IntegrityError протекает сквозь core-сервис на несуществующем student_id и problem_id. Смежно: в _validate_mark_item условие «if sheet is not None» означает, что для неизвестной задачи проверка issued_at МОЛЧА пропускается. (4) Два занятия на одну дату допускаются, find_on берёт order by id desc limit 1 — отметка задним числом молча уедет на позже созданное занятие. (5) Мёртвый код: AttendanceAlreadyStanding и _sheet_id_of_problem объявлены и не используются нигде. Верификатор прогонял СВОЙ скрипт (не pytest автора) на реальных часах и файловой БД; проверки 11-12 переведены с grep на AST после ложного FAIL на прозе в докстрингах.
ПЕРЕАДРЕСОВАНА: 2026-09-04T02:34 · требует содержательной правки кода/конфига (core/services/sessions.py, infra/sessions_repo.py) — вне прав git-контура: этому проходу дано только merge именованных веток в main, вывоз, коммит хвоста Cowork (индекс/автологи) и гашение веток; правку кода делает заход-разработчик
