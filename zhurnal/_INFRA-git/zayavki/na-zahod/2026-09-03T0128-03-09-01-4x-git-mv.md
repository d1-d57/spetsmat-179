ЗАЯВКА: 2026-09-03T01:28 · автор: host · арка: не названа
СРОЧНОСТЬ: obychnaya
РОД: pravka-koda
АДРЕСАТ: _generator/tools/git_zona.py

ДВЕРЬ НЕ УМЕЕТ КОММИТИТЬ СТАДИРОВАННОЕ ПЕРЕИМЕНОВАНИЕ, И ОТВЕЧАЕТ «ЗОНА ЧИСТА» ВМЕСТО ОТКАЗА. Замер 03.09 01:4x: пять документов перенесены git mv из zhurnal/2026-09-02_spetsmat-bot в doc/ (переезд предписан заходом ARHITEKTURA-DOKUMENTACII-0120 §2 прямым текстом «переносить git mv, история файла должна уцелеть»). После git mv индекс содержит пять записей вида R старый -> новый, рабочее дерево чисто. git_zona.py commit --zone doc --zone zhurnal отвечает «✅ Зона doc, zhurnal чиста — коммитить нечего» и НЕ коммитит; git status --porcelain при этом показывает пять стадированных переименований. Проверено трижды: зона doc, зона zhurnal, обе вместе — ответ один и тот же. ПОЧЕМУ ЭТО ОПАСНО, А НЕ НЕУДОБНО: дверь говорит «чисто» ровно там, где работа НЕ сохранена, то есть даёт ЛОЖНО-ЗЕЛЁНОЕ на несохранённом переносе — тот же класс, что «зона-префикс даёт ложно-зелёное» и «grep находит сам себя». Оркестратор, доверившийся ответу двери, ушёл бы с пятью файлами между состояниями, а следующая сессия увидела бы и старые пути, и новые. ПРИЧИНА, предположительно: commit двери делает add <зона> и потом commit -- <зона>; на путях, уже добавленных в индекс, add ничего не меняет, и предикат «есть ли что коммитить» смотрит на вывод status в форме, где R не учитывается. ПОЧИНКА: предикат «зона чиста» обязан считать стадированные изменения тоже (git diff --cached --name-only -- <зона>), а не только рабочее дерево. ПРОВЕРКА, ОБЯЗАННАЯ КРАСНЕТЬ: git mv одного файла внутри зоны, затем git_zona.py commit — сегодня отвечает «чисто», после починки обязан закоммитить перенос. ОБХОД, применённый сегодня: голый git commit с пятью путями, отступление объявлено в ответе захода. ЗОНА: disciplina/_generator/tools/git_zona.py.
ПЕРЕАДРЕСОВАНА: 2026-09-04T02:34 · адресат _generator/tools/git_zona.py — сам инструмент, живёт в disciplina, не в spetsmat-bot; правка инструмента вне зоны этого прохода. адресат лежит в чужом репозитории disciplina, не в spetsmat-bot — вне зоны и прав git-контура этого прохода (права: merge/push/commit хвоста Cowork/гашение веток здесь, в spetsmat-bot)
