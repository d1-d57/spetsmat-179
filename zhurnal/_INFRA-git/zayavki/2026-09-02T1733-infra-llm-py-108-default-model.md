ЗАЯВКА: 2026-09-02T17:33 · автор: host · арка: не названа
СРОЧНОСТЬ: obychnaya
РОД: pravka-koda
АДРЕСАТ: infra/llm.py bot.env.example

УМОЛЧАНИЕ МОДЕЛИ ЗРЕНИЯ НЕ УМЕЕТ ДЕЛАТЬ РАБОТУ: с ним КАЖДОЕ фото возвращается как «не разобралось». infra/llm.py:108 DEFAULT_MODEL = minimax/minimax-m3:free — это КОДОВАЯ текстовая модель, которой волна писала заходы, а не распознаватель бланков. Замер 02.09 17:3x, ПЕРВЫЙ живой вызов конвейера фото за всю волну (P7 в отчёте прямо признаёт: «настоящего прогона против живой модели не было»): VisionModel.read_sheet на ключе владельца поднимает BadModelAnswer «ответ не разобрался как JSON: Extra data: line 7 column 1». Модель картинку ПРИНИМАЕТ — проверено отдельно, на синтетическом пикселе отвечает словом, — но строгий JSON не держит и досыпает прозу после объекта; регулярка _FENCE снимает заборы из обратных кавычек, а хвостовой текст не снимает никто. Отказ ИМЕНОВАННЫЙ, бот не падает — и именно поэтому дефект тихий: преподаватель увидит «не разобралось» на каждом бланке и решит, что виновата его фотография. ЗАМЕР ПО КАНДИДАТАМ тем же read_sheet, тем же ключом, той же картинкой: google/gemini-2.5-flash РАЗОБРАЛОСЬ, google/gemma-3-12b-it РАЗОБРАЛОСЬ, minimax/minimax-m3:free ОТКАЗ, openai/gpt-5-nano и qwen/qwen3.5-flash HTTP 400. Из 421 модели OpenRouter картинку принимают 252, из них строгий JSON заявляют 218 — выбор есть. ПОЧИНКА: DEFAULT_MODEL сменить на google/gemini-2.5-flash и вписать то же значение в bot.env.example рядом с LLM_MODEL, чтобы умолчание кода и шаблон говорили одно (урок 17 волны: у константы один дом). ПРОВЕРКА, КОТОРАЯ ОБЯЗАНА КРАСНЕТЬ: тест под отдельным маркером, зовущий read_sheet против ЖИВОГО эндпоинта, краснеет на модели, не держащей JSON; в основном прогоне он выключен, чтобы не жечь ключ. Найдено ПРИЁМКОЙ волны, не заходом: у P7 все 18 гейтов были зелёные.
