# 🔴 НАСТОЯЩИЙ ФОРМАТ ЗАПИСИ ПРЕПОДАВАТЕЛЯ — ВЛАДЕЛЕЦ, 02.09 16:10

Срочно, пока `P15` ещё в работе. Я описал ей формат по памяти разработчика, а не
по своей практике. Ниже то, как я записываю занятие НА САМОМ ДЕЛЕ. Сверь с
заданием `P15` и, если расходится, поправь до её закрытия.

---

## КАК Я ПИШУ

Лист делится горизонтальными чертами на блоки, по блоку на ученика. В блоке —
имя и следом номера задач в строку. Не таблица. Таблицы у меня не бывает никогда.

```
Лёня Санин 7, 9а, 11б, 12, 13, 15а, 15в, 2б, 4, 6
Катя Долкирева 2а, 2б, 4, 5в, 8, 10а
Аня Бочарова [3д] 5, 12
Влад Быков —
```

**Пять правил, каждое из практики:**

1. **Задач обычно около десяти на человека**, не три. Запись занимает несколько
   строк, и перенос строки внутри одного ученика — норма, а не ошибка.
2. **Листок по умолчанию — текущий.** Весь класс сдаёт один и тот же, это
   обычный случай.
3. **Другой листок — в квадратных скобках перед списком**, как `[3д]`. Так же
   помечаю доплистки и старые листки.
4. 🔴 **ПРОЧЕРК = пришёл и не сдал ничего.** Раньше я такого просто не записывал,
   но так правильнее: молчание неотличимо от «меня не было». Пусть бот ЖДЁТ
   прочерк и понимает его как явную явку без сдач. Это то же различение, что
   `P6` уже строила красной строкой.
5. **Имена я пишу так, как зову детей вслух**, и это главная трудность:

   | я напишу | в каталоге | что сломано |
   |---|---|---|
   | Лёня Санин | **Исанин Леонид** (id 23) | потеряна первая буква фамилии + уменьшительное |
   | Катя Долкирева | **Долгирева Екатерина** (id 18) | г→к + уменьшительное |
   | Аня Бочарова | **Бочарова Анна** (id 9) | порядок имя-фамилия |
   | Влад Быков | **Быков Владислав** (id 11) | сокращение имени |

   🔴 **Возьми эти четыре строки именованными тестами.** Они настоящие: я
   перечислил их не думая, а потом сверил с базой. Если `P15` узнаёт «Лёня Санин»
   как Исанина — она узнает что угодно. Если нет — на занятии я буду набирать
   имена по паспорту, и вся скорость текстового ввода пропадёт.

   И прежний запрет в силе: при сомнении — КНОПКИ, а не догадка. «Лёня Санин»
   имеет право уйти в переспрос, но не имеет права молча попасть на другого
   ребёнка.

## ЧЕГО В МОЕЙ ЗАПИСИ ЕЩЁ ПОЛНО

Это рваный кусок бумаги, на котором параллельно идёт объяснение математики:
черновики, рисунки, чужие пометки. Полезные строки — вкраплениями среди мусора.

**Из этого НЕ следует, что бот должен читать мусор.** Из этого следует, что
переписываю в бота я сам — глазами, выбирая нужные строки. Поэтому вход текстовый,
а не фотографический. **Фотоканал остаётся ровно тем, чем сделан: печатный бланк
с кодами**, и мой рваный листок туда не пойдёт никогда — на нём фамилии детей.

Одна надежда сверху: человек, который знает, что пишет для бота, пишет аккуратнее.
Но проектировать на это нельзя, и я не прошу.

## ЧТО ЭТО МЕНЯЕТ ПО ФАЗАМ

Печатать бланк мне сегодня нечем — принтера дома нет. Значит **ворота фазы 2
сдвигаются: фото я проверю в школе, а сегодня прогоняю текст и голос.**
Соответственно `P15` из «механической» становится тем каналом, на котором завтра
всё и поедет. Пересмотри её приоритет с учётом этого.

---

## ОТВЕТ ОРКЕСТРАТОРА

Отвечено 16:20.

## 🔴 СНАЧАЛА ПОПРАВКА ФАКТА: `P15` НЕ БЫЛА В РАБОТЕ

Вы писали «срочно, пока `P15` ещё в работе». **Она не запускалась ни разу** — лога
`/tmp/zahod-P15-tekst.log` нет, в процессах её нет. Шли `P9`, `P14`, `P16`, `P19`, `P20`.

**И это хорошая новость, а не плохая:** сверять было нечего — я переписал задание
ЦЕЛИКОМ, а не залатал на ходу. Вы успели ровно вовремя.

## ЧТО БЫЛО НАПИСАНО НЕВЕРНО — И ЭТО НАПИСАЛ Я

Прежняя редакция описывала формат `[7] Петров 3,5,7б`. **Его придумал я**, взяв из
`спецмат-бот-модель.md` строчку про квадратные скобки и достроив остальное «как
логично». Ни одна строка того формата не была вашей.

Расхождений оказалось четыре, и каждое ломало бы разбор:

| в моём задании было | у вас на самом деле |
|---|---|
| `[7]` в НАЧАЛЕ строки, до имени | `[3д]` ПОСЛЕ имени, перед списком |
| листок обязателен | листок по умолчанию ТЕКУЩИЙ, скобки — исключение |
| три задачи, одна строка | около ДЕСЯТИ, перенос строки внутри блока — норма |
| про прочерк не было ни слова | прочерк = ПРИШЁЛ И НЕ СДАЛ, явная явка |

Третье — самое опасное: разбор, считающий конец строки концом блока, на вашей записи
терял бы половину задач МОЛЧА. Теперь в задании прямо: **блок кончается там, где
начинается следующее ИМЯ, а не там, где кончается строка.**

Прочерк отправил в явку (`P6`), а не в отметки, и связал с её именованным тестом
`test_red_line_present_no_marks_differs_from_absent` — это то же различение, вы правы.
Принимаются все три тире: `—`, `-`, `–`.

## ЧЕТЫРЕ ИМЕНИ — ПРОВЕРИЛ ПО ЖИВОЙ БАЗЕ, ВСЕ ЧЕТЫРЕ СОШЛИСЬ

Не поверил на слово, запросил базу:

    «Лёня Санин»      → id=23  Исанин Леонид        ✅
    «Катя Долкирева»  → id=18  Долгирева Екатерина  ✅
    «Аня Бочарова»    → id=9   Бочарова Анна        ✅
    «Влад Быков»      → id=11  Быков Владислав      ✅

Они стали **именованными тестами и критерием готовности**, с вашей формулировкой
внутри задания дословно: «узнаёт „Лёня Санин“ как Исанина — узнает что угодно».

**И я дописал то, чего вы не просили, но без чего эти тесты не пройдут.** Одного
нечёткого сравнения фамилий не хватит: «Лёня Санин» ломается сразу тремя способами.
В задание вписаны три требования к механике:

- пробовать ОБА порядка токенов и сравнивать каждый токен и с фамилией, и с именем;
- 🔴 **уменьшительные — это СЛОВАРЬ, а не нечёткость.** «Лёня»/«Леонид» и
  «Катя»/«Екатерина» по буквам далеки, никакой `fuzz.ratio` их не сведёт. Словарь
  данными, покрыть все 56 имён каталога, число покрытых — в отчёт;
- индекс `P7` импортировать, третий не писать.

Запрет на догадку оставлен вашими словами: переспрос законен, молча попасть на другого
ребёнка — нет. Добавил от себя цену: **ошибка здесь это плюс, поставленный чужому
ребёнку, и заметит её только тот, у кого он пропал.**

## ПРИОРИТЕТ ПЕРЕСМОТРЕН, КАК ВЫ ПРОСИЛИ

`P15` была помечена «механическая». **Больше нет.** Раз принтера нет и бланк вы
проверите только в школе — текстовый ввод это тот канал, на котором завтра всё и
поедет. Вписал это в само задание первым абзацем, чтобы исполнитель знал, что делает
не четвёртый вход, а основной.

**Пущена в 16:19**, `opus`. Сейчас в работе шесть окон: `P9` `P14` `P15` `P16` `P19` `P20`.

⚠ Предупредил её об одном соседстве: `P19` прямо сейчас решает РОДСТВЕННУЮ задачу —
сопоставление заявки регистрации с тем же каталогом. Зоны у них разные, но механизм
просится общий. Велел не строить общий модуль на ходу, а сказать в `## ВОПРОСЫ`:
два захода в один новый файл — то, чего волна себе не позволяет.

## ПО ФАЗЕ 2 — ВОРОТА СДВИНУТЫ, ПРИНЯТО

Сегодня прогоняете **текст и голос**, фото — в школе. Значит от меня вам нужны:

1. **`P20`** — без неё голос молча уходит в заглушку. Идёт, 39 минут.
2. **`P15`** — только что пущена.
3. **имена переменных SpeechKit** — назову в отчёте `P20` одним списком.

О закрытии каждой скажу строкой. Фото не пробуйте и в школе, пока не скажу про `P20`.
