# УРОЖАЙ ВОЛНЫ «ВЕЧЕР» — фаза знания, шаг (a) СБОР

Собирается ПО ХОДУ волны, а не в конце: мандат велит собрать всё, рождённое ЭТОЙ
волной, и посчитать. Судить их будет СВЕЖИЙ бесплатный заход, и НЕ та модель, что
писала, — автор себе плохой судья, в этом весь смысл фазы.

Источники: журнал `UROKI-VOLNY-VECHER.md` и файлы `kod_*` волны.
Состояние на 17:37, волна не закрыта — список пополняется.

---

## У1 · Проба живости бесплатной модели ЕСТЬ ЕЁ РАСХОД
**ЦЕНА:** 6 минут, одна модель выбыла из волны.
Прогон `opencode run --model <бесплатная> "Reply ALIVE"` вернул `ALIVE`, а через минуту
заход на той же модели упал с `rate-limited upstream`. У бесплатного тира лимит меряется
запросами, и проба тратит тот самый запрос, ради которого спрашивают.
**ДОМ-КАНДИДАТ:** `skills/disciplina-zahod` — там живёт урок 55 «перед волной прогнать
пробу живости». Урок верен для платной модели и ЛОЖЕН для бесплатной.

## У2 · Сборку захода судить по ФАКТУ ФАЙЛА, а не по хвосту вывода
**ЦЕНА:** 9 минут, три невидимых отказа подряд.
`bootstrap_zahod.py` печатает отказ ПЕРВОЙ строкой, а последние строки бодрые
(«Ветку НЕ переключать, она уже стоит»). `tail -3` показывает успех при несозданном файле.
**ДОМ-КАНДИДАТ:** `skills/disciplina-zahod`, рядом с вызовом генератора: проверка
`test -f kod_<тема>.md` обязательна после каждой сборки.

## У3 · Три двери генератора, на которые волна налетела подряд
**ЦЕНА:** входит в те же 9 минут.
(1) `--verifikator-dolya` требует ЦИФРУ; (2) второй заход в арку с незакоммиченным
`kod_*.md` не собирается — порядок «собрал → дозаполнил → закоммитил → следующий»;
(3) линтер З27 запрещает число рядом со словом «сегодня» — исполнитель прочтёт его как цель.
**ДОМ-КАНДИДАТ:** `skills/disciplina-zahod`, раздел про сборку — три двери назвать заранее.

## У4 · Имя файла-захода — КОНТРАКТ С ХУКОМ, а не свободный выбор аналитика
**ЦЕНА:** надзор не работал бы всю волну.
Хук берёт имя позиции из ВТОРОГО поля строки состава и ищет `kod_<то самое имя>.md`.
Свои слаги = хук не находит файлов = «позиций без вердикта нет» = волна отпущена.
**ДОМ-КАНДИДАТ:** `hook_volna_zhiva.sh` (шапка) и `skills/disciplina-zahod`.

## У5 · Устаревший `VOLNA-POZICII` молча вычёркивает позиции живой волны
**ЦЕНА:** та же — молчащий хук.
Файл-фильтр от прошлой волны перечислял её восемь позиций и вычеркнул все мои.
Шапка хука предупреждает ровно об этом дефекте — и он всё равно случился.
**ДОМ-КАНДИДАТ:** `hook_volna_zhiva.sh` — фильтр обязан ОТКАЗЫВАТЬ, если ни одна его
строка не совпала с составом активной волны, а не тихо обнулять область.

## У6 · Прогон хука — обязательный шаг взятия указателя, а не по желанию
**ЦЕНА:** предотвращённая — вся волна без надзора.
Молчащий хук снаружи неотличим от исправного до первого конца хода. У4 и У5 поймались
за две минуты только прогоном.
**ДОМ-КАНДИДАТ:** `skills/disciplina-orkestrator`.

## У7 · Замер «ДО» рычагом — ПЕРВЫМ ходом волны, не четвёртым
**ЦЕНА:** три позиции из пяти открывали бы условие запуска каждая заново.
Сервер не стартует без `SPETSMAT_VEB_SECRET`; это условие запуска, а не дефект позиции.
**ДОМ-КАНДИДАТ:** `skills/disciplina-orkestrator`.

## У8 · Критерий позиции обязан быть выполним В ЗОНЕ позиции
**ЦЕНА:** предотвращённая — одна позиция из пяти вернулась бы красной не по своей вине.
Критерий S5 «читается без логина» упирался в `veb/server.py:245`, которого в её зоне не было.
**ДОМ-КАНДИДАТ:** `skills/disciplina-zahod` — механическая сверка «файл, который критерий
требует изменить, лежит в зоне?» при сборке.

## У9 · НЕСОВПАДЕНИЕ ПЕЧАТАЕТСЯ КАК УСПЕХ — четыре механизма, одна болезнь
**ЦЕНА:** сутки простоявшего дефекта (56=56), плюс три пойманных сегодня.
| где | зеленело | на самом деле |
|---|---|---|
| сторож сайта | «ЖИВ 200» | чужой баннер провайдера |
| ночная сверка состава | 56 = 56 | 53 разных ребёнка |
| `sqlite3` на миграции | `rc=0` | `Parse error: no such column` |
| `git_zona.py --zone "a b"` | «зона чиста» | 29 строк вне git |
**ДОМ-КАНДИДАТ:** `skills/disciplina-kachestvo` — это не частный баг, это класс.
Правило-кандидат: инструмент, чей предикат НИ С ЧЕМ НЕ СОВПАЛ, обязан краснеть, а не зеленеть.

## У10 · `git_zona.py --zone` — флаг ПОВТОРЯЕМЫЙ, два пути одной строкой дают ложно-зелёное
**ЦЕНА:** 29 строк S1 остались бы вне git при бодром ✅.
**ДОМ-КАНДИДАТ:** `git_zona.py` — отказ на аргументе `--zone` с пробелом.

## У11 · Признак мёртвой позиции, снимаемый из лога ЗАРАНЕЕ
**ЦЕНА:** 6 минут вхолостую.
Модель, которая десяток раз подряд перечитывает СВОЙ ЖЕ заход кусками по 1–20 строк и
не трогает диск, до работы не дойдёт. Поднимать, не дожидаясь конца.
**ДОМ-КАНДИДАТ:** `skills/disciplina-orkestrator`.

## У12 · Файл-заход лежит в ГЛАВНОЙ папке — и заход коммитит в main мимо своей ветки
**ЦЕНА:** пока 0, механизм опасен: обходится контракт зоны целиком.
Не вина позиции: файл-заход физически вне worktree, любая запись в него — запись в main.
**ДОМ-КАНДИДАТ:** долг фабрики — либо заход не коммитит свой файл вовсе, либо файл живёт в worktree.

## У13 · Модели волны — ОЧЕРЕДЬ, а не таблица «позиция → модель»
**ЦЕНА:** 3 отказа на 6 запусков за 20 минут.
z-ai — квота · nemotron-3-super — пустой возврат · devstral — недоступна.
Живучесть бесплатной модели проверяется только запуском.
**ДОМ-КАНДИДАТ:** `skills/disciplina-zahod`, раздел выбора модели.

## У14 · minimax-m3 не тянет точечную правку большого файла
**ЦЕНА:** 18 минут S1 без единого своего коммита.
Пять отказов `Edit … SchemaError(Missing key at ["newString"])` подряд. Модель годна там,
где файл пишется целиком.
**ДОМ-КАНДИДАТ:** реестр моделей — заметка о роде работы.

---

**ПОСЧИТАНО НА 17:37: РОЖДЕНО 14.** Ни один ещё не судим — судить будет свежий заход
на бесплатной модели, батчами примерно по десять, и НЕ той моделью, что писала.
