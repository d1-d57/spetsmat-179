# УРОКИ ВОЛНЫ «ВЕЧЕР» — журнал оркестратора

Волна VECHER. Старт 2026-09-04 17:08. Дедлайн 2026-09-04 20:08.
Позиции: S1 (одна, первой) → S2, S3, S4, S5 параллельно.

---

## Круг 1 — 17:08

**Указатель волны.** Файл `VOLNA-AKTIVNAYA` СУЩЕСТВУЕТ, вторая строка `ORK-DEN` —
соседняя волна в репозитории disciplina добивает последний ход. Не перезаписываю,
не жду. Беру указатель первым же кругом после того, как он исчезнет.
🔴 СЛЕДСТВИЕ ДЛЯ МЕНЯ: меня не держит ничто, я молча встану на первом конце хода.
Ходы держу короткими, коммичу сразу после каждого куска.

**Прогноз.** Закрыто 0 из 5. Осталось 5. S1 одна ~40 мин, затем четвёрка параллельно
~60 мин, приёмка по рычагу ~10 мин. Жду закрытия волны к 19:00, запас до дедлайна 68 мин.

**Факт против прошлого прогноза.** Прогноза не было — это первый круг.

**Что сломалось.** Пока ничего. · ЦЕНА 0 · —

**Что сломалось в МОЕЙ работе.** Ничего.

**Сверка базы командой (не по памяти):** students 56 · teachers 19 · enrollment 56.
Совпало с составом — цифры состава подтверждены, не приняты на веру.

---

## Круг 2 — 17:14

**Указатель волны.** Проверил: файл на месте, вторая строка по-прежнему `ORK-DEN`.
Указатель не мой. Меня не держит ничто — ходы короткие.

**Прогноз.** Закрыто 0 из 5, осталось 5. S1 перезапущена в 17:13 на minimax-m3,
жду её отчёт к 17:50, четвёрку параллельно к 19:00. Дедлайн 20:08.

**Факт против прогноза круга 1.** Прогноз «S1 одна ~40 мин» ПРОВАЛЕН на первом же
запуске: S1 умерла через 33 секунды, работы ноль. Потеряно 6 минут.

🔴 **Что сломалось · ЦЕНА 6 минут и одна сожжённая бесплатная модель · что бы помогло.**
Проба живости модели СЪЕЛА КВОТУ ТОЙ ЖЕ МОДЕЛИ. Я прогнал
`opencode run --model openrouter/z-ai/glm-5.2:free "Reply ALIVE"` — вернулось `ALIVE`,
модель признана живой. Через минуту заход на ТОЙ ЖЕ модели упал:
`[Decart] z-ai/glm-5.2:free is temporarily rate-limited upstream`.
Проба живости на бесплатной модели — это не измерение, а РАСХОД: у бесплатного тира
лимит меряется запросами, и проба тратит тот самый запрос, ради которого спрашивают.
Урок 55 велит «перед волной прогнать пробу живости» — на ПЛАТНОЙ модели это верно,
на бесплатной проба сама и есть отказ.
ЧТО БЫ ПОМОГЛО: не пробовать бесплатную модель отдельным запросом вовсе — судить по
первым 60 секундам самого захода, он всё равно печатает ту же ошибку в лог. Либо
пробовать модель, на которой заход НЕ пойдёт.

**Что сломалось в МОЕЙ работе, а не в предмете позиции.** Ровно это: лишний запрос
сделал я. Позиция S1 к отказу отношения не имеет — её файл-заход собран и в git
(коммит волны 17:11), перезапуск пошёл с той же ветки без потерь.

**Модель и время у закрытых позиций.** Закрытых нет.

**Перераспределение моделей после отказа** — по-прежнему по одному провайдеру на позицию:
S1 minimax-m3 · S2 thinkingmachines/inkling · S3 poolside/laguna-s-2.1 ·
S4 nvidia/nemotron-3-super · S5 cohere/north-mini-code. z-ai выведена из оборота до конца волны.

---

## Круг 3 — 17:22 (в первой редакции строки стояло 17:28 — время взято из головы, а не командой; исправлено прогоном date)

**Указатель волны ВЗЯТ.** ORK-DEN отпустила указатель между 17:16 и 17:22, я взял его
первым же кругом. Строки: арка · `VECHER` · мой session_id `4028c4ca-…`, сверенный с
собственным путём скретчпада, а не угаданный по времени файла.
**Прогон хука предъявлен:** `{"decision":"block"}`, «волна VECHER, привязка по session_id»,
позиций без вердикта 4, круг 1 из 60. Меня держит.

**Прогноз.** Закрыто 0 из 5. S1 работает 15 минут, план записала, идёт по делу.
Жду S1 к 17:50, четвёрку параллельно к 19:10. Дедлайн 20:08.

**Факт против прогноза круга 2.** Прогноз «S1 к 17:50» пока в силе. Сборка четырёх
заходов заняла 12 минут вместо ожидаемых 5 — три отказа генератора подряд.

🔴 **Что сломалось · ЦЕНА 9 минут и два ложно-зелёных вывода · что бы помогло.**
ТРИ отказа сборки, и все три я СНАЧАЛА НЕ УВИДЕЛ, потому что смотрел `tail -3`
вывода генератора, а отказ печатается ПЕРВОЙ строкой. Хвост показывал бодрое
«Ветку НЕ переключать, она уже стоит» — и я решил, что заход собран. Файла не было.
Три причины отказа, каждая законная:
  1. `--verifikator-dolya` без ЦИФРЫ («все пять чисел» не годится, нужно «5 из 5»);
  2. в арке лежал НЕЗАКОММИЧЕННЫЙ kod_*.md — второй заход в грязную арку не собирается;
     порядок: собрал → дозаполнил → закоммитил → только потом следующий;
  3. линтер З27 поймал в обосновании модели «z-ai выжжена сегодня в 17:12» — число
     рядом со словом «сегодня» исполнитель прочтёт как ЦЕЛЬ.
ЧТО БЫ ПОМОГЛО: не судить сборку по хвосту вывода — судить по ФАКТУ наличия файла
`test -f kod_<тема>.md`. Я это и сделал третьим заходом, и только тогда причины стали видны.

🔴 **Второе, дороже первого · ЦЕНА — надзор не работал бы всю волну · что бы помогло.**
Первый прогон хука после взятия указателя напечатал ПУСТО, то есть отпустил меня.
Две причины, обе — тихая потеря надзора:
  1. **имена файлов-заходов разошлись с составом.** Хук берёт имя позиции из ВТОРОГО
     поля строки состава (`S2|sostav-klassa-i-112-strok|…` → ищет
     `kod_sostav-klassa-i-112-strok.md`), а я назвал файлы своими слагами `kod_S2-…`.
     Файлов с ожидаемыми именами нет → «позиций без вердикта нет» → волна отработала.
     Переименовал четыре файла и заменил слаг внутри них.
  2. **`VOLNA-POZICII` от ПРОШЛОЙ волны.** Файл-фильтр остался от волны «сайт» и
     перечислял её восемь позиций. Он ДОПОЛНИТЕЛЬНО сужает область, и мои позиции
     вычёркивались все до одной. Шапка самого хука предупреждает ровно об этом
     дефекте — «посторонний VOLNA-POZICII в арке молча вычёркивал позиции живой
     волны», — и он всё равно случился. Убрал в `VOLNA-POZICII.ustarel-volna-sajt`,
     не удаляя.
ЧТО БЫ ПОМОГЛО: прогон хука — ОБЯЗАТЕЛЬНЫЙ шаг сразу после взятия указателя, а не
по желанию. Оба дефекта поймались за две минуты и оба были невидимы иначе: молчащий
хук снаружи неотличим от исправного до первого конца хода.

**Что сломалось в МОЕЙ работе, а не в предмете позиций.** Обе поломки — мои: слаги
имён придумал я, хвост вывода читал я. К предмету S2–S5 ни одна отношения не имеет.

**Модель и время у закрытых позиций.** Закрытых нет. Расклад моделей:
S1 minimax-m3 (старт 17:13) · S2 thinkingmachines/inkling · S3 poolside/laguna-s-2.1 ·
S4 nvidia/nemotron-3-super · S5 cohere/north-mini-code.

**ДОЛГ, названный вслух:** файл S1 всё ещё зовётся `kod_S1-slot-i-routy.md` и в счёт
хука не попадает — переименовать нельзя, позиция работает и пишет в него отчёт.
Хук держит по четырём из пяти, этого достаточно. Переименовать сразу после возврата S1.

---

## Круг 4 — 17:26 (время снято `date '+%H:%M'`)

**Указатель.** Мой, `VECHER`, привязка по session_id. Хук вернул меня кругом 2 из 60,
застой 0 из 5 — надзор работает.

**Прогноз.** Закрыто 0 из 5. S1 идёт 13 минут, чинит идемпотентность миграции —
нашла настоящую дыру САМА: повторный `rename column weekday to slot` падает с
«no such column: weekday», потому что условие в SQL-скрипте не ветвит выполнение.
Жду S1 к 17:50, четвёрку к 19:10, закрытие волны к 19:30 при дедлайне 20:08.

**Факт против прогноза круга 3.** Держится: S1 в работе, коммитов нет 13 минут при
нормативе 20 — не нарушено, в файле лежит `migrations/003_slot_vmesto_weekday.sql`.

🔴 **ЗАМЕР «ДО» СНЯТ РЫЧАГОМ, и он поймал то, чего не было ни в одном отчёте · ЦЕНА
предотвращённая — три позиции из пяти краснели бы на пустом месте · что бы помогло.**
Первый прогон рычага не поднял сервер вовсе:
`RuntimeError: veb/vhod.py: missing required environment variable(s): SPETSMAT_VEB_SECRET`.
`veb/server.py main()` зовёт `vhod.proverit_okruzhenie()`, и без переменной процесс
падает ДО первого запроса. Это условие запуска, а не дефект позиции — но по нему
одинаково спотыкались бы S1, S3 и S5, всем троим нужен живой сервер. Рычаг научен
выставлять три переменные сам.
ЧТО БЫ ПОМОГЛО: замер «ДО» рычагом ДО запуска позиций, а не при приёмке. Я снял его
на четвёртом круге, а надо было на первом — тогда условие запуска попало бы в тексты
заходов, и трём позициям не пришлось бы открывать его заново каждой самостоятельно.

**ЗАМЕР «ДО» на main, для приёмки — предъявляю числами:**
* `/` `/raspredelenie` `/listki` `/listki-8` `/urovni` → **302 все пять**, редирект на
  форму входа. То есть СЕГОДНЯ САЙТ ЦЕЛИКОМ ЗАКРЫТ ЛОГИНОМ, и критерий владельца
  «распределение открывается без логина и показывает данные» сейчас КРАСНЫЙ. Это
  предмет S5, и теперь у него есть замер «до», а не слова;
* `/api/view?slot=…` → пусто (слота ещё нет, это предмет S1);
* `grep -c weekday veb/server.py` → **20**;
* `pytest tests/veb -q` → **18 passed**.

**Что сломалось в МОЕЙ работе.** Порядок: рычаг надо было писать и прогонять первым
ходом волны, а не четвёртым. Позиции уже стартовали без знания про переменные.

**Модель и время у закрытых позиций.** Закрытых нет.

---

## Круг 5 — 17:27

**Указатель.** Мой, VECHER. Хук: круг 3 из 60, застой 0 из 5.

**Прогноз.** Без изменений: S1 к 17:50, четвёрка к 19:10, закрытие к 19:30.

**Факт против прогноза круга 4.** S1 идёт 14 минут, коммитов ноль, на диске
`veb/server.py` правится и лежит `migrations/003_slot_vmesto_weekday.sql`. Норматив
20 минут ещё не нарушен, но запас кончается.

🔴 **Что сломалось · ЦЕНА 2 пустых круга · что бы помогло.**
Я крутил круги вхолостую: хук возвращает меня быстрее, чем работает позиция, и
каждый мой круг — это проверка «а не появился ли коммит», то есть трата лимита на
ожидание. Три круга подряд я делал одно и то же чтение лога.
ЧТО БЫ ПОМОГЛО: армировать ОЖИДАНИЕ СОБЫТИЯ, а не опрашивать руками. Поставил
фоновый цикл, который возвращает меня по ПЕРВОМУ КОММИТУ ветки S1, а не по таймеру.
Это надо было делать сразу после запуска первой позиции.

**Наблюдение по инструменту исполнителя, не по предмету:** в логе S1 дважды
`Edit veb/server.py failed — SchemaError(Missing key at ["newString"])`. Модель
minimax-m3 зовёт инструмент правки с неполными аргументами и переигрывает. Пока это
шум, а не отказ: работа на диске движется. Если повторится третий раз — это цена
модели, и её надо будет записать в выбор моделей следующей волны.

**Что сломалось в МОЕЙ работе.** Ровно ожидание опросом вместо ожидания событием.

**Модель и время у закрытых позиций.** Закрытых нет.

---

## Круг 6 — 17:28

**Указатель.** Мой, VECHER. Хук: круг 4 из 60, застой 0 из 5.

**РЕШЕНИЕ КРУГА, названное вслух, потому что оно отступает от буквы состава.**
Запустил S4 (`postoyannyj-adres-pages-i-tunnel`) ПАРАЛЛЕЛЬНО с S1, не дожидаясь её
влития. Состав говорит «S1 идёт первой и одна», и ПРИЧИНА там названа прямо: «она
меняет схему, остальные её читают». S4 схему НЕ читает: её зона `deploy/ infra/ ops/`
не пересекается с зоной S1 `migrations/ core/ veb/server.py tests/veb/test_server.py`
ни одним путём, а работает она в своём worktree с копией main. То есть я соблюдаю
причину правила, а не только его букву. S2 и S3 продолжают ждать S1: S2 читает схему
напрямую, S3 обязана сверить имена роутов с её кодом.
Если это отступление окажется неверным — оно записано здесь, а не растворено в работе.

**Прогноз.** Закрыто 0 из 5. S1 к 17:50, S4 к 18:15, S2 и S3 стартуют по влитию S1,
закрытие волны к 19:30. Дедлайн 20:08.

**Факт против прогноза круга 5.** S1 идёт 15 минут, коммитов ноль. Процесс ЖИВОЙ
(pid 69588), лог 80 КБ и растёт, работа идёт над `_build_views`. Не завис — медленный.
Норматив 20 минут ещё не пробит, но пробьётся на следующем круге, и тогда это будет
предметом разбора, а не ожидания.

**Что сломалось.** Ничего нового · ЦЕНА 0 · —

**Что сломалось в МОЕЙ работе.** Ничего этим кругом. Отступление от порядка позиций
сделано осознанно и записано выше, а не молча.

**Модель и время у закрытых позиций.** Закрытых нет.
S1 minimax-m3, старт 17:13, идёт. S4 nvidia/nemotron-3-super, старт 17:28, идёт.

---

## Круг 7 — 17:29

**Указатель.** Мой, VECHER. Хук: круг 5 из 60, застой 0 из 5.

🔴 **НАЙДЕНА БЛОКИРУЮЩАЯ ДЫРА В САМОМ СОСТАВЕ ВОЛНЫ · ЦЕНА предотвращённая —
одна позиция из пяти получила бы «доработку» не по своей вине · что бы помогло.**
Критерий S5 — «страница распределения открывается без логина» — в её зоне
НЕВЫПОЛНИМ. Шлагбаум стоит в `veb/server.py:245-247`, сплошной редирект 302 на любой
GET без куки, а зона S5 была `veb/vhod.py` + `vhod.css` + тест. Снять шлагбаум из
`veb/vhod.py` нельзя ничем. Позиция сделала бы всё правильно и всё равно вернулась бы
красной.
Починил ДО запуска: зона S5 расширена на `veb/server.py`, вписана ПРАВКА 1 с точным
адресом строк, с запретом трогать в этом файле что-либо кроме шлагбаума чтения, с
прямым указанием НЕ снимать проверку в `do_POST` (она и есть требование владельца
«правит только организатор») и с предупреждением, что тот же файл параллельно правит S1.
ЧТО БЫ ПОМОГЛО: критерий позиции надо сверять с ЗОНОЙ позиции при сборке захода —
механически, «есть ли в зоне файл, в котором живёт то, что критерий требует изменить».
У меня такой проверки не было; нашлось только потому, что я прогнал рычаг сам и
увидел 302 там, где ждал 200.

**Как нашлось.** Не чтением отчёта и не рассуждением: прогоном рычага на `main`,
который дал пять 302 вместо 200, и последующим `grep -n "302" veb/server.py`.
Это ровно то, ради чего мандат велит вести приёмку ПО РЫЧАГУ.

**Цена модели, третье повторение — теперь это не шум.** `minimax-m3` в третий раз
уронила правку файла: `Edit veb/server.py failed — SchemaError(Missing key at
["newString"])`. Модель зовёт инструмент правки с неполными аргументами и
переигрывает круг за кругом. S1 идёт 16 минут без единого коммита при живом процессе
и растущем логе — она не висит, она буксует на инструменте.
ЦЕНА: пока 16 минут S1 и норматив коммита на грани.
ЧТО БЫ ПОМОГЛО: в выбор моделей следующей волны — не ставить minimax-m3 на позиции,
где много точечных правок большого файла; ей годятся задачи, где файл пишется целиком.

**Прогноз.** Закрыто 0 из 5. S1 к 18:00 (сдвинул с 17:50 из-за буксовки), S4 к 18:15,
S2 и S3 по влитию S1, S5 после S1 из-за общего файла. Закрытие к 19:40, дедлайн 20:08.

**Факт против прогноза круга 6.** Прогноз «S1 к 17:50» под угрозой: 16 минут, ноль
коммитов, три отказа инструмента.

**Что сломалось в МОЕЙ работе.** Отсутствие сверки «критерий против зоны» при сборке
заходов. Нашёл сам и до запуска — но нашёл поздно, на седьмом круге.

**Модель и время у закрытых позиций.** Закрытых нет.

---

## Круг 8 — 17:30

**Указатель.** Мой, VECHER. Хук: круг 6 из 60, застой 0 из 5.

**ПРИЁМКА ПО РЫЧАГУ НАЧАТА ДО ВОЗВРАТА ПОЗИЦИИ.** Не стал ждать отчёта S1: снял
её работу с диска и прогнал критерий сам, на КОПИИ базы, чтобы не тронуть живую.

**Что показал прогон — числами:**
* прогон 1: `rc=0`, enrollment 56 строк;
* прогон 2 тем же файлом подряд: `Parse error near line 54: no such column: "weekday"`,
  enrollment 56 строк.
Миграция В ТЕКУЩЕМ ВИДЕ НЕ ИДЕМПОТЕНТНА. «Страж идемпотентности» в файле — это
`select case when exists(...) then 0 else 1 end as needs_rename;`, то есть ОБЫЧНЫЙ
SELECT: он печатает число и ничего не ветвит, следующий `alter table rename column`
выполняется всё равно. S1 эту же ошибку видела в своём прогоне и сейчас её чинит —
состояние промежуточное, вердикт по нему не выношу.

🔴 **ВТОРАЯ НАХОДКА, ДОРОЖЕ ПЕРВОЙ · ЦЕНА предотвращённая — ложно-зелёный критерий
идемпотентности у ЛЮБОЙ будущей миграции · что бы помогло.**
`sqlite3 proba.db < migraciya.sql` при ошибке разбора вернул **rc=0**. То есть
проверка вида «прогнать дважды и посмотреть код возврата» ЗЕЛЕНЕЕТ НА СЛОМАННОЙ
МИГРАЦИИ. Ровно тот класс ложно-зелёного, за который волна уже заплатила сутками
(совпавшее число 56=56) и который сегодня же поймали у сторожа сайта (200 от чужого
баннера).
ЧТО БЫ ПОМОГЛО: критерий идемпотентности обязан судить ВЫВОД (пусто ли на stderr),
а не код возврата. Записываю это в приёмку S1: буду смотреть текст, а не rc.

**Что хорошо у S1, засчитано вперёд.** Отображение слотов выведено ИЗ ДАННЫХ, а не
выдумано: она сама сняла, что сегодня 53 строки с weekday=1 и 3 строки с weekday=4,
и отобразила 1→1, 4→2. Откат назван в файле явным рецептом. Диапазон значений слота
не выдуман. Это ровно то, что просил заход.

**Прогноз.** Закрыто 0 из 5. S1 к 18:00, S4 к 18:15, S2/S3/S5 по влитию S1,
закрытие к 19:40. Дедлайн 20:08.

**Факт против прогноза круга 7.** S1 идёт 17 минут, коммитов ноль. На диске рабочая
миграция и 27 вставок / 20 удалений в `veb/server.py` — работа есть, коммита нет.
Норматив 20 минут истекает в 17:33.

**Что сломалось в МОЕЙ работе.** Ничего этим кругом.

**Модель и время у закрытых позиций.** Закрытых нет.

---

## Круг 9 — 17:32

**Указатель.** Мой, VECHER. Хук: круг 7 из 60, застой 0 из 5.

**СПАСЕНИЕ РАБОТЫ S1.** Позиция шла 18 минут без единого своего коммита, роняя правку
файла в ЧЕТВЁРТЫЙ раз. Забрал её зону в git сам, коммит `016b594`: миграция 003 и
незаконченные правки `veb/server.py` (29 вставок / 22 удаления). По уроку P5 —
незаконченный коммит дешевле потерянных строк. Позиция продолжает работать поверх.

🔴 **ТРЕТЬЯ ЛОВУШКА ЛОЖНО-ЗЕЛЁНОГО ЗА ВЕЧЕР, И ЭТА — В МОИХ СОБСТВЕННЫХ РУКАХ ·
ЦЕНА — работа S1 осталась бы вне git при бодром ✅ · что бы помогло.**
Первый вызов спасения:
  `git_zona.py commit --zone "migrations/ veb/server.py" -m ...`
ответил `✅ Зона чиста — коммитить нечего`. На диске в этот момент лежали
`M veb/server.py` (29/22) и неотслеженная `migrations/003_...sql`.
Причина: ДВА пути одной строкой в `--zone`. Строка «migrations/ veb/server.py»
трактуется как ОДИН путь, не совпадает ни с чем — и «ничего не совпало» печатается
как «зона чиста». Проверил тут же: `check --zone "veb/server.py"` одним путём —
сразу ❌ «правлено и не закоммичено 1».
Верная форма — флаг ПОВТОРЯЕМЫЙ: `--zone "veb/server.py" --zone "migrations/"`.
Так и сделано, коммит прошёл.
ЧТО БЫ ПОМОГЛО: `git_zona.py commit` обязан ОТКАЗЫВАТЬ на аргументе `--zone`,
содержащем пробел, а не молча зеленеть — пробел в пути возможен, но «путь с пробелом,
не совпавший ни с чем» и «зона чиста» обязаны звучать по-разному. Это ровно тот же
класс, что зона-префикс `.../kod_`, что сторож на чужом баннере и что `sqlite3`
с `rc=0` на ошибке разбора. Четыре разных механизма, одна болезнь: НЕСОВПАДЕНИЕ
ПЕЧАТАЕТСЯ КАК УСПЕХ.

**Что сломалось в МОЕЙ работе.** Ровно это: я сам позвал инструмент неверной формой и
сам чуть не принял его ✅ за факт. Спасло то, что я не поверил и сверил `git status`
напрямую — правило «судить по факту, а не по бодрому выводу» сработало на мне самом.

**Прогноз.** Закрыто 0 из 5. S1 к 18:00, S4 к 18:15 (читает свой заход, стартовала
нормально), S2/S3/S5 по влитию S1. Закрытие к 19:40, дедлайн 20:08.

**Факт против прогноза круга 8.** S1 норматив коммита пробила — 18 минут без своего
коммита; закрыто спасением оркестратора, а не ожиданием. Работа не потеряна.

**Модель и время у закрытых позиций.** Закрытых нет.
S1 minimax-m3, старт 17:13, 4 отказа инструмента правки. S4 nemotron-3-super, старт 17:28.

---

## Круг 10 — 17:33

**Указатель.** Мой, VECHER. Хук: круг 8 из 60, застой 0 из 5.

**Состояние позиций, снято командой.**
* **S1** — жива, прошла `veb/server.py` и правит JS распределения (`state.slot`
  вместо `state.day` в `index.html`). Последний коммит ветки — моё спасение `016b594`.
  То есть после спасения она работает поверх сохранённого, обрыв больше ничего не стоит.
* **S4** — жива, но за 5 минут ещё не начала работу: читает свой файл-заход
  кусками по 1–20 строк, десяток чтений подряд. Это цена `nemotron-3-super`:
  модель не удерживает файл целиком и перечитывает его по кускам. Пока не отказ,
  но если к 17:50 работы на диске не будет — поднимаю на другой модели.

**Ожидание переведено на событие.** Армировал ожидание ВЛИТИЯ S1 в `main` — это мои
ворота для запуска S2, S3 и S5. Опрашивать руками перестал: круг 5 уже стоил двух
пустых кругов, повторять эту цену нельзя.

**Прогноз.** Закрыто 0 из 5. S1 к 18:00, S4 к 18:20, S2/S3/S5 стартуют по влитию S1
и идут ~45 минут параллельно. Закрытие к 19:40, дедлайн 20:08. Запас 28 минут.

**Факт против прогноза круга 9.** Держится. S1 после спасения движется по задаче,
S4 стартовала без отказов.

**Что сломалось.** Ничего нового этим кругом · ЦЕНА 0 · —

**Что сломалось в МОЕЙ работе.** Ничего этим кругом.

**Модель и время у закрытых позиций.** Закрытых нет.

---

## Круг 11 — 17:35

**Указатель.** Мой, VECHER. Хук: круг 9 из 60, застой 0 из 5.

🔴 **S4 ВЕРНУЛАСЬ ПУСТОЙ · ЦЕНА 6 минут и одна позиция вхолостую · что бы помогло.**
`nemotron-3-super-120b` шла 6 минут (17:28→17:34) и не сделала НИ ОДНОЙ строки работы:
перечитала файл-заход десятком кусков по 1–20 строк, записала в `## ОТЧЁТ` единственный
символ `0` (вывод команды гигиены), закоммитила эту единицу и вышла. Запускалка честно
сказала «🔴 ВЫШЕЛ БЕЗ ОТЧЁТА С ХЭШЕМ» — сторож готовности сработал, ложно-зелёного не
случилось.
Признак, видимый ЗАРАНЕЕ, я его отметил на круге 10 и не действовал: модель, которая
читает свой заход кусками по одной строке, не удерживает контекст задания и до работы
не дойдёт. Надо было поднимать её тогда, а не через 6 минут.
ЧТО БЫ ПОМОГЛО: правило «десять чтений своего же захода подряд без единой правки на
диске = поднимать позицию, не дожидаясь конца». Признак дешёвый и снимается из лога.
Поднял на `mistral/devstral-latest` — другой провайдер, модель под агентную работу с
инструментами. Пробу живости НЕ гоняю: урок круга 2 — на бесплатном тире проба и есть
расход.

🔴 **НАРУШЕНИЕ ЗОНЫ, названное вслух · ЦЕНА пока 0, но механизм опасен.**
S4 сделала ДВА коммита прямо в `main` (`eb7dec8`, `7a3fa8d`) вместо своей ветки
`zahod/postoyannyj-adres-pages-i-tunnel`. Содержимое безобидное — правка своего же
файла-захода на 1 строку, — но механизм опасен: заход, коммитящий в основную ветку,
обходит контракт зоны целиком. Причина: файл-заход физически лежит в ГЛАВНОЙ рабочей
папке (`spetsmat-bot/zhurnal/…`), а не в worktree позиции, и любая запись в него —
это запись в main.
ЧТО БЫ ПОМОГЛО: это не дефект модели, это устройство арки — файл-заход общий, а работа
в worktree. Либо заход не коммитит свой файл вовсе (его коммитит аналитик при приёмке —
так и написано в каркасе), либо файл-заход обязан жить в worktree. Записываю как долг
фабрики, а не как вину позиции.

**Прогноз.** Закрыто 0 из 5. S1 к 18:00, S4 (второй заход) к 18:20, S2/S3/S5 по влитию
S1. Закрытие к 19:45, дедлайн 20:08. Запас 23 минуты — сократился на 5.

**Факт против прогноза круга 10.** Прогноз «S4 к 18:20» устоял только потому, что я
поднял её сразу; первая попытка потеряна целиком.

**Что сломалось в МОЕЙ работе.** Увидел признак смерти позиции на круге 10, назвал его
в журнале и НЕ ДЕЙСТВОВАЛ — ждал конца. Наблюдение без действия стоило 6 минут.

**Модель и время у закрытых позиций.** Закрытых нет.
S4 попытка 1: nemotron-3-super, 17:28→17:34, ПУСТО. Попытка 2: devstral, старт 17:35.

---

## Круг 12 — 17:36

**Указатель.** Мой, VECHER. Хук: круг 10 из 60, застой 0 из 5.

🔴 **ТРЕТЬЯ МОДЕЛЬ ДЛЯ S4 · ЦЕНА 1 минута · что бы помогло.**
`mistral/devstral-latest` умерла на первой же секунде:
`UnknownError: Unexpected server error, ref err_8cc62b15`. Не rate-limit, а
недоступность модели у провайдера. Поднял S4 в третий раз, на
`openrouter/thinkingmachines/inkling:free` — она была зарезервирована под S2, но S2
всё равно ждёт влития S1, а S4 идёт СЕЙЧАС. Модель S2 подберу по живости в момент её
старта.
ЧТО БЫ ПОМОГЛО: список моделей волны — не назначение раз и навсегда, а ОЧЕРЕДЬ.
Я расписал пять моделей по пяти позициям заранее, и за вечер две из пяти отвалились
(z-ai по квоте, devstral по недоступности), а третья не потянула задачу (nemotron).
Живучесть модели проверяется только запуском, и значит распределение обязано быть
переназначаемым на ходу, а не таблицей в составе.

**Счёт отказов моделей за вечер: 3 из 6 запусков.**
z-ai/glm-5.2 — квота · nemotron-3-super — пустой возврат · devstral — недоступна.
Работают: minimax-m3 (S1, буксует на инструменте правки, но идёт), inkling (S4, старт).

**Состояние S1 — идёт по делу.** Прогнала тесты: `2 failed, 3 passed`. Правит три
файла: `veb/server.py`, `tests/veb/test_server.py`, `veb/templates/index.html`.
То есть дошла до части 1 критерия и чинит красное. Это здоровый ход, а не буксовка.

**Прогноз.** Закрыто 0 из 5. S1 к 18:05, S4 к 18:25, S2/S3/S5 по влитию S1.
Закрытие к 19:45, дедлайн 20:08.

**Факт против прогноза круга 11.** S4 сдвинулась ещё на минуту из-за мёртвой модели.

**Что сломалось в МОЕЙ работе.** Планирование моделей таблицей вместо очереди.

**Модель и время у закрытых позиций.** Закрытых нет.

---

## Круг 13 — 17:36

**Указатель.** Мой, VECHER. Хук: круг 10 из 60, застой 0 из 5.

**Состояние, снято командой.**
* **S1** — думает над двумя красными тестами (`test_post_enrollment_with_same_teacher_is_a_no_change`
  и ещё один), `2 failed, 3 passed`. Работа поверх спасающего коммита `016b594`.
* **S4 попытка 3 (inkling)** — жива и, в отличие от попытки 1, СРАЗУ ушла в свою
  рабочую папку: `cd …-wt/postoyannyj-adres-pages-i-tunnel && git rev-parse` →
  `zahod/postoyannyj-adres-pages-i-tunnel`. То есть в main она коммитить не будет.

**Прогноз.** Без изменений: S1 к 18:05, S4 к 18:25, S2/S3/S5 по влитию S1,
закрытие к 19:45. Дедлайн 20:08.

**Факт против прогноза круга 12.** Держится.

**Что сломалось.** Ничего · ЦЕНА 0 · —

**Что сломалось в МОЕЙ работе.** Ничего этим кругом. Отмечаю темп: настенные часы
идут медленнее моих кругов — хук возвращает меня быстрее, чем позиции успевают
что-либо сделать. Круги 10–13 уложились в одну минуту настенного времени.
ЧТО БЫ ПОМОГЛО: держать круг коротким и не повторять снятие состояния, если с
прошлого круга прошло меньше минуты — событие всё равно придёт ожидателем.

**Модель и время у закрытых позиций.** Закрытых нет.

---

## Круг 15 — 17:38

**Указатель.** Мой, VECHER. Хук: круг 12 из 60, застой 0 из 5.

**Состояние, снято командой.**
* **S1** — лог   100120 байт. Хвост лога не менялся три круга подряд: висит на двух
  красных тестах (`test_post_enrollment_with_same_teacher_is_a_no_change` и второй).
  Ветка стоит на моём спасающем коммите `016b594` — своих коммитов у неё по-прежнему НЕТ,
  25 минут от старта.
* **S4 попытка 3 (inkling)** — РАБОТАЕТ ПО СУЩЕСТВУ: правит `ops/storozh_sajta.py`,
  в диффе видна ветка `мёртв: status=%d` с чтением ТЕЛА ответа. То есть позиция взялась
  ровно за то, за что была вердиктом «доработка»: сторож, зеленевший на чужом баннере,
  учится судить по содержимому. Лог    42075 байт.

**Прогноз.** S1 к 18:10 (сдвиг +5, висит на тестах), S4 к 18:25, S2/S3/S5 по влитию S1,
закрытие к 19:50. Дедлайн 20:08. Запас 18 минут — тает.

**Факт против прогноза круга 14.** S1 сдвинулась ещё на 5 минут.

🔴 **РЕШЕНИЕ, ПРИНЯТОЕ СЕЙЧАС, чтобы не повторить цену S4.**
Урок У11 говорит: признак застрявшей позиции снимается из лога заранее, и ждать конца
дорого. У S1 признак другой, но такой же читаемый — хвост лога не меняется три круга,
своих коммитов ноль за 25 минут при пяти отказах инструмента правки.
Ставлю себе ЯВНУЮ ОТСЕЧКУ: если к 17:50 у S1 не появится СВОЕГО коммита, поднимаю её
добором (`--dobor`) на другой модели. Работа не потеряется — она вся в `016b594`,
а добор велит продолжать с места обрыва, а не начинать с нуля.
Отсечка записана ДО срока, а не придумана после — иначе это не решение, а оправдание.

**Что сломалось в МОЕЙ работе.** Ничего этим кругом.

**Модель и время у закрытых позиций.** Закрытых нет.

---

## Круг 16 — 17:39

**Указатель.** Мой, VECHER. Хук: круг 13 из 60, застой 0 из 5.

**S4 ЗАКОММИТИЛА ЧАСТЬ 1** — `c3d81ac` «сторож перестаёт врать». Принял ПО РЫЧАГУ,
не по отчёту: прочитал сам дифф и код.
Суть правки верна: заведён `OUR_CONTENT_MARKER = "Распределение"`, и теперь
200 + маркер → `жив`, 200 БЕЗ маркера → `мёртв: content_marker_MISSING (banner?)`.
Это ровно закрывает дефект, за который позиция получила «доработку»: сторож зеленел
на `https://console.serveo.net`, отдававшем 200 без нашего содержимого.

🔴 **ОШИБКА МОЕЙ ЖЕ ПРОВЕРКИ, ПОЙМАНА НА СЕБЕ · ЦЕНА 1 круг · что бы помогло.**
Первым делом я прогнал сторожа так:
`SPETSMAT_ADRES="https://console.serveo.net" python3 ops/storozh_sajta.py`
и получил `RED ADRES.txt missing`. Я чуть не записал это как «сторож краснеет на
баннере — критерий выполнен». Но краснел он НЕ НА БАННЕРЕ: адрес сторож берёт из
файла `deploy/ADRES.txt` (строка 20), а переменную `SPETSMAT_ADRES` не читает вовсе.
В рабочей папке позиции этого файла нет, и сторож краснел на ЕГО ОТСУТСТВИИ.
То есть я поставил бы зелёную галочку по красному, полученному не оттуда — ровно
зеркало болезни У9, только с другой стороны: СОВПАДЕНИЕ ОЖИДАЕМОГО ЦВЕТА, полученное
не тем механизмом.
ЧТО БЫ ПОМОГЛО: проверяя критерий, снимать не только ЦВЕТ, но и ПРИЧИНУ цвета.
Красное по нужной причине и красное по любой причине — разные вещи. Записываю
пятнадцатым уроком.

🔴 **СЦЕПКА МЕЖДУ ПОЗИЦИЯМИ, найдена до влития · ЦЕНА предотвращённая —
ложно-КРАСНЫЙ сторож на здоровом сайте · дом: приёмка.**
Маркер `"Распределение"` — это заголовок страницы `index.html`. А позиция S1 прямо
сейчас переносит распределение с `/` на `/raspredelenie`, и на `/` встаёт
`glavnaya.html`, которую пишет S3. Если `deploy/ADRES.txt` указывает на корень, то
после влития S1 и S3 маркер с корневой страницы ИСЧЕЗНЕТ, и починенный сторож начнёт
рапортовать `мёртв` о живом сайте. Тревога преподавателей от ложной тревоги — ровно
то, что владелец назвал дороже функционала.
Ни одна из трёх позиций этого не увидит: каждая права в своей зоне. Это видно только
отсюда.
ЧТО ДЕЛАЮ: беру на СЕБЯ при финальном влитии — маркер обязан совпасть с тем, что
реально отдаёт адрес из `ADRES.txt` после влития всех веток. Проверю рычагом, а не
рассуждением.

**Прогноз.** S1 к 18:10 (отсечка на добор 17:50 в силе), S4 к 18:25,
S2/S3/S5 по влитию S1, закрытие к 19:50 при дедлайне 20:08.

**Факт против прогноза круга 15.** S4 обогнала прогноз: часть 1 закрыта на 5 минут
раньше ожидаемого. S1 без изменений, лог 100 КБ, стоит на тех же двух тестах.

**Что сломалось в МОЕЙ работе.** Проверил критерий не тем механизмом и почти принял
результат. Поймал сам, до записи вердикта.

**Модель и время у закрытых позиций.** Закрытых нет. S4 часть 1 — inkling, 17:35→17:39.

---

## Круг 17 — 17:41

**Указатель.** Мой, VECHER. Хук: круг 15 из 60, застой 0 из 5.

**S4 ВЕРНУЛАСЬ И ВЕРНУЛАСЬ ЧЕСТНО.** Часть 1 сделана и влита (`c3d81ac` → `614d2d7`),
часть 2 названа НЕСДЕЛАННОЙ списком. Это законный исход, объявленный в самом заходе, и
он ценнее рапорта о готовности. Влитие проверено ФАКТОМ, а не словом:
`grep -c OUR_CONTENT_MARKER ops/storozh_sajta.py` в main → 2.

🔴 **ПРИЧИНА, ПО КОТОРОЙ ЧАСТЬ 2 БЫЛА НЕВЫПОЛНИМА — И ОНА НЕ В ПОЗИЦИИ ·
ЦЕНА один прогон позиции целиком · что бы помогло.**
S4 написала «`deploy/ADRES.txt` отсутствует». Это ПРАВДА ПРО ЕЁ ПАПКУ и НЕПРАВДА про
репозиторий: файл лежал в главной рабочей папке, но НЕ БЫЛ В GIT, а неотслеженный файл
в worktree не попадает. Позиция физически не могла увидеть адрес туннеля.
ЧТО БЫ ПОМОГЛО: перед волной прогонять `git_zona.py check` по зонам ВСЕХ позиций и
забирать в git всё, что позиции обязаны видеть. Неотслеженный файл рядом с worktree
для захода не существует, и он честно доложит, что этого нет.
Забрал файл в git (`6cfce9c`), вписал ПРАВКУ 1 в заход, поднял S4 добором.
**Это шестнадцатый урок волны.**

🔴 **ВТОРОЕ РЕШЕНИЕ КРУГА, отступающее от порядка — называю вслух.**
Отпустил S3 (`glavnaya-i-stranicy-listkov`) НЕ ДОЖИДАЯСЬ влития S1.
Причина: S1 висит 28 минут, лог замер ровно на 100120 байтах несколько кругов подряд,
своих коммитов ноль. Из-за неё ТРИ позиции из пяти стоят без дела полчаса — это и есть
настоящая цена ожидания.
Почему именно S3 и почему это безопасно: её работа — четыре шаблона в `veb/templates/`,
контракт имён (`glavnaya.html` · `listki.html` · `listki8.html` · `urovni.html`)
зафиксирован ПИСЬМЕННО в её задаче и от кода S1 не зависит. Зоны не пересекаются.
Почему НЕ отпускаю S2 и S5: S2 читает схему напрямую — без слота ей работать не с чем;
S5 правит `veb/server.py`, который S1 держит прямо сейчас, и это был бы конфликт.
Риск, который я принимаю: критерий S3 «страница открыта в браузере» не позеленеет,
пока роуты S1 не влиты. Позиция напишет шаблоны, а предъявление добьём после влития.

**Отсечка по S1 остаётся 17:50** — она записана на круге 15 и я её не двигаю задним
числом. Сейчас 17:41.

**Прогноз.** S1 к 18:15 (или добор в 17:50), S4 часть 2 к 18:05, S3 к 18:20,
S2/S5 по влитию S1. Закрытие к 19:50, дедлайн 20:08.

**Факт против прогноза круга 16.** S4 вернулась в 17:40 — раньше прогноза 18:25, но
половиной работы. S1 провалила прогноз полностью.

**Что сломалось в МОЕЙ работе.** Не проверил перед волной, что всё нужное позициям
лежит в git. Стоило это одного прогона S4 целиком.

**Модель и время у закрытых позиций.**
S4 часть 1 — inkling, 17:35→17:40, часть 1 принята по рычагу, часть 2 в доборе.

---

## Круг 18 — 17:42

**Указатель.** Мой, VECHER. Хук: круг 16 из 60, застой 0 из 5.

**Три позиции в работе одновременно — впервые за волну.**
* **S1** — лог замер на 100120 байтах шестую минуту подряд. Процесс opencode:
  `29:49 0.2`. Отсечка 17:50 в силе.
* **S3 (laguna)** — стартовала, в её `veb/templates/` пока только `index.html`,
  то есть работа ещё не легла на диск.
* **S4 добор (inkling)** — поднялась, процесс жив.

**Прогноз.** S1 — добор в 17:50, если лог не сдвинется. S4 часть 2 к 18:05,
S3 к 18:20, S2/S5 по влитию S1. Закрытие к 19:50, дедлайн 20:08.

**Факт против прогноза круга 17.** Держится.

**Что сломалось.** Ничего нового · ЦЕНА 0 · —

**Что сломалось в МОЕЙ работе.** Ничего этим кругом.

**Модель и время у закрытых позиций.** Закрытых нет; S4 часть 1 принята (inkling, 5 минут).

---

## Круг 19 — 17:43

**Указатель.** Мой, VECHER. Хук: круг 17 из 60, застой 0 из 5.

🔴 **ЧЕТВЁРТЫЙ ОТКАЗ МОДЕЛИ · ЦЕНА 2 минуты · подтверждает урок У13.**
S3 умерла на первой секунде: `[Poolside] poolside/laguna-s-2.1:free is temporarily
rate-limited upstream`. Поднял на `groq/openai/gpt-oss-120b` — ДРУГАЯ инфраструктура
целиком, не OpenRouter.
**Счёт отказов моделей: 4 из 8 запусков — ровно половина.**
z-ai/glm-5.2 квота · nemotron-3-super пустой возврат · mistral/devstral недоступна ·
poolside/laguna квота. Работали: minimax-m3 (буксует), inkling (две части подряд).
Урок У13 «модели — очередь, а не таблица» подтверждён замером, а не рассуждением:
половина назначенных заранее моделей не дожила до своей позиции.

🟢 **S4 В ДОБОРЕ СДЕЛАЛА РОВНО ТО, ЧТО НАДО — и это стоит записать как успех механизма.**
Коммит `23ed51d`: «Fix сторож content-marker path: check /raspredelenie per correction 1
(post-S1)». То есть позиция ПРОЧИТАЛА ПРАВКУ 1 и сама закрыла сцепку, которую я нашёл на
круге 16 и которую не видела ни одна позиция изнутри своей зоны: маркер «Распределение»
после переезда исчезнет с корня, и сторож начнёт врать в обратную сторону — рапортовать
мёртв о живом сайте.
ЧТО ЭТО ДОКАЗЫВАЕТ: механизм ПРАВКИ ПОСЛЕ ВЫДАЧИ работает — но только на позиции,
которую поднимают ЗАНОВО. Правка, вписанная в файл работающего захода, до него не
доезжает: он файл не перечитывает. Здесь сработало потому, что был добор.

**Прогноз.** S1 — отсечка 17:50, лог замер седьмую минуту. S4 часть 2 к 18:00,
S3 (попытка 2) к 18:25, S2/S5 по влитию S1. Закрытие к 19:50, дедлайн 20:08.

**Факт против прогноза круга 18.** S3 провалила старт целиком, сдвиг +5 минут.
S4 обгоняет прогноз.

**Что сломалось в МОЕЙ работе.** Ничего этим кругом; отказ модели — не моя работа,
но выбор «пять моделей заранее таблицей» был мой, и он стоил половины запусков.

**Модель и время у закрытых позиций.** Закрытых нет.

---

## Круг 20 — 17:46

**Указатель.** Мой, VECHER. Хук: круг 18 из 60, застой 0 из 5.

🔴🔴 **ГЛАВНЫЙ ЗАМЕР ВОЛНЫ: ТУННЕЛЬ НЕ ДАЁТ ПОСТОЯННОГО АДРЕСА. Прогнал сам,
потому что до него не дошла ни одна позиция.**

Ни S4, ни её предшественница туннель не поднимали — обе упирались в отсутствие
`ADRES.txt` в git. Поднял сайт и туннель своими руками и прогнал критерий владельца
дословно: «адрес предъявлен ДВАЖДЫ с перезапуском процесса между предъявлениями».

**ТЕСТ A — перезапуск процесса САЙТА, туннель не трогаем. ✅ ПРОЙДЕН.**
```
адрес                     https://598c6edac618b6.lhr.life
предъявление 1            → 302
сайт убит (pid 93172)     → во время простоя: 000, «Empty reply from server»
сайт поднят заново        pid 93535
предъявление 2, ТОТ ЖЕ адрес → 302
```
Пустой ответ во время простоя доказывает, что проверка настоящая, а не из кэша.
302 — это ответ НАШЕГО сервера (шлагбаум входа), а не баннер провайдера: туннель
доходит до сайта.

**ТЕСТ B — перезапуск процесса ТУННЕЛЯ. 🔴 ПРОВАЛЕН, и это главный факт вечера.**
```
адрес ДО                  https://598c6edac618b6.lhr.life
туннель убит              старый адрес → 503
туннель поднят заново     bash deploy/tunnel.sh 8765
адрес ПОСЛЕ               https://f0a4bf9fecabd2.lhr.life   ← ДРУГОЙ
новый адрес               → 302 (работает, но он новый)
```
`localhost.run` на бесплатном тире выдаёт НОВЫЙ поддомен на каждое соединение.
Значит по критерию владельца — дословно «иначе это не постоянный адрес, а
сегодняшний» — ТУННЕЛЬ КРИТЕРИЮ НЕ УДОВЛЕТВОРЯЕТ. Пока живёт процесс, адрес живёт;
процесс перезапустился — ссылка, которую вы раздали преподавателям, умерла.

**ЧТО ЭТО ЗНАЧИТ ДЛЯ ВОЛНЫ.** Второе из двух горящих дел владельца — «страница по
постоянному адресу» — туннелем НЕ закрывается в принципе, а не по недоделке. Остаётся
GitHub Pages (у него адрес действительно постоянный) либо платный/резервированный
поддомен у туннель-провайдера. Оба требуют решения и учётки ВЛАДЕЛЬЦА.
Это вопрос владельцу, а не задача, которую можно домучить — и он идёт в нижнюю
половину мандата как `[V1]`.

**Что сломалось.** Ничего в моей работе; сломано ДОПУЩЕНИЕ волны, что туннель годится
под «постоянный адрес». Допущение прожило от ночного хэндоффа до этой минуты
непроверенным, потому что критерий «предъявить дважды с перезапуском» никто не прогнал.
ЦЕНА: вся позиция S4 и её предшественница целились в цель, которой там нет.
ЧТО БЫ ПОМОГЛО: критерий, который ОПРОВЕРГАЕТ допущение, прогонять ПЕРВЫМ, до работы
по нему — это два curl и один kill, пять минут против двух позиций.
**Это семнадцатый урок волны.**

**Прогноз.** S1 отсечка 17:50 (лог замер 10 минут). S3 попытка 3 на dots. Закрытие
к 19:40 — сдвинул РАНЬШЕ: часть работы отпала как невыполнимая, её не надо доделывать.

**Модель и время у закрытых позиций.** S4: часть 1 принята (inkling), часть 2 —
отвечено замером оркестратора, не позицией.

---

## Круг 21 — 17:49

**Указатель.** Мой, VECHER. Хук: круг 20 из 60, позиций без вердикта 3 — вердикт S4
хук УВИДЕЛ, счётчик упал с 4 до 3. Механизм записи вердикта работает.

**ОТСЕЧКА ПО S1 СРАБОТАЛА — та самая, что записана на круге 15 ДО срока.**
Доказательства смерти, три независимых:
* лог замер ровно на 100120 байтах — двенадцать минут;
* процесс `69588`: `etime 34:39`, `%cpu 0.9` — не считает, ждёт;
* своих коммитов ноль за 36 минут.
Спас работу вторым коммитом `5873a0f` (`veb/server.py` · `tests/veb/test_server.py` ·
`veb/templates/index.html`), остановил процесс, поднял добором на
`deepseek-v4-flash-free` — шестая модель волны.
Добор велит продолжать С МЕСТА ОБРЫВА, и место обрыва теперь в git целиком: два
спасающих коммита, `016b594` и `5873a0f`.

🔴 **ЗАМЕЧЕНО ПРИ СПАСЕНИИ: S1 ПРАВИЛА ЧУЖУЮ ЗОНУ · ЦЕНА пока 0, риск конфликта ·
что бы помогло.**
`veb/templates/index.html` — это зона S3, а не S1. Формально нарушения нет: я САМ
написал в задаче S1 «если её JS шлёт weekday, поменяй ТОЛЬКО имя параметра,
минимальной правкой». То есть я выдал двум позициям право на один файл, и сделал это
осознанно, но не назвал этого ни S3, ни в составе.
Риск реальный: S3 работает в `veb/templates/` прямо сейчас. Если она тронет
`index.html`, будет конфликт при влитии.
ЧТО БЫ ПОМОГЛО: разрешение «тронуть один файл чужой зоны» обязано быть НАЗВАНО ОБЕИМ
позициям, а не только той, кому дано. Я предупредил S3 про `vhod.css` (зона S5) и
забыл про `index.html` (зона S3, отдана S1). Асимметрия моего же внимания.
**Это восемнадцатый урок волны.**

**Счёт моделей: 5 отказов из 10 запусков.**
z-ai квота · nemotron-3-super пусто · devstral недоступна · laguna квота ·
gpt-oss-120b лимит 8000 токенов в минуту при заходе на 8515 · minimax-m3 зависла.
Работали: inkling (S4, две части), dots (S3, идёт), deepseek (S1, старт).

**Прогноз.** S1 добор к 18:20, S3 к 18:15, S2 по влитию S1, S5 после S1.
Закрытие к 19:30. Дедлайн 20:08.

**Факт против прогноза круга 20.** S1 не ожила сама, как и следовало из замера CPU.

**Что сломалось в МОЕЙ работе.** Асимметрия предупреждений о пересечении зон.

**Модель и время у закрытых позиций.** S4 — принято, inkling, 17:35→17:44 в два прогона.

---

## Круг 22 — 17:50

🔴 **ЛОВУШКА В ФОРМЕ ИМЕНИ МОДЕЛИ · ЦЕНА 1 минута и один запуск · что бы помогло.**
Поднял S1 на `deepseek-v4-flash-free` — модель из списка `modeli.py`, то есть законная.
Заход умер за 6 секунд:
```
⚠ Bedrock-доступа на машине нет — иду штатной авторизацией claude, модель «deepseek-v4-flash-free».
[claude-code:unrecognized_model] {"model":"deepseek-v4-flash-free"}
```
Причина в запускалке, строки выбора движка:
```
case "$MODEL" in
  *:free|*/*) DVIZHOK=opencode ;;
  *)          DVIZHOK=claude ;;
esac
```
Движок выбирается ПО ФОРМЕ ИМЕНИ. `deepseek-v4-flash-free` не содержит ни слэша, ни
`:free` — суффикс `-free` через ДЕФИС под шаблон не подходит, — и модель ушла в `claude`,
который её не знает. А `modeli.py` эту модель принимает как валидную бесплатную.
То есть ДВА инструмента одной волны расходятся в том, что считать бесплатной моделью:
один судит по списку, другой по форме строки.
ЧТО БЫ ПОМОГЛО: движок обязан выбираться по СПИСКУ `modeli.py`, а не по виду строки.
Пока не починено — брать только имена со слэшем или `:free`.
**Это девятнадцатый урок волны.**

Поднял S1 в третий раз, на `openrouter/minimax/minimax-m2.7:free` — форма имени верная.

**Счёт моделей: 6 отказов из 11 запусков.**

**Прогноз.** S1 к 18:25, S3 к 18:15, S2 по влитию S1, S5 после S1. Закрытие к 19:30.

**Что сломалось в МОЕЙ работе.** Взял имя модели из списка, не сверив с формой,
которую требует запускалка. Список и запускалка расходятся, и это не моя вина, но
проверить форму перед запуском было моей работой.

---

## Круг 23 — 17:51

**Указатель.** Мой, VECHER. Хук: круг 21 из 60, без вердикта 3.

**ТРИ ПОЗИЦИИ В РАБОТЕ ОДНОВРЕМЕННО.** S1 (m2.7, добор) · S2 (north-mini-code) ·
S3 (dots). Впервые волна идёт в три ствола.

**РЕШЕНИЕ КРУГА: отпустил S2, не дожидаясь влития S1** — третье отступление от
порядка состава, и снова называю причину.
Разобрал задачу S2 по зависимостям, а не по формуле «после S1»:
* часть 0 (замер «до») и **часть 1 (инструмент проверки)** — от схемы НЕ зависят:
  считают строки в `enrollment` по `student_id`, имя второй колонки им безразлично.
  Это ГЛАВНАЯ часть задачи S2 — та, ради которой позиция и существует;
* часть 2 (ушли двое, пришёл один) — это таблица `students`, схемы не касается;
* часть 3 (достройка второго слота) — зависит, и только она.
Вписал ПРАВКУ 1: делать 0–2 сразу, а по части 3 сначала посмотреть КОМАНДОЙ, какая
колонка реально в базе, и не выдумывать миграцию — это чужая зона.
Держу только S5: она правит `veb/server.py`, который S1 держит прямо сейчас,
и это был бы конфликт, а не параллельность.

**ЧТО ЭТО ЗА ПРИЁМ, ЧТОБЫ НЕ ПОВТОРЯТЬ ОШИБКУ КАЖДЫЙ РАЗ.**
Порядок «S1 первой и одна» был записан как СВОЙСТВО ПОЗИЦИИ, а на деле это свойство
ОТДЕЛЬНЫХ ЧАСТЕЙ позиций. Из пяти позиций реально ждали S1 не четыре, а полторы:
часть 3 у S2 и целиком S5. Остальное могло идти с самого начала.
ЦЕНА уже понесённая: S2 и S3 простояли по 35 минут на пустом месте.
ЧТО БЫ ПОМОГЛО: в составе волны писать зависимость НЕ «позиция за позицией», а
«часть за частью» — тогда параллельность видна сразу, а не выводится на 23-м круге.
**Это двадцатый урок волны.**

**Прогноз.** S1 к 18:25, S2 к 18:20, S3 к 18:15, S5 по влитию S1. Закрытие к 19:30.
Дедлайн 20:08.

**Факт против прогноза круга 22.** Держится.

**Что сломалось в МОЕЙ работе.** Принял порядок состава буквально и продержал две
позиции без дела 35 минут, вместо того чтобы разобрать зависимости по частям сразу.

**Модель и время у закрытых позиций.** S4 — принято, inkling.

---

## Круг 24 — 17:52

**Указатель.** Мой, VECHER. Хук: круг 22 из 60, без вердикта 3.

**S3 НАПИСАЛА ГЛАВНУЮ — предмет дословной жалобы владельца.** 82 строки,
`veb/templates/glavnaya.html`. Разобрал её сам, ещё до отчёта позиции.

**ЧТО СДЕЛАНО ПО СПЕКЕ — засчитано вперёд:**
* оба вопроса на ПЕРВОМ экране: «Сейчас идёт — 16 · Деревья · сдать лучше к 10 сентября»
  и «Куда идти — свой кабинет и принимающего найдёшь в распределении, по фамилии»
  с полем поиска прямо на главной;
* внизу МЕЛКО — уровни · листки 8 класса · распределение, ровно как просила спека;
* никаких приветствий и описаний курса. Их нет вовсе;
* про данные честно: «16 · **одна версия на диске**» — не выдумала трёх версий,
  которых нет. Это прямое требование, и оно выполнено.

**ПРОВЕРИЛ ИСТОЧНИКИ ДАННЫХ КОМАНДОЙ, а не поверил на глаз:**
* «10 сентября, дедлайн мягкий» → дословно из
  `teksty/2026-09-04_post-pro-tri-listka.md:26`. НЕ выдумано ✅
* «понедельник и четверг» → совпадает с базой: S1 намерила `weekday=1` (53 строки) и
  `weekday=4` (3 строки). НЕ выдумано ✅

🔴 **НАЙДЕНО ВЫДУМАННОЕ · ЦЕНА — неверное время на первом экране у всего класса ·
что бы помогло.**
`«Время 16:00 – 19:00 (МСК)»` — ИСТОЧНИКА НЕТ. Прогон
`grep -rn "16:00\|19:00" ../materials/spetsmat-2026/*.md` → ПУСТО.
Позиция получила прямой запрет выдумывать и почти везде его соблюла, но час занятий
взяла из головы. Школьник прочтёт его как факт.
Туда же вторая строка: `«Следующее — четверг, 16:00»` вписана В ШАБЛОН ЖЁСТКО, а не
считается от даты. В пятницу она будет врать.
ЭТО РОВНО «НАДЁЖНОСТЬ ВЫШЕ ФУНКЦИОНАЛА»: страница, уверенно называющая неверное время
занятия, хуже страницы без времени.
ЧТО БУДУ ТРЕБОВАТЬ ПРИ ПРИЁМКЕ: либо источник на время, либо строку убрать; «следующее»
— считать от даты либо убрать. Вопрос о реальном времени занятий уйдёт владельцу.
**Это двадцать первый урок волны.**

**Ещё одна сцепка, замечена сейчас.** Поиск на главной ведёт на `/raspredelenie?q=<фамилия>`.
Роута `/raspredelenie` в `main` ЕЩЁ НЕТ (`grep` по `veb/server.py` пуст), а параметра `q`
S1 не обещала вовсе — в её задаче про `q` ни слова. То есть кнопка «Найти себя» после
влития приведёт на страницу, которая фамилию проигнорирует. Держу на приёмку.

**Состояние прочих.** S1 (m2.7) идёт молча. S2 (north-mini-code) своего инструмента в
`tools/` ещё не создала.

**Прогноз.** S1 к 18:25, S2 к 18:20, S3 к 18:10, S5 по влитию S1. Закрытие к 19:30.

**Что сломалось в МОЕЙ работе.** Ничего этим кругом.

---

## Круг 25 — 17:54

**Указатель.** Мой, VECHER. Хук: круг 23 из 60, без вердикта 3.

🔴🔴 **СТРУКТУРНЫЙ ДЕФЕКТ УСТРОЙСТВА ВОЛНЫ · ЦЕНА — ВСЕ ЧЕТЫРЕ ПОЗИЦИИ, работающие с
данными, были обречены · что бы помогло.**

S2 упала: `sqlite3.OperationalError: unable to open database file`. Разобрал:
```
git ls-files data/            → ПУСТО (база не в git)
.gitignore:7   data/
.gitignore:9   *.db
ls .../spetsmat-bot-wt/sostav-klassa-i-112-strok/data/  → No such file or directory
```
База НАМЕРЕННО не в git — там данные класса, и это правильное решение. Но заходы
работают в WORKTREE, а worktree содержит только то, что в git. Значит **ни одна
позиция волны не видела базы вообще**.
Это то же самое, на чём умерла S4 с `ADRES.txt` (урок У16), только больнее: там был
один файл, здесь — предмет работы трёх позиций из пяти.
* S1 не могла прогнать миграцию на реальных данных;
* S2 не могла снять ни одного из пяти спорных чисел;
* S5 не могла поднять сервер с данными.
И ни одна из них не могла об этом догадаться: они видели пустоту и честно докладывали,
что данных нет.

**ЧТО СДЕЛАЛ.**
1. **Резерв живой базы** — `data/REZERV-pered-volnoj-VECHER-1753.db`, 3 874 816 байт.
   Точка возврата есть до того, как кто-либо тронул данные.
2. **Копию** базы (`spetsmat.db` + `roster.db`) положил в `data/` КАЖДОГО из четырёх
   worktree. Проверил: S2 теперь читает `строк 56 детей 53` — те же числа, что в живой.
3. **Живую базу позициям НЕ отдал.** Причина: S1 (схема) и S2 (строки) работают
   ОДНОВРЕМЕННО, и две параллельные записи в один файл SQLite — это блокировки и
   риск повреждения. Позиции меряют и проверяют идемпотентность на копиях;
   миграцию и достройку на ЖИВУЮ базу применю САМ при сведении, их же скриптами,
   имея резерв. Это и есть «надёжность выше функционала» в моём собственном ходе.

ЧТО БЫ ПОМОГЛО: перед волной прогонять по зонам всех позиций проверку «а видно ли из
worktree то, с чем позиция обязана работать». Всё, что в `.gitignore`, для захода не
существует — и он честно доложит, что этого нет.
**Это двадцать второй урок волны, и он же самый дорогой по охвату.**

**Прогноз.** S1 к 18:25, S2 к 18:25 (перезапуск не нужен — база появилась под ней),
S3 к 18:10 (пишет третий шаблон), S5 по влитию S1. Закрытие к 19:30.

**Что сломалось в МОЕЙ работе.** Не проверил доступность ПРЕДМЕТА РАБОТЫ из worktree —
хотя ровно этот урок уже поймал полчаса назад на `ADRES.txt` и записал его. Поймал
частный случай и не обобщил. Это дороже самого дефекта.
