# Канал исполнителя — bystro-i-bezopasno (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — продукт захода — ДИАГНОЗ, а не готовая починка: план рождается по ходу замеров.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Opus 5 — продукт захода ДИАГНОЗ, а не расписанная починка: план рождается по ходу замеров.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/bystro-i-bezopasno.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/2026-09-02_spetsmat-bot/kod_bystro-i-bezopasno.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add bystro-i-bezopasno --branch zahod/bystro-i-bezopasno, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.

Заход мог быть ПОПРАВЛЕН после того, как ты стартовал: аналитик правит
тот же файл, а ты прочитал его один раз. Смотри секцию ## ПРАВКИ ПОСЛЕ
ВЫДАЧИ в конце файла — при старте и всякий раз, когда владелец пишет
тебе в чат. Правка с номером, которого ты не читал, отменяет любое
противоречащее ей место выше.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
ГРАНИЦА ОБЛАСТИ: сырые подстроки в `kod_*.md` (пункт 4) — НЕ парсер очереди `dostavit_urok` (который считает только пары ДОМ:/ДОСТАВЛЕНО:). Разница в числах — законна.
🔴 снимок при сборке 2026-09-08, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/schet_nezakrytogo.py zhurnal/2026-09-02_spetsmat-bot`
Область: «zhurnal/2026-09-02_spetsmat-bot» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — ни одного skills/*/DOLG.md нет на диске (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 31
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 329
     из них разбором очереди (парсер `dostavit_urok`, записи с парой ДОМ:/ДОСТАВЛЕНО:): 189
       живых (чинится доставкой — «дом есть»)  : 49
       к владельцу (решение за человеком)      : 75
       адрес недоступен (нет/папка/код/указат.) : 63
       адрес не разобран                        : 0
       отработавших (машинный след закрытия)    : 0
       доставлено                               : 2
       🔴 не проверяется машиной: содержательная отработанность записей БЕЗ следа закрытия (метки в доме, строки ✅/ЗАКРЫТО) — нужна ревизия человеком; сырой греп сверх разбора — шаблонные строки формы.

🔴 ДВЕРЬ НЕЗАКОММИЧЕННЫХ `kod_*.md` ОТКРЫТА КЛАПАНОМ: в арке `zhurnal/2026-09-02_spetsmat-bot` есть 8 незакоммиченных файлов-заходов `kod_veb-priyom-zadach.md`, `kod_dozabivka-dolgov-sklejki.md`, `kod_dve-stranicy-raspredelenia.md`, `kod_https-i-domen.md`, `kod_listki-kak-baza-zadach.md`, `kod_profil-bezopasnosti.md`, `kod_sloj-zanyatia.md`, `kod_vygruzka-v-tablicu.md` — второй заход в такую арку обычно не собирается (долг 4 `disciplina-git`), но АНАЛИТИК открыл клапан, причина дословно: «живой инцидент: сайт у владельца открывается минутами, занятие 10.09 в 13:10; грязь — восемь kod_*.md зоны аналитика, на них подана блокирующая заявка 2026-09-08T1327, разбирается приёмкой ЭТОЙ ЖЕ сессии параллельно». Отключение видно здесь, в артефакте, а не осталось решением в голове аналитика.

КОНТЕКСТ. Сайт спецмата (`math-kluychiki.ru`, он же `159.194.254.52`) — рабочий инструмент пятнадцати преподавателей: плюсики тапом, распределение по дням, листки базой задач. Прошлый этап: ночью 08.09 отработал заход `vygruzka-v-tablicu`, утром в 05:40 — `profil-bezopasnosti`; последний правил nginx ЖИВЬЁМ на сервере (в git его конфига нет) и выкатил `veb/vhod.py` через `deploy/vykatka.sh`. ЦЕЛЬ: назвать ПРИЧИНУ, по которой у владельца сайт открывается минутами, и предложить решение, при котором профиль безопасности Google остаётся целым, а сайт быстрым.

🔴 **СТОП ДО ЦЕЛИ.** Получишь ДИАГНОЗ и предложение. НЕ получишь: снятой пометки Google — кнопку «Запросить проверку» жмёт владелец и только после этого захода (решение интервью п.3).

Приёмка — по ОТЧЁТУ, без построчной сверки.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-09-08) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Владелец открывает сайт по https://math-kluychiki.ru — то есть через 443
2. Откат вслепую ЗАПРЕЩЁН: решение обязано сохранить требования Google по безопасности И дать скорость
3. Кнопку «Запросить проверку» в Search Console НЕ жать, пока тормоза не разобраны
4. Работаем параллельно с приёмкой двух заходов, которую ведёт аналитик
5. Занятие в четверг 10.09 в 13:10 — крайний срок работоспособности сайта

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../spetsmat-bot-wt/bystro-i-bezopasno`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/bystro-i-bezopasno`. 🔴 **ДАЛЬШЕ — ТОЛЬКО ПУТИ ОТНОСИТЕЛЬНО ЭТОЙ ПАПКИ** (или `cd` в неё безусловно, каждым ходом): абсолютный путь в главную папку репозитория здесь — типичная ошибка, правка утекает МИМО worktree и найдётся только на коммите («вне git» в `git_zona.py check --zone` из рабочей папки, на файле, который уже правил, — цена, оплаченная живьём: 5 файлов, ручное копирование и откат главной папки). 🔴 **Заводишь её САМ — ВТОРЫМ ходом, СРАЗУ ПОСЛЕ пункта 1 блока §0.1 ГИТ-КОНТУР ниже (не первым ходом захода целиком), из корня репозитория:**
  ```
  python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add bystro-i-bezopasno --branch zahod/bystro-i-bezopasno
  cd ../spetsmat-bot-wt/bystro-i-bezopasno
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце вливает САМ ЗАХОД, последним ходом** (решение 20.08, заход `granica-priyomki`, пересмотр решения 05.08; WARNING-блок ниже печатает тот же порядок): цепочкой коммит зоны → пуш → влитие в основную (`git_zona.py vlit-v-osnovnuyu`) → пост-проверка ИЗ ГЛАВНОЙ папки → гашение СВОЕЙ ветки. Причина пересмотра — замер 20.08: изнутри захода не видно разницы между «механизм новый в рабочей папке» и «в `main` он старый», а приёмке это невидимо по построению. Влитие УСЛОВНОЕ: красная пост-проверка = откат влития самим заходом и строка в отчёт, а не «доложу, пусть приёмка решает». Файл-заход по-прежнему НЕ коммитить — его коммитит аналитик при приёмке.
- **ЗОНА (можно менять):** `veb/vhod.py` `deploy/` `ops/` `tests/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4); коммить ПО ХОДУ работы, не одним последним ходом (§4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **SCRATCHPAD — ТОЛЬКО ЛИЧНЫЙ.** Черновики, выкладки, промежуточные версии — в личную папку СВОЕГО захода `scratchpad/bystro-i-bezopasno/`. Общие пути (`scratchpad/otchet.md`, любой `scratchpad/*` без имени твоей темы) ЗАПРЕЩЕНЫ: чужой отчёт уедет в твой файл или твой — в чужой, а приёмка читает отчёт без построчной сверки и подмену НЕ ЛОВИТ по построению. *Цена 25.08: готовый `## ОТЧЁТ` захода konvejer-incidentov был записан в общий `scratchpad/otchet.md`, и 92 строки чужого отчёта простояли в `kod_slovari-v-kod.md`.*
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода «kod_registracia-bez-obhoda.md»).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает названные через `--vlit` ЧУЖИЕ ветки В ОСНОВНУЮ, и только ПОТОМ ты заводишь свою рабочую папку; СВОЮ ветку он не трогает никогда — её вливаешь ты сам последним ходом (граница прав ниже).** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. 🔴 ПОРЯДОК ПРИ ПАДЕНИИ ЭТОГО ВЫЗОВА — исполняй, не изобретай (движок роняет `network_error` на вызове субагента и уносит с собой ВЕСЬ заход, замер волны 3A — 4 захода из 13). Пауза 45 секунд, до трёх попыток; время меряй `date`, не суммой своих `sleep`. После третьей — работай БЕЗ субагента: контур остаётся неразобранным, и это ОТДЕЛЬНАЯ строка в `## ОТЧЁТ`, а не молчание. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты сам, по ходу работы (§4). 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone veb/vhod.py --zone deploy/ --zone ops/ --zone tests/ --kommitit 'свою зону veb/vhod.py deploy/ ops/ tests/ по ходу работы, каждым осмысленным шагом' --zakryt 'ветку НЕ закрывать и в main НЕ вливать — влитие есть ход приёмки'
```

🔴 ГРАНИЦА ПРАВ, ЧЕТЫРЕ ОТВЕТА (та же, что в самом задании субагенту — одно место в тексте, а не пересказ): **кто вливает ЧУЖИЕ названные (`--vlit`) ветки** — субагент, в ОСНОВНУЮ ветку, до заведения твоей папки; **кто вливает СВОЮ ветку этого захода** — ты сам, последним ходом, после коммита зоны (`git_zona.py vlit-v-osnovnuyu`; решение владельца 25.08 — оно сняло противоречие волны 4, когда машинное §0.1 и текстовое «ветку НЕ вливать» спорили молча, и машинное побеждало); **кто закрывает заявки** — субагент, `zayavka-zakryt`; **кто коммитит пути ВНЕ зоны захода** — субагент (хвост Cowork и что назовёт пункт 3 его задания). Ты коммитишь ТОЛЬКО зону этого захода, по ходу работы (§4). 🔴 Конфликт на `README.md` при ЛЮБОМ слиянии разрешается ОБЪЕДИНЕНИЕМ записей реестра, НИКОГДА выбором стороны: параллельные заходы волны дописали в реестр по строке — обе записи правы, выбор одной молча уничтожает регистрацию соседа.

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-09-08, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged main | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.


- деплоя в этом заходе нет.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/bystro-i-bezopasno`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add veb/vhod.py deploy/ ops/ tests/` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО ЭТО, в этом порядке. Проект сверх названного не изучай.
  1. `ops/storozh_sajta.py` — сторож, специально построенный различать «сайт лёг» и «443 отфильтрован». Его докстрока несёт замер 06.09 (проба 1: 30 успехов из 30; проба 2 через час: 3 таймаута из 3; порт 8443 с тем же сертификатом отвечал за 0.10 с) и объясняет, почему http на этом сайте держат нередиректящимся: он контрольный образец. Состояние он держит НА СЕРВЕРЕ во временном файле — путь читается из константы STATE\_FILE в самом модуле; на ноутбуке этого файла нет по построению. Тревога только на смену состояния, значит история — в журнале systemd, не в файле.
  2. `veb/vhod.py`, строки 175–185 — форма пароля. Утренний заход зашил `action="https://math-kluychiki.ru/vhod"` абсолютным.
  3. `zhurnal/2026-09-02_spetsmat-bot/kod_profil-bezopasnosti.md`, секция `## ОТЧЁТ` — что именно сделано утром: два `server`-блока, `return 444` на неизвестный Host, CSP, отдача robots из nginx. Там же путь к копиям конфига ДО правки: `~ivan/nginx-backup-profil-bezopasnosti/{spetsmat.conf,spetsmat-obshchee.conf}.orig`.
  4. `zhurnal/2026-09-02_spetsmat-bot/ZAMYSEL-profil-bezopasnosti.md` — какие требования Google закрывались и чем; нужен, чтобы понимать, что именно НЕЛЬЗЯ уронить.
  5. `deploy/README.md` — как ходить на сервер.
  🔴 Ловушка адреса: `deploy/ADRES.txt` — МЁРТВЫЙ туннель прошлой эпохи, живой адрес в `deploy/ADRES-SAJTA.txt`. Сказано в докстроке сторожа; не перепутай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

🔴 **WRITE YOUR `## ОТЧЁТ`, `## ПЛАН` AND `## ВОПРОСЫ` IN ENGLISH, AND EVERY FILE AND EVERY COMMIT MESSAGE YOU PRODUCE TOO.** Owner's decision 30.08. It is a каркас-level rule, not a preference — wave 2 lost it twice because the pass text listed the report SECTIONS and never said «every file you create». Fixed Russian addresses stay Cyrillic: `ЦЕНА:` · `ВЕРДИКТ:` · `ДОМ:` · `ДОСТАВЛЕНО:` · `ПОДЪЁМ:` · `[ДОЛГ: …]` · every `## ` heading of this file · every path and command.
### ЧТО СЛУЧИЛОСЬ — жалоба владельца дословно, 08.09 около 13:10

> «сейчас постарался зайти на сайт, он не работает, не открывается. пока писал, страница загрузилась, но дико медленно, каждая кнопка загружается по несколько минут»

Открывает он `https://math-kluychiki.ru` (подтверждено на интервью).

### ЗАМЕР АНАЛИТИКА 08.09 13:20 — из ЧУЖОГО канала, НЕ из сети владельца

| адрес | результат |
|---|---|
| `http://159.194.254.52/` | 200, TTFB 74 мс, полная загрузка 254 мс, 38.6 КБ |
| `http://math-kluychiki.ru/` | 200, DNS 73 мс, TTFB 76 мс, загрузка 324 мс |
| `https://math-kluychiki.ru/` | тело страницы отдалось целиком одним каналом; в браузерной панели соединение НЕ встало |

🔴 **Это замер из песочницы, а не из сети владельца, и потому доказывает ровно одно: сервер по http жив и быстр.** Про 443 у владельца он не говорит НИЧЕГО — сеть другая. Ты работаешь на ноутбуке владельца, то есть на ЕГО пути. Этот замер — твой, и он единственный, который считается.

### ГИПОТЕЗА АНАЛИТИКА — проверить, а НЕ подтвердить

Форма пароля с утра уходит жёстко на `https://` (`veb/vhod.py:181`), а 443 на пути владельца задокументирован как перемежающийся. Тогда каждая отправка формы обязана пройти через 443, зависает на рукопожатии до таймаута и повторяется — это и читается как «кнопка грузится несколько минут».

🔴 **НЕ ПОДЫГРЫВАЙ.** Аналитик — заинтересованное лицо: гипотеза его, и услужливое подтверждение её ничего не стоит. **Опровержение — полноценный успех.** «Не воспроизвелось» — законный исход, если сказано, сколько проб, в каком окне и чего не хватило. Брак — только выдуманный вердикт. Держи в поле зрения альтернативы, которые гипотеза заслоняет: `return 444` на неизвестный `Host` (браузер на закрытое без ответа соединение ходит повторно), CSP из `snippets/spetsmat-obshchee.conf`, ночной таймер `spetsmat-vygruzka-v-tablicu.timer`, поставленный вчера и ходящий в Google (нагрузка, `OnFailure`, рестарт-петля), и просто провайдер владельца.

### ЗАДАЧА

**A. Замерить.** С ноутбука владельца, обе схемы, повторяемо: медиана времени до первого байта и до полной загрузки для `https://math-kluychiki.ru/`, `http://math-kluychiki.ru/`, `https://math-kluychiki.ru/vhod` и голого IP. Не одна проба — серия; число проб назвать.

**B. Поднять историю.** Что говорил сторож 443 в окне 08.09 05:00–14:00 и что видно в журнале systemd на сервере. Это делает ДО-верификатор ПЕРВЫМ ходом (§3): состояние сторожа перетирается, после починки его не снять.

**C. Назвать диагноз.** Одной фразой: что именно медленное, на чьей стороне и с какого момента. Если утренняя правка ни при чём — сказать это прямо.

**D. Предложить решение, сохраняющее ОБА требования.** Владелец дословно: «надо придумать, как сохранить требования гугла по безопасности и одновременно сделать, чтобы работало быстро». Наблюдение, которое стоит проверить и которое НЕ является указанием: пометку Google снимал nginx (`444` на чужой Host, заголовки, `robots.txt`), и ни один из этих пунктов не требует, чтобы 443 был доступен владельцу; жёсткий `action="https://…"` в `vhod.py` требованием Google не был — в `ZAMYSEL` §4 максимумом названа видимая строка-предупреждение, а не принуждение к https. Если замеры это подтвердят, развилка узкая. Если опровергнут — предлагай своё.
🔴 Правку ВНЕДРЯЙ только после того, как диагноз назван числами. Порядок: замер → диагноз → правка → повторный замер.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**

1. **Живой прогон на реальном объекте — на боевом сайте, а не на фикстуре и не на копии.** Замер с ноутбука владельца по ВСЕМ четырём адресам пункта A, не менее 20 проб на адрес; печатаются медиана и худшая проба рядом. Число проб названо.
2. История сторожа за окно 08.09 05:00–14:00 поднята ДО-верификатором и процитирована: что он говорил — «жив», «мёртв» или «443 отфильтрован», и в какие моменты. Пусто — сказать «пусто» и назвать, чем смотрел.
3. Диагноз назван одной фразой и опирается на числа пунктов 1–2, а не на рассуждение.
4. Если предложена правка — после неё **все 7 клауз критерия `profil-bezopasnosti` перегнаны заново и печатают «7 из 7»** (взять их дословно из `kod_profil-bezopasnosti.md`, секция `## ОТЧЁТ`, подраздел «ЧТО ПРОВЕРИЛ САМ»), И медиана из пункта 1 по каналу владельца улучшилась — оба числа, до и после, печатаются рядом.
5. Если правка НЕ предлагается — назван виновник вне нашего кода, чем это установлено, и что владельцу с этим делать.
6. `pytest tests/ -q` — зелёных не меньше, чем было на входе. 🔴 Число входа снять КОМАНДОЙ до работы и напечатать оба рядом; в заход оно не вписано намеренно — между сборкой и прогоном числа устаревают.
7. Зона чиста: `git_zona.py check --zone veb/vhod.py`, `--zone deploy/`, `--zone ops/`, `--zone tests/` — все ✅.

**ОХВАТ:** финальная строка «проверено X из 7 клауз», X считается по этому списку.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

🔴 **ЧЕГО НЕ ДЕЛАТЬ.**
- Не жать «Запросить проверку» в Search Console и не советовать владельцу это сделать — решение интервью п.3.
- Не вводить `301` с http на https и не включать HSTS. Запрет из захода `https-i-domen` в силе: это уже клало сайт целиком, и раскопки ночью 07.09 отменили ровно такую правку аналитика.
- Не откатывать nginx к `.orig` вслепую: откат снимет и то, за что Google снимет пометку. Решение интервью п.2.
- Не трогать `docs/index.html` и заглавную — забетонированы.
- Копию конфига НИКОГДА не класть в каталог sites-enabled на сервере: главный конфиг nginx подключает его содержимое глобом БЕЗ фильтра по расширению, копия становится вторым активным конфигом и `nginx -t` падает с `duplicate listen options`. Утренний заход на это уже наступил; бэкапы держать в `~ivan/nginx-backup-*`.
- Порядок работы с nginx — из `https-i-domen`: копия конфига до правки → `nginx -t` перед каждым `reload` → `curl` после каждого шага; не ответил — возвращаешь копию немедленно. Ночью 06.09 сайт на этой территории падал и не поднимался несколько попыток.
- Перед любой выкаткой — `python3 ops/raspisanie.py`: идёт занятие, выкатки нет.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)

Верификатор нужен, тип — **ДО-типа** — судит СОСТОЯНИЕ, которое работа уничтожит, и идёт ПЕРВЫМ ходом, до самой работы: после неё он неисполним. Свежий субагент, ДРУГИМ методом (свежий субагент снимает состояние сторожа 443 и журнал systemd ДО любой правки — после починки это состояние неисполнимо), не перечитывает свою же правку. Доля сплошной выборки: сплошная выборка по окну 08.09 05:00-14:00, назвать число проб. Финальная строка ответа обязательна дословно: «выдано N позиций из M найденных» — без неё ответ считается усечённым и в отчёт не вставляется.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПО ХОДУ РАБОТЫ, НЕ ОДНИМ ПОСЛЕДНИМ ХОДОМ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая. Каждую завершённую часть работы коммить СРАЗУ, теми же двумя ходами — не копи всё к финальному ходу:
```
git --no-optional-locks add -- veb/vhod.py deploy/ ops/ tests/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- veb/vhod.py deploy/ ops/ tests/   # отсекает всё чужое
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **КОММИТЬ ПО ХОДУ — РЕШЕНИЕ ВЛАДЕЛЬЦА 25.08 (В11), ПЕРЕВЕРНУВШЕЕ прежний канон «одним последним ходом».** Цена прежнего канона: за сутки ДВА обрыва — канал `opencode run --auto` односторонний и умирает вместе с сессией (владелец закрыл ноутбук), и незакоммиченная работа пропадала целиком. Закончил кусок — закоммитил его; последний ход только ПРОВЕРЯЕТ, что коммитить нечего (`git status --porcelain` пуст, `git_zona.py check --zone` ✅).
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `veb/vhod.py` `deploy/` `ops/` `tests/`

- **Г1. Зона доехала в git.** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone veb/vhod.py` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone deploy/` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone ops/` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone tests/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `spetsmat-bot` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged main` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## ⚠️🔴 WARNING · ПОСЛЕДНИЙ ХОД ПЕРЕД ОТЧЁТОМ — ПОЛНАЯ ГИТ-ГИГИЕНА. НЕ ПРОПУСКАТЬ 🔴⚠️

**СТОП. Прежде чем писать хоть строку в `## ОТЧЁТ` — прогони это целиком.** Требование владельца
2026-08-16, поводом стал заход, который сделал работу и не закоммитил НИЧЕГО: *«он ничего не
коммитит… всегда, когда собираешь по генератору, там должна быть фраза, что начинать надо с полной
проверки гигиены»*. Гит-контур §0.1 стоит в НАЧАЛЕ и разбирает то, что накопилось ДО тебя; этот блок
стоит в КОНЦЕ и разбирает то, что накопил ты сам. Один другого не заменяет.

**ПОРЯДОК ЖЁСТКИЙ, ОН НАЗВАН ВЛАДЕЛЬЦЕМ: коммит → влитие своей ветки в основную → пост-проверка
ИЗ ГЛАВНОЙ ПАПКИ → гашение → вывоз.** Обратный порядок не работает технически: влитие отказывает
на грязном дереве, пост-проверка неисполнима до влития, вывоз — на невлитом. **Решение владельца
20.08 пересматривает решение 05.08** («в конце может это worktree вливать, но, наверное, это не
нужно делать») — теперь вливает САМ заход, но только при зелёной пост-проверке.

**1 · ВСЕ КОММИТЫ.** Ничего не осталось вне git — ни в рабочем репозитории, ни в соседних, до
которых ты дотянулся по ходу работы:
```
for R in <репозитории, которых ты касался>; do
  echo "== $R"; git -C $R --no-optional-locks status --porcelain
done
```
Своя зона — своими путями (`add` + `commit -- <пути>`). Чужая содержательная работа — НЕ твоя:
называешь строкой в отчёте и оставляешь. Пусто у всех — так и напиши числом «вне git 0».

**2 · ВЛИТИЕ СВОЕЙ ВЕТКИ В ОСНОВНУЮ.** Только после того, как шаг 1 дал «вне git 0» на своей
зоне — влитие отказывает на грязном дереве:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py vlit-v-osnovnuyu zahod/bystro-i-bezopasno --zone <своя зона> \
    --vsyo-ravno "своя рабочая папка ещё жива — влитие последним ходом захода, штатно"
```
Конфликт — ЗАКОННЫЙ исход, не повод форсировать: разрешай по существу, если понимаешь обе
стороны; не понимаешь — `git_zona.py vlit-v-osnovnuyu --abort`, ветка остаётся невлитой,
строка в отчёт и заявка на влитие (`git_zona.py zayavka --rod git-operaciya`).
🔴 Конфликт на `README.md` — только ОБЪЕДИНЕНИЕМ записей реестра, никогда выбором стороны:
параллельные заходы волны дописали по строке — обе записи правы, выбор одной молча уничтожает
регистрацию соседа.

**3 · ПОСТ-ПРОВЕРКА ИЗ ГЛАВНОЙ ПАПКИ.** Отвечает на вопрос «механизм ВСТАЛ», а не «коммит
виден»: прогон изменённого механизма из `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot`, НЕ из рабочей папки `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/bystro-i-bezopasno` плюс `grep` по ЖИВОМУ файлу,
который его зовёт (хук, конвейер, генератор):
```
cd /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot && <команда прогона механизма, который заход менял> && echo $?
grep -n '<как механизм назван в вызывающем коде>' <живая точка вызова>
```
🔴 **Красная пост-проверка = ОТКАТ ВЛИТИЯ И СТРОКА В ОТЧЁТ**, а не «доложу, пусть приёмка
решает»: `git_zona.py vlit-v-osnovnuyu --abort`, если слияние ещё не закоммичено, иначе
`git -C /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot --no-optional-locks reset --hard <хэш ДО влития>`. Заход, который влил
и сломал `main`, обязан вернуть `main` сам.

**4 · ГАШЕНИЕ.** Невлитого не осталось: `git --no-optional-locks branch --no-merged main`.
Каждая оставшаяся ветка названа поимённо с причиной, почему она жива. 🔴 Ветку-витрину (`main`
там, где с неё публикуется сайт) НЕ вливать — влитие туда есть публикация и решение владельца.

**5 · ВЫВОЗ.** Вывези СВОЮ ветку; `main` НЕ вывози: если после работы в нём есть невывезенное,
поставь заявку `--rod git-operaciya` и назови число в отчёте. Команда для своей ветки:
`git --no-optional-locks log --oneline @{u}.. | wc -l` → 0.
Ненулевое на своей ветке означает, что работа существует только на этом диске.

**6 · ПРОВЕРКА ФАКТОМ, А НЕ ПАМЯТЬЮ.** Числа по каждому репозиторию — вне git · невлитых своих
и чужих; невывезенных СВОЕЙ ВЕТКИ, не по каждому репозиторию · результат пост-проверки
(зелёная/откачена) — печатаются командой и уходят в `## ОТЧЁТ` дословно. Ненулевое число или
красная пост-проверка без объяснения — приёмка читает как несделанную работу: она гоняет те же
команды первым ходом.

🔴 **Отчёт без этих чисел не принимается.** «Я закоммитил» — не то же самое, что `status --porcelain`
пустой: за одну сессию работа не доезжала трижды, каждый раз с честным «сделано» в отчёте.
## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Стартовая строка велит запустить `git_zona.py worktree add` ПЕРВЫМ ходом и не говорит, ИЗ КАКОЙ ПАПКИ — инструмент молча берёт репозиторий по cwd
Стартовое сообщение владельца («Первым ходом: python3 …/git_zona.py worktree add bystro-i-bezopasno …») стоит ВЫШЕ контракта зоны, который единственный говорит «из корня репозитория». Исполнитель с пустым контекстом исполняет стартовую строку буквально — из той папки, где его запустили (`/Users/ivanyakovlev`), — и инструмент без единого предупреждения заводит worktree и ветку в СОСЕДНЕМ репозитории `disciplina`. Защита в инструменте есть и она работает (позже он отказал: «Повтори с переменной `GIT_ZONA_REPO`»), но срабатывает она, только когда cwd — внутри какого-то репозитория; из домашней папки, где репозитория нет вовсе, тот же вызов проходит ЗЕЛЁНЫМ и выбирает репозиторий сам.
ЦЕНА: заведён worktree `/Users/ivanyakovlev/Documents/GitHub/disciplina-wt/bystro-i-bezopasno` и ветка `zahod/bystro-i-bezopasno` в чужом репозитории `disciplina` (169 → 170 веток); на уборку ушло 4 хода (`worktree drop`, `poteri`, `zakryt-vetku`, перепроверка), в `disciplina` осталось надгробие `mogila/zahod/bystro-i-bezopasno`. Починка — одна строка: в стартовую строку добавить `cd <корень репозитория> &&` или `GIT_ZONA_REPO=<корень>`; либо в самом инструменте отказывать, когда cwd не внутри репозитория, — как он уже отказывает в остальных случаях.

### Тест предыдущего захода прибивает ровно тот атрибут, который следующий заход обязан отменить, а клауза «зелёных не меньше» скрывает это до полного прогона
Утренний заход `profil-bezopasnosti` закрепил свою правку тестом `test_entry_form_posts_to_absolute_https`, который утверждает `assert 'action="https://math-kluychiki.ru/vhod"' in html` И `assert 'action="/vhod"' not in html`. Этот заход построен на замере, который ровно это поведение отменяет. Клауза «pytest зелёных не меньше, чем было» такой случай законным не называет, и исполнитель узнаёт о столкновении только из полного прогона (110 с) — причём в форме «994 → 993», где не видно ни чей это тест, ни почему он красный. Тест лежал в зоне захода, так что чинить его было чем; будь он вне зоны — заход встал бы между двумя обязательными требованиями.
ЦЕНА: один красный тест, найденный не сразу; разбор занял два лишних хода (`diff` списков FAILED, затем прогон `tests/veb/ -rf`) плюс два полных прогона по 110 с. Починка правилом: заход, отменяющий поведение предшественника, обязан ПЕРВЫМ ходом грепнуть тесты по отменяемой строке (`grep -rn '<отменяемая строка>' tests/`) — одна команда, дешевле полного прогона.

### Критерий требовал перегнать клаузу, исполнимую только вводом настоящего пароля, — а исполнителю ввод паролей запрещён
Клауза 4 требует «все 7 клауз критерия `profil-bezopasnosti` перегнаны заново и печатают „7 из 7“». Клауза 5 того критерия звучит «Вход по https и по http — оба дают `Set-Cookie`», и утренний заход исполнил её, прочитав пароль из `secrets/veb.env` на сервере. Исполнителю ввод паролей в формы запрещён — значит клауза по построению неисполнима им целиком, и заход, честно её не выполнивший, обязан печатать не «7 из 7», а оговорку, которой форма критерия не предусматривает.
ЦЕНА: охват по клаузе 4 напечатан как «6 из 7 полностью + клауза 5 в двух частях из трёх» вместо требуемого «7 из 7» — критерий формально провален там, где работа выполнена. Починка: клаузу, требующую секрета, формулировать через наблюдаемое без секрета (например «POST принимается обеими схемами и приземляется на ту же схему»), либо помечать её как исполняемую только владельцем.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

ПРАВКИ ПРОЧИТАНЫ: 1, 2 (both read before any action; ПРАВКА 1 governs the measurement design of step 3, ПРАВКА 2 is executed in step 2).

**Order is measurement -> diagnosis -> fix -> re-measurement. Nothing is rolled back or edited before numbers exist.**

0. **Correction of my own first move (already done, before this plan).** The start line's command was run from the home directory, so `git_zona.py` resolved the repository by cwd and created the worktree in `disciplina-wt/`, not in `spetsmat-bot-wt/`. Dropped it (`worktree drop`) and closed the stray branch in `disciplina` (`zakryt-vetku`, tombstone `mogila/zahod/bystro-i-bezopasno` -> b23074df, revivable by `voskresit`). The real worktree is created from the `spetsmat-bot` root in step 2. This goes into `## ОТЧЁТ` as a factory lesson: the start line tells the executor to run the command as the very first move without saying from which directory, and the tool silently picks the wrong repository.

1. **ДО-verifier FIRST (§3), because the state it judges is destroyed by the work.** Fresh subagent, another method: pull the 443 watchdog state on the server (STATE_FILE constant read out of `ops/storozh_sajta.py`) and the systemd journal for the window 08.09 05:00-14:00 — continuous sweep, number of probes named, answer must end with «выдано N позиций из M найденных».

2. **§0.1 git contour, one subagent, before my own working folder**; then ПРАВКА 2 by my own hand (commit of the eight `kod_*.md` of the arc, closing of заявки 2026-09-08T1327 / T0031 / T0853) — it is addressed to the executor explicitly and it writes to `.git`, so it must not race the contour subagent. Then create the worktree from the `spetsmat-bot` root and `cd` into it. Entry numbers snapped by command: unmerged branches, `pytest tests/ -q` green count.

3. **A. Measure from this laptop — the owner's channel (clause 1).** >= 20 probes per address, median and worst probe printed side by side, for `https://math-kluychiki.ru/`, `http://math-kluychiki.ru/`, `https://math-kluychiki.ru/vhod`, and the bare IP. Per ПРАВКА 1 the run must walk the WHOLE path, not the front page: open `/vhod` over http, actually POST the form, record which scheme the response lands on, and then measure again AFTER login. Median-before-login and median-after-login are printed as two separate numbers; equal numbers refute the hypothesis, and that is a full result.

4. **Hold the alternatives in view, do not play along (§2).** `return 444` on unknown Host, CSP from `snippets/spetsmat-obshchee.conf`, the nightly `spetsmat-vygruzka-v-tablicu.timer` installed yesterday, and simply the owner's provider. Each is either excluded by a number or named as not excluded.

5. **C. Diagnosis in one sentence, resting on the numbers of steps 1 and 3.** If the morning edit is innocent, say so plainly.

6. **D. Fix only if the numbers point at our code.** The narrow fork named in ПРАВКА 1: `action` back to relative, warning line with the https link stays. Then re-measure the same way, re-run all 7 clauses of `profil-bezopasnosti` verbatim from its `## ОТЧЁТ` and print «7 из 7», print `pytest` green count against the entry number, and print median before/after side by side. If no fix is proposed — name the culprit outside our code, what established it, and what the owner should do.

7. **Hygiene and closing (§4, §4.1, WARNING block).** Commit my zone as I go, then Г1-Г6, then merge my own branch into main last, post-check from the MAIN folder, extinguish, export. Report in English, with ОХВАТ «проверено X из 7 клауз», the ARTEFACT path, the COMMIT line, and the irreversible-actions list.

**Objection to the readiness criterion (§1 requires it to be stated before work, not after).** Clause 4 demands that «медиана из пункта 1 по каналу владельца улучшилась» after a fix. If the diagnosis turns out to be that 443 is intermittently filtered on the owner's path, the median of the plain `https://` address CANNOT improve — nothing in our repository can change a filtered port. What the fix would improve is the median of the owner's real path (login and every click after it), because that path stops going through 443 at all. I therefore read clause 4 as being about the path median of ПРАВКА 1 (before-login / after-login), not about the raw `https://` address median, and I will print both pairs so that the acceptance can judge either reading. If this reading is wrong, it is wrong before the work, not after it.

## ВОПРОСЫ — (заполняет исполнитель)

1. The login POST itself costs ~1.0 s of TTFB on BOTH schemes (10 probes over http: 0.986–1.184 s, median ~1.02 s; the same over https: 1.132 s median of 20). That is not the failure the owner reported and it is not caused by 443 — but it is the single slowest thing on the whole path, it happens on every login, and nothing in this заход explains where the second goes. Worth one measurement before someone else blames the network for it.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. The site watchdog `ops/storozh_sajta.py` is not installed as anything — no systemd unit, no timer, no cron entry on the server (`systemctl list-unit-files | grep -i storozh` empty, `crontab -l` for both `ivan` and `root` empty of it), and its state file `/tmp/spetsmat-storozh-sajta.state.json` does not exist on either machine. It is a watchdog that has never watched: this заход needed exactly its history for the window 05:00–14:00 and got nothing. Its own docstring says «Alarm only on change», which for a hand-run tool means the first run is always silent.
   ДОМ: deploy/README.md
   ДОСТАВЛЕНО: нет

3. The alert unit `spetsmat-alert@` fired twice on 08.09 05:41–05:42 and the SECOND one failed with `status=2/INVALIDARGUMENT` — it never reached the owner. The first one did arrive and said «unit gave up: five failed starts in five minutes means broken code, not a broken network», while the actual cause in the same journal is `TelegramNetworkError: Request timeout error` — i.e. exactly a broken network. An alarm that misnames the cause is worse than none: it sends the reader to the one thing that is not broken.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

4. `main` is 17 commits ahead of `origin/main` and cannot be pushed by any заход: `vyvezti` refuses on open заявки, and the override would publish over the still-open incident `2026-09-02T1431` (a live bot token in the PUBLIC repository `d1-d57/spetsmat-179`). The unblocking order is the owner's: `/revoke` in @BotFather and a new token in `secrets/bot.env`, then `push origin main`. Until then all of today's work — both night заходы, the acceptance verdicts, and this fix — exists on one disk only. Заявка `2026-09-08T1436` carries the current number.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

5. The morning заход's own отчёт records that it ran `git checkout <ветка>` twice IN THE MAIN WORKING FOLDER, which the зона contract forbids. This заход hit the neighbouring version of the same trap from the other side (worktree created in the wrong repository, see УРОКИ ФАБРИКЕ). Both are the same shape: a git command whose target is decided by ambient state (cwd, current HEAD) rather than by an explicit argument. Worth one rule rather than two lessons.
   ДОМ: zhurnal/2026-09-02_spetsmat-bot/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> 🔴 **`ДОМ:` — ОБЯЗАТЕЛЬНОЕ ПОЛЕ, И АДРЕС В НЁМ ОБЯЗАН СУЩЕСТВОВАТЬ В МОМЕНТ, КОГДА ТЫ ЕГО ПИШЕШЬ.** Путь, которого нет на диске, — не адрес: такую запись нельзя ни доставить, ни спросить, и она не чинится ничем. Замер 2026-09-06 по 632 файлам `kod_*.md`: 370 пунктов очереди из 1570 родились ровно так — больше, чем всех доставимых (195) вместе взятых. Проверить СВОЙ файл до отчёта — одна команда:
> ```
> python3 _generator/tools/bootstrap_zahod.py --proverit-doma <этот файл>
> ```
> rc=0 — все дома достижимы; rc=1 — назван дом, которого нет (команда печатает какой именно). Тот же разбор гоняет `Г7` приёмки, и у него храповик: у ЭТОГО захода база 0, поэтому первый же недостижимый дом здесь — красный на приёмке, а не запись, которую через неделю никто не найдёт.
> `ДОМ: владелец` — законный адрес и НЕ недостижимый дом: он значит «дома-файла нет вовсе, решение за человеком». Не знаешь пути — пиши его, а не выдуманный путь. Для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит и «доставлено» без метки на месте, и недостижимый дом сверх базы; достижимое-недоставленное печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(вставлен ДОСЛОВНО из ответа субагента гит-контура; исполнитель эти пункты сам не исполнял)*

```
$ git --no-optional-locks branch --no-merged main
(пустой вывод)

$ git --no-optional-locks status --porcelain | wc -l
      33

$ git --no-optional-locks log --oneline @{u}.. | wc -l
      10

$ python3 .../git_zona.py zayavki
✅ заявок нет

Охват: заявок открыто 0, переадресовано 0, постоянных исключений 0, сторож краснеет на 0, держателей 0, двойной захват на 0
```

🔴 Четвёртая строка снимка — **ложно-зелёная**. Без `GIT_ZONA_REPO` инструмент отвечал про СВОЙ репозиторий (disciplina), а не про spetsmat-bot. Перепрогон с переменной: **открытых заявок 5**, переадресовано 27.

*(Исполнитель подтвердил ложно-зелёное своей командой, дословно: `python3 …/git_zona.py zayavki` из корня `spetsmat-bot` без переменной → «✅ заявок нет»; та же команда с `GIT_ZONA_REPO=/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot` → «Открытых заявок: 5».)*

**ЧТО СДЕЛАНО** *(с хэшами)*

- Закоммичено `9b157dc` — хвост Cowork: `zhurnal/_INFRA-git/INCIDENTY.md`, `_studio/zhurnal/_INFRA-git/INCIDENTY.md`, `UROKI-FABRIKE.md`, `VYGRUZKA-2026-09-08.md`, `VYGRUZKA-2026-09-08-2.md`
- Закоммичено `3ebaa06` — `README.md` (индекс регистрации из вайтлиста `korni.КОРНИ_*`; `_studio/docs/KARTA.md` в этом репозитории не существует)
- Закоммичено `da0a509` — две заявки `1342` с отметкой ЗАСТРЯЛА
- Заявки закрыты: `2026-09-08T1342-bootstrap-zahod-py-scratchpad-kod-slovari` и `2026-09-08T1342-sdelat-handoff-py-bez-zakrytiya-6` — обе ЗАСТРЯЛА
- Влито: 0. Вывезено: 0 (отказ инструмента). Погашено: ничего

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `нет`

*(Двоеточие вынесено за жирный АНАЛИТИКОМ на приёмке: Г12 краснел на форме, а не на содержании — субагент написал `**…ЗАКРЫТЫ: нет.**`, гейт ищет `**…ЗАКРЫТЫ:**` и значение отдельно. Содержание не тронуто ни словом, список ниже — его.)*

Осталось непроходимого правами гит-контура:
1. **Вывоз main на origin (13 коммитов)** — `vyvezti` отвечает отказом: открыта заявка `2026-09-08T0853` рода `git-operaciya` (и `0031`, и старая `2026-09-02T1431` про живой токен бота в публичном репозитории). Обе блокирующие заявки — из тройки ПРАВКИ 2, закрывать их гит-контуру запрещено; обход `--vsyo-ravno` означал бы публикацию поверх открытого инцидента с токеном — решение владельца, не его.
2. **Восемь `kod_*.md` + три заявки** — оставлено исполнителю по ПРАВКЕ 2. **ИСПОЛНЕНО исполнителем:** коммит `aca18f4` (13 путей арки), заявка `2026-09-08T1327` закрыта (`c707211`). Две заявки на вывоз (`0031`, `0853`) НЕ закрыты — см. отчёт, вывоза не было, закрывать неисполненное значило бы поставить ложно-зелёное.
3. **Две заявки `1342`** — адресаты `_generator/tools/bootstrap_zahod.py` и `sdelat_handoff.py` в spetsmat-bot отсутствуют (проверено: `_generator` — No such file or directory), живут в чужом disciplina; род `pravka-koda` вне прав гит-контура.
*(`нет` законно — но ТОЛЬКО со списком поимённо: что осталось и почему это непроходимо ТВОИМИ
правами (чужая живая рабочая папка, нужно решение владельца, конфликт, обеих сторон которого
не понимаешь). «Сложно» и «не моя тема» причинами не являются. `нет` без списка = красный.)*

## ОТЧЁТ — (заполняет исполнитель)

ПРАВКИ ПРОЧИТАНЫ: 1, 2

### ДИАГНОЗ, одной фразой (clause 3)

**Nothing on the server was slow — the owner's SESSION was: the morning edit pinned it to port 443 from 05:40 on 08.09, and 443 on this path is the one thing here known to come and go.**

Unpacked, each half resting on a number below: the site and both ports were fast throughout every window I measured (240 probes over 35 minutes, zero non-200; the front page over https rendered in the owner's own browser in 183 ms; nginx did not restart once and logged nothing in 05:00–14:00; server load average 0.04). What changed at 05:40 on 08.09 is that the login form began POSTing to an ABSOLUTE `https://` address, and the 302 that answers a login carries a RELATIVE `Location` — so the visitor lands on the https origin and every click after the login goes over 443 too. Measured on the owner's laptop before the fix: **20 form submissions out of 20 landed on `https://`**. Before the morning edit the form was relative, the owner stayed on http from beginning to end, and 443 never touched him. Port 443 on this path is filtered INTERMITTENTLY, and that is documented, not assumed: probe of 06.09 gave 30 successes out of 30 and, an hour later, 3 timeouts out of 3, with port 8443 carrying the same certificate answering in 0.10 s; at 13:20 on 08.09, ten minutes after the owner's complaint, the analyst's https connection did not come up at all.

🔴 **What I did NOT reproduce, said plainly.** In my own windows 443 was healthy every single time — 125 probes 14:16–14:18, 240 probes 14:19–14:54, 125 probes 14:29–14:31, not one failure, not one timeout. So I did not catch the slowness itself. What I measured is the DEPENDENCY: that the morning edit moves the owner onto the port whose instability was already measured, and that removing it moves him back off. Someone who insists the diagnosis be the slowness caught in the act should read this as «not reproduced in 490 probes across 38 minutes», and the fix as removing an unnecessary dependency rather than as a proven cure.

### A. MEASUREMENTS — the owner's laptop, both schemes, all four addresses (clause 1)

25 probes per address per series, medians and worst probe side by side, seconds:

| address | med total ДО | worst ДО | med total ПОСЛЕ | worst ПОСЛЕ | non-200 |
|---|---|---|---|---|---|
| `http://159.194.254.52/` | 0.290 | 1.151 | 0.280 | 1.084 | 0 / 0 |
| `http://math-kluychiki.ru/` | 0.312 | 0.787 | 0.254 | 1.926 | 0 / 0 |
| `https://math-kluychiki.ru/` | 0.337 | 0.760 | 0.308 | 0.987 | 0 / 0 |
| `https://math-kluychiki.ru/vhod` | 0.161 | 1.764 | 0.156 | 2.085 | 0 / 0 |
| `http://math-kluychiki.ru/vhod` | 0.101 | 1.283 | 0.083 | 0.838 | 0 / 0 |

Number of probes: **250** (25 × 5 addresses × 2 series, ДО 14:16:26–14:17:57, ПОСЛЕ 14:29:52–14:31:22). Plus a third, longer series left running the whole заход to catch an intermittent window: **240 probes over 14:19:11–14:54:07** across three addresses, **zero non-200**, worst probe 4.831 s, p90 ниже 1.09 s на каждом из трёх адресов (`http://…/` med 0.371 · `https://…/` med 0.377 · `https://…/vhod` med 0.165). Окно фильтрации за 35 минут наблюдения не наступило ни разу. 🔴 Every address improved slightly, and I am NOT claiming that as the fix working: 443 was healthy in both windows, the differences are 20–60 ms, and that is noise. The fix does not act on these numbers at all; it acts on the ones below.

**THE PATH, per ПРАВКА 1 — the measurement that actually separates the two worlds.** The script opens `/vhod` over http, reads the `action` attribute FROM THE LIVE PAGE, submits the form to exactly that address, and records which scheme the session ends up on. 20 walks of the whole path before, 20 after:

| | med ДО входа | med ПОСЛЕ входа | worst | where the session landed |
|---|---|---|---|---|
| **ДО правки** | 0.089 | **1.271** | 2.464 | **https — 20 из 20** |
| **ПОСЛЕ правки** | 0.093 | **1.182** | 2.497 | **http — 20 из 20** |

That flip — 20/20 https to 20/20 http — is the whole result. The medians barely move because 443 was working; on a filtered window the ДО row is a 15-second timeout per click and the ПОСЛЕ row is unchanged.

🔴 **An honest subtraction I nearly got wrong.** The 1.2 s after login looks like the smoking gun and is not: the same POST over plain http, 10 probes, costs 0.986–1.184 s (median ~1.02 s). So ~1 second of it is the server checking the password, NOT 443. The share attributable to 443 in a healthy window is the TLS handshake, ~0.05–0.11 s. I would have blamed the network for the server's own second had I not run the http POST. Filed as `## ВОПРОСЫ` п.1.

**Alternatives the hypothesis was hiding, each excluded by a number, not by an argument:**
- `return 444` on unknown Host — fires only on a foreign `Host` (`evil.example` → `000`); the owner's browser sends the right one, and the bare IP still answers `200`. Not it.
- CSP from `snippets/spetsmat-obshchee.conf` — browser console clean on both schemes, 0 errors, page renders, 0 sub-resources blocked. Not it.
- Nightly `spetsmat-vygruzka-v-tablicu.timer` — ran at 02:00, finished in 4 seconds with zero errors, **no entries at all** in the 05:00–14:00 window; next firing 09.09 02:00. Load average 0.04. Not it.
- Page weight — the front page is 144 499 bytes uncompressed but gzip is on and working: 38 354 bytes to any client that asks (the owner's browser transferred 38 654). Not it.
- OCSP stapling — off (`OCSP responses: no responses sent`); named here because it is the classic «fast in curl, slow in a browser» cause and I checked it rather than assumed. Certificate valid to 2026-12-05, no IPv6 record on the domain, so no Happy-Eyeballs stall either.
- **The owner's provider — NOT excluded, and it is the remaining suspect.** 443 comes and goes on the path while the server sees it fine from inside; that is a network between them, and nothing in this repository can fix it. What this заход does is stop the site from depending on it.

### B. WATCHDOG HISTORY — ДО-verifier, first move (clause 2)

🔴 **Empty, and here is what it was looked at with.** The ДО-verifier (fresh subagent, another method, continuous sweep of the window) reports: the state file `/tmp/spetsmat-storozh-sajta.state.json` (path read from the `STATE_FILE` constant, line 53) **does not exist on the server nor on the laptop** (`cat` → rc=1, `stat` → rc=1 on both). There is no systemd unit and no timer (`systemctl list-unit-files | grep -i storozh` → empty, `list-units --all` → empty), and no cron entry for either `ivan` or `root`. The word `storozh` appears **0 times** in the journal for the window. **Watchdog probes in 08.09 05:00–14:00: 0.** The watchdog has never watched; its own docstring's «Alarm only on change» means a hand-run tool is silent on first run by construction. Nothing was destroyed by this работа — there was nothing to destroy. Filed as `## ВОПРОСЫ` п.2.

What the journal DID hold for the window, sweep of **5211 records** (3140 journal lines + 1990 `spetsmat-access.log` + 81 default-vhost lines + 2 `spetsmat-error.log`):
- `nginx` — `-- No entries --`, and running since 06.09 11:39, i.e. never restarted in or near the window.
- `spetsmat-veb.service` — exactly one restart, at 05:40:03, clean, serving since.
- **443 provably reached the server mid-window:** `spetsmat-error.log` carries `2026/09/08 09:04:17 … host: "159.194.254.52:443"` (a scanner probing `/static/js/../.env`), with the symmetric line on port 80 a minute later. So at 09:04 nothing was filtered on either port from that route.
- 1990 site requests in the window, all 200; the 09:00 peak of 1395 is GoogleOther crawling `?den=`, not people.
- `spetsmat-bot.service` failed twice at 05:41 and 05:42 — cause is `TelegramNetworkError: Request timeout error` (outbound to Telegram, not inbound 443), recovered on the second restart. The `FileNotFoundError: … '/opt/spetsmat-bot/-m'` in the same traceback is a false trail (an artefact of printing a `<frozen runpy>` frame). Unrelated to the complaint, but see `## ВОПРОСЫ` п.3 — the alert about it failed to deliver.

Verifier's closing line, verbatim: «выдано 25 позиций из 25 найденных».

### C. WHAT I DID, AND WHY (clause 4)

**One edit, one file, plus its test.** `veb/vhod.py`: `action="https://math-kluychiki.ru/vhod"` → `action="/vhod"`, and the docstring rewritten to carry the measurement so the next заход does not re-derive it. **The warning line stays exactly as it was**, with its link — verified on the live page in the owner's browser: on http it is visible and links to `https://math-kluychiki.ru/vhod`, on https it is hidden. That line, and not the forced action, is what `ZAMYSEL-profil-bezopasnosti.md` §4 names as the maximum here, verbatim: «Максимум, что здесь законно, — заметная строка на самой странице входа». Forcing https was never a Google requirement, and all seven clauses of `profil-bezopasnosti` are closed by nginx, not by this attribute — which is why the fork was narrow, exactly as ПРАВКА 1 predicted.

**Deployed** (`ops/raspisanie.py` → `free: no lesson window around 2026-09-08 14:26`, then `bash deploy/vykatka.sh` → «deployed; the site answered 200 after 1s»), then re-measured. Live page now serves `action="/vhod"`; the old absolute form is gone from the served HTML (`grep -c 'action="https://math-kluychiki' → 0`).

**THE SEVEN CLAUSES OF `profil-bezopasnosti`, RE-RUN AFTER THE FIX:**
1. `evil.example` Host → **`000`** (not 200) ✅
2. bare IP → **`200`** ✅
3. headers → **`3`** ✅
4. `robots.txt` → **`200`**, body `User-agent: *` / `Allow: /` ✅
5. login over https and over http — **⚠ two parts of three.** Both schemes accept the POST and each now lands on ITS OWN scheme (`http → http://…/?vhod=ne-pustil`, `https → https://…/?vhod=ne-pustil`, code 200 both); `action=` on the live page → **`1`**, not 0. The third part — `Set-Cookie` on a SUCCESSFUL login — I did **not** run: it requires typing the real password from `secrets/veb.env` into the form, and entering passwords is forbidden to me. What can be said without it: the cookie is set in `veb/server.py` (lines 619, 1076), my diff touches one file, `veb/vhod.py`, so nothing in the cookie path changed. Named as a factory lesson rather than papered over.
6. `pytest tests/ -q` → **994 passed** — see the clause-6 line below ✅; `tests/veb/test_vizualnaya_shema.py` → **6/6** ✅; rendered in the browser on both schemes, console clean, CSP broke neither styles nor scripts, warning appears only on http ✅
7. HSTS → **`0`**; `http://math-kluychiki.ru/` → **`200`**, `http://math-kluychiki.ru/vhod` → **`200`**, neither a redirect ✅

**Итог по клаузе 4: 6 из 7 полностью зелёные, клауза 5 — в двух частях из трёх.** Печатать «7 из 7» я не могу и не буду: третья часть требует ввода пароля.

**Clause 6, both numbers side by side:** вход **994 passed** (17 failed, 13 skipped, 30 errors) → выход **994 passed** (17 failed, 13 skipped, 30 errors). 🔴 Between them it was 993/18: my edit turned `tests/veb/test_vhod.py::test_entry_form_posts_to_absolute_https` red — the morning заход's own test, which asserts the exact attribute this заход was sent to remove. It lies in my zone, so I replaced it with its opposite (`test_entry_form_posts_relative_and_never_pins_the_session_to_443`), carrying the measurement in its docstring, and left the separate warning-line test untouched so the ZAMYSEL §4 requirement stays covered. Factory lesson filed.

### ЧТО НЕ ТРОГАЛ

nginx on the server — not one line, no `reload`, no rollback to `.orig`: the diagnosis pointed at an HTML attribute, and every clause the nginx work closes is still green. No `301`, no HSTS. `docs/index.html` and the front page — untouched. Search Console — not opened, «Запросить проверку» not pressed and not recommended (интервью п.3). `data/spetsmat.db` and `docs/index.html` show as modified in the main folder — they were already dirty before me (the гит-контур subagent listed both as «содержательное не моё»); the site writes the database as it runs. `secrets/` — not read, not printed. The eight `kod_*.md` were committed per ПРАВКА 2, but **my own file-заход was deliberately left out of that commit** — the зона contract forbids committing it, and ПРАВКА 2 names eight files, not nine; the analyst commits it at приёмка.

### НЕОБРАТИМОЕ — отдельным списком

1. **Deploy to the production server** (`bash deploy/vykatka.sh`, 14:26) · `/opt/spetsmat-bot` on `159.194.254.52` · восстанавливается `bash deploy/vykatka.sh --otkat`, снапшот снят самим скриптом перед переносом; предыдущий known-good — `/opt/spetsmat-bot-bak-20260908T054000Z`, и коммит до правки — `fc646dc`.
2. **Worktree и ветка, заведённые ПО ОШИБКЕ в чужом репозитории `disciplina`** (первый ход, см. УРОКИ ФАБРИКЕ) · `/Users/ivanyakovlev/Documents/GitHub/disciplina-wt/bystro-i-bezopasno` + ветка `zahod/bystro-i-bezopasno` там же · сняты (`worktree drop`, затем `zakryt-vetku` после `poteri` → «Потерь нет: проверено 1 из 1»); восстанавливается `python3 _generator/tools/git_zona.py voskresit --branch zahod/bystro-i-bezopasno` из `disciplina` — надгробие `mogila/zahod/bystro-i-bezopasno` → `b23074df` на месте. Содержимого в ней не было: она была отпочкована от `main` и ни одного коммита не получила.
3. **Замер писал POST-запросы на боевой вход** — 60 отправок формы с заведомо неверным паролем `zavedomo-nevernyj-parol-dlya-zamera` (20 ДО + 20 ПОСЛЕ + 20 в клаузах и разведке). Ни одного настоящего пароля не вводилось и ни один секрет не читался; следы — строки `POST /vhod` со `302 → /?vhod=ne-pustil` в `spetsmat-access.log`. Восстанавливать нечего, назван для полноты: это записи в боевой лог, сделанные мной.

Кроме этих трёх — необратимого нет: ни удалений, ни перезаписей чужого, ни `reset`/`checkout` поверх несохранённого, ни одной правки за пределами зоны.

### ПОВТОРЯЕМОСТЬ НАХОДОК

- 🔴 **Повторится на СЛЕДУЮЩЕМ заходе, а значит это заход ДО следующего прогона, не запись в очередь:** старт из папки, которая не является корнем нужного репозитория. Стартовая строка одинакова у всех заходов этой фабрики, значит каждый следующий исполнитель наступит на то же место с той же вероятностью — и в моём случае это уже стоило четырёх ходов и ветки в чужом репозитории. Чинится одной строкой в шаблоне (`cd <корень> &&` или `GIT_ZONA_REPO=`).
- 🔴 **Повторится:** ложно-зелёный ответ `git_zona.py zayavki` без `GIT_ZONA_REPO`. Он соврал субагенту гит-контура в СНИМКЕ ВХОДА этого захода («заявок нет» при пяти открытых) — то есть уже испортил один обязательный артефакт, и испортит следующий.
- **Не повторится, законно уходит в очередь:** медленный POST входа (`## ВОПРОСЫ` п.1), незаведённый сторож (п.2), сломанная тревога (п.3), невывезенный `main` (п.4) — каждое чинится один раз.
- Столкновение теста предшественника с задачей преемника (`## ВОПРОСЫ` п.5 и урок) повторится ровно тогда, когда следующий заход снова отменит поведение предыдущего — то есть не на каждом, но регулярно; поэтому оформлено правилом «грепни тесты по отменяемой строке первым ходом», а не запретом.

### РЕЗУЛЬТАТ ВЕРИФИКАТОРА

ДО-типа, свежий субагент, другой метод, сплошная выборка по окну 08.09 05:00–14:00 (фактически до 11:16 UTC — дальше окна на момент съёма не существовало, и это ограничение выборки названо им самим, а не скрыто). Просмотрено 5211 записей. Главный вывод — уничтожать было нечего: истории у сторожа нет по построению. Сверх задания поднял два дефекта, которых заход не касался (падение `spetsmat-alert@` с `status=2/INVALIDARGUMENT` и его же неверный текст про «broken code, not a broken network»). Финальная строка получена: «выдано 25 позиций из 25 найденных».

### ШЕСТЬ СТРОК СУБАГЕНТА ГИТ-КОНТУРА (вставлены дословно)

1. заявок было 5, закрыто 0, застряло 2 (`2026-09-08T1342-bootstrap-zahod-py-scratchpad-kod-slovari`, `2026-09-08T1342-sdelat-handoff-py-bez-zakrytiya-6` — адресаты в чужом репозитории disciplina, род pravka-koda вне прав); три (`2026-09-08T0031`, `2026-09-08T0853`, `2026-09-08T1327`) оставлено исполнителю по ПРАВКЕ 2, не трогал.
2. влито в основную 0 веток из 0 названных: основная ветка — `main` (снято `git symbolic-ref refs/remotes/origin/HEAD` → `refs/remotes/origin/main`); `git --no-optional-locks branch --no-merged main` перепрогнан сейчас и дал ПУСТОЙ вывод, `grep -c 'zahod/'` = 0, `branch -r --no-merged main` тоже пуст — невлитых чужих `zahod/*` нет ни локально, ни на origin.
3. хвост Cowork ЗАБРАН пунктом 0 (`9b157dc`): `zhurnal/_INFRA-git/INCIDENTY.md`, `_studio/zhurnal/_INFRA-git/INCIDENTY.md`, `zhurnal/2026-09-02_spetsmat-bot/UROKI-FABRIKE.md`, `zhurnal/2026-09-02_spetsmat-bot/VYGRUZKA-2026-09-08.md`, `zhurnal/2026-09-02_spetsmat-bot/VYGRUZKA-2026-09-08-2.md`; отдельно `README.md` (`3ebaa06`) и две заявки 1342 (`da0a509`); вне git осталось 25 путей по `git_zona.py check` — исполнителю: 8 `kod_*.md`, `kod_skleit-adminku-s-sajtom.md.otmenen`, `M kod_veb-priyom-zadach.md`, 3 заявки; содержательное не моё: `data/spetsmat.db`, `docs/index.html`, `doc/PLAN-veb-2026-09.md`, `doc/TZ-raspredelenie-dizajn-i-dva-sloya.md`, `doc/TZ-sloj-zanyatia.md`, `SESSIYA.md`, `VHOD-2026-09-09.md`, `ZAMYSEL-profil-bezopasnosti.md`, `ZAMYSEL-vygruzka-v-tablicu.md`; мусор в `.gitignore`, не в коммит: `.DS_Store`, `zhurnal/.DS_Store`, `.commit-plan`.
4. погашено: ничего — заданием гашение не названо, ветку захода `zahod/bystro-i-bezopasno` не трогал (её в репозитории ещё нет), чужие worktree (49 штук в `spetsmat-bot-wt/`) не трогал; НЕ вывезен `main` — `vyvezti` rc=1, отказ по открытой заявке `2026-09-08T0853` рода git-operaciya, обход `--vsyo-ravno` не применял.
5. необратимое: не было — ни удалений, ни force-push, ни `--vsyo-ravno`; всё сделанное — три коммита на локальной `main`, откатываются `git reset --hard fc646dc`.
6. выдано 6 позиций из 6 найденных.

*(Строки 3 и 4 отражают состояние ДО работы исполнителя: восемь `kod_*.md` с тех пор закоммичены — `aca18f4`; ветка захода с тех пор заведена, влита в `main` и жива.)*

### ГИГИЕНА Г1–Г6 (§4.1)

- **Г1.** `git_zona.py check --zone veb/vhod.py` → ✅ · `--zone deploy/` → ✅ · `--zone ops/` → ✅ · `--zone tests/` → ✅ (все четыре: «работа доехала в git, вне git ничего нет»).
- **Г2.** Неприменимо: все пути зоны лежат внутри `spetsmat-bot`, зона за его пределы не расширялась. Проверено, а не предположено: правка коснулась ровно `veb/vhod.py` и `tests/veb/test_vhod.py`.
- **Г3.** Невлитых на входе — 0 (снимок гит-контура). Сейчас `git --no-optional-locks branch --no-merged main` → **1**, и это МОЯ собственная `zahod/bystro-i-bezopasno`: она влита в `main` (`git log main..HEAD` → 0), но продолжает существовать намеренно — см. «гашение» ниже. Чужих невлитых не прибавилось.
- **Г4.** Ни одного нового `.py` в `_generator/**` не заводил — так и пишу.
- **Г5.** Ни одного нового `.md` не заводил, `register_doc.py` не звал — так и пишу. Единственный `.md`, который я правил, — этот файл-заход, рождённый зарегистрированным.
- **Г6.** `git show --stat` по обоим своим коммитам: `53fda6a` → `tests/veb/test_vhod.py`; `c7807d0` → `veb/vhod.py`. Чужих путей нет.

### WARNING-БЛОК: ПОЛНАЯ ГИТ-ГИГИЕНА ПЕРЕД ОТЧЁТОМ

**1 · ВСЕ КОММИТЫ.** Репозитории, которых касался: `spetsmat-bot` (рабочий) и `disciplina` (только уборка своей ошибки первого хода — там не осталось ничего моего). **Вне git по своей зоне: 0** — `git_zona.py check` зелёный по всем четырём зонам. В главной папке `spetsmat-bot` `git status --porcelain` даёт **9 строк**, и ни одна не моя работа: `data/spetsmat.db` (пишется работающим сайтом) и `docs/index.html` — были грязными до меня; `doc/PLAN-veb-2026-09.md`, `doc/TZ-raspredelenie-dizajn-i-dva-sloya.md`, `doc/TZ-sloj-zanyatia.md` — чужая содержательная работа, которую гит-контур назвал «не моё»; `.DS_Store`, `zhurnal/.DS_Store`, `.commit-plan` — мусор; и девятой строкой **этот самый файл-заход**, который контракт зоны запрещает мне коммитить и который коммитит приёмка. Автолог `zhurnal/_INFRA-git/INCIDENTY.md`, отросший от моих же вызовов `git_zona.py` по ходу работы, забран отдельным коммитом `e269d4f`.

**2 · ВЛИТИЕ СВОЕЙ ВЕТКИ.** `git_zona.py vlit-v-osnovnuyu zahod/bystro-i-bezopasno --zone veb/vhod.py --zone tests/ --vsyo-ravno "своя рабочая папка ещё жива — влитие последним ходом захода, штатно"` → **✅ Влито в `main` без конфликтов**, затронуто путей 2 (`veb/vhod.py`, `tests/veb/test_vhod.py`). Конфликтов не было.

**3 · ПОСТ-ПРОВЕРКА ИЗ ГЛАВНОЙ ПАПКИ** (`/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot`, не из рабочей) — **зелёная**:
```
$ grep -n 'action="/vhod"' veb/vhod.py
193:  <form method="post" action="/vhod">
$ grep -c 'action="https://math-kluychiki' veb/vhod.py
0
$ grep -rn "obrabotchik_vhoda" veb/*.py          # живая точка вызова
veb/vhod.py:98:    return {"/vhod": obrabotchik_vhoda, "/vyhod": obrabotchik_vyhoda}
$ python3 -m pytest tests/veb/test_vhod.py -q
6 passed in 0.04s   → rc=0
```
Отката влития не потребовалось.

**4 · ГАШЕНИЕ.** `git --no-optional-locks branch --no-merged main` → **1**, поимённо: `zahod/bystro-i-bezopasno` — моя, влитая, оставлена живой намеренно. Причина: раздел `## ФАЗА ПРИЁМКИ` этого же файла требует, чтобы ветка работы СУЩЕСТВОВАЛА и была влита (Г14 краснеет, если её нет), а рабочую папку по канону гасит приёмка (`worktree drop`). Гашение своей ветки сейчас противоречило бы её собственному гейту. Чужих веток не гасил ни одной.

**5 · ВЫВОЗ. 🔴 Не вывезено, и это НЕ забывчивость.** `git_zona.py vyvezti --yes` → отказ: «Открыта заявка `2026-09-08T0853` … вывоз основной ветки `main` не иду». Обход `--vsyo-ravno` я не применял сознательно: он означал бы вывоз `main` вопреки прямому запрету §5 этого захода И публикацию поверх открытого инцидента `2026-09-02T1431` — живой токен бота в ПУБЛИЧНОМ репозитории `d1-d57/spetsmat-179`. Числа: **`main` опережает `origin/main` на 19 коммитов** (в заявке `1436` стоит 17 — число снято на момент её подачи, потом добавились коммит самой заявки и автолог; расхождение законное и названо здесь); своя ветка влита в `main` и upstream не имеет (`no upstream configured`), то есть вывозится вместе с `main` и только с ним. Поставлена заявка **`2026-09-08T1436-main-origin-main-17-bystro-i`** с актуальным числом и с порядком, снимающим блокировку (сначала `/revoke` токена владельцем, потом `push origin main`); закоммичена. Заявки `0031` и `0853` НЕ закрыты — вывоза не было, закрыть их значило бы поставить ложно-зелёное.

**6 · ПРОВЕРКА ФАКТОМ.** `spetsmat-bot`: вне git по своей зоне **0**, чужого вне git **2** (названы); невлитых **1** (своя, влитая, названа); невывезенных `main` **19** (заявка стоит); пост-проверка **зелёная**. `disciplina`: вне git **0** моего, невлитых моих **0** (ветка погашена, надгробие названо).

### ВРЕМЯ ПРОГОНА И ТОКЕНЫ

Неприменимо на канале `app`: лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении такого следа не оставляет. Снимать неоткуда.

### ОТКРЫТОЕ «ВОЗВРАЩАТЬСЯ»

1. **Проверить в четверг ДО 13:10.** Если владелец снова упрётся в медленный сайт, первое, что нужно снять, — на какой схеме он находится (адресная строка: `http://` или `https://`). После этой правки он должен оставаться на `http://` от начала до конца. Оказался на `https://` — значит он пришёл туда сам (по закладке, по ссылке из предупреждения, по автодополнению браузера), и тогда развилка другая: либо чинить 443 у провайдера, либо поднимать альтернативный порт (8443 с тем же сертификатом отвечал 06.09 за 0.10 с; сейчас он закрыт — TCP-таймаут).
2. **Кнопку «Запросить проверку» в Search Console по-прежнему не жать** — решение интервью п.3; заход тормоза разобрал, но снятие пометки остаётся ходом владельца, и жать её я не советую до четверга.
3. Незакрытое из очереди: `## ВОПРОСЫ` пп. 1–5, из них п.4 (вывоз `main`) блокирует всю сегодняшнюю работу на одном диске.

### ОХВАТ

**Проверено 7 из 7 клауз** — с одной названной оговоркой внутри клаузы 4: семь клауз `profil-bezopasnosti` перегнаны, из них **6 полностью зелёные, клауза 5 — в двух частях из трёх** (третья требует ввода настоящего пароля, что исполнителю запрещено). Клауза 5 нашего критерия неприменима: правка предложена и внедрена, поэтому судит клауза 4.

**АРТЕФАКТ:** `https://math-kluychiki.ru/vhod` — открыть в браузере: это сам боевой сайт, и продуктом захода является его поведение, а не файл. Проверяется глазами за пять секунд: адресная строка после нажатия «Войти» обязана остаться `http://`, а не перескочить на `https://`. Исходник правки — `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/veb/vhod.py` (строка 193), открывать любым редактором. Сырые замеры, если приёмка захочет пересчитать медианы: `/private/tmp/claude-501/-Users-ivanyakovlev/3f7fbcf2-b44f-4972-9395-e29364e0e45d/scratchpad/bystro-i-bezopasno/` — `seria_do.csv`, `seria_posle.csv`, `seria_dolgo.csv`, `put_do.csv`, `put_posle.csv` (CSV, 490 проб адресов + 80 проходов полного пути). 🔴 Они лежат в личном scratchpad сессии и переживут её не навсегда — числа, на которые опирается диагноз, перенесены в отчёт выше.
**РОД АРТЕФАКТА:** `исходник`
*(`собранный` — колода, PDF, картинка, любой файл, ПОРОЖДЁННЫЙ этим заходом: он обязан быть моложе файла-захода, и Г3 приёмки сверяет ВРЕМЯ. `исходник` — заход, чей продукт есть КОД: он коммитится РАНЬШЕ отчёта, потому что отчёт цитирует хэш коммита, и сверка по времени дала бы вечное ложное красное — тогда Г3 сверяет не время, а «доехал ли артефакт в названный §4 коммит». Не заполнено — Г3 работает по времени, как раньше.)*
**КОММИТ:** `c7807d0` — «vhod: form action back to relative /vhod, so the owner's session stops being pinned to the intermittently filtered port 443» · `53fda6a` — «tests/veb: replace test_entry_form_posts_to_absolute_https with its opposite, on measured grounds» · обе влиты в `main`. `git_zona.py check --zone veb/vhod.py` → ✅ · `--zone deploy/` → ✅ · `--zone ops/` → ✅ · `--zone tests/` → ✅

Сверх своей зоны, по ПРАВКЕ 2 и по указанию самого инструмента: `aca18f4` — 13 путей арки `zhurnal/2026-09-02_spetsmat-bot` (восемь `kod_*.md`, `SESSIYA.md`, `VHOD-2026-09-09.md`, оба `ZAMYSEL-*`, `.otmenen`); `c707211` — закрытие заявки `2026-09-08T1327`; `1436` — открытие заявки на вывоз. Мой собственный файл-заход в `aca18f4` НЕ вошёл намеренно (контракт зоны), его коммитит приёмка.

## ПРАВКИ ПОСЛЕ ВЫДАЧИ — (заполняет АНАЛИТИК; исполнитель ЧИТАЕТ)
> 🔴 **Пусто — значит заход не правился с момента выдачи.** Непустой блок читается ПЕРЕД продолжением работы: правка отменяет любое противоречащее ей место выше по файлу, каким бы категоричным оно ни было.
> **Форма строки — жёсткая, по ней судит приёмка:** `### ПРАВКА N · ГГГГ-ММ-ДД ЧЧ:ММ · <что изменилось, одной фразой>`, дальше — что именно перечитать и что откатить, если уже сделано по старой редакции.
> **Аналитик:** внёс правку — обязан ОТДЕЛЬНО послать владельцу короткое сообщение для пересылки исполнителю. Правка, лежащая только в файле, до работающего исполнителя не доезжает: он файл не перечитывает сам.
> **Исполнитель:** прочитал правку — назови её номер в `## ОТЧЁТ` строкой `ПРАВКИ ПРОЧИТАНЫ: 1, 2`. Нет строки при непустом блоке = отчёт не принимается: неизвестно, по какой редакции работали.

### ПРАВКА 1 · 2026-09-08 13:55 · механизм гипотезы уточнён: дело не в кнопке входа, а в том, что ПОСЛЕ входа сессия целиком переезжает на https

Перечитать §2, подраздел «ГИПОТЕЗА АНАЛИТИКА». Прежняя формулировка объясняла медленной только отправку формы — и не сходилась с жалобой владельца «КАЖДАЯ кнопка». Уточнение сходится, и оно проверяемо.

**Замер приёмки 13:50, тем же каналом, что и таблица в §2:**

| проба | результат |
|---|---|
| `http://159.194.254.52/robots.txt` | 200, тело `User-agent: *` / `Allow: /` |
| `https://math-kluychiki.ru/robots.txt` | 200, то же тело — **443 у сервера ЖИВ, снаружи отвечает** |
| `http://math-kluychiki.ru/vhod` | 200, редиректа НЕТ, остаётся на http |

**Механизм целиком.** Страница входа по http отдаётся мгновенно — её владелец и увидел («страница загрузилась»). Но форма на ней с утра уходит на `https://math-kluychiki.ru/vhod` абсолютным адресом. Значит: нажал «Войти» → POST идёт по 443 → ответ приходит с https-ориджина → куку и всю дальнейшую сессию владелец получает НА HTTPS → **каждый следующий клик тоже идёт по 443**. До утренней правки форма была относительной, владелец оставался на http от начала до конца, и 443 его не касался вовсе.

**Что это меняет в работе:**
1. Замер по пункту A обязан пройти ВЕСЬ путь, а не только заглавную: открыть `/vhod` по http, отправить форму, посмотреть, на какой схеме оказался после ответа, и мерить время уже ПОСЛЕ входа. Медиана до входа и медиана после входа печатаются как два разных числа. Одинаковые — гипотеза опровергнута, и это полноценный результат.
2. Если подтвердится — развилка узкая: `action` формы возвращается относительным, а строка-предупреждение «этот адрес без шифрования, откройте https://» со ссылкой ОСТАЁТСЯ (в `ZAMYSEL-profil-bezopasnosti.md` §4 именно она названа максимумом; принуждение к https требованием Google не было). Тогда все семь клауз профиля остаются зелёными по построению: их закрывает nginx, а не форма.
3. Ничего не откатывать до замера. Порядок прежний: замер → диагноз → правка → повторный замер.

**Отменяет** в §2 любое чтение гипотезы как «медленная только кнопка входа». Всё остальное в силе — включая запрет подыгрывать: опровержение здесь по-прежнему полноценный успех.

### ПРАВКА 2 · 2026-09-08 13:45 · РАЗОВОЕ расширение git-контура: закоммитить файлы-заходы арки

Это НЕ расширение зоны и НЕ вторая задача. Разовое поручение внутри §0.1 ГИТ-КОНТУР, который и так про состояние репозитория, а не про содержание работы. Выполняется ПЕРВЫМ ходом, до замеров, и на этом заканчивается.

**Что сделать.** Закоммитить восемь файлов-заходов арки — они вне git:

```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py commit --zone zhurnal/2026-09-02_spetsmat-bot -m "приёмка 08.09: вердикты по vygruzka-v-tablicu и profil-bezopasnosti, четыре урока фабрике, дневник" --push
```

Затем закрыть три заявки, которые этим ходом исполняются, — `2026-09-08T1327` (коммит `kod_*.md`), `2026-09-08T0031` и `2026-09-08T0853` (обе про вывоз `main` на origin, поданы исполнителями и висят незакрытыми):

```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki
```
— и по каждой исполненной `zayavka-zakryt --rezultat "<что сделано>"`.

**Почему это поручено тебе, а не сделано аналитиком.** Cowork сидит в песочнице, где `commit` запрещён по построению (git не может удалить свои временные файлы и оставляет мины, блокирующие репозиторий для всех). Владельцу git-строки не выдаются — это правило фабрики. Единственный, кто здесь host-side, — ты.

**Цена бездействия, названная числом.** Вне git восемь `kod_*.md`, среди них `kod_profil-bezopasnosti.md` (105 КБ) и `kod_vygruzka-v-tablicu.md` (87 КБ) — полные отчёты обоих ночных заходов ВМЕСТЕ с только что вписанными вердиктами приёмки. Обрыв сессии уносит их целиком, и восстановить их будет нечем. Плюс `main` опережает `origin/main`: работа существует на одном диске.

🔴 **Границы этого поручения.** Коммитишь ЗОНУ `zhurnal/2026-09-02_spetsmat-bot` и ничего сверх — ни чужих рабочих папок, ни `veb/`, ни `data/`. `git checkout` в главной папке по-прежнему ЗАПРЕЩЁН (это ровно то нарушение, за которое утренний заход получил урок фабрике). Не прошло по внешней причине — не чини чужое состояние репозитория: строка в `## ОТЧЁТ`, и дальше по своей задаче.

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `принято` — центральное утверждение отчёта приёмка перегнала САМА, живым браузером по обеим схемам, и оно сошлось до последней детали; отчёт при этом НЕ выдал желаемое за доказанное там, где доказательства не было.

**ЧТО ПЕРЕГНАЛА ПРИЁМКА СВОИМ КАНАЛОМ (08.09, живая страница, не отчёт):**

| утверждение отчёта | чем перегнано | сошлось |
|---|---|---|
| `action` стал относительным | `document.querySelector('form').getAttribute('action')` на живой странице → **`/vhod`** | ✅ |
| абсолютной формы в отдаче нет (`grep -c → 0`) | поиск `action="https://math-kluychiki` в `outerHTML` живой страницы → **false** | ✅ |
| сессия остаётся на своей схеме | `form.action` резолвится: на http-странице → `http://math-kluychiki.ru/vhod`, на https-странице → `https://math-kluychiki.ru/vhod`. **Это и есть весь результат захода, и он виден напрямую** | ✅ |
| строка-предупреждение цела (требование `ZAMYSEL` §4) | на http: `hidden=false`, видима, текст «Этот адрес открыт без шифрования. Откройте…», ссылка ведёт на `https://math-kluychiki.ru/vhod`. На https: `hidden=true`, невидима | ✅ |
| клауза 3 профиля: три заголовка | `x-content-type-options` + `referrer-policy` + `x-frame-options` → **3**, CSP на месте. 🔴 Это та клауза, которую приёмка `profil-bezopasnosti` перегнать НЕ смогла — теперь закрыта | ✅ |
| клауза 7: HSTS нет, редиректа нет | `strict-transport-security` → `null`; `http://…/vhod` → **200** | ✅ |
| 443 у сервера жив | https-страница открылась: TLS-рукопожатие 121 мс, TTFB 46 мс, полная загрузка 323 мс | ✅ |

**ОХВАТ ПРИЁМКИ: перегнано своим каналом 7 утверждений из 7, которые вообще перегоняемы без боевого пароля и без доступа к серверу.** Не перегонялись: 490 проб исполнителя (его канал — ноутбук владельца, мой — другая сеть; повторить его окно нельзя по построению), журнал systemd и вывод `pytest` (сервер).

**ПОЧЕМУ ПРИНЯТО, ХОТЯ КРИТЕРИЙ ФОРМАЛЬНО ПРОВАЛЕН ПО КЛАУЗЕ 4.** Клауза 4 требовала напечатать «7 из 7» по критерию `profil-bezopasnosti`. Исполнитель напечатал «6 из 7 полностью, клауза 5 — в двух частях из трёх» и отдельной строкой отказался писать «7 из 7». Он прав, а неправ критерий: клауза 5 требует `Set-Cookie` на УСПЕШНОМ входе, то есть ввода настоящего пароля, а ввод паролей исполнителю запрещён. **Я написал клаузу, неисполнимую им по построению — это дефект аналитика, а не брак работы.** Отказ печатать круглое число вместо честной дроби — ровно то поведение, ради которого в заходе стоит «не подыгрывай». Урок записан.

🔴 **ЧТО ИСПОЛНИТЕЛЬ НЕ СТАЛ ВЫДАВАТЬ ЗА РЕЗУЛЬТАТ — и это главное в отчёте.** Он **не воспроизвёл тормоза**: 490 проб за 38 минут, ни одного не-200, ни одного таймаута, 443 здоров в каждом окне. И сказал это прямо, отдельным красным абзацем: «I did not catch the slowness itself. What I measured is the DEPENDENCY». Отдельно вычел из подозрительной секунды после входа то, что ей не принадлежит: тот же POST по голому http стоит 0.99–1.18 с, то есть ~1 секунда — это сервер проверяет пароль, а не 443; доля 443 в здоровом окне это рукопожатие, 0.05–0.11 с. Он сам пишет: «I would have blamed the network for the server's own second had I not run the http POST». Это работа против собственного удобного вывода.

**ЧЕМ ДОКАЗАНА СВЯЗЬ, если тормоза не пойманы.** Пройден весь путь целиком, 20 прогонов до правки и 20 после, с чтением `action` С ЖИВОЙ СТРАНИЦЫ, а не из кода: **до — 20 из 20 приземлялись на https, после — 20 из 20 на http.** Медианы почти не двинулись, и исполнитель прямо запретил считать это победой: 443 в обоих окнах работал, разница 20–60 мс — шум. Правка действует не на эти числа, а на зависимость: в отфильтрованном окне строка «до» — таймаут на каждый клик, строка «после» не меняется вовсе.

**АЛЬТЕРНАТИВЫ ЗАКРЫТЫ ЧИСЛОМ, А НЕ ДОВОДОМ** — `return 444` (чужой Host → `000`, свой → 200), CSP (консоль чиста, 0 заблокированных ресурсов), ночной таймер выгрузки (отработал за 4 секунды в 02:00, в окне 05:00–14:00 ни одной записи, load average 0.04), вес страницы (gzip работает: 144 КБ → 38 КБ), OCSP stapling (выключен — проверен, а не предположен), IPv6 (записи нет, Happy-Eyeballs исключён). Остался один невыключенный подозреваемый, и он назван честно: **провайдер владельца.** Заход его не чинит — он убирает зависимость сайта от него.

🔴 **НАХОДКА, РАДИ КОТОРОЙ СТОИЛО ИДТИ ЗА ИСТОРИЕЙ: СТОРОЖ НИКОГДА НЕ СТОРОЖИЛ.** `ops/storozh_sajta.py` не установлен ничем — ни systemd-юнита, ни таймера, ни cron ни у `ivan`, ни у `root`; файла состояния нет ни на сервере, ни на ноутбуке; слово `storozh` встречается в журнале за окно **0 раз**. Проб сторожа за 08.09 05:00–14:00 — **0**. Инструмент был сдан заходом `https-i-domen` как продукт, принят, и не был подключён; «тревога только на смену состояния» для запускаемого руками инструмента означает молчание всегда. Именно его история требовалась сегодня — и её не существует. Это чистый случай «правило без рычага есть надежда», только про инструмент: сторож без точки вызова зелен ровно потому, что его никто не звал. → урок фабрике, дом `priyomka` (ловить обязана она — гейт Г16 проверяет «механизм ВСТАЛ» только для `.py` под `_generator/**`, а `ops/**` не смотрит).

**ВТОРОЙ СЛОМАННЫЙ РЫЧАГ, найденный тем же ходом.** Тревога `spetsmat-alert@` 08.09 в 05:41–05:42 сработала дважды: первая дошла и НАЗВАЛА ПРИЧИНУ НЕВЕРНО («five failed starts means broken code, not a broken network» — при том, что в том же журнале лежит `TelegramNetworkError: Request timeout error`, то есть ровно сломанная сеть), вторая не дошла вовсе (`status=2/INVALIDARGUMENT`). Тревога, посылающая читателя чинить единственное, что не сломано, хуже отсутствующей. → пункт владельцу.

**ДЕФЕКТЫ ФОРМЫ, НАЗВАННЫЕ ВСЛУХ.**
1. **Г3 остаётся красным, и я его не закрашиваю.** Строка `**АРТЕФАКТ:**` несёт URL `https://math-kluychiki.ru/vhod`, а гейт резолвит путь от корня репозитория — правильный адрес здесь `veb/vhod.py`. Существо гейта («артефакт не старше источника») при этом выполнено СИЛЬНЕЕ, чем он умеет проверить: приёмка открыла живую страницу и увидела на ней новый `action`, то есть отдаётся именно правленый код, а не mtime файла. Красное честное, содержание зелёное — так и записано, вместо правки чужой половины файла.
2. **Г12 краснел на форме галочки** (`**…ЗАКРЫТЫ: нет.**` вместо `**…ЗАКРЫТЫ:** нет`) — двоеточие вынесено за жирный аналитиком, содержание не тронуто.
3. **Мой собственный промах, найденный исполнителем.** Две заявки `2026-09-08T1342` я подал в очередь `spetsmat-bot`, а их адресаты — `bootstrap_zahod.py` и `sdelat_handoff.py` — живут в `disciplina`. Гит-контур закрыл обе как ЗАСТРЯЛА и был прав: заявка о дефекте инструмента идёт в очередь ТОГО репозитория, где инструмент лежит. Перевыставить в `disciplina`.

**ВЕТКА РАБОТЫ:** `zahod/bystro-i-bezopasno`
*Проверено фактом: `git branch --merged main` содержит её — ВЛИТА (Г14 зелёный). Ветка-двойник, заведённая по ошибке в чужом репозитории `disciplina`, снята самим заходом: `poteri` → «Потерь нет, проверено 1 из 1», надгробие `mogila/zahod/bystro-i-bezopasno` → `b23074df` на месте, воскрешается одной командой.*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ:**

- `2026-09-08T1436-main-origin-main-17-bystro-i` — `git-operaciya` — поставлена самим заходом, приёмка её ПОДТВЕРЖДАЕТ и перегнала число своей командой: `git rev-list --count origin/main..main` → **19** (на момент приёмки, было 17 на момент захода). Вся сегодняшняя работа — оба ночных захода, оба вердикта приёмки и эта починка — существует на одном диске.

*Своих новых заявок эта приёмка не ставила: всё, что требовалось, уже в очереди; закрывать заявки из песочницы нельзя (`zayavka-zakryt` только host-side).*

**ПУНКТЫ ВЛАДЕЛЬЦУ — три, все требуют человека:**

1. 🔴 Заявка `2026-09-02T1431` (живой токен бота в ПУБЛИЧНОМ `d1-d57/spetsmat-179`) блокирует вывоз 19 коммитов. 07.09 в 11:33 сказано «токен давно поправлен, всё можно вывозить», но 07.09 в 16:02 гит-контур перепроверил КОМАНДОЙ и подтвердил только часть условия: `git remote` → репозиторий публичный. Что токен отозван — не проверял никто. Развилка на один ответ: токен уже отозван в @BotFather → заявку закрыть и вывозить; не отозван → `/revoke` и новый токен в `secrets/bot.env`, потом вывоз.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
2. Сторож сайта не подключён ничем и не сторожил ни разу. Подключать таймером — отдельная работа; решение, делаем ли её до четверга, за владельцем.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
3. Тревога `spetsmat-alert@` называет причину неверно и вторым срабатыванием не доходит вовсе.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

**ВЕТКА РАБОТЫ:** `zahod/bystro-i-bezopasno`
*(проверяется фактом, не словом: ветка обязана существовать и быть либо ВЛИТА в основную, либо названа в открытой заявке на влитие. Ни того, ни другого — Г14 краснеет. Снять состояние: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py poteri --branch <ветка>`)*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**
> Адрес списка: `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/_INFRA-git/zayavki`
> Читается командой (из любой папки, в том числе из worktree): `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki`
> Ставится командой: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavka --rod <git-operaciya|pravka-koda> "<текст>"`
> 🔴 Вопрос здесь НЕ «что ты хочешь сделать», а «что ты УЖЕ положил в очередь». Дубль сверяется с очередью по id машинно; намерение сверить не с чем.

*(Список продублирован выше, в блоке вердикта — там же названо, почему своих новых заявок эта приёмка не ставила.)*

*(Заявок эта приёмка не ставила — так и напиши строкой «заявок нет: <почему ни одна из пяти операций не понадобилась>». Пустая строка и прочерк не принимаются: молчание неотличимо от «забыл».)*
