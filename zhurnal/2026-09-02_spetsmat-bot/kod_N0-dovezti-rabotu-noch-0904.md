# Канал исполнителя — N0-dovezti-rabotu-noch-0904 (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: openrouter/z-ai/glm-5.2:free** — ВРЕМЕННАЯ — реестр протух; первый по приоритету под род работы «instrumenty» из профиля `BESPLATNYE`; подменяется оркестратором при запуске (`orkestr.py --rezhim progon`)..

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

🔴 ПРОСТОЙ ПУТЬ (канал app, рекомендуется): открыть Claude Code в папке
`~/Documents/GitHub/spetsmat-bot` и дать ОДНУ строку:

```
Открой zhurnal/2026-09-02_spetsmat-bot/kod_N0-dovezti-rabotu-noch-0904.md — это твоё задание, читай целиком и выполни. Ничего сверх него не делай.
```

Оркестратор для этой работы не нужен: заход одиночный, без субагентов и без ветки.
Строка ниже — путь через оркестратор, на случай если заход подхватывает автоматический прогон.


> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
cd /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot && python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/orkestr.py zhurnal/2026-09-02_spetsmat-bot --rezhim progon --dvizhok opencode --rod instrumenty --model openrouter/z-ai/glm-5.2:free --zahody kod_N0-dovezti-rabotu-noch-0904.md < /dev/null 2>&1 | tee /tmp/zahod-N0-dovezti-rabotu-noch-0904.log
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
ГРАНИЦА ОБЛАСТИ: сырые подстроки в `kod_*.md` (пункт 4) — НЕ парсер очереди `dostavit_urok` (который считает только пары ДОМ:/ДОСТАВЛЕНО:). Разница в числах — законна.
🔴 снимок при сборке 2026-09-04, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/schet_nezakrytogo.py zhurnal/2026-09-02_spetsmat-bot`
Область: «zhurnal/2026-09-02_spetsmat-bot» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 25
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 222
     из них разбором очереди (парсер `dostavit_urok`, записи с парой ДОМ:/ДОСТАВЛЕНО:): 122
       живых (чинится доставкой — «дом есть»)  : 19
       к владельцу (решение за человеком)      : 44
       адрес недоступен (нет/папка/код/указат.) : 57
       адрес не разобран                        : 0
       отработавших (машинный след закрытия)    : 0
       доставлено                               : 2
       🔴 не проверяется машиной: содержательная отработанность записей БЕЗ следа закрытия (метки в доме, строки ✅/ЗАКРЫТО) — нужна ревизия человеком; сырой греп сверх разбора — шаблонные строки формы.

🔴 ДВЕРЬ НЕЗАКОММИЧЕННЫХ `kod_*.md` ОТКРЫТА КЛАПАНОМ: в арке `zhurnal/2026-09-02_spetsmat-bot` есть 7 незакоммиченных файлов-заходов `kod_N1-slot-vmesto-weekday.md`, `kod_N2-chelovek-bez-klassa.md`, `kod_N3-vhod-cherez-telegram.md`, `kod_N4-priyom-zadach.md`, `kod_N5-konduit-proshlogo-goda.md`, `kod_N6-stranica-pages-i-zaglushka.md`, `kod_N7-repozitorii-pages-tunnel.md` — второй заход в такую арку обычно не собирается (долг 4 `disciplina-git`), но АНАЛИТИК открыл клапан, причина дословно: «вся арка и есть предмет этого захода: он существует ровно для того, чтобы незакоммиченное стало закоммиченным». Отключение видно здесь, в артефакте, а не осталось решением в голове аналитика.

КОНТЕКСТ. spetsmat-bot — телеграм-бот учёта сданных задач спецмата 179-й школы; рядом
строится веб-страница распределения, которую надо разослать преподавателям. Прошлый этап:
в ночь на 04.09 аналитик починил две регрессии тестов, собрал мандат новой волны ОСНОВАНИЕ,
семь заходов N1..N7 и три документа с решениями — и НИЧЕГО из этого не закоммичено, потому
что коммит из песочницы Cowork запрещён, а владелец отказался исполнять команды руками.
ЦЕЛЬ: довезти эту работу в git двумя коммитами и предъявить два хэша.
Приёмка — по ОТЧЁТУ, без построчной сверки. Стоп до цели: ты получишь два коммита в
локальном репозитории, но НЕ вывоз на origin — его у репозитория нет вовсе, и заводить его
в этом заходе НЕ надо (это предмет позиции N7 той же волны).

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-09-04) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. владелец отказался исполнять команды коммита руками, дословно: «Я не могу, извини. Это очень сложно… тут слишком много команд, и они сейчас начнут путаться, теряться. Я не понимаю, откуда что запускать». Заход заведён вместо ручного исполнения — это и есть его причина существования
2. у spetsmat-bot НЕТ origin — вывозить некуда, флаг --push НЕ ставить. Это не упущение, а известное состояние: заведение origin входит в позицию N7 той же волны
3. работа ночи 04.09 существует в ЕДИНСТВЕННОМ экземпляре на диске владельца: мандат волны, семь заходов N1..N7, три документа с решениями и пять уроков. До этих двух коммитов потеря диска означает потерю всего

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `main` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `veb/vhod.py` `veb/server.py` `tests/veb/test_server.py` `deploy/ustanovka.sh` `deploy/spetsmat-veb.service` `zhurnal/2026-09-02_spetsmat-bot` `./README.md` `zhurnal/2026-09-02_spetsmat-bot/kod_N0-dovezti-rabotu-noch-0904.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4); коммить ПО ХОДУ работы, не одним последним ходом (§4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **SCRATCHPAD — ТОЛЬКО ЛИЧНЫЙ.** Черновики, выкладки, промежуточные версии — в личную папку СВОЕГО захода `scratchpad/N0-dovezti-rabotu-noch-0904/`. Общие пути (`scratchpad/otchet.md`, любой `scratchpad/*` без имени твоей темы) ЗАПРЕЩЕНЫ: чужой отчёт уедет в твой файл или твой — в чужой, а приёмка читает отчёт без построчной сверки и подмену НЕ ЛОВИТ по построению. *Цена 25.08: готовый `## ОТЧЁТ` захода konvejer-incidentov был записан в общий `scratchpad/otchet.md`, и 92 строки чужого отчёта простояли в `kod_slovari-v-kod.md`.*
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода «kod_registracia-bez-obhoda.md»).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает названные через `--vlit` ЧУЖИЕ ветки В ОСНОВНУЮ, и только ПОТОМ ты заводишь свою рабочую папку; СВОЮ ветку он не трогает никогда — её вливаешь ты сам последним ходом (граница прав ниже).** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. 🔴 ПОРЯДОК ПРИ ПАДЕНИИ ЭТОГО ВЫЗОВА — исполняй, не изобретай (движок роняет `network_error` на вызове субагента и уносит с собой ВЕСЬ заход, замер волны 3A — 4 захода из 13). Пауза 45 секунд, до трёх попыток; время меряй `date`, не суммой своих `sleep`. После третьей — работай БЕЗ субагента: контур остаётся неразобранным, и это ОТДЕЛЬНАЯ строка в `## ОТЧЁТ`, а не молчание. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты сам, по ходу работы (§4). 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone veb/vhod.py --zone veb/server.py --zone tests/veb/test_server.py --zone deploy/ustanovka.sh --zone deploy/spetsmat-veb.service --zone zhurnal/2026-09-02_spetsmat-bot --zone README.md --kommitit 'ДВА коммита, ровно в этом порядке и с этими сообщениями. ПЕРВЫЙ, код регрессий: пути veb/vhod.py veb/server.py tests/veb/test_server.py deploy/ustanovka.sh deploy/spetsmat-veb.service, сообщение: регрессии: сторож окружения переехал с импорта на старт сервера, секреты читаются при вызове, veb.service вписан в ustanovka.sh и получил OnFailure' --kommitit 'ВТОРОЙ, волна и журнал: пути zhurnal/2026-09-02_spetsmat-bot и README.md, сообщение: волна ОСНОВАНИЕ: мандат, состав, семь заходов; сверка волны сайт с реальностью, диагноз stop-хука, уроки 22-26. README.md обязателен В ЭТОМ ЖЕ коммите: в нём строки регистрации всех новых документов, без него они уедут сиротами' --zakryt 'веток эта работа не заводит, гасить нечего; работа идёт прямо на main'
```

🔴 ГРАНИЦА ПРАВ, ЧЕТЫРЕ ОТВЕТА (та же, что в самом задании субагенту — одно место в тексте, а не пересказ): **кто вливает ЧУЖИЕ названные (`--vlit`) ветки** — субагент, в ОСНОВНУЮ ветку, до заведения твоей папки; **кто вливает СВОЮ ветку этого захода** — ты сам, последним ходом, после коммита зоны (`git_zona.py vlit-v-osnovnuyu`; решение владельца 25.08 — оно сняло противоречие волны 4, когда машинное §0.1 и текстовое «ветку НЕ вливать» спорили молча, и машинное побеждало); **кто закрывает заявки** — субагент, `zayavka-zakryt`; **кто коммитит пути ВНЕ зоны захода** — субагент (хвост Cowork и что назовёт пункт 3 его задания). Ты коммитишь ТОЛЬКО зону этого захода, по ходу работы (§4). 🔴 Конфликт на `README.md` при ЛЮБОМ слиянии разрешается ОБЪЕДИНЕНИЕМ записей реестра, НИКОГДА выбором стороны: параллельные заходы волны дописали в реестр по строке — обе записи правы, выбор одной молча уничтожает регистрацию соседа.

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-09-04, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged main | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.


- деплоя в этом заходе нет.

- Проверь ветку: `git branch --show-current` — обязано быть `main`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add veb/vhod.py veb/server.py tests/veb/test_server.py deploy/ustanovka.sh deploy/spetsmat-veb.service zhurnal/2026-09-02_spetsmat-bot README.md zhurnal/2026-09-02_spetsmat-bot/kod_N0-dovezti-rabotu-noch-0904.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: `названные файлы-якоря`. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

🔴 **WRITE YOUR `## ОТЧЁТ`, `## ПЛАН` AND `## ВОПРОСЫ` IN ENGLISH, AND EVERY FILE AND EVERY COMMIT MESSAGE YOU PRODUCE TOO.** Owner's decision 30.08. It is a каркас-level rule, not a preference — wave 2 lost it twice because the pass text listed the report SECTIONS and never said «every file you create». Fixed Russian addresses stay Cyrillic: `ЦЕНА:` · `ВЕРДИКТ:` · `ДОМ:` · `ДОСТАВЛЕНО:` · `ПОДЪЁМ:` · `[ДОЛГ: …]` · every `## ` heading of this file · every path and command.
Конкретные шаги — у автора. **КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):** живой прогон на реальном объекте репозитория (не только фикстура) — `python3 -c '<команда прогона>'` на собранном файле или `bash _generator/tools/fixtures/bootstrap_zahod/PROGNAT.sh` → `rc=0` и все ловушки зелёные..
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)

Верификатор нужен, тип — **ПОСЛЕ-типа** — судит результат, стоит в конце, после задачи. Свежий субагент, ДРУГИМ методом (после обоих коммитов предъявить ДВА хэша и прогнать git status: ни один из перечисленных в зоне путей не должен остаться со статусом M или вопросительными знаками. Метод именно прогон status и log, а не перечитывание собственных команд коммита), не перечитывает свою же правку. Доля сплошной выборки: все семь путей зоны, 7 из 7. Финальная строка ответа обязательна дословно: «выдано N позиций из M найденных» — без неё ответ считается усечённым и в отчёт не вставляется.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПО ХОДУ РАБОТЫ, НЕ ОДНИМ ПОСЛЕДНИМ ХОДОМ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая. Каждую завершённую часть работы коммить СРАЗУ, теми же двумя ходами — не копи всё к финальному ходу:
```
git --no-optional-locks add -- veb/vhod.py veb/server.py tests/veb/test_server.py deploy/ustanovka.sh deploy/spetsmat-veb.service zhurnal/2026-09-02_spetsmat-bot README.md zhurnal/2026-09-02_spetsmat-bot/kod_N0-dovezti-rabotu-noch-0904.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- veb/vhod.py veb/server.py tests/veb/test_server.py deploy/ustanovka.sh deploy/spetsmat-veb.service zhurnal/2026-09-02_spetsmat-bot README.md zhurnal/2026-09-02_spetsmat-bot/kod_N0-dovezti-rabotu-noch-0904.md   # отсекает всё чужое
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **КОММИТЬ ПО ХОДУ — РЕШЕНИЕ ВЛАДЕЛЬЦА 25.08 (В11), ПЕРЕВЕРНУВШЕЕ прежний канон «одним последним ходом».** Цена прежнего канона: за сутки ДВА обрыва — канал `opencode run --auto` односторонний и умирает вместе с сессией (владелец закрыл ноутбук), и незакоммиченная работа пропадала целиком. Закончил кусок — закоммитил его; последний ход только ПРОВЕРЯЕТ, что коммитить нечего (`git status --porcelain` пуст, `git_zona.py check --zone` ✅).
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `veb/vhod.py` `veb/server.py` `tests/veb/test_server.py` `deploy/ustanovka.sh` `deploy/spetsmat-veb.service` `zhurnal/2026-09-02_spetsmat-bot` `README.md` `zhurnal/2026-09-02_spetsmat-bot/kod_N0-dovezti-rabotu-noch-0904.md`

- **Г1. Зона доехала в git.** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone veb/vhod.py` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone veb/server.py` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone tests/veb/test_server.py` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone deploy/ustanovka.sh` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone deploy/spetsmat-veb.service` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone zhurnal/2026-09-02_spetsmat-bot` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone README.md` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone zhurnal/2026-09-02_spetsmat-bot/kod_N0-dovezti-rabotu-noch-0904.md` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `spetsmat-bot` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged main` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — НЕПРИМЕНИМО: движок `opencode`, счётчика стоимости в логе нет** (лог `.log` — обычный текст без `result`-строки, число снимать неоткуда; строку не заполнять числом и не извиняться за его отсутствие) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## ⚠️🔴 WARNING · ПОСЛЕДНИЙ ХОД ПЕРЕД ОТЧЁТОМ — ПОЛНАЯ ГИТ-ГИГИЕНА. НЕ ПРОПУСКАТЬ 🔴⚠️

**СТОП. Прежде чем писать хоть строку в `## ОТЧЁТ` — прогони это целиком.** Требование владельца
2026-08-16, поводом стал заход, который сделал работу и не закоммитил НИЧЕГО: *«он ничего не
коммитит… всегда, когда собираешь по генератору, там должна быть фраза, что начинать надо с полной
проверки гигиены»*. Гит-контур §0.1 стоит в НАЧАЛЕ и разбирает то, что накопилось ДО тебя; этот блок
стоит в КОНЦЕ и разбирает то, что накопил ты сам. Один другого не заменяет.

**ПОРЯДОК ЖЁСТКИЙ, ОН НАЗВАН ВЛАДЕЛЬЦЕМ: коммит → влитие своей ветки в основную → пост-проверка
ИЗ ГЛАВНОЙ ПАПКИ → гашение → вывоз.** Обратный порядок не работает технически: влитие отказывает
на грязном дереве, пост-проверка неисполнима до влития, вывоз — на невлитом. **Решение владельца
20.08 пересматривает решение 05.08** («в конце может это worktree вливать, но, наверное, это не
нужно делать») — теперь вливает САМ заход, но только при зелёной пост-проверке.

**1 · ВСЕ КОММИТЫ.** Ничего не осталось вне git — ни в рабочем репозитории, ни в соседних, до
которых ты дотянулся по ходу работы:
```
for R in <репозитории, которых ты касался>; do
  echo "== $R"; git -C $R --no-optional-locks status --porcelain
done
```
Своя зона — своими путями (`add` + `commit -- <пути>`). Чужая содержательная работа — НЕ твоя:
называешь строкой в отчёте и оставляешь. Пусто у всех — так и напиши числом «вне git 0».

**2 · ВЛИТИЕ СВОЕЙ ВЕТКИ В ОСНОВНУЮ.** Только после того, как шаг 1 дал «вне git 0» на своей
зоне — влитие отказывает на грязном дереве:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py vlit-v-osnovnuyu main --zone <своя зона> \
    --vsyo-ravno "своя рабочая папка ещё жива — влитие последним ходом захода, штатно"
```
Конфликт — ЗАКОННЫЙ исход, не повод форсировать: разрешай по существу, если понимаешь обе
стороны; не понимаешь — `git_zona.py vlit-v-osnovnuyu --abort`, ветка остаётся невлитой,
строка в отчёт и заявка на влитие (`git_zona.py zayavka --rod git-operaciya`).
🔴 Конфликт на `README.md` — только ОБЪЕДИНЕНИЕМ записей реестра, никогда выбором стороны:
параллельные заходы волны дописали по строке — обе записи правы, выбор одной молча уничтожает
регистрацию соседа.

**3 · ПОСТ-ПРОВЕРКА ИЗ ГЛАВНОЙ ПАПКИ.** Отвечает на вопрос «механизм ВСТАЛ», а не «коммит
виден»: прогон изменённого механизма (здесь она же — checkout-режим, папка одна) плюс `grep` по ЖИВОМУ файлу,
который его зовёт (хук, конвейер, генератор):
```
cd /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot && <команда прогона механизма, который заход менял> && echo $?
grep -n '<как механизм назван в вызывающем коде>' <живая точка вызова>
```
🔴 **Красная пост-проверка = ОТКАТ ВЛИТИЯ И СТРОКА В ОТЧЁТ**, а не «доложу, пусть приёмка
решает»: `git_zona.py vlit-v-osnovnuyu --abort`, если слияние ещё не закоммичено, иначе
`git -C /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot --no-optional-locks reset --hard <хэш ДО влития>`. Заход, который влил
и сломал `main`, обязан вернуть `main` сам.

**4 · ГАШЕНИЕ.** Невлитого не осталось: `git --no-optional-locks branch --no-merged main`.
Каждая оставшаяся ветка названа поимённо с причиной, почему она жива. 🔴 Ветку-витрину (`main`
там, где с неё публикуется сайт) НЕ вливать — влитие туда есть публикация и решение владельца.

**5 · ВЫВОЗ.** Вывези СВОЮ ветку; `main` НЕ вывози: если после работы в нём есть невывезенное,
поставь заявку `--rod git-operaciya` и назови число в отчёте. Команда для своей ветки:
`git --no-optional-locks log --oneline @{u}.. | wc -l` → 0.
Ненулевое на своей ветке означает, что работа существует только на этом диске.

**6 · ПРОВЕРКА ФАКТОМ, А НЕ ПАМЯТЬЮ.** Числа по каждому репозиторию — вне git · невлитых своих
и чужих; невывезенных СВОЕЙ ВЕТКИ, не по каждому репозиторию · результат пост-проверки
(зелёная/откачена) — печатаются командой и уходят в `## ОТЧЁТ` дословно. Ненулевое число или
красная пост-проверка без объяснения — приёмка читает как несделанную работу: она гоняет те же
команды первым ходом.

🔴 **Отчёт без этих чисел не принимается.** «Я закоммитил» — не то же самое, что `status --porcelain`
пустой: за одну сессию работа не доезжала трижды, каждый раз с честным «сделано» в отчёте.
## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### `git_zona.py zayavki` без `GIT_ZONA_REPO` молча читает ЧУЖОЙ репозиторий и отвечает «✅ заявок нет»
ЦЕНА: замерено в этом прогоне двумя вызовами подряд из одной и той же папки `spetsmat-bot`. Без переменной: `✅ заявок нет · Охват: заявок открыто 0, переадресовано 0`. С `GIT_ZONA_REPO=/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot`: `Открытых заявок: 1`, а всего в очереди 24 записи. Ноль против двадцати четырёх — и ноль приходит с зелёной галочкой и словом «Охват», то есть выглядит как проверенное состояние, а не как чтение не того диска. Эта команда стоит ЧЕТВЁРТОЙ строкой в обязательном «СНИМКЕ ВХОДА», который заход требует снять ДОСЛОВНО и первым ходом, — то есть дефект попадает ровно в тот блок, который приёмка считает доказательством. В этом прогоне ложный ноль в снимок и попал: субагент гит-контура снял его добросовестно и обнаружил подмену только потому, что СЛЕДУЮЩАЯ команда (`plan`) сама предупредила про переменную. Не предупреди она — 24 заявки чужого репозитория были бы объявлены пустой очередью, а Г12 позеленел бы на этом. Тот же класс, что урок «зона-префикс даёт ложно-зелёное»: инструмент отвечает не «не знаю», а «пусто».

### `schet_nezakrytogo.py` не видит арку, живущую вне репозитория `disciplina`, и отказывает — а шапка захода печатает его числа как снятое состояние
ЦЕНА: команда стоит в шапке с пометкой «🔴 ПРОВЕРЬ ПЕРВЫМ ХОДОМ», и проверить её нечем. Три формы области дали `rc=1` и «под область не попало ни одного файла»: `zhurnal/2026-09-02_spetsmat-bot`, подстрочная `2026-09-02_spetsmat-bot` и тот же вызов из корня `disciplina`. Причина структурная, вычитана из исходника: `REPO_ROOT` прибит к `disciplina`, реестр журналов — `korni.все_журналы()` → `('_studio/zhurnal', 'ot-maksa/arka')`, а `_roots_dlya_oblasti` разрешает область как `REPO_ROOT / oblast` и абсолютный путь отвергает явной проверкой `os.path.isabs`. Арка `spetsmat-bot` не лежит ни под одним корнем, и попасть туда ей нечем. Итог: напечатанные в шапке «25 уроков без ВЕРДИКТ» и «222 пункта очереди» не воспроизводимы НИ ОДНИМ исполнителем этого репозитория, а на них опирается напечатанный тут же приоритет владельца «разобрать инциденты важнее, потом закрыть долги». Прямая цена этого прогона — четыре вызова инструмента и чтение его исходника, только чтобы отличить «я неверно назвал область» от «инструмент не видит этот репозиторий»; постоянная цена — каждый следующий заход в любую арку вне `disciplina` заплатит то же самое заново, и заплатит молча, потому что отказ выглядит как ошибка исполнителя.

### КРИТЕРИЙ ГОТОВНОСТИ приехал шаблоном от захода, который строит инструмент, и называет файл, которого в этом репозитории нет
ЦЕНА: критерий дословно требует «`bash _generator/tools/fixtures/bootstrap_zahod/PROGNAT.sh` → rc=0 и все ловушки зелёные». В `spetsmat-bot` нет ни этого файла, ни каталога `_generator` вовсе (`ls _generator` → `No such file or directory`); файл существует только в `disciplina`. Исполненный буквально, критерий либо падает на несуществующем пути, либо — что хуже и вероятнее — исполняется в ЧУЖОМ репозитории и даёт зелёное, заработанное на чужой фикстуре: заход, не написавший ни строки кода, предъявил бы зелёный прогон генератора как доказательство своей работы. Здесь это стоило явной замены критерия в `## ПЛАН` до работы (прогон собственного набора тестов репозитория, 954 passed / 13 skipped, rc=0) и риска, что приёмка сочтёт замену самовольной. Общая закономерность: «род работы» в сборке (`--rod instrumenty`) подставляет критерий, не спрашивая, есть ли в целевом репозитории названный им объект.

> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Goal, restated in one line.** The night-04.09 work exists in a single copy on this disk and is not in git. Land it on `main` in the main folder as TWO commits and report both hashes. No push: this repository has no `origin` at all, and creating one belongs to position N7 of the same wave.

**Entry state, taken by command on the first move — not from the assembly header.**
- `git branch --show-current` -> `main`. The contract's МЕСТО РАБОТЫ holds, so no `--worktree` and no `git checkout` anywhere in this pass.
- `git remote -v` -> empty (rc=0). Confirms interview finding 2. `--push` / `vyvezti` are never invoked.
- `git branch --no-merged main` -> empty; `... | grep -c 'zahod/'` -> `0`. Matches the assembly snapshot; nothing to merge.
- `git status --porcelain | wc -l` -> `30`.
- `.git/hooks/` contains no hooks at all (not even the shipped samples). The whole `pre-commit` red-hook branch of §4 — including any legitimate `--no-verify` — is inapplicable in this repository. Stated here so that its absence in the report reads as "checked", not as "forgotten".

**Contested premise 1 — the СЧЁТ НЕЗАКРЫТОГО command in the header cannot be run against this arc, and its printed numbers are therefore unreproducible.** `schet_nezakrytogo.py zhurnal/2026-09-02_spetsmat-bot` returns `rc=1` with "под область не попало ни одного файла", and so does the substring form `2026-09-02_spetsmat-bot`, and so does the same call made from the `disciplina` repository root. Reason, read out of the tool: `REPO_ROOT` is pinned to `disciplina`, the registry of journals is `korni.все_журналы()` -> `('_studio/zhurnal', 'ot-maksa/arka')`, and `_roots_dlya_oblasti` resolves the area as `REPO_ROOT / oblast` while explicitly rejecting absolute paths. This arc lives in `spetsmat-bot`, i.e. outside every root the tool can see. By the tool's own words this is a refusal, not a state — so the header's "25 lessons without ВЕРДИКТ" and "222 queue items" are numbers no first move can confirm. Filed as a factory lesson with its price, not silently worked around.

**Contested premise 2 — the КРИТЕРИЙ ГОТОВНОСТИ as written names the wrong object, and I am replacing it before doing the work, as §1 invites.** It reads "живой прогон на реальном объекте репозитория ... `python3 -c '<команда прогона>'` на собранном файле или `bash _generator/tools/fixtures/bootstrap_zahod/PROGNAT.sh`". That is the criterion of a pass that BUILDS a generator tool; this pass builds nothing and ships no `.py`. Ran literally it would be a green light earned on somebody else's fixture. The mechanism this pass actually lands is the web entry guard plus the unit wiring, so the criterion I hold myself to, and which can genuinely fail, is: the repository's own test suite runs green on the working tree that goes into commit 1 — `python3 -m pytest tests/veb -q` and `python3 -m pytest -q`, both `rc=0` — plus a `grep` over the live call sites that the two deploy files changed. This is stated before the work, not after the result was known.

**Steps.**
1. §0.1 git contour: one subagent, full rights, its six lines quoted verbatim into `## ОТЧЁТ` and its snapshot into `## ГИГИЕНА ВХОДА`. `--vlit` is empty, so its merge point is a confirmation, not an action.
2. Live run of the mechanism of commit 1 (criterion above), before committing, so a red suite blocks the commit rather than being discovered after it.
3. COMMIT 1 — code regressions: `veb/vhod.py` `veb/server.py` `tests/veb/test_server.py` `deploy/ustanovka.sh` `deploy/spetsmat-veb.service`. Two moves, `add` then `commit -- <same paths>`, `--no-optional-locks` on both.
4. COMMIT 2 — wave and journal: `zhurnal/2026-09-02_spetsmat-bot` and `README.md`, in one commit because `README.md` carries the registry lines of every new document and splitting them would orphan the documents.
5. Г1–Г6 hygiene, each by command, output verbatim.
6. ПОСЛЕ-verifier: a fresh subagent, method `git status` + `git log` on all seven zone paths, 7 of 7, never a re-reading of my own commit commands.
7. Report, then a THIRD commit landing this file. Explained below, because it is a deviation from "two commits" and must not look like one slipped in.

**Why there is a third commit, and why it does not make this "three commits of work".** The deliverable is two commits, and it is two: the code regressions and the wave. But `## ОТЧЁТ` has to quote the hashes of those two, so this file necessarily changes after they exist — while Г1 demands `git_zona.py check --zone zhurnal/2026-09-02_spetsmat-bot` green, and that path contains this file. The two cannot both hold without a commit that lands the report itself. §5 already anticipates exactly this case and names it: РОД АРТЕФАКТА `исходник`, "он коммитится РАНЬШЕ отчёта, потому что отчёт цитирует хэш коммита". So: commits 1 and 2 are the two hashes of the deliverable; commit 3 carries nothing but this pass file with its report. Its own hash is not quotable from inside itself — the report names the command that prints it instead.

**What I do not touch,** even though it sits in `git status` right next to my zone: `_studio/veb-raspredelenie/TZ.md`, `deploy/ADRES.txt`, `zhurnal/.DS_Store`. All three are outside the zone; the contour subagent already reported them as content work that is nobody's index tail. They go into the report as a line, not into a commit.

## ВОПРОСЫ — (заполняет исполнитель)

1. `git_zona.py zayavki` без `GIT_ZONA_REPO` отвечает «✅ заявок нет» вместо отказа, читая чужой репозиторий; в обязательном СНИМКЕ ВХОДА это даёт ложно-зелёный Г12. Развилка на выбор владельца: инструмент определяет репозиторий по CWD, либо отказывается работать без переменной. Молчаливое чтение чужого диска — единственный исход, который не годится.
   ДОМ: zhurnal/2026-09-02_spetsmat-bot/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

2. `schet_nezakrytogo.py` отказывает на любой арке вне репозитория `disciplina` (`REPO_ROOT` прибит, абсолютный путь области отвергается явной проверкой), а шапка захода печатает его числа как снятое состояние и велит проверить их первым ходом.
   ДОМ: zhurnal/2026-09-02_spetsmat-bot/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

3. КРИТЕРИЙ ГОТОВНОСТИ подставляется по `--rod` и называет `_generator/tools/fixtures/bootstrap_zahod/PROGNAT.sh` — файл, которого нет в целевом репозитории. Сборка не проверяет, существует ли в репозитории объект, названный критерием.
   ДОМ: zhurnal/2026-09-02_spetsmat-bot/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

4. Заход требует РОВНО ДВА коммита, а §5 требует, чтобы `## ОТЧЁТ` цитировал их хэши, и Г1 требует, чтобы папка `zhurnal/2026-09-02_spetsmat-bot` (в которой лежит сам файл захода с отчётом) была чиста. Три требования вместе неисполнимы без третьего коммита, несущего один только отчёт. Здесь третий коммит сделан и назван прямо; развилка для канона — либо разрешить его явно одной строкой, либо перестать требовать чистоты папки захода.
   ДОМ: zhurnal/2026-09-02_spetsmat-bot/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

5. В арку во время прогона писал ЧУЖОЙ живой процесс: между снимком входа и коммитом 2 появились `VYGRUZKA-2026-09-04.md`, `-2`, `-3` (02:27, 02:28, 02:29 — три файла по 22–23 КБ, почти одинаковых) и `NADEZHNOST-zakaz-na-resyorch.md`, а `SESSIYA.md` был переписан уже ПОСЛЕ коммита 2, в 02:42. Три почти совпадающие выгрузки одной сессии похожи на повторный прогон одного и того же дампа. Вопрос владельцу: нужны ли все три, или две из них — мусор, который стоит убрать до того, как он закрепится в истории.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

6. В арке лежит нулевой файл `zhurnal/2026-09-02_spetsmat-bot/.probe_del2` (0 байт, чей-то пробник удаления). Он попал в коммит 2, потому что pathspec зоны — вся папка, а исключить его значило оставить зону грязной и Г1 красным. Убирать чужой файл я не стал: это необратимое действие вне задания. Решение за владельцем.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

7. Вне git и вне любой названной зоны остались `_studio/veb-raspredelenie/TZ.md` (содержательная правка), `deploy/ADRES.txt` (новый файл) и `zhurnal/.DS_Store` (мусор macOS). Ни один не мой; субагент гит-контура тоже оставил их, назвав строкой. `.DS_Store` просится в `.gitignore`, два других — в чью-то зону.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

8. У `spetsmat-bot` нет `origin`, и после этих коммитов работа ночи 04.09 существует в двух копиях на ОДНОМ диске — в рабочем дереве и в `.git`. Отказ диска по-прежнему означает потерю всего. Это ровно предмет позиции N7 той же волны, и до её исполнения потеря остаётся возможной.
   ДОМ: zhurnal/2026-09-02_spetsmat-bot/kod_N7-repozitorii-pages-tunnel.md
   ДОСТАВЛЕНО: нет

> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снять ПЕРВЫМ ходом, до всякой работы)*
```
$ git --no-optional-locks branch --no-merged main
rc=0
(вывод пуст — невлитых веток нет)

$ git --no-optional-locks status --porcelain | wc -l
rc=0
      33

$ git --no-optional-locks log --oneline @{u}.. | wc -l
fatal: no upstream configured for branch 'main'
       0
rc=0
(git упал ожидаемо — у ветки main нет upstream; доп. проверка git remote -v: пусто, rc=0 — origin у репозитория нет вовсе, вывоз невозможен структурно)

$ python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki
✅ заявок нет
Охват: заявок открыто 0, переадресовано 0, постоянных исключений 0, сторож краснеет на 0, держателей 0, двойной захват на 0
rc=0
```

🔴 **ПОПРАВКА СУБАГЕНТА К ЧЕТВЁРТОЙ СТРОКЕ СНИМКА, ОСТАВЛЕНА НА ВИДУ.** Команда `zayavki` ответила «заявок нет» ОШИБОЧНО: она была вызвана без `GIT_ZONA_REPO` и в этом случае читает чужой репозиторий вслепую (предупредил сам инструмент при следующем вызове, `plan`). С `GIT_ZONA_REPO=/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot` очередь оказалась НЕПУСТОЙ: 24 открытых заявки. Снимок оставлен дословно, как снят, — протокол требует именно этого; решения по пункту 1 приняты по верному повторному прогону. Исполнитель перепроверил обе команды своими руками, вывод совпал: без переменной `✅ заявок нет`, с переменной `Открытых заявок: 1` при 24 записях в очереди. Дефект вынесен уроком фабрике.

**ЧТО СДЕЛАНО** *(с хэшами)*

**Пункт 0 — хвост Cowork (индекс и автологи):**
- `92b9383` — забран `zhurnal/_INFRA-git/INCIDENTY.md`: слита пропущенная строка инцидента `sklejka-vhoda-s-serverom` (запуск с мусорными аргументами), спасённая из стрелового артефакта `_studio/zhurnal/_INFRA-git/` (побочный эффект незаданной `GIT_ZONA_REPO` в прошлом вызове; содержимое слито в канонический файл, сам артефакт и пустая `_studio/zhurnal/_INFRA-git/zayavki/` удалены).
- `39f4cee` — забран `zhurnal/_INFRA-git/VERDIKTY.md`: отрос ПО ХОДУ работы (классы К6–К9), забран между операциями, как предписано.
- Удалён стале-черновик `.commit-plan` (служебный файл `git_zona.py plan`, не часть git).

**Пункт 1 — очередь заявок (найдено 24 после исправления `GIT_ZONA_REPO`):**
- 23 заявки переадресованы заходу-разработчику командой `zayavka-peredat` с обоснованием `--pochemu` по каждой (код/конфиг требует содержательной правки — вне прав гит-контура; либо адресат лежит в репозитории `disciplina`, не в `spetsmat-bot`; либо требуется суждение о легитимности чужого выхода за зону; либо адресат — зона самого захода).
- 1 заявка (`2026-09-02T1431-02-09-14-2x-conduit179-bot`) оставлена открытой НАМЕРЕННО: постоянный долг с условием срабатывания («первый push / первый деплой / показ доступа кому-то кроме владельца»); проверено командой `git remote | wc -l` → 0, условие не наступило, долг корректно спит.
- `0f5678b` — закоммичено состояние очереди (46 путей: 23 удаления + 23 новых файла в `zhurnal/_INFRA-git/zayavki/na-zahod/`) — индексная инфраструктура очереди, не содержательная работа.

**Пункт 2 — влитие в основную:** вливать нечего. Повторная проверка `git --no-optional-locks branch --no-merged main` после всех правок — вывод пуст. Основная ветка, снятая командой: `main`.

**Пункт 3:** после пункта 0 отдельным прогоном `git_zona.py check` подтверждено — автологов и индексов вне git больше нет. Остальное субагент НЕ коммитил: зона самого захода (её коммитит исполнитель, §4) и то, что не входит ни в чью зону — `deploy/ADRES.txt`, `_studio/veb-raspredelenie/TZ.md`, `zhurnal/.DS_Store`. Два коммита из тела пункта 3 («регрессии…» и «волна ОСНОВАНИЕ…») субагент НЕ делал: это зона захода, они сделаны исполнителем (`0306643`, `5879b4a`).

**Пункт 4 — гашение:** ничего не гашено. Работа идёт прямо на `main`, веток не заводит. Ветки `zahod/*` и `veb-*` не тронуты — все влиты, но живут под чужими рабочими папками (`git worktree list`).

**Вывоз:** не запускался ни разу, `origin` отсутствует структурно.

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `нет`

Поимённо, что осталось и почему непроходимо правами субагента гит-контура:
1. Вся `zhurnal/2026-09-02_spetsmat-bot/` и `README.md` — прямой запрет: это зона самого захода. **Закрыто исполнителем в тот же прогон, коммит `5879b4a`.**
2. `veb/vhod.py`, `veb/server.py`, `tests/veb/test_server.py`, `deploy/ustanovka.sh`, `deploy/spetsmat-veb.service` — тот же запрет, та же зона. **Закрыто исполнителем в тот же прогон, коммит `0306643`.**
3. `deploy/ADRES.txt`, `_studio/veb-raspredelenie/TZ.md` — содержательная работа/конфиг, не индекс и не автолог; граница пунктов 0/3 прямо запрещает их коммитить. **Осталось открытым:** не входит ни в зону захода, ни в права контура. Пункт очереди 7.
4. `zhurnal/.DS_Store` — мусор macOS; решение (`.gitignore` или удаление) за владельцем. **Осталось открытым.** Пункт очереди 7.

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/2026-09-02_spetsmat-bot/mandate_volna-OSNOVANIE.md` — открывать любым просмотрщиком Markdown; это мандат волны ОСНОВАНИЕ, который до этого прогона существовал в единственном незакоммиченном экземпляре. Рядом, в том же коммите и той же папке, лежат остальные предметы работы ночи: `VOLNA-OSNOVANIE-SOSTAV.txt` и семь заходов `kod_N1-slot-vmesto-weekday.md` … `kod_N7-repozitorii-pages-tunnel.md`. Одной командой всё это перечисляет:
`git -C /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot show --pretty= --name-only 5879b4a`
**РОД АРТЕФАКТА:** `исходник`
*(Заход не порождает собранного файла: его продукт — состояние репозитория. Артефакт закоммичен РАНЬШЕ отчёта, потому что отчёт цитирует хэши; сверять его по времени бессмысленно, Г3 обязана сверять «доехал ли артефакт в названный §4 коммит».)*
**КОММИТ:** `0306643` — `regressions: the environment guard moved from import time to server start, secrets are read at call time, veb.service is wired into ustanovka.sh and got OnFailure` · `5879b4a` — `wave ОСНОВАНИЕ: mandate, composition, seven passes; the sajt wave checked against reality, the stop-hook diagnosis, lessons 22-26` · `git_zona.py check --zone <каждая из восьми>` → ✅ восемь из восьми.
**ПРАВКИ ПРОЧИТАНЫ:** правок нет — блок `## ПРАВКИ ПОСЛЕ ВЫДАЧИ` пуст, работал по редакции выдачи.

---

### ЧТО СДЕЛАНО И ЗАЧЕМ

Работа ночи 04.09 доехала в git. До этого прогона она существовала в единственном экземпляре на диске владельца, потому что собиравшая её сессия живёт в песочнице Cowork и в `.git` не пишет, а владелец на интервью прямо отказался исполнять команды руками. Это и есть весь смысл захода, и он исполнен ровно в объявленной форме — ДВА коммита на `main`, в главной папке, без ветки и без worktree.

**`0306643` — код регрессий,** 5 файлов, +68/−17. Сторож окружения уехал из времени импорта в старт сервера: он срабатывал у каждого читателя модуля, и pytest, СОБИРАЮЩИЙ `tests/veb/test_server.py`, ронял весь веб-набор (945 passed → 1 collection error). Три значения окружения перестали быть константами модуля по той же причине: первый импортировавший модуль замораживал их, а pytest собирает по алфавиту, так что `test_server.py` всегда замораживал их раньше, чем `test_vhod.py` успевал их выставить. `spetsmat-veb.service` вписан в `ENABLE_UNITS` установщика (юнит ставился и не включался) и получил `OnFailure`.

**`5879b4a` — волна и журнал,** 26 файлов, +4380/−8. Мандат волны ОСНОВАНИЕ и её состав, семь заходов N1..N7, `SVERKA-2026-09-04.md`, `DIAGNOZ-huka-2026-09-04.md`, `RESHENIA-2026-09-05.md`, `VHOD-2026-09-05.md`, выгрузки 04.09, обновлённые `NAVIGATOR` / `SESSIYA` / `UROKI-FABRIKE` / `PLAN` — и `README.md` В ЭТОМ ЖЕ коммите, потому что он реестр документов корня и несёт строку регистрации каждого из этих файлов; разнеси их по двум коммитам — и каждый документ был бы сиротой на длину промежутка.

**`<третий, хэш печатается командой ниже>` — этот отчёт.** Заход требует ДВА коммита; §5 требует, чтобы отчёт цитировал их хэши; Г1 требует, чтобы папка `zhurnal/2026-09-02_spetsmat-bot`, в которой лежит файл захода с отчётом, была чиста. Три требования вместе неисполнимы без третьего коммита, несущего один только отчёт. Он сделан и назван здесь прямо, а не тихо: двумя коммитами доставлена РАБОТА, третий — бухгалтерия самого захода. Свой хэш он процитировать не может по построению; печатает его команда
`git -C /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot log -1 --oneline -- zhurnal/2026-09-02_spetsmat-bot/kod_N0-dovezti-rabotu-noch-0904.md`
В него же уехал `SESSIYA.md`: чужой живой процесс переписал его в 02:42, уже ПОСЛЕ коммита 2 — это нашёл верификатор, а не я.

### КАК ПРОВЕРЕНО

Критерий готовности заменён в `## ПЛАН` ДО работы, с названной причиной (напечатанный в заходе критерий называет `_generator/tools/fixtures/bootstrap_zahod/PROGNAT.sh`, которого в этом репозитории нет — нет и каталога `_generator`). Взамен — прогон собственного механизма:
```
$ python3 -m pytest tests/veb -q     → rc=0 · 18 passed
$ python3 -m pytest -q               → rc=0 · 954 passed, 13 skipped
$ python3 ops/proverka_ustanovki.py  → rc=0 · verdict: GREEN -- passed 10 of 10 checks
                                        installed  PASS  all 11 unit file(s) in deploy/ are installed by ustanovka.sh
$ grep -n 'proverit_okruzhenie' veb/server.py   → veb/server.py:405:    vhod.proverit_okruzhenie()
$ grep -n 'spetsmat-veb.service' deploy/ustanovka.sh → 31:  spetsmat-veb.service
$ grep -n 'OnFailure' deploy/spetsmat-veb.service    → 21:OnFailure=spetsmat-alert@%n.service
```
Полный набор прогнан дважды — до коммита 1 и повторно на закоммиченном дереве, оба раза `rc=0`, оба раза 954 passed / 13 skipped. Регрессия, ради которой писался коммит 1, действительно закрыта: веб-набор собирается и проходит.

### ГИГИЕНА Г1–Г6 (§4.1) — командами, не пересказом

- **Г1. Зона доехала в git — ✅ восемь из восьми.** `git_zona.py check --zone <путь>` при `GIT_ZONA_REPO=/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot`, `rc=0` по каждому из `veb/vhod.py`, `veb/server.py`, `tests/veb/test_server.py`, `deploy/ustanovka.sh`, `deploy/spetsmat-veb.service`, `zhurnal/2026-09-02_spetsmat-bot`, `README.md`, `zhurnal/2026-09-02_spetsmat-bot/kod_N0-dovezti-rabotu-noch-0904.md`. Дословный вердикт, одинаковый по всем восьми: `✅ зона <путь>: работа доехала в git, вне git ничего нет.`
- **Г2. Второй репозиторий — применим ЧАСТИЧНО, и вот факт вместо предположения.** Все восемь путей зоны лежат внутри `spetsmat-bot`, так что по букве пункт неприменим. Но заход по ходу дотянулся до соседнего репозитория (инструменты `disciplina`), поэтому команда всё-таки прогнана: `cd /Users/ivanyakovlev/Documents/GitHub/disciplina && git --no-optional-locks status --porcelain | wc -l` → **127**, `rc=0`. Это НЕ моя работа: чужой живой прогон волны ORK-NOCH пишет туда прямо сейчас (файлы в `_studio/zhurnal/_INFRA-git/zayavki/` с временем 02:25 и именами `o9-ork-noch`, `o12-teach-bootstrap-mandate-py`). Отдельно проверено, что мой прогон там ничего не потерял: четыре пути со статусом `D` — три `PODYOM-VOLNY-7/8/9.md` чужой арки и `zayavki/na-zahod/2026-09-03T0237-doctor-rc-124-2026-09-03.md`, который не удалён, а ПЕРЕЕХАЛ в `zayavki/sdelano/` (найден там командой `find`) ещё в 01:54, до начала этого прогона.
- **Г3. Невлитых веток не прибавилось.** На входе `git --no-optional-locks branch --no-merged main` → пусто. На выходе — пусто. Было 0, стало 0.
- **Г4. Новый инструмент имеет живую точку вызова — неприменимо: ни одного нового `.py` заход не завёл.** Проверено командой, не памятью: `git show --pretty= --name-only 0306643 5879b4a | grep -cE '_generator/.*\.py$'` → `0`. Каталога `_generator` в этом репозитории нет вовсе.
- **Г5. Новый `.md` зарегистрирован.** `korni.карта_для('<путь файла захода>')` → `None`: у этого репозитория своей карты в реестре `korni` нет, дверь `register_doc.py` сюда не ведёт. Реестр здесь — `README.md`, «§6. Карта документов корня», и он уехал В ТОМ ЖЕ коммите `5879b4a`. Строки на диске, `grep -c '<имя>' README.md` → `1` по каждому: `mandate_volna-OSNOVANIE.md`, `kod_N0-dovezti-rabotu-noch-0904.md`, `kod_N1-slot-vmesto-weekday.md`, `kod_N7-repozitorii-pages-tunnel.md`, `RESHENIA-2026-09-05.md`, `SVERKA-2026-09-04.md`, `DIAGNOZ-huka-2026-09-04.md`, `VHOD-2026-09-05.md`. Ни одного `register_doc.py` я не звал: все эти строки уже стояли в `README.md`, их вписала собиравшая волну сессия — моя работа была довезти их, а не завести.
- **Г6. В коммите нет чужих путей.** `git show --pretty= --name-only 0306643 5879b4a`, отфильтровано по восьми путям зоны → **0** путей вне зоны. `0306643` — ровно 5 файлов, `5879b4a` — `README.md` плюс 25 файлов внутри папки арки.

### ПОСЛЕДНИЙ ХОД ПЕРЕД ОТЧЁТОМ — ПОЛНАЯ ГИТ-ГИГИЕНА, ЧИСЛАМИ

1. **ВСЕ КОММИТЫ.** `spetsmat-bot`: **вне git 3** — `_studio/veb-raspredelenie/TZ.md` (`M`), `deploy/ADRES.txt` (`??`), `zhurnal/.DS_Store` (`??`). Ни один не в моей зоне и ни один не мой: это чужая содержательная работа и мусор macOS; субагент гит-контура оставил их по той же границе. Называю строкой и оставляю, как предписано. `disciplina`: **вне git 127**, чужой живой прогон, см. Г2.
2. **ВЛИТИЕ СВОЕЙ ВЕТКИ — неприменимо, потому что своей ветки нет.** Контракт зоны назначил местом работы `main` в главной папке; `git branch --show-current` → `main` на входе и на выходе. Вливать нечего, `git_zona.py vlit-v-osnovnuyu` не звался ни разу.
3. **ПОСТ-ПРОВЕРКА ИЗ ГЛАВНОЙ ПАПКИ — ЗЕЛЁНАЯ.** Папка одна, режим checkout. Прогон изменённого механизма: `python3 -m pytest -q` → `rc=0`, 954 passed / 13 skipped; `python3 ops/proverka_ustanovki.py` → `rc=0`, `verdict: GREEN -- passed 10 of 10 checks`. Grep по живым точкам вызова: `veb/server.py:405: vhod.proverit_okruzhenie()`, `deploy/ustanovka.sh:31: spetsmat-veb.service`, `deploy/spetsmat-veb.service:21: OnFailure=spetsmat-alert@%n.service`. Откатывать нечего.
4. **ГАШЕНИЕ.** `git --no-optional-locks branch --no-merged main` → пусто, **невлитых 0**. Живые ветки в репозитории есть (`zahod/P1..R3`, `veb-*`, `sklejka-vhoda-s-serverom`), но все ВЛИТЫ и все заняты чужими рабочими папками (`git worktree list`) — не гашу и не трогаю: под ними может идти чужой заход.
5. **ВЫВОЗ — неприменимо структурно, и это решение владельца, а не упущение.** `git remote -v` → пусто, `rc=0`; `git log --oneline @{u}..` падает с `fatal: no upstream configured for branch 'main'`. **Невывезенных своей ветки: н/д, ветки нет; вывозить некуда.** Заявку не ставил намеренно: заведение `origin` — предмет позиции N7 той же волны, она уже написана и лежит в коммите `5879b4a`. Открытая заявка ровно на это в очереди уже есть — `2026-09-02T1431-02-09-14-2x-conduit179-bot`, постоянный долг с условием «первый push»; условие не наступило.
6. **ПРОВЕРКА ФАКТОМ.** `spetsmat-bot`: вне git 3 · невлитых своих 0 · невлитых чужих 0 · невывезенных н/д (нет origin) · пост-проверка ЗЕЛЁНАЯ. `disciplina`: вне git 127, чужие, не трогал.

### ШЕСТЬ СТРОК СУБАГЕНТА ГИТ-КОНТУРА (§0.1) — ВСТАВЛЕНЫ ДОСЛОВНО, НЕ ПЕРЕСКАЗАНЫ

1. Заявок было 24 (снимок дал ложное «0» из-за не выставленной `GIT_ZONA_REPO`; исправлено повторным прогоном) — закрыто 0, застряло 0, переадресовано 23 (полный список id — в секции «ЧТО СДЕЛАНО»), осталась открытой 1 (`2026-09-02T1431-02-09-14-2x-conduit179-bot`, постоянный долг, условие срабатывания не наступило — проверено).
2. Влито 0 веток из 0 названных (флаг `--vlit` не задан ни разу, повторная проверка `--no-merged main` пуста). Основная ветка: `main`.
3. Хвост Cowork: забрано в git пунктом 0 — `zhurnal/_INFRA-git/INCIDENTY.md` (92b9383), `zhurnal/_INFRA-git/VERDIKTY.md` (39f4cee, отрос по ходу). Осталось вне git (не индекс/автолог, не мои): `deploy/ADRES.txt`, `zhurnal/.DS_Store`, `_studio/veb-raspredelenie/TZ.md`, плюс вся зона захода (`veb/vhod.py`, `veb/server.py`, `tests/veb/test_server.py`, `deploy/ustanovka.sh`, `deploy/spetsmat-veb.service`, `zhurnal/2026-09-02_spetsmat-bot/*`, `README.md`).
4. Погашено 0 веток/папок — работа не заводит веток, гасить нечего (по указанию исполнителя); непогашенного с `rc≠0` нет, гашение не запускалось вовсе.
5. Необратимое: удалён каталог `_studio/zhurnal/_INFRA-git/` (стреловой артефакт, не часть отслеживаемой `_studio`) — содержимое (1 строка инцидента) предварительно слито в канонический `zhurnal/_INFRA-git/INCIDENTY.md` (коммит 92b9383), восстановление не требуется, потерь нет.
6. `выдано 24 позиций из 24 найденных`

*(Строка 6 на месте — ответ не усечён, вставлять его законно. Расхождение по строке 5: субагент пишет «1 строка инцидента», фактический `git show 92b9383` даёт 12 вставленных строк — сверено мной, потерь всё равно нет, все 12 на месте в каноническом файле. Оставляю его формулировку дословно и называю расхождение здесь, а не правлю его текст.)*

### РЕЗУЛЬТАТ ВЕРИФИКАТОРА (ПОСЛЕ-типа, свежий субагент, метод — `git status` и `git log`, не перечитывание моих команд)

Сплошная выборка 7 из 7 путей зоны. Ответы дословно: **A.** Да — оба хэша существуют, оба типа `commit`, `5879b4a` это HEAD, `0306643` его прямой родитель. **B. НЕТ** — шесть из семи путей чисты, зона 6 не чиста: `M zhurnal/2026-09-02_spetsmat-bot/SESSIYA.md`. **C.** Нет, путей вне семи зон в коммитах не найдено. **D.** Да, все семь `kod_N1..N7` и `mandate_volna-OSNOVANIE.md` реально лежат в дереве `5879b4a`, а не только упомянуты в сообщении. **E.** Грязь вне зоны: `M _studio/veb-raspredelenie/TZ.md`, `?? deploy/ADRES.txt`, `?? zhurnal/.DS_Store`.
**ВЕРДИКТ ВЕРИФИКАТОРА: не подтверждено** — «ни один путь зоны не остался со статусом M или ??» ложно, `SESSIYA.md` внутри зоны 6 модифицирован; проверено 7 из 7 путей зоны, дыра найдена в одном.
`выдано 7 позиций из 7 найденных`

🔴 **ЧТО Я С ЭТИМ СДЕЛАЛ, И ПОЧЕМУ ЭТО НЕ СПОР С ВЕРИФИКАТОРОМ.** Он прав фактически: в момент его прогона `SESSIYA.md` был `M`. Причина — не пропуск: файл переписан ЧУЖИМ живым процессом в 02:42:07, то есть ПОСЛЕ коммита `5879b4a` (02:41). Проверено `stat` и `git diff`: правка содержательная, чужая — переписана запись дневника от 02:28, две записи слиты в одну. Я не откатывал её и не отделял от своей: файл лежит в моей зоне, и оставить его грязным значило бы завалить Г1. Он уехал в третий коммит вместе с отчётом. Претензия верификатора закрыта не возражением, а коммитом, и это видно командой `git status --porcelain -- zhurnal/2026-09-02_spetsmat-bot` — она обязана быть пустой; приёмка гоняет её первым ходом.

### ЧТО НЕ ТРОГАЛ

`_studio/veb-raspredelenie/TZ.md`, `deploy/ADRES.txt`, `zhurnal/.DS_Store` — вне зоны, названы строкой и оставлены. Ветки `zahod/*`, `veb-*`, `sklejka-vhoda-s-serverom` и их рабочие папки — не гасил, не трогал. Репозиторий `disciplina` — только читал его инструменты; 127 его незакоммиченных путей чужие. Ни одной строки кода вне двух регрессий не правил: заход существует, чтобы довезти работу, а не переписать её. Файл `.probe_del2` (0 байт, чужой пробник) не удалял — необратимое действие вне задания; он уехал в коммит 2 вместе со всей папкой, потому что pathspec зоны — вся папка, а исключить его значило оставить зону грязной и Г1 красным. Пункт очереди 6.

### НЕОБРАТИМОЕ

Своей рукой — **необратимого нет**: `git checkout` не делался, `reset` не делался, `--force` не применялся, ни один файл не удалён и не перезаписан вне двух коммитов; всё, что изменено, лежит в git и восстанавливается из `0306643`, `5879b4a` и третьего коммита.
Рукой субагента гит-контура, у которого §0.1 даёт права шире моих — **одно, и оно названо им самим:**
- **Удалён каталог** `_studio/zhurnal/_INFRA-git/` (стреловой артефакт, побочный эффект незаданной `GIT_ZONA_REPO` в прошлом вызове) · **где:** корень `spetsmat-bot` · **чем восстанавливается:** содержимое НЕ потеряно — 12 строк инцидентов слиты в канонический `zhurnal/_INFRA-git/INCIDENTY.md` коммитом `92b9383`, проверено `git show 92b9383` (12 insertions, строки от 2026-09-03 23:05 до 2026-09-04 00:58 на месте). Восстановление самого каталога не требуется и невозможно: он был untracked. **Владелец этого действия не видел в момент, когда оно происходило** — прогон идёт без запроса разрешений.
- Удалён служебный черновик `.commit-plan` (файл `git_zona.py plan`, не часть git, порождается заново).

### ПОВТОРЯЕМОСТЬ НАХОДОК — что повторится на СЛЕДУЮЩЕЙ единице работы

**Повторятся — значит это заход ДО следующего прогона, а не пункт очереди (класс НЕМЕДЛЕННОЕ):**
- **`git_zona.py zayavki` без `GIT_ZONA_REPO` даёт зелёный ноль вместо отказа.** Повторится в КАЖДОМ заходе любой арки вне `disciplina`, потому что команда вшита четвёртой строкой в обязательный СНИМОК ВХОДА, а снимок гоняет каждый субагент гит-контура. Цена повторения — ложно-зелёный Г12 на непустой очереди; здесь спасла случайность (предупреждение соседней команды `plan`).
- **`schet_nezakrytogo.py` отказывает на арке вне реестра `korni`.** Повторится в каждом заходе этого репозитория: причина структурная (`REPO_ROOT` прибит к `disciplina`), а команда стоит в шапке с пометкой «проверь первым ходом».
- **Критерий готовности, подставленный по `--rod`, называет объект чужого репозитория.** Повторится на каждом заходе волны ОСНОВАНИЕ (N1..N7 собраны той же сборкой): семь раз подряд, и каждый раз исполнитель будет либо спорить с критерием в `## ПЛАН`, либо предъявит зелёное, заработанное на чужой фикстуре.

**НЕ повторятся — законно уходят пунктами очереди в `## ВОПРОСЫ`:** `.probe_del2` (пункт 6), три почти одинаковые выгрузки 04.09 (пункт 5), `deploy/ADRES.txt` / `TZ.md` / `.DS_Store` (пункт 7). Это разовые следы конкретной ночи, а не устройство фабрики.

### ОТКРЫТОЕ «ВОЗВРАЩАТЬСЯ»

Работа ночи 04.09 теперь в git, но всё ещё на ОДНОМ диске: `origin` у репозитория нет, и отказ диска по-прежнему означает потерю всего. Это предмет позиции N7, она написана и лежит в `5879b4a`. Пока N7 не исполнена, цель «работа не может пропасть» достигнута ЧАСТИЧНО, и говорить иначе было бы неправдой.
Второе: во время прогона в арку писал чужой живой процесс (четыре новых файла и переписанный `SESSIYA.md` между 02:26 и 02:42). Значит `git status` этой папки может снова оказаться непустым к моменту приёмки — не потому, что заход что-то не довёз, а потому, что работа в ней продолжается. Проверять надо командой на момент приёмки, а не сравнением с числами этого отчёта.

### ВРЕМЯ ПРОГОНА И ТОКЕНЫ

Время: около 25 минут настенных, с 02:20 до 02:45 (снято `date`, не оценкой). Токены — **счётчика в этом канале нет**: заход собран под движок `opencode`, где строки стоимости в логе не бывает, а фактически прогнан через канал app (Claude Code), где итог сессии исполнителю тоже не выдаётся. Числом не заполняю и за отсутствие не извиняюсь.

### ХВОСТ, ДОПИСАННЫЙ ПОСЛЕ ТРЕТЬЕГО КОММИТА (и потому четвёртым)

Два факта, найденных уже после того, как отчёт уехал в git. Они дописаны отдельным коммитом, который не несёт НИЧЕГО, кроме этих строк, — рвать историю `--amend` ради них я не стал.

**Хэш третьего коммита — `72f944b`** (`pass N0: the report itself, plus the arc files a concurrent session rewrote after commit 2`). Выше он назван командой, потому что процитировать себя изнутри не мог; теперь он известен и назван прямо.

**УНАСЛЕДОВАННОЕ КРАСНОЕ, КОТОРОЕ Я НЕ ЧИНИЛ И НЕ ОБХОДИЛ — оно чужое.** `check_uroki.py` на этом файле краснеет, но не на нём: `❌ ДОМ УРОКА НЕ РЕЗОЛВИТСЯ` указывает на `zhurnal/2026-09-02_spetsmat-bot/UROKI-FABRIKE.md:72` и `:80` — пункты `P1-yadro#1` и `P1-yadro#2`, где `ДОМ:` записан АБСОЛЮТНЫМ путём, а резолвер абсолютную форму не принимает. Обе строки написаны заходом `P1-yadro` 2026-09-02 и лежали в дереве ещё в `0f5678b`, до всех трёх моих коммитов — проверено `git show 0f5678b:zhurnal/2026-09-02_spetsmat-bot/UROKI-FABRIKE.md`. Мои собственные `ДОМ:` в `## ВОПРОСЫ` гейт не отмечает: `check_uroki.py ... | grep -c 'kod_N0'` → `0`. Обхода `--no-verify` не потребовалось: в `.git/hooks/` этого репозитория нет ни одного хука, гейт ничего не блокирует. Чинить чужие две строки я не стал — это правка вне зоны. Тот же класс, что и остальные три урока: резолвер, как и `schet_nezakrytogo.py`, знает только адреса реестра `disciplina`, а арка живёт в `spetsmat-bot`.

### СЧЁТ НЕЗАКРЫТОГО — ОТКАЗ, А НЕ СОСТОЯНИЕ

Команда из шапки прогнана первым ходом, как велено, и вернула `rc=1`: «под область не попало ни одного файла». То же в обеих законных формах области и то же при вызове из корня `disciplina`. Числа шапки (25 уроков без ВЕРДИКТ, 222 пункта очереди) в этом репозитории не воспроизводимы; причина разобрана уроком фабрике. Печатаю это отдельной строкой, потому что молчание здесь читалось бы как «всё чисто».

## ПРАВКИ ПОСЛЕ ВЫДАЧИ — (заполняет АНАЛИТИК; исполнитель ЧИТАЕТ)
> 🔴 **Пусто — значит заход не правился с момента выдачи.** Непустой блок читается ПЕРЕД продолжением работы: правка отменяет любое противоречащее ей место выше по файлу, каким бы категоричным оно ни было.
> **Форма строки — жёсткая, по ней судит приёмка:** `### ПРАВКА N · ГГГГ-ММ-ДД ЧЧ:ММ · <что изменилось, одной фразой>`, дальше — что именно перечитать и что откатить, если уже сделано по старой редакции.
> **Аналитик:** внёс правку — обязан ОТДЕЛЬНО послать владельцу короткое сообщение для пересылки исполнителю. Правка, лежащая только в файле, до работающего исполнителя не доезжает: он файл не перечитывает сам.
> **Исполнитель:** прочитал правку — назови её номер в `## ОТЧЁТ` строкой `ПРАВКИ ПРОЧИТАНЫ: 1, 2`. Нет строки при непустом блоке = отчёт не принимается: неизвестно, по какой редакции работали.

<правок нет>

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `<принято | доработка | отклонено>` — `<почему именно так, одной фразой: что проверено и чем>`

**ВЕТКА РАБОТЫ:** `main`
*(проверяется фактом, не словом: ветка обязана существовать и быть либо ВЛИТА в основную, либо названа в открытой заявке на влитие. Ни того, ни другого — Г14 краснеет. Снять состояние: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py poteri --branch <ветка>`)*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**
> Адрес списка: `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/_INFRA-git/zayavki`
> Читается командой (из любой папки, в том числе из worktree): `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki`
> Ставится командой: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavka --rod <git-operaciya|pravka-koda> "<текст>"`
> 🔴 Вопрос здесь НЕ «что ты хочешь сделать», а «что ты УЖЕ положил в очередь». Дубль сверяется с очередью по id машинно; намерение сверить не с чем.

- `<id заявки>` — `<род>` — `<суть одной строкой: влитие / коммит / вывоз / деплой / гашение>`

*(Заявок эта приёмка не ставила — так и напиши строкой «заявок нет: <почему ни одна из пяти операций не понадобилась>». Пустая строка и прочерк не принимаются: молчание неотличимо от «забыл».)*
