# Канал исполнителя — vygruzka-v-tablicu (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — механика по расписанному заданию: решения владельца приняты 07.09, инструмент экспорта и формат уже существуют.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Sonnet 5 — механика по расписанному заданию: решения владельца приняты 07.09, инструмент экспорта и формат уже существуют.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/vygruzka-v-tablicu.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/2026-09-02_spetsmat-bot/kod_vygruzka-v-tablicu.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: cd /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/vygruzka-v-tablicu, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.

Заход мог быть ПОПРАВЛЕН после того, как ты стартовал: аналитик правит
тот же файл, а ты прочитал его один раз. Смотри секцию ## ПРАВКИ ПОСЛЕ
ВЫДАЧИ в конце файла — при старте и всякий раз, когда владелец пишет
тебе в чат. Правка с номером, которого ты не читал, отменяет любое
противоречащее ей место выше.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
ГРАНИЦА ОБЛАСТИ: сырые подстроки в `kod_*.md` (пункт 4) — НЕ парсер очереди `dostavit_urok` (который считает только пары ДОМ:/ДОСТАВЛЕНО:). Разница в числах — законна.
🔴 снимок при сборке 2026-09-08, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/schet_nezakrytogo.py zhurnal/2026-09-02_spetsmat-bot`
Область: «zhurnal/2026-09-02_spetsmat-bot» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — ни одного skills/*/DOLG.md нет на диске (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 31
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 317
     из них разбором очереди (парсер `dostavit_urok`, записи с парой ДОМ:/ДОСТАВЛЕНО:): 183
       живых (чинится доставкой — «дом есть»)  : 54
       к владельцу (решение за человеком)      : 75
       адрес недоступен (нет/папка/код/указат.) : 52
       адрес не разобран                        : 0
       отработавших (машинный след закрытия)    : 0
       доставлено                               : 2
       🔴 не проверяется машиной: содержательная отработанность записей БЕЗ следа закрытия (метки в доме, строки ✅/ЗАКРЫТО) — нужна ревизия человеком; сырой греп сверх разбора — шаблонные строки формы.

🔴 ДВЕРЬ НЕЗАКОММИЧЕННЫХ `kod_*.md` ОТКРЫТА КЛАПАНОМ: в арке `zhurnal/2026-09-02_spetsmat-bot` есть 6 незакоммиченных файлов-заходов `kod_veb-priyom-zadach.md`, `kod_dozabivka-dolgov-sklejki.md`, `kod_dve-stranicy-raspredelenia.md`, `kod_https-i-domen.md`, `kod_listki-kak-baza-zadach.md`, `kod_sloj-zanyatia.md` — второй заход в такую арку обычно не собирается (долг 4 `disciplina-git`), но АНАЛИТИК открыл клапан, причина дословно: «шесть незакоммиченных kod_*.md — чужие позиции волны веб/сборки, их коммитит приёмка своих заходов; сборка идёт из репозитория disciplina, права коммитить чужие файлы spetsmat-bot у этого прохода нет, а зона ops/ deploy/ tools/ tests/ с ними не пересекается». Отключение видно здесь, в артефакте, а не осталось решением в голове аналитика.

КОНТЕКСТ. `spetsmat-bot` — бот и сайт спецмата 179-й: кондуит (кто какую задачу сдал) живёт в `data/spetsmat.db`, показывается на сайте и выгружается в xlsx инструментом `tools/export_xlsx.py`. Прошлый этап: штатные снимки базы делает `ops/rezervnaya_kopia.py` по таймерам `deploy/*.timer`, и он ПО ПОСТРОЕНИЮ не имеет никакой отправки наружу — решение против выгрузки в телеграм-канал, «that absence is the feature». Отсюда цена: сегодня ВСЕ копии базы лежат на одном сервере, и смерть машины = потеря всего; сайт однажды уже стёрся вместе с историей плюсиков, восстановить было нечем.
ЦЕЛЬ: сервер сам, каждую ночь, кладёт кондуит в Google-таблицу владельца — через СЕРВИСНЫЙ аккаунт и Sheets API, преподавателям на чтение, «чтобы любой обеспокоенный человек мог зайти и посмотреть, что с этими версиями, есть ли новые плюсики, новые листки» (слово владельца 07.09).
Приёмка — по ОТЧЁТУ, без построчной сверки. Если стоп до цели: получишь инструмент выгрузки и, возможно, зелёную пробу — но НЕ получишь работающий ежедневный таймер и НЕ получишь доказательства, что остановка выгрузки видна; то есть копия наружу по-прежнему не едет.

🔴 ЧТО УЖЕ СДЕЛАНО ВЛАДЕЛЬЦЕМ РУКАМИ — НЕ ПЕРЕИЗОБРЕТАТЬ И НЕ ЗАВОДИТЬ ВТОРОЕ:
- экспорт кондуита — `tools/export_xlsx.py`: лист на листок, школьники вниз, задачи вправо, знаки из `VALUE_REGISTRY` (`tools/import_konduit.py`), проекция через `ProgressService.states_for_many`. 🔴 Второго чтения журнала не заводить: берёшь кондуит ТЕМ ЖЕ путём, а не своим запросом.
- ключ робота — `secrets/psychic-heading-495410-u7-e4d4bd56d890.json`, на диске, `type: service_account`, `client_email` сходится.
- робот — `vygruzka-konduita@psychic-heading-495410-u7.iam.gserviceaccount.com`, заведён владельцем.
- таблица — `1ZQzvk0r-_oojZTcub--md7wpyPLxVq8mb9AihdCg0lw`, заведена, роботу выдан доступ «Редактор».
- образец таймера — `deploy/spetsmat-rezervnaya-kopia-sutochnyj.timer` и `deploy/spetsmat-rezervnaya-kopia@.service`, они работают.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-09-08) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. выгрузка идёт через СЕРВИСНЫЙ аккаунт и Sheets API, не публичным CSV — наружу торчать не должно ничего
2. содержимое — формат tools/export_xlsx.py: лист на листок, школьники вниз, задачи вправо, знаки из VALUE_REGISTRY
3. преподавателям доступ на чтение, чтобы любой мог сам убедиться, что данные на месте
4. таймер ежедневный (systemd), OnFailure на тот же алерт, что у остальных юнитов: молчание = тревога
5. ключ сервисного аккаунта лежит в secrets/ — папка под .gitignore, коммитить её нельзя никогда
6. в первую ячейку каждого листа — дата обновления: иначе остановившаяся выгрузка неотличима от работающей
7. снимок самой базы в Drive этим заходом НЕ везём — это остаётся открытым долгом, названным строкой в отчёте

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/vygruzka-v-tablicu`** — ТОЛЬКО ДЛЯ КОДА (worktree захода, ветка `zahod/vygruzka-v-tablicu` в ней уже стоит). 🔴 **ДАЛЬШЕ — ТОЛЬКО ПУТИ ОТНОСИТЕЛЬНО ЭТОЙ ПАПКИ** (или `cd` в неё безусловно, каждым ходом): абсолютный путь в главную папку репозитория здесь — типичная ошибка, правка утекает МИМО worktree и найдётся только на коммите («вне git» в `git_zona.py check --zone` из рабочей папки, на файле, который уже правил, — цена, оплаченная живьём: 5 файлов, ручное копирование и откат главной папки). 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) при этом остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия в рабочей папке: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути, названному в стартовой строке, а сам файл НЕ коммитишь — это делает аналитик при приёмке (цена обратного правила — полсуток 03.08: отчёт писали в рабочую папку, владелец и приёмка её не видели, приёмка трижды объявила отчёт пустым). 🔴 **Ветку в конце вливаешь САМ, последним ходом, после коммита зоны** (решение владельца 25.08; полный порядок печатает WARNING-блок ниже).
- **ЗОНА (можно менять):** `ops/` `deploy/` `tools/` `tests/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4); коммить ПО ХОДУ работы, не одним последним ходом (§4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **SCRATCHPAD — ТОЛЬКО ЛИЧНЫЙ.** Черновики, выкладки, промежуточные версии — в личную папку СВОЕГО захода `scratchpad/vygruzka-v-tablicu/`. Общие пути (`scratchpad/otchet.md`, любой `scratchpad/*` без имени твоей темы) ЗАПРЕЩЕНЫ: чужой отчёт уедет в твой файл или твой — в чужой, а приёмка читает отчёт без построчной сверки и подмену НЕ ЛОВИТ по построению. *Цена 25.08: готовый `## ОТЧЁТ` захода konvejer-incidentov был записан в общий `scratchpad/otchet.md`, и 92 строки чужого отчёта простояли в `kod_slovari-v-kod.md`.*
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода «kod_registracia-bez-obhoda.md»).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала САМ прогоняешь две команды самопроверки контура (пункт 1 ниже), и только ПОТОМ заводишь свою рабочую папку** — её ветка отпочковывается от основной такой, какая она есть на момент запуска: контур пуст, доносить инструмент влитием нечего.

**1. ВЕСЬ КОНТУР ПУСТ — САМОПРОВЕРКА ВМЕСТО СУБАГЕНТА.** При сборке проверены три числа контура, и все три нулевые: невлитых `zahod/*`-веток 0 (🔴 снимок при сборке 2026-09-08, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged main | grep -c 'zahod/'`); открытых заявок 0 (снимок при сборке 2026-09-08, пересчитать самому: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki`); названных `--vlit` 0. Звать субагента не за чем — выполни САМ две команды и вставь их вывод в `## ОТЧЁТ` дословно:
```
git --no-optional-locks branch --no-merged main | grep -c 'zahod/'   # снимок при сборке 2026-09-08: 0
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone ops/ deploy/ tools/ tests/
```
Первая вернула не 0 — НИЧЕГО чужого не вливай (свою ветку вольёшь последним ходом, см. ниже), назови число строкой в `## ОТЧЁТ` и работай дальше. Вторая красная — сначала приведи в порядок свою зону.

Если при следующей сборке хоть одно из трёх чисел окажется ненулевым, генератор сам вернёт сюда задание субагенту гит-контура — печатает его дверь `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/bootstrap_zahod.py --zadanie-subagentu`; звать его в этом заходе не надо.

🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси.

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-09-08, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged main | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.


- задеплоить: ключ сервисного аккаунта на сервер по ssh: владелец spetsmat, права 600, вне git
- задеплоить: systemd-таймер ежедневной выгрузки + OnFailure-алерт

- `cd /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/vygruzka-v-tablicu` — рабочая папка ДЛЯ КОДА. Ветку НЕ переключай: `zahod/vygruzka-v-tablicu` в ней уже стоит.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/vygruzka-v-tablicu`.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add ops/ deploy/ tools/ tests/` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти якоря, проект не изучай:
  · `tools/export_xlsx.py` — формат листа и путь к кондуиту через `ProgressService.states_for_many` (строки 14–18 и 184);
  · `tools/import_konduit.py` — `VALUE_REGISTRY`, откуда берутся знаки;
  · `ops/rezervnaya_kopia.py` — как в этом проекте пишется ops-инструмент и почему в нём нет отправки наружу;
  · `deploy/spetsmat-rezervnaya-kopia-sutochnyj.timer` и `deploy/spetsmat-rezervnaya-kopia@.service` — образец таймера и `OnFailure=`;
  · `.gitignore` — строка 2 `secrets/`;
  · `zhurnal/2026-09-02_spetsmat-bot/ZAMYSEL-vygruzka-v-tablicu.md` — замысел владельца, из которого собран этот заход.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

🔴 **WRITE YOUR `## ОТЧЁТ`, `## ПЛАН` AND `## ВОПРОСЫ` IN ENGLISH, AND EVERY FILE AND EVERY COMMIT MESSAGE YOU PRODUCE TOO.** Owner's decision 30.08. It is a каркас-level rule, not a preference — wave 2 lost it twice because the pass text listed the report SECTIONS and never said «every file you create». Fixed Russian addresses stay Cyrillic: `ЦЕНА:` · `ВЕРДИКТ:` · `ДОМ:` · `ДОСТАВЛЕНО:` · `ПОДЪЁМ:` · `[ДОЛГ: …]` · every `## ` heading of this file · every path and command.
**A. Инструмент `ops/vygruzka_v_tablicu.py`.** Берёт кондуит ТЕМ ЖЕ путём, что `tools/export_xlsx.py` (через `ProgressService`, не своим запросом), и пишет его в таблицу владельца по Sheets API. Лист на листок. По умолчанию — ПРОБА (печатает, что записал бы, и ничего не пишет); запись — только по `--primenit`.

**B. 🔴 Дата обновления — в ПЕРВУЮ ячейку КАЖДОГО листа.** Без неё остановившаяся выгрузка выглядит ровно так же, как работающая, и «зайти проверить» перестаёт что-либо значить — то есть теряется весь смысл затеи.

**C. Таймер.** Ежедневно, systemd, по образцу `deploy/spetsmat-rezervnaya-kopia-sutochnyj.timer`, с `OnFailure=` на тот же алерт, что у остальных юнитов. Молчание = тревога.

**D. Секрет.** Ключ кладётся на сервер по ssh, владелец `spetsmat`, права `600`. 🔴 `secrets/` под `.gitignore` (строка 2, снято при сборке 2026-09-08) — не коммитить НИКОГДА, в отчёт не печатать, в логи не писать. Ни один путь из `secrets/` не входит в зону этого захода.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — шесть клауз, все шесть в отчёт числами:**
1. ЖИВОЙ ПРОГОН на реальном объекте — боевой сервер, живая база `data/spetsmat.db`, настоящая таблица владельца, НЕ ТОЛЬКО ФИКСТУРА: `sudo -u spetsmat python3 ops/vygruzka_v_tablicu.py` (проба, без `--primenit`) → `rc=0`, печатает, что записал бы, и в таблицу не пишет ничего.
2. После `--primenit`: **верификатор читает таблицу ТЕМ ЖЕ API** и сверяет с живой базой — листов столько же, сколько листков текущего года, и в каждом число непустых клеток совпадает с числом, посчитанным по базе. 🔴 Оба числа печатать РЯДОМ, по каждому листу. Листков текущего года при сборке 3 из 21 всего — снимок 2026-09-08, пересчитать: `python3 -c "import sqlite3; print(sqlite3.connect('data/spetsmat.db').execute(\"select count(*) from sheets where issued_at >= '2026-09-01'\").fetchone()[0])"`.
3. Первая ячейка каждого листа несёт сегодняшнюю дату (пункт B) — проверяется чтением через API, не памятью.
4. `systemctl list-timers | grep vygruzka` → таймер активен, следующий запуск в будущем; число строк вывода назвать.
5. Секрет не уехал, три проверки: `python3 <путь>/git_zona.py check --zone ops/ deploy/ tools/ tests/` чист; `git --no-optional-locks show --stat <каждый свой коммит>` не содержит ни одного пути из `secrets/`; `grep -rIl private_key ops deploy tools tests | wc -l` → 0 (снимок при сборке 2026-09-08: 0 — то есть клауза стартует зелёной и обязана остаться такой).
6. Остановка ВИДНА: подделать провал (неверный id таблицы) → юнит падает, `OnFailure` срабатывает, алерт пришёл. Без этой клаузы пункты B и C — вера, а не механизм.

**ОХВАТ ПОСЛЕДНЕЙ СТРОКОЙ ОТЧЁТА, ДОСЛОВНО:** «проверено 6 из 6 клауз; листков 3 из 3».

🔴 **ГРАНИЦА, НАЗВАННАЯ ВЛАДЕЛЬЦЕМ:** снимок самой базы в Drive этим заходом НЕ везём — владелец выбрал таблицу с кондуитом. Сжатая копия самой базы вне сервера остаётся ОТКРЫТЫМ ДОЛГОМ: назови его в `## ОТЧЁТ` отдельной строкой, не молча.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)

Верификатор нужен, тип — **ПОСЛЕ-типа** — судит результат, стоит в конце, после задачи. Свежий субагент, ДРУГИМ методом (читает записанную таблицу тем же Sheets API и сверяет с живой базой: число листов и число непустых клеток на каждом листе, оба числа печатаются рядом), не перечитывает свою же правку. Доля сплошной выборки: сплошная: 6 из 6 клауз критерия, 3 из 3 листков текущего года. Финальная строка ответа обязательна дословно: «выдано N позиций из M найденных» — без неё ответ считается усечённым и в отчёт не вставляется.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПО ХОДУ РАБОТЫ, НЕ ОДНИМ ПОСЛЕДНИМ ХОДОМ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая. Каждую завершённую часть работы коммить СРАЗУ, теми же двумя ходами — не копи всё к финальному ходу:
```
git --no-optional-locks add -- ops/ deploy/ tools/ tests/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- ops/ deploy/ tools/ tests/   # отсекает всё чужое
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **КОММИТЬ ПО ХОДУ — РЕШЕНИЕ ВЛАДЕЛЬЦА 25.08 (В11), ПЕРЕВЕРНУВШЕЕ прежний канон «одним последним ходом».** Цена прежнего канона: за сутки ДВА обрыва — канал `opencode run --auto` односторонний и умирает вместе с сессией (владелец закрыл ноутбук), и незакоммиченная работа пропадала целиком. Закончил кусок — закоммитил его; последний ход только ПРОВЕРЯЕТ, что коммитить нечего (`git status --porcelain` пуст, `git_zona.py check --zone` ✅).
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `ops/` `deploy/` `tools/` `tests/`

- **Г1. Зона доехала в git.** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone ops/` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone deploy/` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone tools/` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone tests/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `spetsmat-bot` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged main` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## ⚠️🔴 WARNING · ПОСЛЕДНИЙ ХОД ПЕРЕД ОТЧЁТОМ — ПОЛНАЯ ГИТ-ГИГИЕНА. НЕ ПРОПУСКАТЬ 🔴⚠️

**СТОП. Прежде чем писать хоть строку в `## ОТЧЁТ` — прогони это целиком.** Требование владельца
2026-08-16, поводом стал заход, который сделал работу и не закоммитил НИЧЕГО: *«он ничего не
коммитит… всегда, когда собираешь по генератору, там должна быть фраза, что начинать надо с полной
проверки гигиены»*. Гит-контур §0.1 стоит в НАЧАЛЕ и разбирает то, что накопилось ДО тебя; этот блок
стоит в КОНЦЕ и разбирает то, что накопил ты сам. Один другого не заменяет.

**ПОРЯДОК ЖЁСТКИЙ, ОН НАЗВАН ВЛАДЕЛЬЦЕМ: коммит → влитие своей ветки в основную → пост-проверка
ИЗ ГЛАВНОЙ ПАПКИ → гашение → вывоз.** Обратный порядок не работает технически: влитие отказывает
на грязном дереве, пост-проверка неисполнима до влития, вывоз — на невлитом. **Решение владельца
20.08 пересматривает решение 05.08** («в конце может это worktree вливать, но, наверное, это не
нужно делать») — теперь вливает САМ заход, но только при зелёной пост-проверке.

**1 · ВСЕ КОММИТЫ.** Ничего не осталось вне git — ни в рабочем репозитории, ни в соседних, до
которых ты дотянулся по ходу работы:
```
for R in <репозитории, которых ты касался>; do
  echo "== $R"; git -C $R --no-optional-locks status --porcelain
done
```
Своя зона — своими путями (`add` + `commit -- <пути>`). Чужая содержательная работа — НЕ твоя:
называешь строкой в отчёте и оставляешь. Пусто у всех — так и напиши числом «вне git 0».

**2 · ВЛИТИЕ СВОЕЙ ВЕТКИ В ОСНОВНУЮ.** Только после того, как шаг 1 дал «вне git 0» на своей
зоне — влитие отказывает на грязном дереве:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py vlit-v-osnovnuyu zahod/vygruzka-v-tablicu --zone <своя зона> \
    --vsyo-ravno "своя рабочая папка ещё жива — влитие последним ходом захода, штатно"
```
Конфликт — ЗАКОННЫЙ исход, не повод форсировать: разрешай по существу, если понимаешь обе
стороны; не понимаешь — `git_zona.py vlit-v-osnovnuyu --abort`, ветка остаётся невлитой,
строка в отчёт и заявка на влитие (`git_zona.py zayavka --rod git-operaciya`).
🔴 Конфликт на `README.md` — только ОБЪЕДИНЕНИЕМ записей реестра, никогда выбором стороны:
параллельные заходы волны дописали по строке — обе записи правы, выбор одной молча уничтожает
регистрацию соседа.

**3 · ПОСТ-ПРОВЕРКА ИЗ ГЛАВНОЙ ПАПКИ.** Отвечает на вопрос «механизм ВСТАЛ», а не «коммит
виден»: прогон изменённого механизма из `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot`, НЕ из рабочей папки `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/vygruzka-v-tablicu` плюс `grep` по ЖИВОМУ файлу,
который его зовёт (хук, конвейер, генератор):
```
cd /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot && <команда прогона механизма, который заход менял> && echo $?
grep -n '<как механизм назван в вызывающем коде>' <живая точка вызова>
```
🔴 **Красная пост-проверка = ОТКАТ ВЛИТИЯ И СТРОКА В ОТЧЁТ**, а не «доложу, пусть приёмка
решает»: `git_zona.py vlit-v-osnovnuyu --abort`, если слияние ещё не закоммичено, иначе
`git -C /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot --no-optional-locks reset --hard <хэш ДО влития>`. Заход, который влил
и сломал `main`, обязан вернуть `main` сам.

**4 · ГАШЕНИЕ.** Невлитого не осталось: `git --no-optional-locks branch --no-merged main`.
Каждая оставшаяся ветка названа поимённо с причиной, почему она жива. 🔴 Ветку-витрину (`main`
там, где с неё публикуется сайт) НЕ вливать — влитие туда есть публикация и решение владельца.

**5 · ВЫВОЗ.** Вывези СВОЮ ветку; `main` НЕ вывози: если после работы в нём есть невывезенное,
поставь заявку `--rod git-operaciya` и назови число в отчёте. Команда для своей ветки:
`git --no-optional-locks log --oneline @{u}.. | wc -l` → 0.
Ненулевое на своей ветке означает, что работа существует только на этом диске.

**6 · ПРОВЕРКА ФАКТОМ, А НЕ ПАМЯТЬЮ.** Числа по каждому репозиторию — вне git · невлитых своих
и чужих; невывезенных СВОЕЙ ВЕТКИ, не по каждому репозиторию · результат пост-проверки
(зелёная/откачена) — печатаются командой и уходят в `## ОТЧЁТ` дословно. Ненулевое число или
красная пост-проверка без объяснения — приёмка читает как несделанную работу: она гоняет те же
команды первым ходом.

🔴 **Отчёт без этих чисел не принимается.** «Я закоммитил» — не то же самое, что `status --porcelain`
пустой: за одну сессию работа не доезжала трижды, каждый раз с честным «сделано» в отчёте.
## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

0. Git-contour self-check (§0.1) — run first, right after this plan, before any other action:
   - `git --no-optional-locks branch --no-merged main | grep -c 'zahod/'` (build-time snapshot: 0)
   - `python3 .../disciplina/_generator/tools/git_zona.py check --zone ops/ deploy/ tools/ tests/` (expect ✅)
   - `git rev-parse --abbrev-ref HEAD` (expect `zahod/vygruzka-v-tablicu`)
   Nonzero on the first → name the number, merge nothing of my own branch now. Red on the second → fix own zone first.

1. Read named anchors only: `tools/export_xlsx.py` (lines 14-18, ~184), `tools/import_konduit.py` (`VALUE_REGISTRY`), `ops/rezervnaya_kopia.py`, `deploy/spetsmat-rezervnaya-kopia-sutochnyj.timer` + `@.service`, `.gitignore` line 2, `zhurnal/2026-09-02_spetsmat-bot/ZAMYSEL-vygruzka-v-tablicu.md`.

2. **Fork I want to name before touching code, per Карпатов discipline (not claiming the readiness criterion is wrong — flagging a dependency it silently assumes):** clauses 1, 4, 6 of the readiness criterion and the two "задеплоить" bullets all require acting as `spetsmat` on the real production server (`sudo -u spetsmat`, `systemctl`, placing the key by `ssh`). This session runs on the owner's Mac in a git worktree, not on that server. First concrete step of the task itself will be checking whether this session has any path to that server (ssh alias, existing tooling). If it does not, I will not fabricate those results — clauses 1/4/6 and the two deploy bullets get reported as genuinely undone, with the exact blocking reason, and a queue item (`ДОМ:`) for whoever does have server access. If it does, I proceed with live deploy + live verification as written.

3. Check two narrow environment facts needed to write the tool (not general project exploration): whether a Google Sheets client library (`google-api-python-client`/`google-auth` or similar) is already available to the project's Python, and the SSH question from step 2.

4. Implement `ops/vygruzka_v_tablicu.py`:
   - Load the conduit the same way as `tools/export_xlsx.py` (`ProgressService.states_for_many`) — no second read path.
   - One sheet per листок, same layout as `export_xlsx.py` (students down, tasks across, marks from `VALUE_REGISTRY`).
   - Writes via Sheets API using the service-account key at `secrets/psychic-heading-495410-u7-e4d4bd56d890.json` (path parametrized, key content never touched/printed) and spreadsheet id `1ZQzvk0r-_oojZTcub--md7wpyPLxVq8mb9AihdCg0lw`.
   - Cell A1 of every sheet = today's update date.
   - Default = dry-run (prints intended writes, writes nothing); `--primenit` performs the real write.

5. Add/adjust the systemd timer + service under `deploy/`, by the pattern of `deploy/spetsmat-rezervnaya-kopia-sutochnyj.timer` / `@.service`: daily, `OnFailure=` pointed at the same alert as the existing units.

6. Deploy (only if step 2's check finds real server access): place the key on the server under user `spetsmat`, mode 600, outside git; install and enable the timer+service. No access → stop here on these two items, report them undone with the reason.

7. Verification, six numbered clauses (fresh after-the-fact pass per §3, same Sheets API, cross-checked against the live DB, not a re-check of my own edit):
   1. live dry run on the real server/db/sheet → rc=0, prints intended writes, writes nothing;
   2. after `--primenit`: sheet count and per-sheet non-empty-cell count read back via the API vs. computed from the live DB, both numbers printed side by side;
   3. A1 of every sheet carries today's date, read via the API;
   4. `systemctl list-timers | grep vygruzka` → active, next run in the future, line count named;
   5. secret-leak checks: `git_zona.py check --zone ...` clean; `git show --stat` on every own commit has no `secrets/` path; `grep -rIl private_key ops deploy tools tests | wc -l` → 0;
   6. forced-failure test (bad sheet id) → unit fails, `OnFailure` fires the same alert as other units.

8. Commit as I go (§4): `add` then `commit -- <paths>` per finished piece, `git_zona.py check --zone <zone>` after each, `show --stat` confirms only own paths. `register_doc.py` + `_studio/docs/KARTA.md` only if a new `.md` gets created (not expected — only `.py`/timer/service files).

9. Final git-hygiene block exactly as mandated at the end of this file: commit → merge own branch into main → post-check from the main repo folder (not this worktree) → prune → push; abort/roll back on any red post-check.

10. Report: six readiness clauses numbered with real pass/fail, coverage line, the open DB-snapshot-to-Drive debt named, irreversible-actions list (or "none"), reproducibility note, absolute artifact path, commit line(s), and — if step 2's fork resolved "no server access" — an explicit, undisguised statement of which clauses/bullets stayed undone and why.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> 🔴 **`ДОМ:` — ОБЯЗАТЕЛЬНОЕ ПОЛЕ, И АДРЕС В НЁМ ОБЯЗАН СУЩЕСТВОВАТЬ В МОМЕНТ, КОГДА ТЫ ЕГО ПИШЕШЬ.** Путь, которого нет на диске, — не адрес: такую запись нельзя ни доставить, ни спросить, и она не чинится ничем. Замер 2026-09-06 по 632 файлам `kod_*.md`: 370 пунктов очереди из 1570 родились ровно так — больше, чем всех доставимых (195) вместе взятых. Проверить СВОЙ файл до отчёта — одна команда:
> ```
> python3 _generator/tools/bootstrap_zahod.py --proverit-doma <этот файл>
> ```
> rc=0 — все дома достижимы; rc=1 — назван дом, которого нет (команда печатает какой именно). Тот же разбор гоняет `Г7` приёмки, и у него храповик: у ЭТОГО захода база 0, поэтому первый же недостижимый дом здесь — красный на приёмке, а не запись, которую через неделю никто не найдёт.
> `ДОМ: владелец` — законный адрес и НЕ недостижимый дом: он значит «дома-файла нет вовсе, решение за человеком». Не знаешь пути — пиши его, а не выдуманный путь. Для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит и «доставлено» без метки на месте, и недостижимый дом сверх базы; достижимое-недоставленное печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. `git_zona.py vlit-v-osnovnuyu`'s "влито, но не встроено" check (`has_live_trigger`) only greps disciplina's own `_generator/tools/*.py`, `_generator/*.py` and `.githooks/*` for a caller reference. In a target repo like `spetsmat-bot`, it can never see a genuine live call site that lives in that repo's own `deploy/*.service` (an `ExecStart=` line) -- so every new `ops/`/`tools/` file merged this way in ANY non-disciplina repo prints a false "not wired up" even when it demonstrably is (both `ops/vygruzka_v_tablicu.py` and `ops/proverka_vygruzki.py` hit this on the first merge of this заход). Recurring cost across every future заход that adds a systemd-called tool to a project repo, not a one-off -- worth either widening the grep scope to the target repo's own deploy/cron directories, or naming the limitation loudly at the print site so исполнители stop re-discovering it by hand.
   ДОМ: zhurnal/2026-09-02_spetsmat-bot/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

2. This заход's own readiness criterion (clause 2, identical wording in `ZAMYSEL-vygruzka-v-tablicu.md`) reads "листов столько же, сколько листков текущего года" and anchors that to 3 -- but §A's blanket "лист на листок", copied verbatim from `tools/export_xlsx.py`'s docstring, reads on first pass as "every листок, no restriction" (which is exactly what `export_xlsx.py` itself does). Building it, then discovering the scope was meant to be narrower and rebuilding, cost a real mid-task course correction that one explicit line in §A ("only this academic year's листки -- see clause 2's count") would have prevented outright. Likely to recur for any future заход that repeats this "лист на листок, but actually scoped" pattern.
   ДОМ: zhurnal/2026-09-02_spetsmat-bot/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снять ПЕРВЫМ ходом, до всякой работы)*
```
git --no-optional-locks branch --no-merged <основная>     # невлитые
git --no-optional-locks status --porcelain | wc -l        # не закоммичено
git --no-optional-locks log --oneline @{u}.. | wc -l      # не вывезено
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki              # открытые заявки
```
<сюда — вывод, дословно>

**ЧТО СДЕЛАНО** *(с хэшами)*
<влито / закоммичено / вывезено / погашено / заявки закрыты — поимённо>

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `да`
*(Заполнено АНАЛИТИКОМ на приёмке 08.09, а не субагентом гит-контура: секция осталась с плейсхолдерами, и Г12 на этом краснел. Содержание снимка входа в файле ЕСТЬ — исполнитель написал его прозой в `## ОТЧЁТ`, абзац «PLAN READ FIRST»: невлитых `zahod/*` — 0, `git_zona.py check` по всем четырём путям зоны — ✅, `HEAD` = `zahod/vygruzka-v-tablicu`. То есть потерян не факт, а его АДРЕС. Долгов входа на входе не было; ставлю `да` по этому выводу и называю дефект формы уроком фабрике.)*
*(`нет` законно — но ТОЛЬКО со списком поимённо: что осталось и почему это непроходимо ТВОИМИ
правами (чужая живая рабочая папка, нужно решение владельца, конфликт, обеих сторон которого
не понимаешь). «Сложно» и «не моя тема» причинами не являются. `нет` без списка = красный.)*

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** артефакта нет: this заход's product is code (a tool + two deploy units), not
a собранный file — nothing here for the owner to open as a document/PDF/deck.
**РОД АРТЕФАКТА:** исходник
**КОММИТ:** five, all zone-checked green, plus the merge —
`d53314f` "ops: add vygruzka_v_tablicu.py, nightly conduit export to a Google Sheet" ·
`f473c97` "deploy: install the nightly conduit-export timer" ·
`abc4e87` "ops: scope the export to this academic year's listki, add tests" ·
`3491869` "ops: add proverka_vygruzki.py, the after-the-fact Sheets verifier" ·
`6d21e7c` "ops: mark both new tools called-by-hand, per vlit-v-osnovnuyu's finding" ·
merged into `main` as `50cd460` · `git_zona.py check --zone ops/ --zone deploy/ --zone
tools/ --zone tests/` → ✅ (checked both in the worktree per-commit and again from the
main folder after merge).

**PLAN READ FIRST.** §0.1 git-contour self-check ran before anything else: unmerged
`zahod/*` branches 0, `git_zona.py check --zone ops/ --zone deploy/ --zone tools/ --zone
tests/` → ✅, `HEAD` = `zahod/vygruzka-v-tablicu`. Confirmed clean; nothing merged in.

**WHAT WAS DONE, AND WHY**

- `ops/vygruzka_v_tablicu.py` — the export tool (§A). Reads the conduit through
  `tools.export_xlsx.open_read_only` and `ProgressService.states_for_many`, the same path
  `tools/export_xlsx.py` uses (no second reading of the journal); reuses that file's
  `SIGN` alphabet and `NAME_HEADER` rather than re-declaring them. Dry-run by default,
  `--primenit` writes for real via the Sheets API. Cell A1 of every sheet carries the
  update timestamp (§B).
- **Scope correction, made explicit rather than silently guessed:** the readiness
  criterion's clause 2 (and `ZAMYSEL-vygruzka-v-tablicu.md`, identical wording) compares
  the table's sheet count against "листков текущего года" and anchors that to **3**, not
  the full 21 листков in the catalogue. §A's "лист на листок" alone reads as "export
  everything," which is what I built first; rereading clause 2 against its own given
  recompute SQL made the narrower scope unambiguous, so I added `academic_year_start()`
  (this Sept 1 from September onward, else last Sept 1 — not a literal date, so the
  boundary keeps working after today) and rebuilt around it. Named as queue item 2 in
  `## ВОПРОСЫ` — a one-line disambiguation in §A would have saved the rebuild.
- `deploy/spetsmat-vygruzka-v-tablicu.service` / `.timer` (§C) — by the pattern of
  `spetsmat-proverka-sredy.*`: daily 02:00 UTC (after the 01:00 backup, so the export never
  races the nightly snapshot), `OnFailure=spetsmat-alert@%n.service`, `Persistent=true`.
  Added to `deploy/ustanovka.sh`'s `ENABLE_UNITS` — that list is hand-kept, not derived
  from `[Install]`, and a unit missing from it installs, works, and silently vanishes at
  the next reboot; `tests/ops/test_deploy_edinicy.py` caught this immediately when I first
  forgot it (2 failed + 17 errored), and is green now (37/37).
- `ops/proverka_vygruzki.py` — the §3 verifier. Never imports the writer's own
  build/write functions; recomputes the expected per-sheet non-empty count straight from
  the database through `ProgressService`, then reads the actual sheet back through the
  Sheets API in its own call, and prints both numbers side by side plus the A1 date check.
- `tests/ops/test_vygruzka_v_tablicu.py` — a focused suite for the one non-obvious
  judgment call this position makes (the academic-year cutoff): the boundary itself, and
  that a sheet from a year ago is excluded while a current one carries the stamp and the
  right signs. Everything about cell-to-sign mapping is already proven by `tests/export`
  against the same `SIGN`/`ProgressService`, so it was not re-proven here.
- Both new `ops/*.py` files carry a `# TOOL-CONTRACT: called-by-hand` marker, matching the
  precedent already in this repo (`tools/proverka_vosstanovlenia.py`): `git_zona.py
  vlit-v-osnovnuyu`'s "влито, но не встроено" check cannot see this repo's own
  `deploy/*.service` `ExecStart=` lines (queue item 1 in `## ВОПРОСЫ`), so it reported both
  files unwired even though `spetsmat-vygruzka-v-tablicu.service` genuinely calls one of
  them nightly.

**HOW I CHECKED — the six readiness clauses, all six numbered**

1. **Live probe on the real server, real database, real table.**
   `sudo -u spetsmat python3 ops/vygruzka_v_tablicu.py` on `159.194.254.52`, `rc=0`, printed
   the intended per-sheet counts (16A 134, 16α 131, 16ℵ 21 cells), wrote nothing.
2. **After `--primenit`, a fresh subagent verified the write independently** (a genuinely
   separate process, not me re-checking my own edit): ran `ops/proverka_vygruzki.py` on the
   server for real, read its own source to confirm it does not reuse the writer's
   build/write code, and independently recomputed "текущего года" from `sheets.issued_at`
   itself. Result: `листов: в базе (текущий год) 3, в таблице 3`; per sheet, cells in the
   database vs. in the table: 16A 134/134, 16α 131/131, 16ℵ 21/21 — all matching, exit 0.
   Subagent's closing line: "выдано 4 позиций из 4 найденных."
3. **A1 date stamp**, read through the API by the same subagent: all three sheets carry
   `обновлено: 2026-09-07 21:26 UTC`. That is genuinely today's UTC date at write time —
   the write ran at 21:26 UTC on 2026-09-07, which is already 2026-09-08 in Moscow local
   time (UTC+3); the verifier itself, run afterward, printed no staleness warning because
   it compares against UTC "today," which still agreed. Noted so the UTC/Moscow date-line
   difference is not mistaken for a defect on a future audit.
4. `systemctl list-timers | grep vygruzka` on the server → **1 line**:
   `Tue 2026-09-08 02:00:00 UTC ... spetsmat-vygruzka-v-tablicu.timer
   spetsmat-vygruzka-v-tablicu.service` — active, next run in the future.
5. **Secret did not leak**, three checks: `git_zona.py check --zone ops/ --zone deploy/
   --zone tools/ --zone tests/` → ✅; `git show --stat` on all 5 own commits (`d53314f`,
   `f473c97`, `abc4e87`, `3491869`, `6d21e7c`) → no `secrets/` path in any; `grep -rIl
   private_key ops deploy tools tests | wc -l` → **0**.
6. **Forced-failure test, and the alert genuinely fired.** Ran the exact writer command
   with `--tablica bad-id-forced-failure-test` under a *transient* systemd unit carrying
   the same `OnFailure=spetsmat-alert@<unit>.service` wiring as the real deployed one — so
   the real `.service` file on disk was never touched or edited. The run failed with a
   real `HttpError 404` from the Sheets API; systemd logged "Triggering OnFailure=
   dependencies," started `spetsmat-alert@vygruzka-test-provocation.service`, which ran
   `ops/opoveshchenie.py` and finished successfully (this is the real alert path through
   the second bot — the same one every other unit's `OnFailure=` uses). Confirmed via
   `journalctl`, then `systemctl reset-failed` and confirmed the transient unit and its
   alert instance left no trace and the real `spetsmat-vygruzka-v-tablicu.service` file on
   disk is byte-identical to what was deployed.

**ОХВАТ ПОСЛЕДНЕЙ СТРОКОЙ, ДОСЛОВНО: проверено 6 из 6 клауз; листков 3 из 3.**

**DEPLOYED (§C/§D, the two "задеплоить" bullets):**
- Key: `secrets/psychic-heading-495410-u7-e4d4bd56d890.json` copied to
  `/opt/spetsmat-bot/secrets/` on the server via `scp` + `sudo install`, owner
  `spetsmat:spetsmat`, mode `600`. Never printed, never logged.
- `google-auth` and `google-api-python-client` installed system-wide on the server
  (`sudo pip3 install --break-system-packages ...`), matching how `aiogram`/`boto3`/etc.
  are already installed there (no venv on this server). This pulled a `requests` upgrade
  (2.31.0 → 2.34.2) as a side effect of dependency resolution — checked immediately
  afterward: `spetsmat-bot.service` and `spetsmat-veb.service` both `active (running)`,
  bot polling, site answering 200.
- `deploy/vykatka.sh` ran for real from the main checkout: snapshot taken, both services
  restarted, site answered 200 after 1s. `sudo bash deploy/ustanovka.sh` installed and
  enabled the new timer; `ops/proverka_ustanovki.py` → **GREEN, 11 of 11 checks**
  (including "live systemd").

**GIT HYGIENE, THE FINAL BLOCK — numbers, not memory**

1. **All commits.** Worktree `git status --porcelain` → 0. Main folder's zone
   (`ops/ deploy/ tools/ tests/`) → 0. (Main folder carries 21 unrelated dirty paths at
   merge time, other заходы's own work in progress — left alone, not mine, matches the
   arka-level note already at the top of this file about the открытый клапан.)
2. **Merge.** `git_zona.py vlit-v-osnovnuyu zahod/vygruzka-v-tablicu --zone ops/ --zone
   deploy/ --zone tools/ --zone tests/ --vsyo-ravno "..."` — two passes (the first 4
   commits, then a 5th fixing the TOOL-CONTRACT markers), both clean, no conflicts. Merge
   commit `50cd460`.
3. **Post-check from the main folder** (`/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot`,
   not the worktree): `pytest tests/ops -q` → 129 passed, the same 5 pre-existing
   `tests/ops/test_vykatka.py` failures present with my files entirely removed (confirmed
   by physically moving them aside and rerunning before writing any of this) — **not
   caused by this position**, named rather than silently left for the next reader to
   rediscover. Dry-run of the tool itself from the main folder → `rc=0`. Green; no
   rollback needed.
4. **Gашение.** `git branch --no-merged main` → **0**.
5. **Вывоз.** My own branch: pushed (`git push -u origin zahod/vygruzka-v-tablicu`),
   `git log --oneline @{u}..` → **0**. `main` was **not** pushed by me (§5 forbids it):
   main is 8 commits ahead of `origin/main` (my merge + 5 feature commits + 2 unrelated
   "автолог" commits from concurrent sessions) — filed as
   `2026-09-08T0031-main-origin-main-8-2-5` (`git-operaciya` / `vyvoz`), number named here.
6. **Fact, not memory** — all of the above numbers came from the commands themselves, run
   just before writing this line, not recalled.

**OPEN DEBTS, NAMED, NOT SILENT**

- **Database snapshot to Drive: not carried this заход** (§ГРАНИЦА — the owner chose the
  Sheets export over a Drive `.db.gz`; this is the boundary, not an oversight).
- **Bot-token rotation (owner's decision 2026-09-02, unrelated to this position's zone):**
  `ustanovka.sh`'s own printed warning names a live `@conduit179_bot` token in this
  checkout's git history and says the debt becomes due once the repo is reachable via a
  remote *or* once it is deployed to a server. Both conditions were already true before
  this session (origin already carries extensive pushed history) — my `vykatka.sh`/
  `ustanovka.sh` run did not newly create the exposure, but it is the exact printed
  reminder, so it is named here rather than left for the next person to notice cold.
  Nothing in `secrets/bot.env` was touched; the fix (`@BotFather` → `/revoke`) needs the
  owner and is outside `ops/ deploy/ tools/ tests/`.
- Two factory-level findings queued in `## ВОПРОСЫ` above (`git_zona.py`'s cross-repo blind
  spot for "встроен"; the §A/clause-2 scope ambiguity) — both `ДОМ:`-verified reachable
  (`bootstrap_zahod.py --proverit-doma` → rc=0, 2 of 2 reachable).

**IRREVERSIBLE ACTIONS**

- Wrote a real key file to `/opt/spetsmat-bot/secrets/` on the production server
  (restorable: re-copy from `secrets/` in this checkout; nothing else reads or depends on
  it yet).
- `sudo pip3 install --break-system-packages google-auth google-api-python-client` on the
  server, system-wide (upgraded `requests` as a dependency; verified both live services
  stayed healthy immediately after — see above; restorable via `pip3 install
  requests==2.31.0` if ever needed, though nothing indicates it is).
- Deployed code to the production server via `deploy/vykatka.sh` (snapshot taken first,
  at `/opt/spetsmat-bot-bak-20260907T212536Z`, per that script's own rollback mechanism).
- Enabled a new systemd timer on the server (`spetsmat-vygruzka-v-tablicu.timer`/
  `.service`); reversible with `sudo systemctl disable --now spetsmat-vygruzka-v-tablicu.timer`.
- Wrote real data into the owner's Google Sheet (the intended, real `--primenit` run) —
  the sheet's prior content, if any, is not recoverable through this tool, but the sheet
  held no conduit data before this заход (it was freshly shared with the robot per
  "ЧТО УЖЕ СДЕЛАНО").
- Ran a forced-failure test that sent one real alert through the second bot (see clause 6)
  — a genuine, intentional one-time diagnostic message, not a mistake; no other side
  effect, and the transient unit that caused it left no trace on disk.
- Pushed `zahod/vygruzka-v-tablicu` to `origin` (new branch on GitHub, not `main`).
- Merged `zahod/vygruzka-v-tablicu` into local `main` (not yet pushed to `origin/main` by
  me — see вывоз above); restorable via `git reset --hard <pre-merge sha>` if the owner
  ever wants it undone, though the post-check was green and no rollback was warranted.

**REPRODUCIBILITY OF FINDINGS**

- Both `## ВОПРОСЫ` queue items are genuinely recurring (next заход that adds a
  systemd-called tool, or writes a similarly-scoped export, hits the same friction) —
  correctly queued as findings for the factory, not filed as this session's own заходы.
- Nothing found here rises to the "class НЕМЕДЛЕННОЕ" bar (a defect that would cost a
  redo of THIS exact position again before the next run) — the one thing that could have
  (the scope ambiguity) was caught and fixed within this same заход, before the live run,
  not left for next time.

**QUESTIONS FOR THE OWNER:** none blocking. The two queue items above are the only
findings that outlive this заход.

**ПРАВКИ ПРОЧИТАНЫ:** блок `## ПРАВКИ ПОСЛЕ ВЫДАЧИ` был пуст при старте и остался пустым.

## ПРАВКИ ПОСЛЕ ВЫДАЧИ — (заполняет АНАЛИТИК; исполнитель ЧИТАЕТ)
> 🔴 **Пусто — значит заход не правился с момента выдачи.** Непустой блок читается ПЕРЕД продолжением работы: правка отменяет любое противоречащее ей место выше по файлу, каким бы категоричным оно ни было.
> **Форма строки — жёсткая, по ней судит приёмка:** `### ПРАВКА N · ГГГГ-ММ-ДД ЧЧ:ММ · <что изменилось, одной фразой>`, дальше — что именно перечитать и что откатить, если уже сделано по старой редакции.
> **Аналитик:** внёс правку — обязан ОТДЕЛЬНО послать владельцу короткое сообщение для пересылки исполнителю. Правка, лежащая только в файле, до работающего исполнителя не доезжает: он файл не перечитывает сам.
> **Исполнитель:** прочитал правку — назови её номер в `## ОТЧЁТ` строкой `ПРАВКИ ПРОЧИТАНЫ: 1, 2`. Нет строки при непустом блоке = отчёт не принимается: неизвестно, по какой редакции работали.

<правок нет>

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `принято` — критерий закрыт 6 из 6 при независимом верификаторе, а два числовых утверждения из шести приёмка перегнала САМА, своей командой на живом репозитории, и они сошлись.

**ЧТО ПЕРЕГНАЛА ПРИЁМКА СВОИМ КАНАЛОМ (08.09, аналитик, не отчёт):**

| утверждение отчёта | чем перегнано | сошлось |
|---|---|---|
| секрет не уехал: `grep private_key` по зоне → 0 | `grep -rIl private_key ops deploy tools tests \| wc -l` → **0** | ✅ |
| в пяти коммитах захода нет путей из `secrets/` | `git show --stat --name-only` по каждому из `d53314f f473c97 abc4e87 3491869 6d21e7c`, счёт строк `^secrets/` → **0, 0, 0, 0, 0** | ✅ |
| — (сверх отчёта) | `git check-ignore -q secrets/` → игнорируется | ✅ |
| листков текущего года 3 | независимый канал, которым не пользовались ни исполнитель, ни верификатор: живая заглавная сайта показывает листок 16 «Деревья» ровно тремя вариантами — A, α, ℵ | ✅ |
| клеток 16A 134 / 16α 131 / 16ℵ 21, база против таблицы | 🔴 **ПЕРЕГНАТЬ НЕ СМОГЛА, и это ограничение канала, а не сомнение в отчёте:** боевая база на сервере, из песочницы ssh недоступен (замерено 07.09), локальный `data/spetsmat.db` не боевой по построению — `deploy/vykatka.sh` исключает `data/` из выкатки. Ключ робота к таблице лежит на сервере и в песочницу не поднимается. Принято по отчёту субагента-верификатора, который читал таблицу тем же API отдельным вызовом и пересчитывал ожидаемое из базы через `ProgressService`, не переиспользуя код писателя. Решение владельца на интервью 08.09: сверку глазами он делать не хочет | не проверено, названо |

**НАБЛЮДЕНИЕ ВНЕ КРИТЕРИЯ — не дефект захода, но записать, чтобы не потерялось.** Клеток у трёх листков 134, 131 и **21**: третий на порядок беднее двух первых. Заход этим не виноват — его критерий «таблица равна базе», и он выполнен точно (21/21). Вопрос в другом: полна ли сама база по 16ℵ. Проверять это — не работа выгрузки; пункт для владельца, поставлен строкой ниже.

**ЧТО ПРИЁМКА СЧИТАЕТ СИЛЬНЫМ.** Клауза 6 сделана честно и полностью: провал подделан на ВРЕМЕННОМ юните с той же обвязкой `OnFailure=`, боевой `.service` не тронут, тревога реально прошла через второго бота, за собой убрано `systemctl reset-failed`, и байтовая идентичность боевого файла проверена после. Это ровно тот случай, когда «остановка видна» доказана, а не обещана.

**ДЕФЕКТ ФОРМЫ, НАЗВАННЫЙ ВСЛУХ.** Секция `## ГИГИЕНА ВХОДА` осталась с плейсхолдерами — её заполняет субагент гит-контура, и он этого не сделал; `priyomka.py` покраснел на Г12. Содержание при этом в файле есть, но лежит прозой в `## ОТЧЁТ`. Потерян адрес, а не факт. → урок фабрике.

**ВЕТКА РАБОТЫ:** `zahod/vygruzka-v-tablicu`
*Проверено фактом, а не словом: `git branch --merged main` содержит её — ветка ВЛИТА (Г14 зелёный, merge-коммит `50cd460`). `git_zona.py poteri` по всем 63 веткам: потерь нет, проверено 63 из 63.*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ:**

- `2026-09-08T1327-kod-md-2026-09-02-spetsmat` — `git-operaciya` / `kommit` — восемь `kod_*.md` арки вне git (среди них ОБА сегодняшних отчёта), плюс напоминание, что две заявки на вывоз `main` (`T0031` на 8 коммитов и `T0853` на 10) лежат незакрытыми. Срочность `blokiruet`: пока не закоммичено, сборка следующего захода отказывает гейтом грязной арки, а обрыв сессии унёс бы оба отчёта целиком.

*Заявок на влитие, деплой и гашение эта приёмка не ставила: ветка влита самим заходом последним ходом (штатный порядок с 20.08), деплой заход сделал сам и проверил `ops/proverka_ustanovki.py` — 11 из 11, гасить нечего — `poteri` зелёный по всем веткам.*

**ПУНКТ ВЛАДЕЛЬЦУ — из наблюдения выше:**

1. У листка 16ℵ в базе 21 непустая клетка против 134 и 131 у 16A и 16α. Проверить, все ли отметки по 16ℵ внесены, или это верная картина (листок раздан позже / решают его немногие).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

**ВЕТКА РАБОТЫ:** `zahod/vygruzka-v-tablicu`
*(проверяется фактом, не словом: ветка обязана существовать и быть либо ВЛИТА в основную, либо названа в открытой заявке на влитие. Ни того, ни другого — Г14 краснеет. Снять состояние: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py poteri --branch <ветка>`)*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**
> Адрес списка: `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/_INFRA-git/zayavki`
> Читается командой (из любой папки, в том числе из worktree): `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki`
> Ставится командой: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavka --rod <git-operaciya|pravka-koda> "<текст>"`
> 🔴 Вопрос здесь НЕ «что ты хочешь сделать», а «что ты УЖЕ положил в очередь». Дубль сверяется с очередью по id машинно; намерение сверить не с чем.

*(Список продублирован выше, в блоке вердикта — там же названо, почему остальные четыре операции не понадобились.)*
