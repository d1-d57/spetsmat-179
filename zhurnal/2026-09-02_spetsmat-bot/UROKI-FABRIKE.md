# УРОКИ ФАБРИКЕ — арка «2026-09-02_spetsmat-bot»

> **Это не дневник и не свалка. Это ВХОД для отдельной сессии, которая при закрытии арки правит канон.**
> Самоценен: сессия читает ТОЛЬКО его и канон — `SESSIYA.md` открывать не должна.
> Механизм и почему он такой — `../../docs/kak-delat/RUKOVODSTVO-zahodami.md §Хук «→ фабрика»`.

## Как сюда писать (после каждого отчёта/хода — минута, не больше)

Одна секция на урок. **Факт и ЦЕНА — не формулировка закона.** Закон сформулирует тот, кто не совершал этой ошибки: автор урока — лицо заинтересованное, красивая формулировка от него подозрительна by design.

```
### N. <что произошло — одной фразой, без морали>
ЦЕНА: <что сломалось и сколько стоило: сколько ходов/заходов/лекций, кто поймал>
ВЕРДИКТ: <пусто до закрытия арки>
```

**ЦЕНА пишется сразу или не пишется вовсе.** Задним числом она не восстанавливается, а урок без цены — наблюдение, ему в канон нельзя.

## Как отсюда читать (закрытие арки — отдельная сессия, пустой контекст)

Вердикт на КАЖДУЮ секцию, одна из трёх форм:

- `ВЕРДИКТ: вписан → <файл>#<место>` — правило внесено, **консолидируя**: если оно уже есть в каноне слабой формулировкой — переписано, а не положено рядом.
- `ВЕРДИКТ: слит с N` — тот же закон в другой одежде.
- `ВЕРДИКТ: отбракован — <почему>` — случай, повторившийся дважды по совпадению; цена не названа; правило есть и достаточно. **Это результат сессии, а не её провал.**

## Гейты — грепом, оба могут провалиться

```
grep -c  '^### '            UROKI-FABRIKE.md   # уроков
grep -cE '^\*{0,2}ЦЕНА:'    UROKI-FABRIKE.md   # разошлось ⇒ урок без цены: он не урок
grep -cE '^\*{0,2}ВЕРДИКТ:' UROKI-FABRIKE.md   # разошлось ⇒ арка НЕ закрыта
```

*Цена жирным (`**ЦЕНА:**`) — законная форма, не «наблюдение»: строгий `^ЦЕНА: ` её не видел, и на арке `2026-07-25_lekcia-1` это дало ложный зелёный на 13 из 19 уроков (закрыто заходом `kod_gejty-kotorye-vrut.md`, 06.08). Отступ перед `ЦЕНА:`/`ВЕРДИКТ:` — НЕ то же самое: строка с отступом внутри списка законна как запись, но в шапке САМОГО этого файла (пример выше) индент — приём, которым запись НАМЕРЕННО прячут от этих же грепов, поэтому гейт индент не подхватывает. Строка в бэктиках (цитата формы, а не запись) — тоже не считается: гейт судит начало строки, а не упоминание слова где-то внутри неё.*

*Чего эти греп-гейты НЕ проверяют (честно, по клаузе 3 гейта): что цена настоящая, а вердикт честен. Считаются строки, не правда. Это ловит только владелец или свежая сессия.*

---
*Локальная память арки, ephemeral. При закрытии уходит в канон `../../docs/kak-delat/` и умирает вместе с аркой. Дневник — `SESSIYA.md`; план — `PLAN.md`.*

### 1. Будильник волны считал окна через `pgrep -c`, которого у macOS нет вовсе
ЦЕНА: заход P1 умер в 08:36 («API Error: Connection lost mid-response»), будильник вышел кодом 3 «тик без событий» в 09:15 — волна простояла 40 минут при полностью живой оснастке. Три дефекта наложились и каждый по отдельности давал ЧЕСТНЫЙ НА ВИД НОЛЬ: (1) `grep -vc` на пустом входе печатает «0» и выходит кодом 1, поэтому `|| echo 0` дописывал второй «0», и сравнение падало «integer expression expected»; (2) у macOS `pgrep` нет флага `-c` — вызов падает с usage, `|| echo 0` превращает падение в ноль; (3) счётчик через `ps | grep` находил САМ СЕБЯ и отвечал 2 при одном живом окне. Первый из трёх был найден и починен в часовом за час до этого — и не перенесён в будильник. Поймал не гейт, а сверка вывода `ps` с ответом счётчика руками.
ВЕРДИКТ:

### 2. Порог пробы живости по умолчанию (45 с) объявляет живой пул мёртвым
ЦЕНА: `modeli.probnit_parallel(taimaut=45)` дал «живых 3 из 25», из них 18 отказов — `rc=124`, то есть ТАЙМАУТ, а не отказ. Та же проба, тот же шлюз, порог 120 с — «живых 12 из 25». На 45 с волна увела бы восемь бесплатных позиций на платные без основания либо встала. Докстринг того же `modeli.py` знает, что «порог 60 с тоже был мал: холодный старт opencode съедает часть его», но параллельная проба взяла 45. Отдельно: `zhivost --prognat` БЕЗ `--rod` падает ValueError, а С `--rod` переписывает реестр только пробованными моделями — остальные исчезают молча.
ВЕРДИКТ:

### 3. Тик будильника был почти равен порогу смерти часового
ЦЕНА: сердце бьёт голова, а голова бьёт его только НА СВОЁМ ХОДУ; ход начинается от выхода будильника. Тик 2400 с при пороге смерти 45 мин оставлял 5 минут запаса — на круге 9 часовой видел «сердце молчит 37м» и был в пяти минутах от подъёма ВТОРОЙ головы поверх живой. Разведено: тик 1200 с, порог 90 мин. Поймано чтением собственного пульса, не гейтом.
ВЕРДИКТ:

### 4. Первый коммит репозитория был нечитаем из-за прав на loose-объекты
ЦЕНА: три объекта `.git/objects/**` имели режим `--w-------` (write-only). `git status` падал `error: unable to open loose object be04710…: Permission denied` → `fatal: bad object HEAD`, и `git_zona.py plan` вместе с ним. Это весь первый коммит — commit, tree, blob. Диагноз «bad object HEAD» читается как порча репозитория, а не как права доступа, и без `chmod 444` P0 не состоялся бы вовсе. Объекты создал процесс с испорченным umask (песочница Cowork).
ВЕРДИКТ:

---

## Пункты очереди заходов, доставленные `dostavit_urok.py`

> Дословные находки из секций `## ВОПРОСЫ` файлов-заходов — голос исполнителя, который об это споткнулся, не редактора. Разбирает их человек: находка описывает, что мешало, но задачей сама не становится.
> Строка `ДОСТАВЛЕНО: <заход>#<N>` внизу каждой записи — та же метка, что стоит в источнике; по ней Г7 `priyomka.py` проверяет, что доставка не объявлена ложно.

#### P1-yadro#1

1. `git_zona.py vlit-v-osnovnuyu --zone` is documented and pasted as one quoted
   multi-path string, but the flag is `action="append"` and `in_zone()` matches one
   prefix per value — the pasted form matches nothing and refuses the merge while
   printing the zone's own files as foreign. Full account with its price in
   `## УРОКИ ФАБРИКЕ` above.
   ДОМ: /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/2026-09-02_spetsmat-bot/UROKI-FABRIKE.md
ДОСТАВЛЕНО: P1-yadro#1 · 2026-09-02

#### P1-yadro#2

2. The готовности criterion runs bare `python3`, which here is 3.9.6 with no pytest and
   no yoyo; a заход that builds only a `.venv` fails the criterion on correct code.
   Full account with its price in `## УРОКИ ФАБРИКЕ` above.
   ДОМ: /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/2026-09-02_spetsmat-bot/UROKI-FABRIKE.md
ДОСТАВЛЕНО: P1-yadro#2 · 2026-09-02
