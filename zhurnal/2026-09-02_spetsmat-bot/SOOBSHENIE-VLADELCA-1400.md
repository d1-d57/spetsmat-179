# СООБЩЕНИЕ ВЛАДЕЛЬЦА ОРКЕСТРАТОРУ — 02.09, 14:00

Читай целиком, отвечай в этот же файл секцией `## ОТВЕТ ОРКЕСТРАТОРА`.
Волну не останавливай, текущий заход не обрывай.

---

## 1 · ЧТО МНЕ НУЖНО ОТ ТЕБЯ ПЕРВЫМ ХОДОМ — ПРОМЕЖУТОЧНЫЙ ОТЧЁТ

Коротко, прозой, без пересказа журнала. Четыре пункта:

1. **Что РАБОТАЕТ прямо сейчас.** Не «позиция принята», а что именно бот умеет,
   если его сегодня поднять. Умеет ли он вообще подняться и ответить в Telegram —
   и проверялось ли это хоть раз живым токеном, или только тестами.
2. **Что идёт в эту минуту.** Какая позиция в работе, на какой модели, сколько
   она уже крутится, чего ждёшь.
3. **Что осталось** — списком позиций, и по каждой одной строкой: это глубокая
   работа или механическая.
4. **Твой прогноз времени** до конца волны при нынешнем ходе. Честный, с
   причиной, а не бодрый.

## 2 · ГЛАВНОЕ РЕШЕНИЕ, КОТОРОЕ Я ПРИНИМАЮ СЕЙЧАС: ОСТАВШИЕСЯ ПОЗИЦИИ — НА ПЛАТНЫЕ

**Замер, на котором это стоит.** Принято девять позиций. Восемь из них — платная
колонка мандата плюс P3 и P6. Всё, что осталось (P5, P9, P10, P14, P15, P16, P17)
— бесплатная колонка. Дисциплина моделей велит бесплатным идти строго по одной;
бесплатный пул за ночь просел, надзиратель не запускается, ты уже объявил одно
отклонение вслух. Отсюда и провал темпа к обеду: не позиции стали труднее, а
канал сузился.

**Что меняю.** Колонка `free`/`paid` в мандате — решение о ЦЕНЕ, а не о качестве.
Цена сегодня перевёрнута: у меня до 23:00 МСК большой платный лимит, который
иначе просто сгорит, а бесплатные лимиты к вечеру будут только хуже.

**Разрешаю и прошу:**

- оставшиеся позиции пускать на `opus`/`sonnet` штатной авторизацией `claude`
  (Bedrock на машине нет — это ты уже измерил);
- пускать их **параллельно, три–четыре разом**, как мандат разрешает платным,
  а не по одной;
- отклонение от модельной колонки объявить вслух в журнале, как ты сделал в 13:46,
  и на этот файл сослаться как на основание.

Если считаешь, что параллель на этих семи позициях столкнётся зонами — скажи
прямо и предложи свой порядок, я не настаиваю на четвёрке.

## 3 · ЧТО Я ОТВЕЧАЮ НА ТВОИ ВОПРОСЫ ПРО ЖИВОСТЬ И СРОКИ

- **Живого прогона бота не было.** Токен @conduit179_bot я отправил тебе до старта
  волны, руками ничего не поднимал и не тестировал. Всё, что о живости известно —
  известно тебе, не мне.
- **Жёсткого дедлайна нет.** Первое занятие может быть завтра, но запасной путь —
  та же гугл-таблица, что и весь прошлый год. Бот её не обязан заменить завтра.
- **Но сегодня — окно.** Хочу к концу дня рабочую версию, которую завтра можно
  попробовать на живом занятии. Если ради этого надо ужать объём — скажи, что
  именно резать; резать буду я, но список кандидатов твой.

## 4 · ПРИОРИТЕТ, ЕСЛИ ВРЕМЕНИ НЕ ХВАТИТ НА ВСЁ

Сначала то, без чего завтрашнее занятие не состоится, потом остальное:

1. **P5 — экраны просмотра.** Там же запрет «ученик не видит чужие отметки».
   Без него бота нельзя показывать детям вообще.
2. **P10 — эксплуатация.** Бот, который не переживает падение процесса, на
   занятии бесполезен.
3. **P17 — константы.** Механика на десять минут, но она про токен и deep link,
   то есть про сам запуск.
4. **P14, P15, P16, P9** — после. Уведомления, быстрый ввод текстом, экспорт и
   загрузка листков переживут неделю на таблице.

## 5 · ЧЕГО НЕ ДЕЛАТЬ

- Не объявлять успех тем, что позиции кончились: критерий провала в мандате прежний.
- Отметку в базу без подтверждения человеком — по-прежнему нельзя нигде и никогда.
- Не глушить чужие процессы по имени движка: инцидент 13:02 уже стоил соседям заходов.

---

## ОТВЕТ ОРКЕСТРАТОРА

Отвечено 02.09 14:10. Волна не останавливалась, P9 в работе не тронута.

### 1 · ЧТО РАБОТАЕТ ПРЯМО СЕЙЧАС

🔴 **Начну с плохого, потому что оно отвечает ровно на ваш вопрос: бот НЕ ПОДНИМАЕТСЯ.
Живого прогона не было ни одного, и дело не в том, что до него не дошли руки.**

Проверил только что, командой, а не по тестам:

    python3 -m bot   →  rc=2,  "BOT_TOKEN is empty. Set the environment variable and try again."

Причина найдена: в `bot/config_local.py` строка 18 — `BOT_TOKEN = ""`, и окружение
она **не читает вовсе**. То есть подставить токен переменной среды нельзя: константа
жёстко пустая. Базы `data/` тоже нет — бот не стартовал ни разу за ночь.

**Токен при этом ЖИВОЙ, и это я тоже проверил, а не предположил.** Прямой `getMe`
вашим токеном из `secrets/bot.env`:

    ok: True · @conduit179_bot · id 8420265757 · «Кондуиты 179»

Телеграм отвечает. Между «486 зелёных тестов» и «бот работает» стоит ровно одна
мелочь — чтение токена, — и это в точности зона позиции **P17**, которую я собрал
как механическую доводку констант. Она перестала быть механической: она теперь
единственный блокер запуска. Меняю ей задание и ставлю первой.

**Что умеет код, если считать по тестам (486 зелёных, `make check` из главной папки):**
журнал отметок append-only с тремя видами события и двумя временами, защищённый
шестью триггерами схемы; импорт всего прошлого года — 15 847 событий, семь оракулов,
негативный контроль краснеет на трёх порчах из трёх; регистрация по deep link и три
роли с middleware; сетка приёма на 544 кнопки, ни одна не длиннее 64 байт; экран
аудитории; поднёвное закрепление, где перевод ученика в декабре не переписывает
октябрь; приём фото и голоса до таблицы подтверждения. Занятия и явка.

**Но всё это доказано ТЕСТАМИ, а не живым Telegram.** Разница честная и большая:
ни один хендлер ни разу не отвечал настоящему человеку.

### 2 · ЧТО ИДЁТ В ЭТУ МИНУТУ

Одна позиция — **P9, загрузка листка**, на `openrouter/minimax/minimax-m3:free`,
крутится 20 минут, один коммит («parser turns a listok into a draft the senior can
approve»), лог растёт. Жду от неё разбора листка на задачи с метками и сверки с
`seed/sheets.json`. Она пущена моей запускалкой в обход надзирателя — то самое
отклонение 13:46.

### 3 · ЧТО ОСТАЛОСЬ — семь позиций

| позиция | что это | глубина |
|---|---|---|
| **P17** константы | чтение токена и deep link из окружения; БЛОКЕР ЗАПУСКА | была механическая — стала критической |
| **P5** экраны просмотра | плюсы и долги ученику, два списка преподавателю, запрет чужих данных | глубокая: приватность и педагогика |
| **P10** эксплуатация | systemd, сторож изнутри цикла опроса, бэкапы, запрет выкатки в час занятия | глубокая: половина её проверок обязана КРАСНЕТЬ |
| **P9** листок | идёт сейчас | средняя |
| **P14** уведомления | вопрос преподавателю, сводка старшему после занятия | средняя |
| **P15** быстрый текст | `[7] Петров 3,5,7б` через готовый индекс P7 | механическая |
| **P16** экспорт и бэкапы | Excel и проверка восстановления, которая краснеет сама | средняя |
| **P11** сквозная приёмка | ничего не пишет, всё проверяет; идёт последней | глубокая |

Плюс **закрывающая фаза знания**: шестнадцать уроков с ценой, каждый судит свежая
модель, не та, что его писала.

### 4 · ПРОГНОЗ, ЧЕСТНЫЙ

**До «завтра можно попробовать на занятии» — 2–3 часа, если ваше решение про платные
принимается.** Основание: восемь платных позиций этой ночи шли по 25–50 минут каждая,
и четыре из них работали параллельно без единого конфликта зон.

**До полного закрытия волны — 4–6 часов.** Разброс не от позиций, а от двух вещей,
обе уже случались сегодня: обрывы `API Error` посреди работы (три раза, каждый стоил
добора) и позиции, которые делают работу и не закрывают форму отчёта (P6 — пять
прогонов).

Причина провала темпа к обеду ровно та, что вы назвали: канал сузился, а не задача
усложнилась. Добавлю к вашему замеру своё: бесплатный пул НЕ мёртв — независимая
проба в 13:36 дала живых 10 из 25. Отказывался запускаться надзиратель, у которого
порог пробы зашит константой 45 с.

---

### ОТВЕТ НА §2 — ПРИНИМАЮ, С ОДНОЙ ПОПРАВКОЙ ПО ПОРЯДКУ

Решение принимаю и на этот файл ссылаюсь как на основание. **Зоны семи оставшихся
позиций чисты: 0 пересечений из 170 пар, посчитано посегментно.** Но четвёрку разом
не советую, и вот измеренная причина — не зоны.

🔴 **Три позиции (P5, P14, P15) обязаны дописать одну строку в `bot/app.py`**, чтобы
их роутер подключился. Этого файла нет ни в одной зоне; P4 и P13 уже правили его вне
зоны и честно объявляли отдельным коммитом. Три одновременных писателя в один файл из
трёх разных рабочих папок — это конфликт слияния или потерянная регистрация.

🔴 **И второе, уже стоившее нам красного `main`:** каждая из P5, P10, P14, P15, P16
заводит свой `tests/<имя>/conftest.py`. Папки тестов не пакеты, все `conftest` живут
в `sys.path` под одним именем модуля — сегодня в 12:57 это уронило тест соседней
позиции (1 failed из 347), и гейт зон такого не видит по построению: столкнулись не
пути, а имена модулей.

**Мой порядок — три полосы по три, а не одна четвёрка:**

- **полоса A, пускаю немедленно:** `P17` (блокер запуска) · `P10` (ваш приоритет №2,
  `bot/app.py` не трогает) · `P5` (ваш приоритет №1, единственный писатель в `app.py`);
- **полоса B:** `P14` (второй писатель в `app.py`) · `P16` · добор P9, если понадобится;
- **полоса C:** `P15` (третий писатель) · затем `P11` последней.

Все — `opus`, штатной авторизацией. Отклонение объявлю в журнале со ссылкой на этот файл.

### ЧТО Я МЕНЯЮ В P17 ПРЯМО СЕЙЧАС

Она была «перенести двадцать строк». Теперь её критерий готовности — **живой запуск**:

    python3 -m bot   →  поднимается, опрашивает Telegram, отвечает на /start
    и НЕ печатает "BOT_TOKEN is empty"

Токен читается из окружения (`BOT_TOKEN`), а не из константы; секрет в git не едет.
Это превращает P17 из косметики в ту позицию, без которой завтрашнее занятие
не состоится.

### СПИСОК КАНДИДАТОВ НА РЕЗ — если время всё же кончится

Режете вы, список мой, по вашему же §4:

1. **P16** (экспорт и бэкапы) — база маленькая, копию можно снять руками одной
   командой `VACUUM INTO`; неделю переживёт.
2. **P9** (загрузка листка) — первый листок сентября вы заводите строкой сами,
   как и планировали изначально.
3. **P14** (уведомления после занятия) — полезно, но занятие без них состоится.
4. **P15** (быстрый текст) — кнопки и фото уже есть, это четвёртый вход.

🔴 **НЕ режется ничто из P17, P5, P10 и P11.** P17 — запуск. P5 — запрет «ученик
видит чужое», без него бота нельзя показывать детям. P10 — переживание падения.
P11 — сквозная проверка, без неё «успех» будет объявлен тем, что позиции кончились,
а это ровно запрещённый мандатом исход.

### ПРО §5

Принято и уже в силе. Отметка без подтверждения человеком — проверяется в каждой
приёмке отдельно. Про глушение чужих процессов: инцидент 13:02 признан, соседям
сообщён с перечнем логов и временами, а правило превращено из текста в форму команды
— образец теперь привязан к моей арке, и после каждого `pkill` я считаю чужие процессы
до и после. Перед последним запуском насчитал 0.
