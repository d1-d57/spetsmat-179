# ЖУРНАЛ ОРКЕСТРАТОРА — ВОЛНА `utro` (10.09, до занятия)

Форма записи та же, что ночью: заголовок `## ЧЧ:ММ — о чём`, внутри обязательные строки
`**ЦЕНА:**` числом и `**ПРО ГОЛОВУ:**`. Время брать из `date`, а не из головы — ночью смещение
дошло до 34 минут и его поймали снаружи.

---

## КРУГ 1 · 10:45–10:57

`10:57 · круг 1 · запущено: 5 из 5 · принято: 0 · без вердикта: 5 · main зелёный (вход 1252 passed) · выкачено до: 1a4cc80 · не в git: 11 · смотрел глазами: да · ЗАКРЫТО 1 из 28 · ВЫКАЧЕНО 1 из 1`

**A1 ЗАКРЫТ И НА БОЕВОМ ЗА 8 МИНУТ ПОСЛЕ ПОПРАВКИ О11.** Потолок пяти снят не в
`core/services/enrollment.py` (зона живой позиции `raspredelenie-kolonki` — два писателя в один
файл), а в `veb/server.py:1625`, `ceiling=TEACHER_CEILING` → `ceiling=None`. Это ЕДИНСТВЕННАЯ
точка, где интерактивная форма получала потолок; сервис и его тесты не тронуты, календарные
отказы остались жёсткими. Коммит `1a4cc80`, выкатка в 10:53, сайт ответил 200 через 2 с.
ЦЕНА ОБХОДА: 0 минут ожидания позиции — она на своём пункте 5 из 6 и до A1 дошла бы не скоро.
ПРО ГОЛОВУ: правку в чужую зону я не делал НАМЕРЕННО и нашёл точку вне всех пяти зон — это и
есть способ доставлять срочное, не ломая параллельность. Обратная сторона названа: позиция
может сделать A1 ещё раз в своей ветке, и при влитии будет конфликт в одном файле. Он дешевле
простоя боевого сайта перед занятием.

**ЦЕНА ЛОЖНОЙ ТРЕВОГИ ЧАСОВОГО: 0, НО ОНА МОГЛА БЫТЬ ВЫСОКОЙ.** Часовой на кругах 117–120 писал
«сердце молчит 244м, зову владельца» и «окон заходов 0» при пяти живых позициях. Различено
командой, а не рассуждением: (1) сердце — круг 120 в 10:44:59, мой первый удар 10:45:11, то есть
часовой мерил ДО моего старта, тревога протухла сама; (2) «окон 0» — часовой считает образцом
`orkestr.py <абсолютный путь> --rezhim progon` (CHASOVOJ-VOLNY-noch.sh:149), а волна УТРО
запущена `ZAPUSK-ZAHODA.sh` по пункту О8 мандата — движок зовётся `claude -p` напрямую.
**Счётчик окон у часового СЛЕП к этой волне по построению**, и его ноль не означает мёртвых
позиций: `ps` показывает пять живых процессов, etime 2:44–2:51.
ПРО ГОЛОВУ: я едва не принял «окон 0» за факт и не пошёл перезапускать живые позиции — то есть
за отказ надзора, который сам и создал бы поломку. Спасло правило «признак жизни бери из
ПРОДУКТА» — рабочие папки правятся (2–4 пути), значит движки идут.

**ЛОГ `claude -p` НЕ РАСТЁТ ПО ПОСТРОЕНИЮ — НЕ СУДИТЬ ПО НЕМУ ЖИВОСТЬ.** Пять логов
`/tmp/zahod-*.log` по 0 байт при пяти работающих позициях: `claude -p` отдаёт вывод одним куском
в конце, `tee` до тех пор не получает ничего. Проверка 1 из «ПЕРВЫХ ДЕСЯТИ МИНУТ» («ноль байт =
движок не вызывался») ВЕРНА ДЛЯ `orkestr.py` И ЛОЖНА ДЛЯ ЭТОЙ ДВЕРИ.
ЦЕНА, ЕСЛИ НЕ ЗНАТЬ: пять снятых живых позиций — вся волна.
ДОМ: `mandate_volna-noch.md`, «ПЕРВЫЕ ДЕСЯТЬ МИНУТ», проверка 1 — нужна вилка по движку.
ДОСТАВЛЕНО: нет

**ТУННЕЛЬ ИЗ `deploy/ADRES.txt` МЁРТВ — ВИЗУАЛЬНАЯ ПРИЁМКА ЧУТЬ НЕ УШЛА В ЗАГЛУШКУ.**
`https://288c65b4b43b89.lhr.life` отвечает страницей «no tunnel here :(» — не таймаутом, а
осмысленной страницей чужого сервиса. Сужу по боевому `http://159.194.254.52/`.
ЦЕНА: 2 минуты. Была бы много выше, если бы я судил по коду ответа: заглушка отдаёт 200.
ДОМ: `deploy/ADRES.txt` — адрес протух и никем не проверяется.
ДОСТАВЛЕНО: нет

**ГЛАЗАМИ НА БОЕВОМ, 10:55, `/raspredelenie` гостем, 1440×900 — ВЛАДЕЛЕЦ ПРАВ ПО ОБОИМ:**
* G3.1 подтверждён: «Тухватулин-Йалчын …» обрезан троеточием, справа от имени ПУСТОЕ МЕСТО до
  колонки принимающего. Обрезка не от тесноты, а от заданной ширины колонки.
* G2.1 подтверждён: значок «обычно у» стоит В КОНЦЕ имени (Афанасьева Полина, Тараканов Роман),
  отдельной колонки нет.
