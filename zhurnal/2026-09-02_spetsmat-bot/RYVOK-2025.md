# 🔴 ПОСЛЕДНИЙ РЫВОК: 20:25 → 22:30. МАНДАТ ВЛАДЕЛЬЦА

**У меня два часа полного платного лимита и я хочу потратить их до дна.**
После 23:00 лимиты сбрасываются и остаток сгорает. Работай широко: `opus`,
четыре окна параллельно, не экономь.

---

## 0 · ГЛАВНАЯ ПРЕТЕНЗИЯ, И ОНА АРХИТЕКТУРНАЯ

🔴 **У волны есть целая машинерия против смерти. У БОТА нет ничего.**

Сердце, часовой, будильник, подъёмный лист, порог смерти, детектор убыли окон —
всё это построено вокруг ТЕБЯ. А продукт, ради которого волна существует, живёт
процессом в моём окне терминала: закрыл окно — умер, упал — лежит, и узнает об
этом человек, стоящий перед восемнадцатью детьми.

Это перевёрнутый приоритет. Оснастка выживания нужна прежде всего боту.

**Задача №1, впереди всего остального:** перенеси на бота ту же дисциплину,
которую мы весь день оттачивали на волне.

- бот поднимается НЕ из моего терминала: `nohup`, `ppid=1`, лог в файл;
- **у него есть свой сторож**, который проверяет живость и ПОДНИМАЕТ упавшего —
  сам, без человека. `P10` написала systemd-юнит, но у меня Mac: значит нужен
  либо `launchd`, либо сторожевой цикл той же природы, что `CHASOVOJ`;
- сторож не молчит: падение и подъём уходят мне в `@watchman57_bot`, тот самый
  `ALERT_TOKEN`, который уже проверен доставленным сообщением;
- 🔴 **доказательство — не тест, а убийство.** Сними бота по номеру процесса и
  покажи мне в отчёте: во сколько убит, во сколько поднялся сам, что пришло мне
  в Telegram. Без этого замера задача не закрыта.

## 1 · ПОРЯДОК НА ДВА ЧАСА. НЕ МЕНЯЙ ЕГО САМ

Пускай по четыре окна, `opus`. Порядок — по цене отказа на занятии.

| # | что | почему здесь |
|---|---|---|
| **A** | живучесть бота (§0) | упавший бот обнуляет всё остальное |
| **B** | три починки: `vy`, обрезка снимка, «снято» | без них занятие не проводится |
| **C** | `P11` сквозная приёмка | единственный независимый судья волны |
| **D** | словарь SpeechKit через v3 gRPC | 192 термина собираются и не уезжают |
| **E** | закрывающая фаза знания, уроки волны | это можно и завтра |

**Если время кончится — оно кончится на E, а не на A.**

## 2 · ЧТО ЗНАЧИТ «ИДЕАЛЬНО» — МОИМИ СЛОВАМИ

Не «все тесты зелёные». Идеально — это когда **каждое требование мандата
проверено ЧИСЛОМ и судьёй, который его не писал.**

- `P11` судит свежая модель — она уже так собрана, не ослабляй;
- **где можешь, ставь второго, независимого верификатора** сверх позиции: сегодня
  верификаторы нашли шесть дефектов в `P1`, шесть в `P7`, три в `P12` — они
  окупались каждый раз;
- **сверяй с ИСХОДНЫМИ документами владельца**, а не только с мандатом:
  `спецмат-бот-модель.md`, `-архитектура.md`, `-механизм.md`, `-ресёрч.md`.
  Мандат — пересказ; расхождение пересказа с источником сегодня уже стоило нам
  формата текстового ввода, который ты придумал сам;
- **чини найденное сразу**, не откладывай в заявки: заявка это отсрочка, а у нас
  сегодня есть чем платить, и завтра не будет.

## 3 · ЗАПРЕТЫ, ЧТОБЫ РЫВОК НЕ УШЁЛ В ПЕСОК

- **Не полировать то, чего не увидит ни ребёнок, ни преподаватель.** Красота
  внутренних имён, лишние абстракции, рефакторинг ради стройности — нет.
- Не объявлять успех тем, что позиции кончились.
- Отметка в базу без подтверждения человеком — нельзя, как и весь день.
- Чужие процессы по имени движка не глушить.

## 4 · ЧТО Я ЖДУ ОТ ТЕБЯ И КОГДА

- **сейчас, одной строкой:** бот поднят под `nohup`, лог там-то, несёт починки
  такие-то — и почему ты молчал с 19:39 до 20:2x;
- **в 21:15 — срез:** что закрыто из A–E, что в окнах, чего уже не успеваем;
- **в 22:20 — итог:** что войдёт в завтрашнее занятие, что осталось, и одна
  честная строка «бот к занятию готов / не готов, потому что …».

Между этими тремя точками меня не спрашивай — работай.

---

## ОТВЕТ ОРКЕСТРАТОРА

Отвечено 22:28. Отвечаю честно: **рывок не состоялся, и причина не в лимитах.**

## ЧТО СЪЕЛО ДВА ЧАСА

Приложение падало ТРИЖДЫ — около 20:50, 20:57 и 22:18. Каждое падение убивало
всё, что было задачей оснастки: будильники, окна заходов, судей веера. Между
первым и вторым падением приложение вдобавок потеряло доступ к `~/Documents` —
`config.py`, `.git` и мандат не читались, `git` падал на `getcwd`, — и вернулся он
сам только после второго перезапуска. Заходы `R1` `R2` `R3` запускались трижды и
все три раза не успевали ничего: их логи оставались по нулю байт.

Последний провал длился 83 минуты, и его поймала НОВАЯ машинерия: часовой в
22:17:55 записал «ПРОСТОЙ ГОЛОВЫ: сердце молчит 83м и будильников НЕТ» и позвал
вас в Telegram — ровно то, ради чего вы велели убрать мою память из контура.
Он же отказался поднимать третью голову: «подъёмов уже 2 из 2, это петля».

🔴 **СТРУКТУРНЫЙ ВЫВОД, И Я ЕГО УЖЕ ПРИМЕНИЛ.** Бот пережил два падения из трёх
именно потому, что стоит под `nohup` с `ppid=1`. Заходы я пускал задачами
оснастки — и терял их каждый раз. Теперь `R1` и `R2` идут под `nohup`
(`ppid 1`, pid 7129 и 7148), и следующее падение их не тронет.

## A — ЖИВУЧЕСТЬ БОТА: ЗАКРЫТО И ДОКАЗАНО УБИЙСТВОМ

    20:29:33  убил бота по номеру (kill -9)
    20:29:50  сторож: «БОТ НЕ НАЙДЕН — поднимаю (подъём 1 из 5 за час)»
    20:29:58  «✅ поднят заново, pid 74971» + сообщение вам в @watchman57_bot

Двадцать пять секунд от смерти до работающего бота, без человека. Потолок пять
подъёмов в час: бот, падающий сразу после старта, — это сломанный код, а не повод
поднимать ещё раз, и сторож в этом случае зовёт человека вместо петли.

**Сейчас: бот `pid 5358`, `ppid 1`, опрашивает Telegram с 22:24:58**, строки старта
в логе `logs/bot.log`. Сторож бота `pid 5646`, часовой волны `pid 5647`, оба
`ppid 1`, будильники взведены тройкой.

## B — ТРИ ПОЧИНКИ: ОДНА ИЗ ТРЁХ

**Сделано: `vy`.** `VoiceConfirm` получил свой префикс `vgo`; было побайтовое
совпадение с `ViewYear` (`vy:1`), и «Записать» под диктовкой уходило в экран
ученика, отказывавший преподавателю по роли — голосовой черновик не мог записать
НИКТО. Сторож перебирает все 30 классов `CallbackData`, доказан красным ДО
починки: «классов 30, различных упаковок 30, столкновений 0». Восемь литералов
`"vy:1"` в тестах заменены вызовом класса — литерал пережил бы смену префикса молча.

**Не сделано: обрезка снимка и «снято».** Идут в `R1-foto` прямо сейчас.

## C, D, E

`P11` собрана, пролинтована, не пущена. `R2-slovar` (словарь SpeechKit) идёт.
Закрывающая фаза знания не начата — она и стояла последней в вашем же порядке.

## ВЕЕР СУДЕЙ: ПУЩЕН ДВАЖДЫ, ПОГИБ ДВАЖДЫ

Шесть судей (`P1` `P2` `P7` на opus, `P4` `P3` `P6` на fable) уходили дважды и
дважды умирали вместе с приложением, не вернув ни одного вердикта. Судьи —
единственный механизм, который под `nohup` не поставить: они живут внутри моей
сессии. Значит их надо пускать МАЛЫМИ партиями и снимать вердикты сразу, а не
разворачивать девятнадцать разом в надежде, что приложение доживёт.

## ЧТО ВСЁ-ТАКИ СДЕЛАНО ЗА ВЕЧЕР, ЧИСЛОМ

Найдено дефектов: **11**. Починено: **5** (`vy` · журнал без приёмника · внутреннее
имя роли в тексте человеку · два теста, сторожившие мусор вместо поведения).
Заведено долгов: **6**, из них блокирующих **4**. Принято позиций: **19 из 20**.

## БОТ К ЗАНЯТИЮ — ОДНОЙ ЧЕСТНОЙ СТРОКОЙ

**Готов для текста и голоса; для фото — с двумя оговорками, пока `R1` не закрылась.**
Текст проверен на вашей записи и живой базе: четыре уменьшительных дали ваши id,
прочерк ушёл в явку. Голос: кнопка «Записать» теперь доходит, ключ SpeechKit живой.
Фото: снимайте так, чтобы **в кадр попал край листа**, и **не ставьте прочерков** —
обрезка режет коды, а прочерк приезжает как сдача.
