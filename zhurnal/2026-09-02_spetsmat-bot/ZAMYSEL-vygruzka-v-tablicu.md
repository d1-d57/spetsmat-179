# ЗАМЫСЕЛ: ночная выгрузка кондуита в Google-таблицу владельца

> **Это НЕ заход.** Файл-заход `bootstrap_zahod.py` собрать не удалось — он трижды отказал
> клаузой З29 (генератор печатает команду-измерение без числа, линтер требует число), причём
> и при нуле невлитых веток. Дефект инструмента записан заявкой
> `2026-09-07T1934-vygruzka-v-tablicu-07-09-bootstrap` и уроком в `UROKI-FABRIKE.md`.
> Здесь лежит содержание будущего захода, чтобы решения владельца не пропали за ночь.
> **Первый ход исполнителя — починить генератор, потом собрать заход из этого файла.**

## ЗАЧЕМ — цена вопроса названа владельцем

Сайт однажды уже стёрся вместе со всей историей плюсиков, и восстановить её было нечем.
Сегодня **все копии базы лежат на том же сервере**, который может умереть: `ops/rezervnaya_kopia.py`
по построению не имеет никакой отправки наружу («offers no upload of any kind; that absence
is the feature» — решение против выгрузки в телеграм-канал). Значит смерть машины = потеря всего.

Владелец 07.09: нужна таблица в его Google-аккаунте, куда сервер сам пишет каждый день,
доступная преподавателям на чтение, — «чтобы любой обеспокоенный человек мог зайти и
посмотреть, что с этими версиями, есть ли новые плюсики, новые листки».

## ЧТО УЖЕ ГОТОВО — не переизобретать

| что | где | состояние |
|---|---|---|
| экспорт кондуита | `tools/export_xlsx.py` | 🟢 готов: лист на листок, школьники вниз, задачи вправо, знаки из `VALUE_REGISTRY`, проекция через `ProgressService.states_for_many` — второго чтения журнала не заводить |
| ключ робота | `secrets/psychic-heading-495410-u7-e4d4bd56d890.json` | 🟢 на диске, `type: service_account`, `client_email` совпадает |
| робот | `vygruzka-konduita@psychic-heading-495410-u7.iam.gserviceaccount.com` | 🟢 заведён владельцем |
| таблица | `1ZQzvk0r-_oojZTcub--md7wpyPLxVq8mb9AihdCg0lw` | 🟢 заведена, роботу выдан доступ «Редактор» |
| штатные снимки и таймеры | `ops/rezervnaya_kopia.py`, `deploy/*.timer` | 🟢 работают, но только локально |

## ЗАДАЧА

**A. Инструмент `ops/vygruzka_v_tablicu.py`.** Берёт кондуит тем же путём, что `export_xlsx.py`
(через `ProgressService`, не своим запросом), и пишет его в таблицу владельца по Sheets API.
Лист на листок. По умолчанию — проба; запись по `--primenit`.

**B. 🔴 Дата обновления — в первую ячейку каждого листа.** Без неё остановившаяся выгрузка
выглядит ровно так же, как работающая, и «зайти проверить» перестаёт что-либо значить.

**C. Таймер.** Ежедневно, systemd, по образцу `deploy/spetsmat-rezervnaya-kopia-sutochnyj.timer`,
с `OnFailure=` на тот же алерт, что у остальных юнитов. Молчание = тревога.

**D. Секрет.** Ключ кладётся на сервер по ssh, владелец `spetsmat`, права `600`.
🔴 `secrets/` под `.gitignore` — не коммитить никогда, в отчёт не печатать, в логи не писать.

## КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ)

1. `sudo -u spetsmat python3 ops/vygruzka_v_tablicu.py` (проба) → `rc=0`, печатает, что записал бы.
2. После `--primenit`: **верификатор читает таблицу тем же API** и сверяет с живой базой —
   листов столько же, сколько листков текущего года (3), в каждом число непустых клеток
   совпадает с числом, посчитанным по базе. Оба числа печатать рядом.
3. Первая ячейка каждого листа несёт сегодняшнюю дату.
4. `systemctl list-timers | grep vygruzka` → таймер активен, следующий запуск в будущем.
5. Секрет не уехал: `git_zona.py check` чист, `git show --stat` по своим коммитам не содержит
   ни одного пути из `secrets/`, `grep -rc "private_key" <зона>` → `0`.
6. Остановка видна: подделать провал (неверный id таблицы) → юнит падает, `OnFailure` срабатывает.

**ОХВАТ:** «проверено 6 из 6 клауз; листков 3 из 3».

## ГРАНИЦЫ

- Зона: `ops/ deploy/ tools/ tests/`. Всё вне — read-only.
- 🔴 Снимок самой базы в Drive этим заходом НЕ везём: владелец выбрал таблицу с кондуитом.
  Копия `.db.gz` вне сервера остаётся открытым долгом — назвать его в отчёте строкой, не молча.
- Модель: Sonnet — механика по расписанному заданию.
