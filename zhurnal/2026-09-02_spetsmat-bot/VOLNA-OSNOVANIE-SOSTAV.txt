# ВОЛНА «ОСНОВАНИЕ» — состав. Собрана 2026-09-04 ночью, после сверки волны «сайт»
# с реальностью (SVERKA-2026-09-04.md). Волна «сайт» закрыта и НЕ дописывается.
#
# ИМЯ ВОЛНЫ ДЛЯ УКАЗАТЕЛЯ (2-я строка VOLNA-AKTIVNAYA): OSNOVANIE
#
# ПОЧЕМУ «ОСНОВАНИЕ». Диагноз вечера 03.09: все позиции волны «сайт» были про ФУНКЦИИ,
# а всё, что блокировало результат, было про ОСНОВАНИЕ — нет удалённого репозитория,
# нет Pages, нет постоянного адреса, нет настоящего состава класса. Единственная
# позиция про основание (P7) не запускалась ни разу, а P6 стояла на ней.
#
# 🔴 ТРЕБОВАНИЕ КО ВСЕМ ПОЗИЦИЯМ ЭТОЙ ВОЛНЫ: ПЕРЕЖИТЬ СМЕРТЬ ОРКЕСТРАТОРА.
# Оркестратор в ночь на 04.09 умирал и будет умирать. Поэтому каждая позиция:
#   * своя ветка и свой worktree — чужой индекс физически не заденет;
#   * состояние на диске, не в голове: план пишется в файл ДО работы;
#   * коммит не реже чем раз в 20 минут, даже незаконченный (урок P5: 453 строки
#     были спасены чужим коммитом, а не своим);
#   * перезапуск с нуля не ломает уже сделанное — миграции идемпотентны, тесты
#     не зависят от порядка;
#   * критерий готовности проверяется КОМАНДОЙ, и команда записана в заходе.
#
# 🔴 МОДЕЛИ: бесплатная везде. Платная — только эскалацией. НЕ ставить три позиции
# на одну бесплатную модель: урок 03.09 23:23 — три параллельных прогона съели квоту
# minimax-m3 целиком. Разводить по разным.
#
# ПОРЯДОК: N1 идёт ПЕРВОЙ И ОДНА (меняет схему, остальные её читают).
# После N1 — N3, N4, N5, N6 параллельно. N2 в любой момент, ни с кем не пересекается.

N1|slot-vmesto-weekday|free|migrations/003_slot.sql core/services/enrollment.py core/services/room.py infra/enrollment_repo.py tools/import_raspredelenie.py tests/enrollment/ tests/room/|pryamoj|ПЕРВОЙ И ОДНА. Обоснование целиком — RESHENIA-2026-09-05.md §3, прочитать до работы. Заменить enrollment.weekday (ISO 1-7) на slot (1|2) — «номер занятия в неделе»; вынести room из enrollment в kabinety(teacher_id, slot, room, valid_from, valid_to); завести raspisanie(slot, weekday, starts_at, valid_from, valid_to). 🔴 ЧИСЛО, РАДИ КОТОРОГО ПОЗИЦИЯ СУЩЕСТВУЕТ, снято командой в живой базе: weekday=1 у 53 детей, weekday=4 у 3 — то есть 53 из 56 не закреплены НИ ЗА КЕМ на втором занятии недели. Причина: импортёр обязан назвать день недели, календаря этого года нет, и в коде стоят константы ПРОШЛОГО года (WEEKDAY_MONDAY=1, WEEKDAY_THURSDAY=4). Правило после правки — умолчание владельца дословно: одно имя в источнике → ДВЕ строки (slot 1 и 2, тот же преподаватель); составное имя → две строки с разными. Убрать ветку `if len(resolved) > 1` и проверку `if not room: continue` (она выбрасывала ребёнка, когда у преподавателя нет кабинета). Индекс и триггер перекладываются дословно, slot на месте weekday. Календарь года НЕ заводить (решение владельца); sessions.held_on остаётся правдой о том, что было. ГОТОВНОСТЬ, проверяется командой: миграция накатывается на КОПИИ боевой базы туда и обратно; после импорта `select count(*) from enrollment` = 112 при 56 детях; `select count(distinct student_id) from enrollment where slot=2` = 56; tests/enrollment и tests/room зелёные.

N2|chelovek-bez-klassa-i-sostav-2026|free|tools/proverka_sostava.py tests/import/test_proverka_sostava.py seed/|pryamoj|Гигиена состава. В students 56 строк, ВСЕ со статусом active, ушедших ноль — а владелец говорит, что несколько детей ушло и несколько добавилось. Сам источник это предупреждает: `_preduprezhdenie` в baza-2025-26.json — «это ЧЕРНОВИК для заполнения, а не истина». Задача НЕ «угадать состав» (данные у владельца), а дать инструмент, который предъявляет подозрительные строки: (1) человек в students без класса — сейчас такой ровно один, Аракелова Дарья id=3, и это НЕ ребёнок, а преподаватель, записанный тестовым слушателем; (2) ребёнок без строки в enrollment — сейчас Гамаюнова Софья 9К и Сухов Даниил 9Л, оба настоящие; (3) ребёнок, у которого есть отметки, но нет закрепления; (4) преподаватель из seed/teachers.csv, у которого students_count не сходится с фактом. Вывод — таблица «строка · почему подозрительна · что с ней делать», rc=1 при непустой таблице. НИЧЕГО НЕ УДАЛЯТЬ САМОМУ: инструмент показывает, решает человек. ГОТОВНОСТЬ: на живой базе инструмент называет ровно три строки выше и объясняет каждую; тесты покрывают все четыре правила на поддельной базе.

N3|vhod-prepodavatelya-cherez-telegram|free|bot/ veb/vhod.py migrations/004_privyazka.sql tests/veb/test_vhod.py tests/privyazka/|pryamoj|РЕШЕНИЕ Б, обоснование — RESHENIA-2026-09-05.md §2. Преподавательского пароля НЕ СУЩЕСТВУЕТ: вход по персональной ссылке от бота. Бот шлёт t.me/<бот>?start=<токен>; токен одноразовый, в базе ХЕШ токена, а не он сам; переход ставит подписанную куку на 30 дней, и кука несёт teacher_id, а не роль «любой преподаватель». Потерял ссылку — просит новую у бота сам, без владельца. Отзыв: снять tg_id и погасить токен. Пароль ОРГАНИЗАТОРА не трогать (три человека, стадия 1). Закрывает долг «пароли открытым текстом в SOSTOYANIE-VOLNY-sajt.md» — перевыпускать будет нечего. Даёт то, чего сейчас нет физически: `_check_password` возвращает роль prepod, все 18 преподавателей для сайта неотличимы, и «свои подсвечены» держится честным словом. 🔴 РЕАЛЬНЫХ tg_id У ТЕБЯ НЕТ И НЕ БУДЕТ — в базе их 0 из 19, список даёт владелец утром. Поэтому позиция строит и доказывает МЕХАНИЗМ на поддельных данных: выпуск токена, погашение, кука с teacher_id, отзыв, повторный выпуск. Привязка живых аккаунтов — отдельный ход владельца. ГОТОВНОСТЬ: тест проводит преподавателя весь путь на фикстурах; повторное использование токена отвергается; кука подделке не поддаётся; после отзыва старая кука не пускает.

N4|veb-priyom-zadach|free|veb/priyom.py veb/static/priyom.css tests/veb/test_priyom.py|pryamoj|Была P3 волны «сайт»: заход собран, налит, прошёл линтер — и НЕ ЗАПУСКАЛСЯ ни разу, кода нет. Сетка «ученик x задачи листка», тап ставит отметку. Только через core/services/marking.py и progress.py. 🔴 Второго журнала отметок НЕ ЗАВОДИТЬ — это названо провалом волны. Помнить, что marks — журнал СОБЫТИЙ: 15112 assert и 735 retract, и кто посчитает сданное как count(*), покажет владельцу 735 несуществующих решённых задач. ГОТОВНОСТЬ: отметка ставится и снимается через сервис, проекция клетки совпадает с core/services/progress.py на всех четырёх исходах (EMPTY/SOLVED/RETRACTED/erratum), тесты зелёные.

N5|veb-konduit-proshlogo-goda|free|veb/konduit.py veb/static/konduit.css tests/veb/test_konduit.py|pryamoj|Была P4: запущена 03.09 в 23:00, снята в 23:11, ничего не сдано. ЧТЕНИЕ архива: 18 листков x 56 учеников, 15847 событий прошлого года, уже лежащих в базе (число снято командой заново — в старом заходе стояло 15112, это только assert). Открывается по паролю организатора. НЕ каталог листков: тот отдельный репозиторий spetsmat-179, его не трогать. ГОТОВНОСТЬ: страница отдаёт архив за приемлемое время на живой базе, число сданных на ней совпадает с проекцией progress.py, а не с count(*) по marks.

N6|stranica-pages-i-chestnaya-zaglushka|free|../spetsmat-179/index.html ../spetsmat-179/build.py ../spetsmat-179/snimok.json tools/opublikovat_snimok.py tests/veb/test_snimok.py|pryamoj|РЕШЕНИЕ А, обоснование — RESHENIA-2026-09-05.md §1. Постоянная ссылка, которую рассылают преподавателям — это адрес GitHub Pages, а НЕ адрес туннеля; туннель живёт ЗА ней и его адрес переменная. Собрать: страницу на spetsmat-179 со снимком распределения (кто за кем закреплён + дата снимка, живёт без машины владельца) и ссылкой на рабочий экран; и 🔴 ЧЕСТНУЮ ЗАГЛУШКУ — когда туннель не отвечает, преподаватель видит нашу страницу «рабочий экран сейчас недоступен: компьютер выключен, отметки принимаются на бумаге», а не ошибку браузера. Это финализированное решение владельца: падение динамической половины обязано быть видимым и объяснённым. Проверка доступности делается на СТОРОНЕ КЛИЕНТА (fetch с таймаутом), потому что Pages статический. Архив кондуита сюда НЕ выкладывать — он за паролем. 🔴 ГРАНИЦА: позиция ГОТОВИТ файлы, но НЕ ПУБЛИКУЕТ — spetsmat-179 сейчас не git-репозиторий вовсе, заводить его и Pages будет владелец своей учёткой. Зона выходит в СОСЕДНЮЮ папку: коммитить там отдельно, зону spetsmat-bot не смешивать. ГОТОВНОСТЬ: build.py собирает index.html из snimok.json; снимок снимается из живой базы командой; заглушка проверена подменой недоступного адреса; ни одной фамилии из кондуита на странице нет.

N7|repozitorii-pages-i-postoyannyj-tunnel|interaktivno-u-vladelca|../spetsmat-179/ deploy/ ops/|pryamoj|🔴 ОСОБАЯ ПОЗИЦИЯ: исполняется НА МАШИНЕ ВЛАДЕЛЬЦА, под его учёткой gh, при нём. Ночному исполнителю не отдавать — он не может авторизоваться под чужой учёткой и умрёт на первом шаге. Внесена в волну решением владельца на интервью 04.09 (Q2 мандата), в отмену прежней договорённости: в волне «сайт» это была позиция P7, она стояла в составе и не запускалась НИ РАЗУ, а на ней стояло всё остальное. Четыре дела: (1) завести spetsmat-179 настоящим git-репозиторием и включить GitHub Pages по готовому VYKLADKA.md — сейчас там `fatal: not a git repository`, и пока это так, при спящей машине владельца недоступно ВООБЩЕ НИЧЕГО; (2) завести приватный origin у spetsmat-bot — сейчас его нет, и вся работа существует в единственном экземпляре на диске владельца; (3) поднять постоянный туннель Tailscale Funnel (бесплатный Personal, имя <машина>.<tailnet>.ts.net, порты 443/8443/10000) вместо нынешнего одноразового lhr.life; (4) автозапуск сервера через launchd, а НЕ systemd — машина на macOS, а в deploy/ лежат systemd-юниты, и spetsmat-veb.service вдобавок не стартует нигде (см. SVERKA-2026-09-04.md §2.3). РАЗБЛОКИРУЕТ: публикацию того, что соберёт N6. ГОТОВНОСТЬ: адрес Pages отдаёт 200 и предъявлен командой; `git remote -v` в spetsmat-bot непуст; адрес туннеля пережил перезапуск процесса и предъявлен дважды с разницей в перезапуск.

# ЧЕГО В ЭТОЙ ВОЛНЕ НЕТ И ПОЧЕМУ — это не забывчивость, а граница.
#
# Ниже — то, чего не знает никто, кроме владельца. Это НЕ позиции: исполнителю
# неоткуда взять эти данные, и позиция на них умрёт на первом шаге.
#
#   * список преподавателей с аккаунтами в Telegram   (вход позиции N3)
#   * кто ушёл из класса и кто добавился              (вход позиции N2)
#
# Это разговор с владельцем минут на десять утром, и он разблокирует N3 и N2.
#
# Границы волны (из мандата mandate_volna-OSNOVANIE.md): личные кабинеты школьников ·
# покупка VPS · шифрование снимка · починка stop-хука (она в disciplina, там своя
# живая волна; диагноз — DIAGNOZ-huka-2026-09-04.md) · второй журнал отметок.
