# ЖУРНАЛ ОРКЕСТРАТОРА — ВОЛНА `sborka-bota`

Ведёт оркестратор волны. Вердикты позиций дописываются в мандат
(`mandate_sborka-bota.md`), а не сюда. Здесь — ход ночи и находки с ценой.

---

## 02.09 08:09–08:20 — РАЗВЕДКА И P0

Прочитаны целиком: мандат, шесть документов владельца (модель, архитектура,
волна, механизм, ресёрч, КЛЮЧИ) и шесть документов памяти арки.

**Найдено ДО первой команды, три поломки в земле:**

1. **Три мёртвых git-лока** в `spetsmat-bot/.git` (HEAD.lock 67 мин,
   index.lock 27 мин, maintenance.lock 67 мин) при НУЛЕ живых процессов `git`
   (`pgrep -lf '^git '` → пусто). Снято `git_zona.py clean`: 3 лока, 3 мусорных
   объекта.
2. 🔴 **Три loose-объекта с режимом `--w-------`** — write-only, читать нельзя.
   `git status` падал с `error: unable to open loose object be04710…:
   Permission denied` → `fatal: bad object HEAD`, и `git_zona.py plan` вместе с
   ним. Это ВЕСЬ первый коммит (commit + tree + blob), то есть репозиторий был
   нечитаем. Починено `chmod 444`, `git fsck` чист.
   **ЦЕНА:** без починки P0 не состоялся бы вовсе, а диагноз «bad object HEAD»
   читается как порча репозитория, а не как права доступа.
3. **Половина зоны 2 УЖЕ была закоммичена.** Мандат велит закоммитить
   `skills/kak-zavesti-telegram-bota/SKILL.md` — а он лежит в git с `94b0ceb`.
   Проверено не «коммит виден», а грепом по живому файлу:
   `grep -c -i polling …/SKILL.md` → 6. Осталась одна `korni.py`.

**P0 закрыт двумя коммитами:**

| зона | коммит | что |
|---|---|---|
| 1 · `spetsmat-bot` | `c4e8188` | арка, seed, tools, .gitignore, bot.env.example, README — 27 файлов, 5427 строк, одним коммитом |
| 2 · `disciplina` | `6e889f8` | `_generator/tools/korni.py` — реестр корней spetsmat-bot |

`secrets/` не уехал: `git check-ignore -v secrets/` → rc=0,
`git ls-files secrets/ | wc -l` → 0, `grep -c secrets` по дереву коммита → 0.

⚠ **Шесть документов владельца легли в арку в 08:14, ровно в минуту коммита** —
и попали в него. Не заслуга, а совпадение; замечено при сверке `git show --stat`.

---

## 02.09 08:17–08:22 — РЕЕСТР ЖИВОСТИ: ПОРОГ ПО УМОЛЧАНИЮ ВРЁТ

**Находка с ценой, годная в канон.** `modeli.probnit_parallel` имеет
`taimaut=45` по умолчанию. Проба всех 25 бесплатных моделей с этим порогом:

    ЖИВ: 3 · МОЛЧАНИЕ: 22   (из них 18 — rc=124, то есть ТАЙМАУТ, а не отказ)

Та же проба, тот же пул, тот же шлюз, порог 120 с:

    ЖИВ: 12 · МОЛЧАНИЕ: 13

**ЦЕНА:** на пороге 45 с я объявил бы бесплатный пул почти мёртвым и либо
остановил восемь бесплатных позиций волны, либо увёл их на платные без
основания. Докстринг самого же `modeli.py` знает, что «порог 60 с тоже был
мал: холодный старт `opencode` съедает часть его», — но параллельная проба
взяла 45. `rc=124` — это таймаут, и склеивать его с «отказ» нельзя.

**Второй дефект того же инструмента.** `modeli.py zhivost --prognat` БЕЗ
`--rod` падает `ValueError` (код зовёт `probnit_parallel(rod=None)` при
`kandidaty=None`), а С `--rod` переписывает реестр ТОЛЬКО пробованными
моделями — остальные исчезают из файла молча. Обход: свой вызов той же
`probnit_parallel` со списком всех 25 и той же сериализацией.

**Живы сейчас (02.09 08:22, порог 120 с), первые пять приоритетов все живы:**
`glm-5.2` · `minimax-m3` · `inkling-small` · `inkling` · `laguna-s-2.1` ·
`nemotron-3-ultra-550b` (проверенный судья, 12 вердиктов из 12) ·
`mistral-vibe-cli-with-tools` · `magistral-medium` · `codestral` ·
`nemotron-3-ultra-free` · `mimo-v2.5-free` · `big-pickle`.

---

## 02.09 08:22 — ПЛАТНЫЙ КАНАЛ: ARN BEDROCK — МЁРТВАЯ ДВЕРЬ

`bootstrap_zahod.py --dvizhok claude` вписывает в стартовую строку
`claude -p --model arn:aws:bedrock:us-east-1:811345154057:…`. Замер:

    ls -d ~/.aws            → No such file or directory
    env | grep -c '^AWS_'   → 0
    ANTHROPIC_BASE_URL      → https://api.anthropic.com

Bedrock-доступа на машине НЕТ вовсе. Та же яма уже стоила соседней волне 6
пяти платных позиций из пяти сегодня в 07:07 — девять минут, в каждом логе
«API Error: Could not load credentials from any providers», и снаружи это
неотличимо от «заход думает».

**Проба лестницей, как велит дисциплина (сначала одна команда, потом волна):**
`claude -p --model sonnet` → rc=0, ответ «живой»; `claude -p --model opus` →
rc=0, ответ «живой». Штатная авторизация работает, родовое имя работает.

Собрана `ZAPUSK-ZAHODA.sh` этой арки: модель приходит АРГУМЕНТОМ, движок
выбирается по имени (со слэшем или `:free` → opencode, родовое → claude),
маршрут — по наличию Bedrock-кредов. Стартовая строка генератора сохранена в
`kod_P1-yadro.md` дословно, в комментарии, с причиной замены.

---

## 02.09 08:31–08:34 — ОСНАСТКА ВЫЖИВАНИЯ, ПЕРЕНЯТА У ВОЛН 6–9

Владелец: параллельно идут четыре оркестратора (волны 6, 7, 8, 9), они по
дороге ломаются и по дороге чинятся; главное не «какой моделью», а **как не
умереть в процессе**. Волна 9 предписала отдать это всем оркестраторам.

**Диагноз оттуда, буквально применимый ко мне:**
🔴 **СЕССИЯ МЕЖДУ ХОДАМИ НЕ СУЩЕСТВУЕТ.** «Проверяй каждые 10 минут»
исполняется как несколько проверок ВНУТРИ ОДНОГО хода, после чего ход
кончается — снаружи это неотличимо от ожидания. Ждать умеет только ПРОЦЕСС,
переживающий сессию; будить умеет только ЗАДАЧА ОСНАСТКИ.
ЦЕНА у соседей: две волны из четырёх простояли, не начав работу, и обе
считали, что ждут.

**Взято три механизма, три разные смерти:**

| механизм | природа | переживает зависание | будит голову |
|---|---|---|---|
| `CHASOVOJ-sborka-bota.sh` | nohup, `ppid=1` | да | нет |
| заходы под `nohup` | nohup | да | нет |
| `BUDILNIK-sborka-bota.sh` | задача оснастки | нет | **да**, выходом |

**Сердце** — `SERDCE-VOLNY-sborka-bota.md`. Бьёт САМА голова, `date >>`, каждым
ходом. Скриптом не бить: удар скрипта доказывает жизнь скрипта.

🔴 **Чем мой часовой отличается от соседского и почему пришлось свой.**
Соседский `STOROZH-VOLNY.sh` в обязанности «поднять мёртвую голову» ЯВНО
пропускает свою волну (`[ "$V" = "$MOYA" ] && return 0`) и держит закрытый
список `VSE_VOLNY` четырёх волн репозитория `disciplina`. Волна `sborka-bota`
живёт в другом репозитории и в тот список не входит — поднимать её при смерти
головы было бы НЕКОМУ. Это ровно первая объявленная слепая зона
`skills/disciplina-orkestrator/SKILL.md`: «единственный наблюдатель снаружи
сессии сегодня — владелец». Мой часовой смотрит за МОЕЙ головой и поднимает ЕЁ:
сердце молчит ≥45 мин, мандат не CLOSED, подъёма нет в процессах, подъёмов
не больше двух — иначе петля и нужен человек.

**Подтверждено командой, не словом:** часовой `pid 96657, ppid=1`, проверка
ОТДЕЛЬНЫМ вызовом после выхода запускавшей оболочки (проверка в том же шелле
отвечает уверенно и неверно — замер соседей 08:15). Пульс растёт.
Будильник — задача оснастки `bltfcwxfp`, взведён на 2400/420.

**Подъёмный лист** `PODYOM-VOLNY-sborka-bota.md` написан ЗАДАНИЕМ, а не сводкой:
его будет читать исполнитель с пустым контекстом, поданный промптом
`claude -p --model opus "[PODYOM-VOLNY-sborka-bota] $(cat лист)"`.

Оснастка закоммичена: `b98b84a`.

---

## 02.09 08:34 — P1 ПУЩЕНА

`bash ZAPUSK-ZAHODA.sh P1-yadro opus`, под `nohup`. Рабочая папка
`spetsmat-bot-wt/P1-yadro`, ветка `zahod/P1-yadro`, маршрут — штатная
авторизация `claude` (Bedrock отсутствует, развилка сработала и сказала это
вслух). P1 блокирует всё остальное: пока она не принята, второй полосы нет.

Заход дозаполнен руками оркестратора поверх генератора: контекст, задача с
пятью частями (config.py · миграции с тремя добавками · core/ без aiogram ·
тесты, включая дифференциальный · Makefile), критерий готовности из трёх
команд, каждая печатает число, и все `<плейсхолдеры>` гит-гигиены развёрнуты
в полные пути — префикс зоны даёт ложно-зелёное.
