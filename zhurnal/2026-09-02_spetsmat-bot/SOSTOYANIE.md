# СОСТОЯНИЕ АРКИ «2026-09-02_spetsmat-bot» — что сделано, что осталось, чем измерено

> **Читается ВТОРЫМ**, сразу после `NAVIGATOR.md`, и до `PLAN.md`. Навигатор отвечает «что мы строим», этот файл — «где мы сейчас», план — «какой следующий шаг».
>
> 🔴 **ЗДЕСЬ НЕ ХРАНЯТСЯ ЧИСЛА — ЗДЕСЬ ХРАНЯТСЯ КОМАНДЫ.** Каждая строка несёт команду, которой значение снимается, и снимок с датой данных. Снимок — справка о прошлом, а не факт о настоящем. **Прочитал строку — прогони её команду**, прежде чем на неё опираться.
>
> **ОБЯЗАННОСТЬ КАЖДОЙ СЕССИИ: обновлять этот файл после КАЖДОГО принятого захода** — прогнать блок §0 целиком и переписать снимки.

## §0. Обновить всё разом — один блок, копируется целиком

```bash
cd ~/Documents/GitHub/spetsmat-bot
echo "=== ДАТА ДАННЫХ: $(date '+%Y-%m-%d %H:%M') ==="
git --no-optional-locks branch --no-merged main
git --no-optional-locks status --porcelain | wc -l
git --no-optional-locks log --oneline @{u}.. | wc -l
python3 core/istochnik.py                         # путь базы + дата последней ЗАПИСИ в ней
python3 tools/vidy_zadach.py                      # виды задач по листкам
python3 tools/gejt_verstki.py                     # обрезка · переносы · выход · скролл · центр
grep -n "BOTTOM_HALF_STATUS" zhurnal/2026-09-02_spetsmat-bot/mandate_volna-utro.md
tail -3 zhurnal/2026-09-02_spetsmat-bot/SERDCE-VOLNY-noch.md   # жива ли голова волны
```

---

## §1. Deliverable арки — из `TZ.md`

| # | что обещано | команда | снимок 2026-09-10 13:08 |
|---|---|---|---|
| 1 | сайт кондуита живёт и отдаёт распределение | `curl -s -o /dev/null -w '%{http_code}' https://math-kluychiki.ru/raspredelenie` | отдаёт 200, за паролем организатора |
| 2 | вёрстка не врёт: без обрезки, переносов, выхода за контейнер, скролла | `python3 tools/gejt_verstki.py` | 12:30 — все четыре нуля по 13 экранам и 2 ролям |
| 3 | два слоя распределения: постоянный и на день | `python3 -c` по `enrollment`/`attendance`, см. `core/services/sostav_na_den.py` | работает; кнопка «Применить постоянное · N» на всех вкладках дня |
| 4 | кондуит показывает виды задач | `python3 tools/vidy_zadach.py` | разметка боевой базы прогнана позицией `razmetka-bazy` 10.09 |
| 5 | тесты зелёные | `python3 -m pytest -q` | 12:31 — 1294 passed против входных 1252 (со слов головы, аналитик не перегонял: нет `aiogram` в песочнице) |

## §2. Разведка — факты о ПРОЕКТЕ

> Форма записи обязательна: факт + КОМАНДА + дата данных.

* **Боевая база — на сервере, не в папке репозитория.** Команда: `python3 core/istochnik.py` — печатает абсолютный путь и дату последней ЗАПИСИ внутри базы, и краснеет, если база старше последнего прошедшего занятия. Дата данных 2026-09-10 12:54, коммит `f1034dd`. 🔴 До этого коммита `data/spetsmat.db` лежала в git, и локальная копия показывала дату файла «сегодня» при отметках по 3 сентября — на этом аналитик построил два ложных доклада владельцу.
* **Кабинет привязан к ГРУППЕ и ДНЮ, не к преподавателю.** Таблица `kabinet_na_den (data, gruppa, kabinet)`. Группы: **В — Ваня Яковлев, Д — Даня Макаров, Н — Наташа Стрелкова** (буква от имени старшего). Команда: `select gruppa, name, kabinet from teachers where gruppa is not null and aktiven=1`. Постоянные кабинеты: В 303, Д 302, Н 203. Дата данных 2026-09-10 13:00.
* **Единственное поле ввода кабинета** — `veb/razdely/gruppy.py:78`, вкладка группы, два поля (пн и чт). В списках распределения у организатора его нет вовсе. Команда: `grep -rn "kab-inp" veb/`.
* **Автосохранение включается единственным условием** `if(el.dataset.den)` в `pomenyalos` (`veb/obshchee/karkas.py:564`). Орган без `data-den` попадает в очередь `pravki`, а на странице распределения кнопки «Сохранить» нет — очередь отправить нечем. Команда: `grep -rn "data-den=" veb/razdely/*.py veb/obshchee/*.py`.
* **Отсутствие преподавателя бывает ДВУХ пород, и они намеренно порознь.** `prepodavatel_ne_prihodit (teacher_id, slot)` — постоянное «по понедельникам не бывает», ДАТЫ В НЕЙ НЕТ. `teacher_attendance (session_id, teacher_id, status)` — «сегодня не пришёл». Команда: `pragma table_info(...)` по обеим.
* **Токен бота `@conduit179_bot` отозван владельцем 08.09**, проверено тогда же `getMe → 401`; корневая заявка `2026-09-02T1431` в `zayavki/sdelano/`. Мёртвые строки токена лежат в четырёх `VYGRUZKA-*.md` публичного репозитория — это ГИГИЕНА, не авария. Команда: `ls zhurnal/_INFRA-git/zayavki/sdelano/ | grep 1431`.
* **Листки 16A · 16α · 16ℵ выданы 03.09**, задачи внесены поимённо и подгружаются в кондуит из базы. 🔴 Подпись расходится с номером: во вкладке `16α` заголовок печатает «16А. ДЕРЕВЬЯ» — заголовок из `sheets.title`, вкладка из `sheets.number` (`veb/razdely/konduit.py:551`). Задачи внутри правильные, подтверждено владельцем глазами 10.09.

## §3. Git — состояние

| что | команда | снимок 2026-09-10 13:08 |
|---|---|---|
| ветка | `git rev-parse --abbrev-ref HEAD` | `main` |
| невлитых | `git --no-optional-locks branch --no-merged main` | пусто — все влиты |
| невывезено | `git --no-optional-locks log --oneline @{u}.. \| wc -l` | было 2 на 12:41 |
| вне git | `git --no-optional-locks status --porcelain \| wc -l` | живая база, лог часового, живой `kod_gejt-pravda.md`; плюс три `doc/TZ-*.md` от 06.09, никогда не забранные |
| рабочих папок позиций | `ls -d ../spetsmat-bot-wt/*/ \| wc -l` | 73 — куча, сторож на ней становится шумом |
| открытые заявки | `git_zona.py zayavki` | 13 на 12:13. 🔴 `--vsyo-ravno` печатает только ПЕРВУЮ — из-за этого голова обошла не ту |

## §4-ИТОГ. ВОЛНА УТРО ЗАКРЫТА — состояние на 2026-09-10 22:34

**Мандат:** `STATUS: CLOSED-S-DOLGOM`, `BOTTOM_HALF_STATUS: COMPLETE`. Восемь позиций, все восемь приняты, без вердикта ноль. Круг 33 (19:10): «main зелёный · выкачено всё, боевой отвечает 200 · не в git: 0».

**Перегнано аналитиком в 22:34:** коммитов за сутки в `main` — **206**, из них после полудня **40**. Невывезенных `@{u}..` — **0**. Невлитых ветвей — **0**. Вне git — **7** (`.DS_Store` ×2, три `doc/TZ-*.md` от 06.09, никогда не забранные, `scratchpad/verifikator/`, лог часового).

**Тесты:** 1252 на входе в день → 1310 перед восьмой позицией → **1328** на закрытии.

**Дверь источника работает — прогнал сам:** `python3 core/istochnik.py` без переменной среды печатает «🔴 ИСТОЧНИК НЕ НАЗВАН: переменная среды `SPETSMAT_BAZA` не выставлена» и называет ДВА законных ответа. Фантомное число стало невозможным. `git ls-files 'data/*.db'` → **0**.

### Восьмая позиция — `adres-bazy`, тот самый корень

| коммит | что |
|---|---|
| `4d7a9b5` | база получает АДРЕС, род помечен ВНУТРИ неё, и уходит из git |
| `dc6c7bc` | ни одного файла базы в репозитории |
| `ba2a739` | каждый читатель спрашивает НАЗВАННЫЙ источник; дверь записи требует живую боевую |
| `ab8f374` · `8e73585` | оболочка сайта отдаёт базу, которую ей ДАЛИ, а не найденную самой; проверки живой страницы честно пропускаются, когда источник не назван |
| `d373478` | 🔴 юниты знают `SPETSMAT_BAZA`: без переменной служба не стартует вовсе — **выкатка без этой правки положила бы боевой** |
| `9a8269e` | метка помнит свой файл: `cp` на той же машине больше не проходит как боевая |
| `457342e` | вердикт верификатора §3: 🔴 в миграции 012 стояло ЛОЖНОЕ ОБЕЩАНИЕ «защита от восстановленного снимка полная» — снимок на своём пути проходил как живая база. Исправлено на месте. Боевая ротация снимков проверена: 16412 отметок, как в живой |
| `8ae7589` | заголовок листка прибит к номеру СВОЕГО листка — секция и подпись берутся из одного объекта |
| `9a31d10` | дверь для бумажных кондуитов `tools/vnesti_s_bumagi.py` |

### 🔴 Названо и НЕ закрыто

**Голый `insert into marks` проходит.** Триггеры стерегут `UPDATE` и `DELETE`, но не `INSERT`. Позиция не стала чинить: запрет означал бы правку схемы `marks`, прямо запрещённую её же разделом «чего не делать». Это правильный отказ — вылезти за границу задачи молча было бы хуже. Дом: схема `marks`, отдельным заходом.

**Романчук Тимофей — 0 из 11 внесённых.** Ответ владельца есть (16A, задачи 1а и 1б), дверь `tools/vnesti_s_bumagi.py` готова и прогнана в обоих состояниях — внесение не выполнено.

**Четыре вопроса владельцу висят:** «2 пункт» у Верхошинского · «16ба» у Виляева · отсутствие Юсуфова на листе Шнитке · правка слоя занятия Виляеву за 07.09.

**Три `doc/TZ-*.md` от 06.09 никогда не были забраны в git.**

### Что закрылось сверх всякого задания

**`docs/index.html` был четвёртым вторым источником, и его убили.** Страница на GitHub Pages зеркалила расписание живого сайта, разъезжалась с ним при любой правке и несла мёртвую ссылку входа «в никуда». Теперь это 1742 байта, не несущие ни одного своего данного — только отправка на живой сайт. Никто этого не заказывал.

---

## §4-АРХИВ. ВОЛНА УТРО — снимок на 2026-09-10 16:24 (промежуточный, оставлен для истории)

**Отчётная строка круга 18 (14:27):** запущено 7 · принято 7 · **без вердикта 0** · main зелёный (grid+kabinet 113 passed) · не в git 25 → 0 · смотрел глазами: да · **ЗАКРЫТО 28 из 28 названных плюс добивка Д1/Д4/K1/G6.1**.

**Перегнано аналитиком в 16:21, командами:** невывезенных коммитов `@{u}..` → **0** · невлитых ветвей `branch --no-merged main` → **0** · вне git → **10** файлов (документы аналитика и логи, накопились после круга 18).

**Голова:** последний удар сердца **16:02:26**, на момент замера молчит 19 минут — кончила либо между ходами. Часовой тревогу НЕ бьёт: порог выше, надзор не отличает паузы от конца. Журнал круга тронут **14:27** при коммитах до **16:02** — отставание полтора часа.

🔴 **Единственный формальный долг волны: мандат `STATUS: OPEN`, `BOTTOM_HALF_STATUS: PENDING`** — вторую волну подряд. Закрывающая сессия читает мандат, а журнал круга не читает по устройству.

### Закрыто по пунктам ТЗ (содержание — в `TZ-DOBOR-10-09.md`, здесь только статус)

**Все 28 названных.** A1 · A2 · B1 · B2 · C1 · C2 · D1 · D3 · G0 · G1 · G2.1 · G2.2 · G2.5 · **G3.1** · G3.2 · G3.4 · G3.5 · G4.3 · G6.1 · G7 · G8 · G9.1 · G9.2 · H1.1 · H1.2 · H1.5 · H1.6 · H1.7 · H1.8 · H2.1 · H2.2 · H2.3 · H3.3 · H3.5 · H4.1 · H4.2 · H4.3 · H4.4 · H4.5 · H4.6 · H4.7 · H4.8 · **H5.1** · J1 · J2.3 · J2.4 · J2.6 · J3 · K1

### Сверх ТЗ — срочные просьбы владельца 10.09, все закрыты

| коммит | что |
|---|---|
| `958e31f` + `64a077e` | кнопка **«Применить постоянное · N»**: две серверные двери, разметка на всех пяти вкладках дня, ноль перекрытий — кнопки нет вовсе, у гостя нет |
| `e46d6e0` | **отсутствие принимающего на день** снимает прикрепление и сохраняет аудиторию; `segodnya=None`, `room` прежний, в слой занятия НИЧЕГО не пишется — состояние вычисляемое |
| `7bc5b6b` | **сохранение кабинета**: поле несло `data-data`, а автосохранение включает единственное условие `data-den`; приведено к соглашению страницы, а не обойдено вторым именем в `pomenyalos` |
| `28e3fa4` | кабинеты на главной брали жёстко понедельничные и в четверг показывали старые — теперь день ближайшего занятия |
| `9566eba` | 🔴 **ОДНА ПРАВДА ПРО ПРИНИМАЮЩЕГО.** Кондуит и кабинет спрашивают ту же службу, что распределение (`SostavService.sostav(den)` с портом отсутствующих). Было — чистый `enrollment`, и правка на день туда не доходила. Гейт: `tests/grid/test_konduit_i_raspredelenie_odna_pravda.py`, сравнивает два ответа между собой |
| `db50f4f` | **H5.1** — вход на `/istoria` восстановлен (`grep -c 'href="/istoria' veb/obshchee/karkas.py` → 3, было 0 везде). Плюс **таймаут отметки 10 с**: при пропавшей сети клетка залипала тёмной навсегда и не принимала тапов |
| `8381e5f` | **подпись листка** врала не данными, а РЕГИСТРОМ: `text-transform:uppercase` переводит «α» в заглавную греческую «Α», неотличимую от русской «А» — три листка схлопывались в один на вид. В данных расхождений ноль, проверено на боевой |
| `b27663a` | кондуит: кнопка **«Отменить»** на один ход; выделение галочек этого принимающего на ТЕКУЩЕМ занятии; шапка на телефоне. 🔴 Причина шапки — **клиентский скрипт падал синтаксической ошибкой**, то есть и другое клиентское поведение могло быть мёртвым |
| `1beb0cc` | **Д1 доведён.** `core/istochnik.py` — говорящая дверь. Прогон 16:21: «источник: …/data/spetsmat.db · последняя запись 2026-09-06 · последнее занятие 2026-09-10 · 🔴 БАЗА МЁРТВАЯ · вердикт: МЁРТВАЯ». Насажен на **12** файлов; базу читают **20**, не зовут дверь **8** — серверная сторона, чисел человеку в терминал не печатает. Плюс `SvidetelRaboty`: работа с данными печатает дату последней записи ДО и ПОСЛЕ и число строк |
| `b14132e` | **Д4** — постоянное распределение в `EKRANY` обеими вкладками; рядом с числом находок печатается ОХВАТ (экраны, роли, узлы) |
| — на сервере | **плюсики за 07.09 внесены.** 36 клеток, 30 новых событий; шесть уже стояли с фото 03.09/05.09 и сервис не задвоил. `MarkingService.set_state`, не `insert`. Копии до и после: `/opt/spetsmat-bot/data/backups/spetsmat-20260910T102341Z-ruchnoj.db.gz` и `…T102903Z` |

### Не закрыто

| что | чем измерено |
|---|---|
| **Романчук Тимофей — 0 из 11**, не внесён вовсе | в рукописи рамка «1а» и «в», в 16α таких меток нет (там 1…15, 14а/б/в). Ждёт ответа владельца |
| **мандат** `BOTTOM_HALF_STATUS: PENDING` | `grep -n BOTTOM_HALF zhurnal/…/mandate_volna-utro.md` |
| **вне git 10 файлов** | `git --no-optional-locks status --porcelain \| wc -l` |
| **консоль браузера не проверяет ни один гейт** | синтаксическая ошибка в клиентском скрипте дожила до владельца и была найдена только через симптом («шапка не встаёт») |
| **часовой не бьёт тревогу на смерть головы** | `tail PULS-CHASOVOGO-noch.log` — печатает «сердце молчит N м» и не эскалирует |

### Пять вопросов головы владельцу — висят

1. Романчук: рамка «1а» и «в», штрих перед «1а» — что это? 2. Верхошинский: приписка «2 пункт» рядом с 11. 3. Виляев: в рукописи «16ба», внесено в 16α — подтвердить. 4. Юсуфова на листе Шнитке нет вовсе, хотя он в постоянной четвёрке. 5. Виляев по постоянному у Амбург, сдавал Шнитке — править ли слой занятия 07.09.

### Хенд-офф-долг головы — адресован аналитику, ни один пункт не доставлен

`deploy/ADRES.txt` (адрес протух) · `tests/veb/test_kanon_verstki.py` (порт зашит) · `mandate_volna-noch.md` цикл п.15 (ритм отчёта) и «ПЕРВЫЕ ДЕСЯТЬ МИНУТ» (вилка по движку) · `tools/gejt_verstki.py` (снимок во временную папку) · `git_zona.py` (`--vsyo-ravno` печатает все заявки, а не первую — открытых было 13) · `veb/razdely/shkolniki.py` (подсветка `.para.krasn` обрывается на 37 px, два выхода по 6 px на группе Д).

### 🔴 Пять промахов аналитика за сутки — один класс, один рычаг

«за сорок минут» (окно грепа названо длительностью) · «отвергается на записи» (усилил требование владельца) · «нет задачи 14а» и «нет занятия за 07.09» (мерил мёртвую локальную копию) · «плюсики не внесены» (искал копию базы локально, она была на сервере) · «`title` разошёлся с `number`» (назвал причину, не измерив; причина была в CSS).

**Класс точнее, чем «неверное измерение»: причина или величина названа БЕЗ источника, из которого взята.** Рычаг тот же, что требуется от гейтов: источник печатается рядом с величиной, всегда — включая сообщения владельцу. Для базы этот рычаг теперь существует и работает (`core/istochnik.py`); для остальных утверждений он пока держится дисциплиной, а не кодом.

## §5. Отложено владельцем — не потеряно, но и не в работе

* **Раздел I ТЗ — «Внести задачи» как ДИАЛОГ**, а не три вкладки: одно окно переписки поверх кондуита, кнопка фото и кнопка аудиозаписи, возможность спорить с разобранным. Кнопка «Показать гипотезу» → «Сохранить». Решение владельца: в волну не входит.
* **K2** — с кнопкой «Внести задачи» «что-то ещё не то», владелец не сформулировал. Исполнителю НЕ угадывать.
* **K3** — выделять клетку, если не сдана ОБЯЗАТЕЛЬНАЯ задача, и выделять столбцы обязательных целиком. Данные для этого есть.
* **G6.2** — галка «пн»/«чт» на вкладке «принимающим»: сам орган питает A2, поэтому вынесен владельцу вопросом, а не удалён.
* **Скорость сохранения распределения** — диагноз в `PLAN.md`, раздел СКОРОСТЬ. Отдельный заход: клиент, сервер, транзакционность.
* **Чистка мёртвых строк токена** — заход `kod_chistka-tokenov.md` написан 08.09 и НЕ запускался. 🔴 Его половина А переписывает публичную историю `push --force`; живых рабочих папок 73, они окажутся на осиротевших коммитах. Только после закрытия волны, и половиной Б прежде половины А.
* **Волна «Фабрика курсов»** — переделка оркестратора по `REFLEKSIA-ORKESTRATOROV.md` (классы 1–26, части Б, В, Д).
* **Новый токен в `secrets/bot.env`** — владелец внёс 10.09. Бот стоит с 04.09 04:03 (SIGTERM в `logs/bot.log`), поднимать отдельным ходом.
* **Кондуиты за понедельник 07.09 — ВНЕСЕНЫ** 10.09 оркестратором волны УТРО, кроме Романчука (см. §4). Буква «п» в записях вида «2п, 3п, 5п» означала **письменно**, а не метку задачи.

---
*Дата данных всех снимков: 2026-09-10 22:34. Устарели — прогони §0.*
