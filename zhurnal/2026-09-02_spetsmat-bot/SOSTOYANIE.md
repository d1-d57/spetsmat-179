# СОСТОЯНИЕ АРКИ «2026-09-02_spetsmat-bot» — что сделано, что осталось, чем измерено

> **Читается ВТОРЫМ**, сразу после `NAVIGATOR.md`, и до `PLAN.md`. Навигатор отвечает «что мы строим», этот файл — «где мы сейчас», план — «какой следующий шаг».
>
> 🔴 **ЗДЕСЬ НЕ ХРАНЯТСЯ ЧИСЛА — ЗДЕСЬ ХРАНЯТСЯ КОМАНДЫ.** Каждая строка несёт команду, которой значение снимается, и снимок с датой данных. Снимок — справка о прошлом, а не факт о настоящем. **Прочитал строку — прогони её команду**, прежде чем на неё опираться.
>
> **ОБЯЗАННОСТЬ КАЖДОЙ СЕССИИ: обновлять этот файл после КАЖДОГО принятого захода** — прогнать блок §0 целиком и переписать снимки.

## §0. Обновить всё разом — один блок, копируется целиком

```bash
cd ~/Documents/GitHub/disciplina
echo "=== ДАТА ДАННЫХ: $(date '+%Y-%m-%d %H:%M') ==="
python3 tools/sostoyanie.py _studio/zhurnal/2026-09-02_spetsmat-bot
git --no-optional-locks branch --no-merged main
git --no-optional-locks status --porcelain | wc -l
git --no-optional-locks log --oneline @{u}.. | wc -l
python3 /sessions/funny-eager-bell/mnt/GitHub/disciplina/_generator/tools/git_zona.py zayavki
python3 /sessions/funny-eager-bell/mnt/GitHub/disciplina/_generator/tools/check_arka.py _studio/zhurnal/2026-09-02_spetsmat-bot
<+ команды под КОНКРЕТНЫЕ deliverable этой арки — дописать на Ф0, вместе с §1>
```

---

## §1. Deliverable арки — из `TZ.md`

| # | что обещано | команда | снимок 2026-09-02 |
|---|---|---|---|
| 1 | <deliverable 1 из `TZ.md`> | <команда, которой проверяется> | <результат> |
| 2 | <deliverable 2, если есть> | <команда> | <результат> |

## §2. Разведка — факты о ПРОЕКТЕ, добытые на входе

> Дом для того, что нашла разведка на входе в арку (`disciplina-arka §3`): путь, число, где что лежит, чего нет. **Обязательная форма записи: факт + КОМАНДА, которой он снят + дата данных.** Без команды запись сюда не идёт — она превращается в утверждение о мире, которое некому перепроверить, и повторный пересчёт даст другое число молча (цена: замер «CLAUDE.md есть у 2 репозиториев из 6» жил только в чате, через два часа пересчитан заново и дал другое число — первый счёт путал git-репозиторий с обычной папкой).
>
> Разведка внутри арки читает СНАЧАЛА этот раздел, потом, если ответа нет, — проект. Разнесение из этого раздела по домам проекта — заход `disciplina-arka §7`, при закрытии, один раз.

<факт — команда, которой снят — дата данных>

## §3. Git — состояние

| что | команда | снимок 2026-09-02 |
|---|---|---|
| ветка | `git rev-parse --abbrev-ref HEAD` | <> |
| невлитых | `git --no-optional-locks branch --no-merged main` | <> |
| невывезено | `git --no-optional-locks log --oneline @{u}.. \| wc -l` | <> |
| вне git | `git --no-optional-locks status --porcelain \| wc -l` | <> |
| открытые заявки | `python3 /sessions/funny-eager-bell/mnt/GitHub/disciplina/_generator/tools/git_zona.py zayavki` | <> |

## §4. Что известно про следующие шаги

<пункты, каждый — что и почему; не план (это `PLAN.md`), а контекст решения>

---
*Дата данных всех снимков: 2026-09-02. Устарели — прогони §0.*
