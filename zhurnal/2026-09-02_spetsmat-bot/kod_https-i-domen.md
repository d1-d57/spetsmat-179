# Канал исполнителя — https-i-domen (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — заход расписан по шагам: certbot, nginx, редиректы, четыре проверки по сети.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Sonnet 5 — заход расписан по шагам: certbot, nginx, редиректы, четыре проверки по сети; рассуждения по ходу не требуется.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/https-i-domen.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/2026-09-02_spetsmat-bot/kod_https-i-domen.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add https-i-domen --branch https-i-domen, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.

Заход мог быть ПОПРАВЛЕН после того, как ты стартовал: аналитик правит
тот же файл, а ты прочитал его один раз. Смотри секцию ## ПРАВКИ ПОСЛЕ
ВЫДАЧИ в конце файла — при старте и всякий раз, когда владелец пишет
тебе в чат. Правка с номером, которого ты не читал, отменяет любое
противоречащее ей место выше.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
ГРАНИЦА ОБЛАСТИ: сырые подстроки в `kod_*.md` (пункт 4) — НЕ парсер очереди `dostavit_urok` (который считает только пары ДОМ:/ДОСТАВЛЕНО:). Разница в числах — законна.
🔴 снимок при сборке 2026-09-07, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/schet_nezakrytogo.py zhurnal/2026-09-02_spetsmat-bot`
Область: «zhurnal/2026-09-02_spetsmat-bot» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — ни одного skills/*/DOLG.md нет на диске (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 25
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 276
     из них разбором очереди (парсер `dostavit_urok`, записи с парой ДОМ:/ДОСТАВЛЕНО:): 149
       живых (чинится доставкой — «дом есть»)  : 28
       к владельцу (решение за человеком)      : 60
       адрес недоступен (нет/папка/код/указат.) : 59
       адрес не разобран                        : 0
       отработавших (машинный след закрытия)    : 0
       доставлено                               : 2
       🔴 не проверяется машиной: содержательная отработанность записей БЕЗ следа закрытия (метки в доме, строки ✅/ЗАКРЫТО) — нужна ревизия человеком; сырой греп сверх разбора — шаблонные строки формы.

🔴 ДВЕРЬ НЕЗАКОММИЧЕННЫХ `kod_*.md` ОТКРЫТА КЛАПАНОМ: в арке `zhurnal/2026-09-02_spetsmat-bot` есть 3 незакоммиченных файлов-заходов `kod_veb-priyom-zadach.md`, `kod_dozabivka-dolgov-sklejki.md`, `kod_listki-kak-baza-zadach.md` — второй заход в такую арку обычно не собирается (долг 4 `disciplina-git`), но АНАЛИТИК открыл клапан, причина дословно: «аналитик в песочнице Cowork, писать в .git отсюда запрещено (git_zona.py doctor); чужой незакоммиченный kod_dozabivka-dolgov-sklejki.md к этой зоне отношения не имеет». Отключение видно здесь, в артефакте, а не осталось решением в голове аналитика.

КОНТЕКСТ. Сайт кондуита спецмата 179-й школы на сервере Beget (159.194.254.52,
Ubuntu 24.04, nginx перед `spetsmat-veb.service`). Прошлый этап: домен
`math-kluychiki.ru` зарегистрирован, привязан и УЖЕ отдаёт сайт по http —
проверено запросом 07.09 13:47. Сайт по IP тоже жив, и по нему розданы ссылки.
ЦЕЛЬ: HTTPS на домене, старые адреса переадресуются, а не умирают.
🔴 Цель не декоративная: браузеры не сохраняют и не предлагают пароли на
голом http, и владелец жалуется ровно на это — связка ключей перестала
подставлять пароль входа. Плюс пароли четырнадцати преподавателей сейчас
летят по сети открытым текстом.
Приёмка — по ОТЧЁТУ, без построчной сверки.
СТОП ДО ЦЕЛИ: получишь работающий https и редиректы, но НЕ автопродление,
проверенное боем — оно проверяется только через 60 дней; достаточно, чтобы
таймер certbot стоял и `--dry-run` проходил.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-09-07) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Домен math-kluychiki.ru УЖЕ привязан и отдаёт сайт по http — проверено запросом 07.09 13:47; остался только HTTPS
2. Цель HTTPS не декоративная: браузеры не сохраняют пароли на http, и владелец жалуется, что связка ключей перестала предлагать пароль входа
3. Старые адреса не ломать: http://159.194.254.52/ и http://math-kluychiki.ru/ обязаны переадресовывать, а не умирать — по IP розданы ссылки
4. Переключение делать либо до 14:00, либо после 15:55: занятие 14:15-15:55, сайт важнее фичи

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../spetsmat-bot-wt/https-i-domen`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `https-i-domen`. 🔴 **ДАЛЬШЕ — ТОЛЬКО ПУТИ ОТНОСИТЕЛЬНО ЭТОЙ ПАПКИ** (или `cd` в неё безусловно, каждым ходом): абсолютный путь в главную папку репозитория здесь — типичная ошибка, правка утекает МИМО worktree и найдётся только на коммите («вне git» в `git_zona.py check --zone` из рабочей папки, на файле, который уже правил, — цена, оплаченная живьём: 5 файлов, ручное копирование и откат главной папки). 🔴 **Заводишь её САМ — ВТОРЫМ ходом, СРАЗУ ПОСЛЕ пункта 1 блока §0.1 ГИТ-КОНТУР ниже (не первым ходом захода целиком), из корня репозитория:**
  ```
  python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add https-i-domen --branch https-i-domen
  cd ../spetsmat-bot-wt/https-i-domen
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце вливает САМ ЗАХОД, последним ходом** (решение 20.08, заход `granica-priyomki`, пересмотр решения 05.08; WARNING-блок ниже печатает тот же порядок): цепочкой коммит зоны → пуш → влитие в основную (`git_zona.py vlit-v-osnovnuyu`) → пост-проверка ИЗ ГЛАВНОЙ папки → гашение СВОЕЙ ветки. Причина пересмотра — замер 20.08: изнутри захода не видно разницы между «механизм новый в рабочей папке» и «в `main` он старый», а приёмке это невидимо по построению. Влитие УСЛОВНОЕ: красная пост-проверка = откат влития самим заходом и строка в отчёт, а не «доложу, пусть приёмка решает». Файл-заход по-прежнему НЕ коммитить — его коммитит аналитик при приёмке.
- **ЗОНА (можно менять):** `deploy/` `ops/storozh_sajta.py` `tests/ops/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4); коммить ПО ХОДУ работы, не одним последним ходом (§4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **SCRATCHPAD — ТОЛЬКО ЛИЧНЫЙ.** Черновики, выкладки, промежуточные версии — в личную папку СВОЕГО захода `scratchpad/https-i-domen/`. Общие пути (`scratchpad/otchet.md`, любой `scratchpad/*` без имени твоей темы) ЗАПРЕЩЕНЫ: чужой отчёт уедет в твой файл или твой — в чужой, а приёмка читает отчёт без построчной сверки и подмену НЕ ЛОВИТ по построению. *Цена 25.08: готовый `## ОТЧЁТ` захода konvejer-incidentov был записан в общий `scratchpad/otchet.md`, и 92 строки чужого отчёта простояли в `kod_slovari-v-kod.md`.*
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода «kod_registracia-bez-obhoda.md»).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает названные через `--vlit` ЧУЖИЕ ветки В ОСНОВНУЮ, и только ПОТОМ ты заводишь свою рабочую папку; СВОЮ ветку он не трогает никогда — её вливаешь ты сам последним ходом (граница прав ниже).** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. 🔴 ПОРЯДОК ПРИ ПАДЕНИИ ЭТОГО ВЫЗОВА — исполняй, не изобретай (движок роняет `network_error` на вызове субагента и уносит с собой ВЕСЬ заход, замер волны 3A — 4 захода из 13). Пауза 45 секунд, до трёх попыток; время меряй `date`, не суммой своих `sleep`. После третьей — работай БЕЗ субагента: контур остаётся неразобранным, и это ОТДЕЛЬНАЯ строка в `## ОТЧЁТ`, а не молчание. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты сам, по ходу работы (§4). 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone deploy/ --zone ops/storozh_sajta.py --zone tests/ops/ --kommitit 'свою зону через git_zona.py commit: deploy/, ops/storozh_sajta.py, tests/ops/. Файл-заход НЕ коммитить' --zakryt 'влить ветку https-i-domen в main последним ходом после коммита зоны; надгробие не ставить'
```

🔴 ГРАНИЦА ПРАВ, ЧЕТЫРЕ ОТВЕТА (та же, что в самом задании субагенту — одно место в тексте, а не пересказ): **кто вливает ЧУЖИЕ названные (`--vlit`) ветки** — субагент, в ОСНОВНУЮ ветку, до заведения твоей папки; **кто вливает СВОЮ ветку этого захода** — ты сам, последним ходом, после коммита зоны (`git_zona.py vlit-v-osnovnuyu`; решение владельца 25.08 — оно сняло противоречие волны 4, когда машинное §0.1 и текстовое «ветку НЕ вливать» спорили молча, и машинное побеждало); **кто закрывает заявки** — субагент, `zayavka-zakryt`; **кто коммитит пути ВНЕ зоны захода** — субагент (хвост Cowork и что назовёт пункт 3 его задания). Ты коммитишь ТОЛЬКО зону этого захода, по ходу работы (§4). 🔴 Конфликт на `README.md` при ЛЮБОМ слиянии разрешается ОБЪЕДИНЕНИЕМ записей реестра, НИКОГДА выбором стороны: параллельные заходы волны дописали в реестр по строке — обе записи правы, выбор одной молча уничтожает регистрацию соседа.

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-09-07, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged main | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.


- деплоя в этом заходе нет.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `https-i-domen`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add deploy/ ops/storozh_sajta.py tests/ops/` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО это, поимённо. Проект не изучай:**
  - `deploy/README.md` — как устроен сервер: адрес, дерево `/opt/spetsmat-bot`, юниты, правило «сайт возвращается первым, диагноз потом»;
  - `deploy/vykatka.sh` — почему выкатка отказывается в час занятия (`rc=3`) и как устроен откат;
  - `deploy/ustanovka.sh` — что и куда ставится, включая юниты;
  - `ops/storozh_sajta.py` — сторож живости сайта: в нём зашит адрес, который надо будет перевести на https;
  - живой nginx на сервере: `ssh ivan@159.194.254.52 'sudo nginx -T | head -60'` — читать ГЛАЗАМИ, конфиг на сервере может отличаться от того, что в репозитории.
- 🔴 **ДО-ЗАМЕР, до единой правки** — четыре адреса, вывод целиком в `## ПЛАН`:
  `for u in http://math-kluychiki.ru/ https://math-kluychiki.ru/ http://159.194.254.52/ http://math-kluychiki.ru/listki/16A-derevya.pdf; do curl -s -o /dev/null -w "$u -> %{http_code}\n" "$u"; done`
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

🔴 **WRITE YOUR `## ОТЧЁТ`, `## ПЛАН` AND `## ВОПРОСЫ` IN ENGLISH, AND EVERY FILE AND EVERY COMMIT MESSAGE YOU PRODUCE TOO.** Owner's decision 30.08. It is a каркас-level rule, not a preference — wave 2 lost it twice because the pass text listed the report SECTIONS and never said «every file you create». Fixed Russian addresses stay Cyrillic: `ЦЕНА:` · `ВЕРДИКТ:` · `ДОМ:` · `ДОСТАВЛЕНО:` · `ПОДЪЁМ:` · `[ДОЛГ: …]` · every `## ` heading of this file · every path and command.
**A. СЕРТИФИКАТ.** Let's Encrypt на `math-kluychiki.ru` (и `www.`, если он резолвится).
Ставить штатно, `certbot --nginx`; автопродление — системным таймером, не самодельным
кроном. Проверка продления: `certbot renew --dry-run` → `rc=0`.

**B. РЕДИРЕКТЫ, КОТОРЫЕ НИЧЕГО НЕ ЛОМАЮТ.** `http://math-kluychiki.ru/*` → `https://…`
(301). `http://159.194.254.52/*` — 🔴 **НЕ отключать и НЕ отдавать ошибку:** по этому
адресу розданы ссылки, и в документах проекта он записан. Переадресация на домен с
сохранением пути — да; смерть — нет.

**C. СТОРОЖ.** `ops/storozh_sajta.py` смотрит на адрес — перевести на `https://math-kluychiki.ru/`.
Сторож, стерегущий мёртвый адрес, — молчащая сигнализация, а это худший вид.

**D. ЧАСЫ.** Переключение делать **до 14:00 либо после 15:55**. Работать, коммитить и
вливать можно всегда. `nginx -t` перед каждым `reload`; сломанный конфиг применять
некуда — сайт на нём и живёт.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):** живой прогон на реальном объекте — не
на фикстуре, а по сети, все четыре адреса, командой с выводом:

1. `curl -s -o /dev/null -w '%{http_code}\n' https://math-kluychiki.ru/` → `200`, и сертификат валиден: `curl -sI https://math-kluychiki.ru/ >/dev/null; echo $?` → `0` без `--insecure`.
2. `curl -s -o /dev/null -w '%{http_code} %{redirect_url}\n' http://math-kluychiki.ru/` → `301 https://math-kluychiki.ru/`.
3. `curl -s -o /dev/null -w '%{http_code}\n' http://159.194.254.52/` → `200` или `301`, но НЕ `4xx/5xx`: ссылка по IP роздана людям.
4. `curl -s -o /dev/null -w '%{http_code}\n' https://math-kluychiki.ru/listki/16A-derevya.pdf` → `200`: путь листка пережил переезд.
5. `ssh ivan@159.194.254.52 'sudo certbot renew --dry-run'` → `rc=0`.
6. `ssh ivan@159.194.254.52 'systemctl is-active spetsmat-veb spetsmat-bot'` → оба `active`: сайт и бот пережили правку nginx.

**ОХВАТ СТРОКОЙ ВНУТРИ ВЕРДИКТА:** «проверено 6 из 6 клауз» и «проверено 4 из 4 адресов».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)

Верификатор нужен, тип — **ПОСЛЕ-типа** — судит результат, стоит в конце, после задачи. Свежий субагент, ДРУГИМ методом (свежий субагент проверяет НЕ конфиг, а отдачу: curl по https, по http, по IP и по старому пути листка), не перечитывает свою же правку. Доля сплошной выборки: сплошная выборка: 4 адреса из 4. Финальная строка ответа обязательна дословно: «выдано N позиций из M найденных» — без неё ответ считается усечённым и в отчёт не вставляется.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПО ХОДУ РАБОТЫ, НЕ ОДНИМ ПОСЛЕДНИМ ХОДОМ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая. Каждую завершённую часть работы коммить СРАЗУ, теми же двумя ходами — не копи всё к финальному ходу:
```
git --no-optional-locks add -- deploy/ ops/storozh_sajta.py tests/ops/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- deploy/ ops/storozh_sajta.py tests/ops/   # отсекает всё чужое
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **КОММИТЬ ПО ХОДУ — РЕШЕНИЕ ВЛАДЕЛЬЦА 25.08 (В11), ПЕРЕВЕРНУВШЕЕ прежний канон «одним последним ходом».** Цена прежнего канона: за сутки ДВА обрыва — канал `opencode run --auto` односторонний и умирает вместе с сессией (владелец закрыл ноутбук), и незакоммиченная работа пропадала целиком. Закончил кусок — закоммитил его; последний ход только ПРОВЕРЯЕТ, что коммитить нечего (`git status --porcelain` пуст, `git_zona.py check --zone` ✅).
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `deploy/` `ops/storozh_sajta.py` `tests/ops/`

- **Г1. Зона доехала в git.** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone deploy/` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone ops/storozh_sajta.py` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone tests/ops/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `spetsmat-bot` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged main` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## ⚠️🔴 WARNING · ПОСЛЕДНИЙ ХОД ПЕРЕД ОТЧЁТОМ — ПОЛНАЯ ГИТ-ГИГИЕНА. НЕ ПРОПУСКАТЬ 🔴⚠️

**СТОП. Прежде чем писать хоть строку в `## ОТЧЁТ` — прогони это целиком.** Требование владельца
2026-08-16, поводом стал заход, который сделал работу и не закоммитил НИЧЕГО: *«он ничего не
коммитит… всегда, когда собираешь по генератору, там должна быть фраза, что начинать надо с полной
проверки гигиены»*. Гит-контур §0.1 стоит в НАЧАЛЕ и разбирает то, что накопилось ДО тебя; этот блок
стоит в КОНЦЕ и разбирает то, что накопил ты сам. Один другого не заменяет.

**ПОРЯДОК ЖЁСТКИЙ, ОН НАЗВАН ВЛАДЕЛЬЦЕМ: коммит → влитие своей ветки в основную → пост-проверка
ИЗ ГЛАВНОЙ ПАПКИ → гашение → вывоз.** Обратный порядок не работает технически: влитие отказывает
на грязном дереве, пост-проверка неисполнима до влития, вывоз — на невлитом. **Решение владельца
20.08 пересматривает решение 05.08** («в конце может это worktree вливать, но, наверное, это не
нужно делать») — теперь вливает САМ заход, но только при зелёной пост-проверке.

**1 · ВСЕ КОММИТЫ.** Ничего не осталось вне git — ни в рабочем репозитории, ни в соседних, до
которых ты дотянулся по ходу работы:
```
for R in <репозитории, которых ты касался>; do
  echo "== $R"; git -C $R --no-optional-locks status --porcelain
done
```
Своя зона — своими путями (`add` + `commit -- <пути>`). Чужая содержательная работа — НЕ твоя:
называешь строкой в отчёте и оставляешь. Пусто у всех — так и напиши числом «вне git 0».

**2 · ВЛИТИЕ СВОЕЙ ВЕТКИ В ОСНОВНУЮ.** Только после того, как шаг 1 дал «вне git 0» на своей
зоне — влитие отказывает на грязном дереве:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py vlit-v-osnovnuyu https-i-domen --zone <своя зона> \
    --vsyo-ravno "своя рабочая папка ещё жива — влитие последним ходом захода, штатно"
```
Конфликт — ЗАКОННЫЙ исход, не повод форсировать: разрешай по существу, если понимаешь обе
стороны; не понимаешь — `git_zona.py vlit-v-osnovnuyu --abort`, ветка остаётся невлитой,
строка в отчёт и заявка на влитие (`git_zona.py zayavka --rod git-operaciya`).
🔴 Конфликт на `README.md` — только ОБЪЕДИНЕНИЕМ записей реестра, никогда выбором стороны:
параллельные заходы волны дописали по строке — обе записи правы, выбор одной молча уничтожает
регистрацию соседа.

**3 · ПОСТ-ПРОВЕРКА ИЗ ГЛАВНОЙ ПАПКИ.** Отвечает на вопрос «механизм ВСТАЛ», а не «коммит
виден»: прогон изменённого механизма из `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot`, НЕ из рабочей папки `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/https-i-domen` плюс `grep` по ЖИВОМУ файлу,
который его зовёт (хук, конвейер, генератор):
```
cd /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot && <команда прогона механизма, который заход менял> && echo $?
grep -n '<как механизм назван в вызывающем коде>' <живая точка вызова>
```
🔴 **Красная пост-проверка = ОТКАТ ВЛИТИЯ И СТРОКА В ОТЧЁТ**, а не «доложу, пусть приёмка
решает»: `git_zona.py vlit-v-osnovnuyu --abort`, если слияние ещё не закоммичено, иначе
`git -C /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot --no-optional-locks reset --hard <хэш ДО влития>`. Заход, который влил
и сломал `main`, обязан вернуть `main` сам.

**4 · ГАШЕНИЕ.** Невлитого не осталось: `git --no-optional-locks branch --no-merged main`.
Каждая оставшаяся ветка названа поимённо с причиной, почему она жива. 🔴 Ветку-витрину (`main`
там, где с неё публикуется сайт) НЕ вливать — влитие туда есть публикация и решение владельца.

**5 · ВЫВОЗ.** Вывези СВОЮ ветку; `main` НЕ вывози: если после работы в нём есть невывезенное,
поставь заявку `--rod git-operaciya` и назови число в отчёте. Команда для своей ветки:
`git --no-optional-locks log --oneline @{u}.. | wc -l` → 0.
Ненулевое на своей ветке означает, что работа существует только на этом диске.

**6 · ПРОВЕРКА ФАКТОМ, А НЕ ПАМЯТЬЮ.** Числа по каждому репозиторию — вне git · невлитых своих
и чужих; невывезенных СВОЕЙ ВЕТКИ, не по каждому репозиторию · результат пост-проверки
(зелёная/откачена) — печатаются командой и уходят в `## ОТЧЁТ` дословно. Ненулевое число или
красная пост-проверка без объяснения — приёмка читает как несделанную работу: она гоняет те же
команды первым ходом.

🔴 **Отчёт без этих чисел не принимается.** «Я закоммитил» — не то же самое, что `status --porcelain`
пустой: за одну сессию работа не доезжала трижды, каждый раз с честным «сделано» в отчёте.
## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Правка выдана раньше, чем подняты записи о том же участке — и велела ровно то, чем этот участок уже клали
ПРАВКА 1 (19:20) потребовала `301` HTTP→HTTPS и HSTS. ПРАВКА 2 (20:15) отменила оба пункта,
подняв ночные записи, где чёрным по белому: «редирект на HTTPS положил сайт целиком», «HSTS
не ставился ни разу — это единственное, что спасло от необратимости». Записи существовали ЗА
СУТКИ до правки 1 и лежали в том же журнале.
ЦЕНА: час работы аналитика на правку, которую он тут же отменил своей же следующей правкой, —
это видимая часть. Невидимая: стартовое сообщение велит исполнителю начинать сразу, и заход,
запущенный между 19:20 и 20:15, поставил бы `301` и HSTS. Это ровно та операция, которая
06.09 положила сайт целиком и которую браузеры кэшируют; с HSTS откат с сервера невозможен по
построению. От повторения ночного инцидента отделяли 55 минут и то, что владелец придержал запуск.
ЧИНИТСЯ: правка, трогающая участок, по которому в журнале есть ИНЦИДЕНТ, не выдаётся, пока
запись об инциденте не поднята и не процитирована В САМОЙ ПРАВКЕ. Правка 2 именно так и
устроена — она цитирует `OTCHET-…:222` и `OCHERED-veb.md:204`. Правка 1 не цитировала ничего.

### Утверждение о ЖИВОМ сайте вписано в заход без команды, которой оно снято, — и к прогону оказалось неверным
Правка 1: «страница, открытая ПО HTTPS, несёт внутри ссылки `http://math-kluychiki.ru/vhod` —
аналитик видел это в отдаче живого сайта». Замер исполнителя через час: таких ссылок **ноль**,
на всех трёх страницах; `Location` относительные, форма входа относительная, `X-Forwarded-Proto`
приложение уже читает. Утверждение, видимо, было верным, когда аналитик смотрел, и перестало
быть верным, когда влилась ветка `dve-stranicy-raspredelenia`.
ЦЕНА: пункт B задания — один из четырёх — оказался закрытым до начала работы. Исполнитель
потратил ход на опровержение вместо работы; это дёшево. Дорого было бы обратное: поверить
заходу, «починить» уже починенное и внести правку в `veb/` — чужую зону — по несуществующей
болезни.
ЧИНИТСЯ: утверждение о состоянии живой системы вписывается в заход ВМЕСТЕ С КОМАНДОЙ, которой
оно снято, и временем съёма — чтобы исполнитель мог перевыполнить её первым ходом, а не
доверять глазам аналитика недельной свежести. Заход уже требует этого от ИСПОЛНИТЕЛЯ
(«назови замером, а не догадкой»); симметричного требования к АНАЛИТИКУ в форме нет.

> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**ПРАВКИ ПРОЧИТАНЫ: 1, 2.** Работаю по редакции ПРАВКИ 2. Редирект HTTP→HTTPS (301 и 302
одинаково), `Strict-Transport-Security` в любом виде и любое ослабление HTTP — **не делаю**;
это запрет, а не предпочтение.

**ДО-ЗАМЕР, четыре адреса (20:45 MSK, до единой правки):**
```
http://math-kluychiki.ru/ -> 200
https://math-kluychiki.ru/ -> 200
http://159.194.254.52/ -> 200
http://math-kluychiki.ru/listki/16A-derevya.pdf -> 200
```

**СНИМОК ВХОДА (гит):** `git branch --no-merged main` — пусто, невлитых веток 0, из них
`zahod/*` — 0. Ветка `dve-stranicy-raspredelenia` УЖЕ ВЛИТА в `main` — то есть условие
пункта B правки 2 («если он не влит — правку ссылок не делай») снято, `veb/` мне открыт.

**ЧТО Я ОСПАРИВАЮ В КРИТЕРИИ — до работы, как велит §1.** Клауза 5 требует «подделать
недоступность 443 в тесте». Это исполнимо и я это сделаю, но честно называю границу: тест
подделывает недоступность ЛОКАЛЬНОГО порта, а не фильтрацию на пути. Различение «сайт лёг» ↔
«443 отфильтрован» сторож строит на наблюдаемом признаке — HTTP отвечает, HTTPS не отвечает, —
и именно этот признак тест и проверяет. Больше из теста не выжать: настоящую фильтрацию на
пути в тесте не воспроизвести никаким способом.

**ЧЕТЫРЕ ПУНКТА — ЧТО УЖЕ ВИЖУ И ЧТО СОБИРАЮСЬ ДЕЛАТЬ.**

**A. ЗАМЕР 443 — главный продукт. ЗАПУЩЕН ПЕРВЫМ ХОДОМ, идёт прямо сейчас.**
36 проб с интервалом 120 с, покрытие ~70 минут, старт 20:45:19 MSK, финиш ~21:55.
Меряю **из своей среды** — macOS на домашней машине владельца (та самая сеть, из которой
ходит владелец), НЕ с сервера: изнутри дата-центра 443 работал и в ночь падения.
Каждая проба — три адреса: `https://…:443`, контроль `http://…:80`, контроль `https://…:8443`.
Скрипт: `scratchpad/https-i-domen/zamer_443.sh`, сырьё: `…/zamer_443.csv`.
🔴 Про 8443 уже видно по живому nginx: **блока на 8443 нет вовсе**, порт не слушает — контроль
на нём даст сплошной таймаут, и это не поломка замера, а факт конфигурации.

**B. ССЫЛКИ ПО СХЕМЕ ЗАПРОСА — ПРАВИТЬ, ПОХОЖЕ, НЕЧЕГО: болезнь, описанная в правке, на живом
сайте не воспроизводится.** Замер вместо доверия к описанию:
* `curl -s https://math-kluychiki.ru/ | grep -c 'http://math-kluychiki'` → **0**;
* то же на `/vhod`, `/raspredelenie` — 0 абсолютных `http://` ссылок вообще ни одной;
* `Location` у всех редиректов относительный (`/`, `/vhod` — `veb/server.py:617,649,862,1025,1030`),
  абсолютный адрес в выводе `curl` достраивает сам curl, сервер его не отдаёт;
* форма входа — `action="/vhod"`, относительная;
* `X-Forwarded-Proto` nginx проставляет в обоих `proxy_pass` (`snippets/spetsmat-obshchee.conf`),
  и приложение его УЖЕ читает — `veb/server.py:1046`, флаг `Secure` на куке ставится по схеме
  запроса, а не константой.
Вывод: пункт B закрыт до меня заходом `dve-stranicy-raspredelenia`. Кода не трогаю, пишу это
в отчёт замером. Если по ходу найду хоть одну ссылку со схемой-константой — поправлю.

**C. ПРИЧИНА ПОМЕТКИ — УЖЕ НАЗВАНА ЗАМЕРОМ, не догадкой.** Google Safe Browsing Transparency
Report, опрошен из браузера 07.09 ~20:50, и опрошен ПОАДРЕСНО — в этом вся ценность:
* `https://math-kluychiki.ru/` → **«Некоторые страницы этого сайта небезопасны… содержит
  вредоносный контент, включая страницы, которые обманным путём заставляют пользователей
  предоставлять личные данные или загружать нежелательное ПО»** (раздел «обманный сайт»,
  social engineering — дословно тот текст, что владелец видел на телефоне);
* `http://math-kluychiki.ru/` → **«Нет данных»**;
* `http://159.194.254.52/` → **«Нет данных»**;
* `https://math-kluychiki.ru/vhod` → «Нет данных».
Проверено дважды с другими опросами между ними — вердикт по `https://` устойчив.
Остаток пункта C — собрать признаки, по которым эвристика могла сработать, каждый замером
(возраст домена, форма пароля по голому HTTP, заголовки, ответ на чужой `Host`), и назвать
отдельной строкой, что снятие пометки — действие ВЛАДЕЛЬЦА в Search Console, не инструмент.

**D. СТОРОЖ — ЕДИНСТВЕННАЯ НАСТОЯЩАЯ ПРАВКА КОДА В ЭТОМ ЗАХОДЕ.** Сейчас `ops/storozh_sajta.py`
читает ОДИН адрес из `deploy/ADRES.txt` (там до сих пор мёртвый туннель
`https://288c65b4b43b89.lhr.life`) и знает три исхода: жив / мёртв / не смог проверить.
Делаю: два адреса — `http://math-kluychiki.ru/` и `https://math-kluychiki.ru/` — и пятый исход
**«443 отфильтрован»** = HTTP отвечает нашим содержимым, HTTPS не отвечает вовсе. Это ровно та
беда, которая сейчас неотличима от «сайт лёг», а действия у них разные: лёг — поднимать сервер,
отфильтрован — сервер трогать нельзя, идти к провайдеру. Тесты в `tests/ops/` — на оба случая.
🔴 Замечено на входе: `tests/ops/test_storozh_sajta.py::test_zhiv_on_live_port` **уже красный
до меня** (заглушка отдаёт тело `ok` без маркера «Распределение», сторож честно говорит
«мёртв»). Чиню, раз всё равно переписываю этот файл, и называю это в отчёте.

**ЧЕГО НЕ ТРОГАЮ:** nginx на сервере — правки не требуется ни одним из четырёх пунктов;
живой конфиг уже ровно такой, какого требует правка 2 (редиректа нет, HSTS нет,
`X-Forwarded-Proto` проставлен). Копию конфига снимаю всё равно, до всего.


## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> 🔴 **`ДОМ:` — ОБЯЗАТЕЛЬНОЕ ПОЛЕ, И АДРЕС В НЁМ ОБЯЗАН СУЩЕСТВОВАТЬ В МОМЕНТ, КОГДА ТЫ ЕГО ПИШЕШЬ.** Путь, которого нет на диске, — не адрес: такую запись нельзя ни доставить, ни спросить, и она не чинится ничем. Замер 2026-09-06 по 632 файлам `kod_*.md`: 370 пунктов очереди из 1570 родились ровно так — больше, чем всех доставимых (195) вместе взятых. Проверить СВОЙ файл до отчёта — одна команда:
> ```
> python3 _generator/tools/bootstrap_zahod.py --proverit-doma <этот файл>
> ```
> rc=0 — все дома достижимы; rc=1 — назван дом, которого нет (команда печатает какой именно). Тот же разбор гоняет `Г7` приёмки, и у него храповик: у ЭТОГО захода база 0, поэтому первый же недостижимый дом здесь — красный на приёмке, а не запись, которую через неделю никто не найдёт.
> `ДОМ: владелец` — законный адрес и НЕ недостижимый дом: он значит «дома-файла нет вовсе, решение за человеком». Не знаешь пути — пиши его, а не выдуманный путь. Для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит и «доставлено» без метки на месте, и недостижимый дом сверх базы; достижимое-недоставленное печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Снятие пометки Google Safe Browsing — действие ВЛАДЕЛЬЦА, инструментом не делается.
   Порядок: подтвердить владение доменом в Google Search Console (проще всего DNS-записью
   TXT через панель Beget), открыть раздел «Проблемы безопасности» —
   https://search.google.com/search-console/security-issues — и подать заявку на пересмотр.
   Пока это не сделано, пометка не уйдёт, даже если на сервере всё безупречно.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. Сторож `ops/storozh_sajta.py` умеет отличать «сайт лёг» от «443 отфильтрован», но его
   НИКТО НЕ ЗОВЁТ: ни таймера, ни юнита, ни строки в `deploy/ustanovka.sh` — грепом по
   репозиторию единственные вызовы это его собственные тесты. Сторож без зовущего зелен
   ровно потому, что его никто не звал. Нужен `spetsmat-storozh-sajta.service` + `.timer`
   рядом с прочими в `deploy/` — и тогда «HTTPS исчезнет молча» перестанет быть возможным.
   Заводить их в этом заходе я не стал: это установка нового юнита на живой сервер, а
   правка 2 велит трогать ровно четыре пункта.
   ДОМ: deploy/README.md
   ДОСТАВЛЕНО: нет

3. `deploy/vykatka.sh` роняет 5 тестов из 16 в `tests/ops/test_vykatka.py`, и это долг ДО
   меня: те же 5 красных на нетронутом `main` в главной папке. Сухой прогон не доходит до
   шагов «snapshot before the migration» и «environment check», и перезапускается больше
   одного юнита. То есть выкатка не делает того, что о ней написано, — а README прямо
   говорит, что цена лгущего рунбука не в бесполезности, а в том, что ему верят.
   ДОМ: deploy/README.md
   ДОСТАВЛЕНО: нет

4. Порт 8443, по которому в ночь инцидента снимали решающий контроль (тот же сертификат
   отвечал за 0.10 с, пока 443 уходил в таймаут), сейчас НЕ СЛУШАЕТ: блока на 8443 в живом
   nginx нет вовсе. Если фильтрация вернётся, повторить то самое различающее измерение
   будет нечем, и «443 режется на пути» снова станет догадкой вместо факта. Решение —
   владельца: держать ли 8443 постоянно поднятым как диагностический дубль.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

5. Сервер отвечает нашим сайтом на ЛЮБОЙ заголовок `Host` (замер: `Host: evil.example` →
   200), потому что блок голого IP объявлен `default_server` с `server_name _`. Любой
   чужой домен, направленный на 159.194.254.52, покажет наш сайт под своим именем — и
   репутация чужого имени приедет к нам. Для Safe Browsing это ровно тот случай, который
   он и ловит. Чинится одним пустым блоком-заглушкой на 444, но это правка живого nginx —
   не в этом заходе, где nginx трогать было незачем.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет


## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снять ПЕРВЫМ ходом, до всякой работы)*
```
git --no-optional-locks branch --no-merged <основная>     # невлитые
git --no-optional-locks status --porcelain | wc -l        # не закоммичено
git --no-optional-locks log --oneline @{u}.. | wc -l      # не вывезено
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki              # открытые заявки
```
```
$ git --no-optional-locks branch --no-merged main
(пустой вывод)

$ git --no-optional-locks status --porcelain | wc -l
      16

$ git --no-optional-locks log --oneline @{u}.. | wc -l
       0

$ GIT_ZONA_REPO=".../spetsmat-bot" python3 .../git_zona.py zayavki
Открытых заявок: 1

   · 2026-09-07T1934-vygruzka-v-tablicu-07-09-bootstrap  (1 ч, blokiruet, род: pravka-koda, адресат: _generator/tools/bootstrap_zahod.py)
     СОБРАТЬ ЗАХОД vygruzka-v-tablicu, КОГДА ГЕНЕРАТОР СНОВА БУДЕТ СОБИРАТЬ. Аналитик 07.09 трижды пытался собрать заход bootstrap_zahod.py и трижды получил отказ З29: генератор печатает в §0.1 команду git --no-optional-locks branch --no-merged main | grep -c 'zahod/' без снятого числа рядом, а check_zahod.py требует число — генератор и линтер спорят между собой. […] ПРИЧИНА СРОЧНОСТИ: сегодня ВСЕ копии базы лежат на том же сервере, который может умереть.

── ждут захода-разработчика (zhurnal/_INFRA-git/zayavki/na-zahod) ──
   Гит-контуру их брать НЕ НАДО: он их исполнить не вправе.
   (26 заявок, переадресованы ранее)

Охват: заявок открыто 1, переадресовано 26, постоянных исключений 0, сторож краснеет на 0, держателей 0, двойной захват на 0
```

**ЧТО СДЕЛАНО** *(с хэшами)*
- **П0. Хвост Cowork забран** (ветка `main`, главная папка):
  - `c86444a` — `UROKI-FABRIKE.md`, `_INFRA-git/INCIDENTY.md`, файл заявки `2026-09-07T1934-…bootstrap.md`;
  - `5e65038` — перенос заявки в `zayavki/na-zahod/` (автолог очереди).
  - `KARTA.md` Cowork не правил — в `status` его не было. Взяты только индекс и автологи.
- **П1. Очередь закрыта.** Единственная открытая заявка `2026-09-07T1934-vygruzka-v-tablicu-07-09-bootstrap` **переадресована** (`zayavka-peredat`) заходу-разработчику: она просит *собрать заход* (работа аналитика), а адресаты `bootstrap_zahod.py` / `check_zahod.py` живут в чужом репозитории `disciplina`. В переадресацию положен снятый командой факт в помощь аналитику: **невлитых веток сейчас 0**, `dve-stranicy-raspredelenia` уже влита — то есть условие, при котором генератор проходил утром, восстановлено, пересборку захода стоит повторить прежде, чем чинить генератор. Сейчас: «✅ заявок нет».
- **П2. Влитие: нечего.** Собственный снимок субагента подтвердил слова захода — `branch --no-merged main` пуст. Ничего не влито, инициативы не проявлял.
- **П3. Вывоз прошёл.** `vyvezti --yes` — `main` на origin, локально-только коммитов 0. Перед вывозом диф просмотрен на секреты (шаблоны токена Telegram, `sk-`, private key, api_key, password) — совпадений нет; ушли только три `.md`-автолога, 27 строк.
- **П3-остаток, НЕ коммичен — доклад заходу** (13 путей): `M data/spetsmat.db` (база), `M docs/index.html` (витрина), `M zhurnal/2026-09-02_spetsmat-bot/kod_veb-priyom-zadach.md` (файл-заход), `?? doc/PLAN-veb-2026-09.md`, `?? doc/TZ-raspredelenie-dizajn-i-dva-sloya.md`, `?? doc/TZ-sloj-zanyatia.md` (содержательные документы), `?? zhurnal/.DS_Store` (мусор macOS — ему место в `.gitignore`), и шесть файлов-заходов `kod_dozabivka-dolgov-sklejki.md`, `kod_dve-stranicy-raspredelenia.md`, `kod_https-i-domen.md`, `kod_listki-kak-baza-zadach.md`, `kod_skleit-adminku-s-sajtom.md.otmenen`, `kod_sloj-zanyatia.md` — их коммитит аналитик при приёмке. Служебный `.commit-plan`, порождённый вызовом `plan`, снят через `git_zona.py delete`.
- **П4. Погашено: ничего, и гасить нечего.** Ни одна ветка не названа ни заявкой, ни влитием. `doctor` сообщает, что все 58 веток безопасны по содержимому, и тут же: «Ветки НЕ удалять — это решение владельца». Рабочая папка `spetsmat-bot-wt/https-i-domen` и её ветка не тронуты, 50 живых worktree целы.

*(Ответ субагента кончался обязательной строкой охвата: «выдано 5 позиций из 5 найденных».)*

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `да`
*(`нет` законно — но ТОЛЬКО со списком поимённо: что осталось и почему это непроходимо ТВОИМИ
правами (чужая живая рабочая папка, нужно решение владельца, конфликт, обеих сторон которого
не понимаешь). «Сложно» и «не моя тема» причинами не являются. `нет` без списка = красный.)*

## ОТЧЁТ — (заполняет исполнитель)

**ПРАВКИ ПРОЧИТАНЫ: 1, 2.** Работал по редакции ПРАВКИ 2. Редирект HTTP→HTTPS и HSTS не ставил
и не мог поставить: nginx на сервере не трогал вовсе — живой конфиг уже ровно такой, какого
правка требует.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/ops/storozh_sajta.py` — открывать любым редактором; прогонять `python3 ops/storozh_sajta.py` из корня репозитория
**РОД АРТЕФАКТА:** `исходник`
**КОММИТ:** `61be90d` — «the watchdog watches both schemes, and «сайт лёг» stops looking like «443 отфильтрован»» и `eedf1b2` — «a certificate we cannot trust proves 443 ARRIVES, so it stops being called filtering» · `git_zona.py check --zone deploy/` → ✅ · `--zone ops/storozh_sajta.py` → ✅ · `--zone tests/ops/` → ✅

**ОХВАТ: проверено 6 из 6 клауз; проб 443 — 36, из них таймаутов 0.**

---

### A. ЗАМЕР НАДЁЖНОСТИ 443 — ГЛАВНЫЙ ПРОДУКТ

**Откуда мерил:** macOS на домашней машине владельца, то есть из той самой сети, из которой
владелец ходит на сайт. **Не с сервера** — изнутри дата-центра 443 работал и в ночь падения,
и замер оттуда ничего бы не значил.
**Чем:** `curl -sS -o /dev/null -w '%{http_code} %{time_total} %{ssl_verify_result}' --max-time 5`,
36 проб с интервалом 120 с. Каждая проба — три адреса: 443, контроль 80, контроль 8443.

```
  n | время MSK |  443  время   | :80  время   | :8443
 ---+-----------+---------------+--------------+-------
   1 | 20:45:19  |  200   0.217   |  200  0.150  |  000
   2 | 20:47:24  |  200   0.183   |  200  0.147  |  000
   3 | 20:49:30  |  200   0.155   |  200  0.139  |  000
   4 | 20:51:35  |  200   0.236   |  200  0.150  |  000
   5 | 20:53:41  |  200   0.174   |  200  0.144  |  000
   6 | 20:55:46  |  200   0.177   |  200  0.144  |  000
   7 | 20:57:51  |  200   0.218   |  200  0.138  |  000
   8 | 20:59:57  |  200   0.154   |  200  0.148  |  000
   9 | 21:02:02  |  200   0.188   |  200  0.178  |  000
  10 | 21:04:08  |  200   0.183   |  200  0.150  |  000
  11 | 21:06:13  |  200   0.174   |  200  0.144  |  000
  12 | 21:08:19  |  200   0.156   |  200  0.162  |  000
  13 | 21:10:24  |  200   0.268   |  200  0.146  |  000
  14 | 21:12:30  |  200   0.237   |  200  0.132  |  000
  15 | 21:14:35  |  200   0.190   |  200  0.139  |  000
  16 | 21:16:41  |  200   0.142   |  200  0.112  |  000
  17 | 21:18:46  |  200   0.145   |  200  0.146  |  000
  18 | 21:20:51  |  200   0.166   |  200  0.148  |  000
  19 | 21:22:57  |  200   0.184   |  200  0.162  |  000
  20 | 21:25:02  |  200   0.177   |  200  0.142  |  000
  21 | 21:27:08  |  200   0.161   |  200  0.145  |  000
  22 | 21:29:13  |  200   0.157   |  200  0.153  |  000
  23 | 21:31:19  |  200   0.154   |  200  0.143  |  000
  24 | 21:33:24  |  200   0.171   |  200  0.153  |  000
  25 | 21:35:29  |  200   0.219   |  200  0.156  |  000
  26 | 21:37:35  |  200   0.229   |  200  0.137  |  000
  27 | 21:39:40  |  200   0.169   |  200  0.149  |  000
  28 | 21:41:46  |  200   0.158   |  200  0.140  |  000
  29 | 21:43:51  |  200   0.180   |  200  0.148  |  000
  30 | 21:45:57  |  200   0.150   |  200  0.145  |  000
  31 | 21:48:02  |  200   0.243   |  200  0.164  |  000
  32 | 21:50:08  |  200   0.166   |  200  0.134  |  000
  33 | 21:52:13  |  200   0.213   |  200  0.143  |  000
  34 | 21:54:19  |  200   0.164   |  200  0.151  |  000
  35 | 21:56:24  |  200   0.182   |  200  0.147  |  000
  36 | 21:58:30  |  200   0.187   |  200  0.148  |  000

ПРОБ ВСЕГО: 36 · успехов по 443: 36 · неудач по 443: 0
время ответа 443, с: мин 0.142 · медиана 0.177 · макс 0.268
охват по часам: с 20:45:19 до 21:58:30 MSK
```

Колонка `:8443` — сплошной `000`, и это НЕ поломка замера: блока на 8443 в живом nginx нет
вовсе, порт не слушает. То есть контрольный порт, по которому в ночь инцидента снималось
решающее различение (тот же сертификат отвечал за 0.10 с, пока 443 уходил в таймаут), сегодня
поднять нечем. Вынес это пунктом очереди 4.
`ssl_verify_result` во всех 36 пробах — `0`: сертификат проходит проверку каждый раз.

**ВЕРДИКТ ПО 443 ОДНОЙ СТРОКОЙ: «стабилен В ОКНЕ ЗАМЕРА» — 36 успехов из 36, таймаутов 0,
окно 73 минуты (20:45–21:58 MSK 07.09), медиана 0.177 с, разброс 0.142–0.268 с.**

🔴 **И тут же — граница этого вердикта, потому что без неё он опасен.** «Стабилен в окне»
НЕ означает «стабилен». Ночная запись говорит, что фильтр держится периодами длиннее часа:
замер 1 дал 30 из 30, замер 2 через час — 3 таймаута из 3. Мой час зелени неотличим от их
первого замера, который тоже был зелёным целиком. Одно окно из одной сети не доказывает
отсутствия перемежающейся фильтрации — оно доказывает только, что **сейчас, отсюда, фильтра
нет**. Ровно поэтому продуктом захода и стал сторож, а не разрешение включить редирект: чтобы
следующее возвращение фильтра было замечено машиной, а не владельцем на телефоне.
И остаётся неотвеченным то, что не смог узнать никто: **что именно разблокировало 443 06.09** —
служба Beget, параллельная сессия или само по себе. Я этого не установил и себе не приписываю.

### B. ССЫЛКИ ПО СХЕМЕ ЗАПРОСА — ПРАВКИ НЕ ПОТРЕБОВАЛОСЬ, И ЭТО ДОКАЗАНО ЗАМЕРОМ

Условие правки 2 («если `dve-stranicy-raspredelenia` не влит — не трогай») снято: ветка **влита**,
`git branch --no-merged main` пуст. То есть `veb/` мне был открыт — и я всё равно ничего там не
менял, потому что чинить оказалось нечего:

* `curl -s https://math-kluychiki.ru/ | grep -c 'http://math-kluychiki'` → **0**; то же на
  `/vhod` и `/raspredelenie` → **0** (страницы непустые: 140251 / 1010 / 7987 байт);
* абсолютных `href|src|action` со схемой в HTML нет вообще ни одного — вся навигация
  относительная и потому наследует схему запроса по построению;
* все `Location` относительные — `veb/server.py:617,649,862,1025,1030`; абсолютный адрес в
  выводе `curl` достраивает сам curl, сервер его не отдаёт;
* форма входа — `action="/vhod"`;
* `X-Forwarded-Proto` nginx проставляет в обоих `proxy_pass`, и приложение его УЖЕ читает
  (`veb/server.py:1046`): флаг `Secure` на куке ставится по схеме запроса, а не константой.

Пункт закрыт до меня заходом `dve-stranicy-raspredelenia`. 🔴 Оговорка верификатора, и она
справедлива: клауза «grep → 0» проходит **тривиально** — она вернула бы 0 и на сломанном сайте,
потому что измеряет отсутствие, а не наличие правильного. Существо достигнуто, но проверка в
критерии была нестрогой.

### C. ПРИЧИНА ПОМЕТКИ «ОПАСНЫЙ САЙТ» — НАЗВАНА ЗАМЕРОМ

Опросил Google Safe Browsing Transparency Report **поадресно**, из браузера, 07.09 ~20:50, и
именно поадресность оказалась ценной:

| адрес | статус |
|---|---|
| `https://math-kluychiki.ru/` | 🔴 «Некоторые страницы этого сайта небезопасны… содержит вредоносный контент, включая страницы, которые **обманным путём заставляют пользователей предоставлять личные данные или загружать нежелательное ПО**» |
| `http://math-kluychiki.ru/` | «Нет данных» |
| `http://159.194.254.52/` | «Нет данных» |
| `https://math-kluychiki.ru/vhod` | «Нет данных» |

Текст дословно совпадает с тем, что владелец видел на телефоне. Проверено дважды, с другими
опросами между ними — вердикт по `https://` устойчив. **Помечен HTTPS-ориджин, и только он.**

**ЧТО ЭТО ЗНАЧИТ.** Категория — «обманный сайт» (social engineering), не «вредоносное ПО» и не
«взлом». Признаки, каждый снят командой:

* домен **зарегистрирован 2026-09-06T17:19:02Z** — сутки назад; `state: REGISTERED, DELEGATED,
  **UNVERIFIED**`, `person: Private Person`, регистратор BEGET-RU (`whois -h whois.tcinet.ru`);
* сертификат выпущен **06.09 19:27:04 GMT** — через 2 ч 08 мин после регистрации домена
  (`openssl s_client`), годен до 05.12.2026;
* **форма ввода пароля отдаётся по голому HTTP** — и по домену, и по IP: `curl http://math-kluychiki.ru/vhod`
  → 200, `grep -c 'type="password"'` → 1. Это и есть буквальное «страница просит личные данные
  по незащищённому соединению»;
* **заголовков безопасности ноль** — ни `X-Content-Type-Options`, ни `CSP`, ни `Referrer-Policy`,
  ни `X-Frame-Options` (снято через `curl -D -` GET-ом: сайт не поддерживает HEAD, отдаёт 501,
  поэтому `curl -I` здесь врёт);
* нет `robots.txt`, нет favicon, нет `security.txt` — все три 404;
* **сервер отвечает нашим сайтом на ЛЮБОЙ `Host`**: `curl -H 'Host: evil.example' http://159.194.254.52/`
  → 200, потому что блок голого IP объявлен `default_server` с `server_name _`. Любой чужой
  домен, направленный на этот IP, покажет наш сайт под своим именем.

**ЧЕГО Я НЕ УСТАНОВИЛ, И ГОВОРЮ ЭТО ПРЯМО.** Какой ИМЕННО признак перевесил — Google не
раскрывает, а Search Console мне недоступна. Гипотезу «нашли через журналы прозрачности
сертификатов» проверить не удалось: `crt.sh` по этому домену отдаёт пустой список (`[]`,
код 200), записей нет. Так что перечисленное — это профиль, объясняющий срабатывание
эвристики, а не доказанная причина. **Точно установлено другое, и оно важнее гипотезы:
помечен именно новорождённый HTTPS-ориджин, а не содержимое сервера** — по HTTP и по IP то же
самое содержимое пометки не имеет. Чужого содержимого на сервере нет: каталоги не листаются
(403/404), PDF отдаётся настоящим PDF (`%PDF-1.5`, 86060 байт, `application/pdf`).

🔴 **ЧТО ТРЕБУЕТСЯ ОТ ВЛАДЕЛЬЦА — ИНСТРУМЕНТОМ ЭТО НЕ ДЕЛАЕТСЯ.** Снятие пометки — заявка на
пересмотр: подтвердить владение доменом в Google Search Console (проще всего TXT-записью через
панель Beget), затем раздел **«Проблемы безопасности» — https://search.google.com/search-console/security-issues**
— и «Запросить проверку». Ответ обычно за сутки-двое. Пока это не сделано, пометка не уйдёт,
даже если на сервере всё безупречно, — а на сервере, как показано выше, ещё есть что подтянуть.

### D. СТОРОЖ — ЕДИНСТВЕННАЯ ПРАВКА КОДА В ЭТОМ ЗАХОДЕ

Было: один адрес из `deploy/ADRES.txt` (там до сих пор мёртвый туннель `288c65b4b43b89.lhr.life`),
три исхода. Стало: два адреса, `http://` и `https://`, и **шесть** исходов, каждый со своим
ДЕЙСТВИЕМ — сторож, называющий беду и не называющий действия, заставляет читателя гадать в два
часа ночи.

| исход | признак | действие |
|---|---|---|
| `жив` | оба несут наше содержимое | ничего |
| `443 отфильтрован` | http несёт наше, https **молчит на уровне сети** | 🔴 СЕРВЕР НЕ ТРОГАТЬ, вопрос к провайдеру |
| `сертификат не проходит проверку` | TLS дошёл, сертификату верить нельзя | `certbot renew` + reload — это СЕРВЕР |
| `http лёг` | https жив, http молчит | смотреть блок `listen 80` |
| `мёртв` | молчат оба, либо чужая страница | поднимать сайт |
| `не смог проверить` | адреса нет на диске | чинить самого сторожа |

**ГРАНИЦА ФИЛЬТРАЦИИ УЗКАЯ НАРОЧНО.** Требуется, чтобы http нёс НАШЕ содержимое, а https не дал
**сетевого** ответа вовсе. `502` по https означает, что 443 доходит и сломалось что-то за ним, —
это `мёртв`. Стоит записать в «отфильтрован» любую неудачу https, и слово перестанет значить
что-либо.

**ВЫВОД СТОРОЖА В ОБОИХ СЛУЧАЯХ (клауза 5).** Живой сайт:
```
адрес: math-kluychiki.ru
  http      PASS  жив: status=200 content_marker_found
  https     PASS  жив: status=200 content_marker_found
verdict: ЖИВ -- first run (recorded, no alarm) -- passed 2 of 2 checks
action: ничего не делать
```
Подделанная недоступность 443 (на порту обычный HTTP-сервер с нашим маркером, TLS обрывается):
```
  http      PASS  жив: status=200 content_marker_found
  https     RED   молчит на уровне сети: [SSL: WRONG_VERSION_NUMBER] wrong version number
verdict: 443 ОТФИЛЬТРОВАН -- жив -> 443 отфильтрован -- passed 1 of 2 checks
action: СЕРВЕР НЕ ТРОГАТЬ — он отвечает по http. Фильтрация на пути: вопрос к провайдеру
ALARM: жив -> 443 отфильтрован
```
Контроль, «сайт лёг» — молчат оба:
```
  http      RED   молчит на уровне сети: [Errno 61] Connection refused
  https     RED   молчит на уровне сети: [Errno 61] Connection refused
verdict: МЁРТВ -- 443 отфильтрован -> мёртв -- passed 0 of 2 checks
action: поднимать сайт: deploy/podnyat_sajt.sh, затем systemctl status spetsmat-veb
```
Тесты: **14 зелёных**, дважды подряд (`tests/ops/test_storozh_sajta.py`).

**ДВЕ ЖИВЫЕ ОШИБКИ, НАЙДЕННЫЕ ПО ХОДУ И ПОЧИНЕННЫЕ.**

1. **Ложное красное у самого сторожа.** Маркер содержимого сверялся регистрозависимо и только
   по первым 512 байтам, а живая страница озаглавлена «… — распределение» **строчными**. Сторож
   объявлял «МЁРТВ» совершенно живой сайт, отдающий 200. Тест `test_zhiv_on_live_port` был
   **красным до меня** по этой же причине — то есть дефект был зафиксирован тестом и не
   разобран. Сверка стала регистронезависимой по 64 КБ тела. *Ложная тревога, неотличимая от
   настоящей, хуже отсутствия сторожа: читатель учится ему не верить.*
2. 🔴 **Дыра, найденную которую я обязан назвать честно: её нашёл ВЕРИФИКАТОР, а не я.** Провал
   проверки сертификата ловился тем же `except ssl.SSLError`, что и таймауты, и объявлялся
   молчанием сети — то есть при живом http давал вердикт «443 отфильтрован» с действием «СЕРВЕР
   НЕ ТРОГАТЬ, вопрос к провайдеру». Самая вредная фраза ровно в том случае, когда чинить надо
   именно сервер. Верификатор проверил это не рассуждением, а на живом `expired.badssl.com`.
   **Ошибка по существу:** рукопожатие, дошедшее до предъявления сертификата, ДОКАЗЫВАЕТ, что
   443 доходит; фильтрация на пути до этого места не пускает. Добавлен шестой исход, стоит ДО
   ветки фильтрации, несёт себя отдельным полем (разбор текста ошибки OpenSSL сделал бы диагноз
   заложником формулировок). Проверено на том же живом эталоне. Текущий сертификат годен до
   **05.12.2026** — случай наступил бы сам, промахнись автопродление.
3. Попутно: порты в тестах были зашиты числами, и один тест покраснел от **соседнего прогона**,
   а не от кода (`Address already in use`). Порт теперь выдаёт ядро.

### ЧТО НЕ ТРОГАЛ И ПОЧЕМУ

* **nginx на сервере — ни одной правки.** Живой конфиг уже ровно такой, какого требует правка 2:
  редиректа нет (и в нём стоит развёрнутое объяснение, почему), HSTS нет, `X-Forwarded-Proto`
  проставлен в обоих `proxy_pass`. Порядок безопасности всё равно соблюдён: копии
  `sites-enabled/spetsmat` и `snippets/spetsmat-obshchee.conf` сняты на сервере ДО всего
  (`.kopia-do-https-i-domen`), `nginx -t` зелёный, `reload` не звался ни разу — потому что
  менять было нечего.
* **`veb/` — ни одной правки** (см. B: чинить нечего).
* **`deploy/vykatka.sh`** — 5 красных тестов, унаследованный долг; проверил, что те же 5 красных
  на нетронутом `main` в главной папке. Не мой заход, пункт очереди 3.
* **Заголовки безопасности и заглушка на чужой `Host`** — настоящая и безопасная работа, но это
  правка живого nginx, а правка 2 очертила ровно четыре пункта. Пункты очереди 5.

### ВЕРИФИКАТОР (ПОСЛЕ-типа, свежий субагент, другим методом — отдача по сети, не конфиг)

Сплошная выборка 4 адреса из 4 + четыре клаузы критерия + прогон сторожа и тестов.
**Общий вердикт: ЗЕЛЁНЫЙ — все 10 позиций сошлись с ожиданием.** `https://` → 200, `http://` →
**200, не 301 и не 302**, IP → 200, старый путь листка → 200 и настоящий PDF; `strict-transport`
→ **0**; `systemctl is-active spetsmat-veb spetsmat-bot` → `active` / `active`. Две оговорки без
снижения, обе перенесены выше по тексту: клауза 8 проходит тривиально, и сторож принимал
истёкший сертификат за фильтрацию (**починено после его доклада**, коммит `eedf1b2`).
Финальная строка получена: «выдано 10 позиций из 10 найденных».

### НЕОБРАТИМОЕ

**ОДНО НЕОБРАТИМОЕ ДЕЙСТВИЕ, И ВОТ ОНО.**
* **Удалена рабочая папка захода** `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/https-i-domen`
  вместе с личным `scratchpad/https-i-domen/` (6 файлов) — `worktree drop --force`. Инструмент
  сначала отказался («в папке 6 путей вне git — НЕ удаляю, работа дороже порядка») и был прав;
  я посмотрел, что именно там лежит, и **сохранил всё ценное ДО удаления**.
  **Чем восстанавливается:** код — из `main` (коммиты `61be90d`, `eedf1b2`, слияние `a852b8e`,
  ветка `https-i-domen` жива); сырьё и инструменты замера — из
  **`/Users/ivanyakovlev/Downloads/zamer-443-2026-09-07/`** (там `zamer_443.sh`, `razbor_zamera.py`,
  `zamer_443.csv`, `gitkontur.md` и `ЧТО-ЭТО.md` с инструкцией перезапуска), копия csv также в
  `/tmp/zamer_443_sohranyon.csv`; таблица замера целиком — в этом отчёте выше.
  🔴 `zamer_443.sh` — **это то, что надо перезапустить, если HTTPS снова начнёт пропадать.**

Кроме этого — необратимого нет: ни одного удаления, перезаписи чужого, переименования, перемещения,
`reset` или `checkout` поверх несохранённого. `ops/storozh_sajta.py` и
`tests/ops/test_storozh_sajta.py` переписаны — но их прежнее содержимое лежит в git и
восстанавливается из `f9e3cb0` (коммит-родитель `61be90d`). На сервере не записано НИЧЕГО, кроме
двух копий конфига рядом с оригиналами (`/etc/nginx/sites-enabled/.spetsmat.kopia-do-https-i-domen`,
`/etc/nginx/snippets/.spetsmat-obshchee.conf.kopia-do-https-i-domen`); сами конфиги не тронуты,
`reload` не звался.

### ПОВТОРЯЕМОСТЬ НАХОДОК

**Повторится на следующей единице работы — значит заход ДО следующего прогона, а не запись:**
* **Урок 1 (правка выдана раньше, чем подняты записи об инциденте на том же участке)** —
  повторится на каждой следующей правке, трогающей территорию с инцидентом, и цена каждого
  повторения — потенциально лежащий сайт. Это класс НЕМЕДЛЕННОЕ.
* **Сторож, которого никто не зовёт** (пункт 3 очереди) — повторится буквально: пока нет таймера,
  каждое следующее исчезновение HTTPS будет замечено владельцем на телефоне, а не машиной.
  Это единственное, что отделяет сегодняшнюю работу от «написали и положили в ящик».

**Не повторится, законно уходит записью:** ложное красное по регистру маркера (починено),
дыра с сертификатом (починена), зашитые порты в тестах (починены), долг `vykatka.sh`
(не растёт сам по себе).

### ВРЕМЯ И ТОКЕНЫ

**НЕПРИМЕНИМО на канале `app`** — снимать неоткуда: лог прогона существует только на канале
`terminal`, где его кладёт `tee`. Это свойство канала, а не недосмотр.

### ГИТ-ГИГИЕНА — ЧИСЛАМИ, КОМАНДАМИ (Г1–Г6)

* **Г1. Зона доехала в git:** `deploy/` → ✅ · `ops/storozh_sajta.py` → ✅ · `tests/ops/` → ✅.
* **Г2. Второй репозиторий:** **неприменимо** — все пути зоны внутри `spetsmat-bot`; зона за его
  пределы не выходила.
* **Г3. Невлитых веток:** на входе **0**, на выходе **1** — собственная `https-i-domen`, которую
  я и вливаю последним ходом. Чужих не прибавилось.
* **Г4. Новый инструмент:** ни одного нового `.py` в `_generator/**` не заводил — **неприменимо**.
* **Г5. Новый `.md`:** ни одного не заводил — **неприменимо**. Заведён один не-`.md` файл,
  `deploy/ADRES-SAJTA.txt`, регистрации не требующий.
* **Г6. Чужих путей в коммитах нет:** `git show --stat` по обоим коммитам — только
  `deploy/ADRES-SAJTA.txt`, `ops/storozh_sajta.py`, `tests/ops/test_storozh_sajta.py`.
* **Вне git:** только `scratchpad/https-i-domen/` — личный черновик захода, вне зоны, не
  коммитится нарочно. Сырьё замера (`zamer_443.csv`) живёт там и умрёт вместе с рабочей папкой;
  таблица целиком напечатана выше, в этом отчёте, — это её постоянный дом.

### ПОСЛЕДНИЙ ХОД — ЧИСЛА КОМАНДАМИ (WARNING-блок, шаги 1–6)

**1 · Вне git.** Своя зона — **0**: `git status --porcelain -- deploy/ ops/storozh_sajta.py tests/ops/`
пуст. В главной папке **14** путей вне git, и все чужие: `data/spetsmat.db`, `docs/index.html`,
`zhurnal/_INFRA-git/INCIDENTY.md` (автолог), `doc/PLAN-veb-2026-09.md`,
`doc/TZ-raspredelenie-dizajn-i-dva-sloya.md`, `doc/TZ-sloj-zanyatia.md`, `zhurnal/.DS_Store`
(мусор macOS) и семь файлов-заходов, включая мой собственный `kod_https-i-domen.md` — его по
контракту коммитит аналитик при приёмке, не я. Чужую содержательную работу не трогал.
В репозитории `disciplina` — 8 путей вне git, ни один не мой: я оттуда только звал инструменты.

**2 · Влитие своей ветки.** `git_zona.py vlit-v-osnovnuyu https-i-domen` → **✅ без конфликтов,
merge-коммит `a852b8e`**. Приехало 2 коммита, 3 пути. Грязные пути главной папки в merge не
поехали (индекс не тронут).

**3 · ПОСТ-ПРОВЕРКА ИЗ ГЛАВНОЙ ПАПКИ — ЗЕЛЁНАЯ.** Прогон механизма из
`/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot`, не из рабочей:
```
адрес: math-kluychiki.ru
  http      PASS  жив: status=200 content_marker_found
  https     PASS  жив: status=200 content_marker_found
verdict: ЖИВ -- passed 2 of 2 checks
action: ничего не делать
rc=0
python3 -m pytest tests/ops/test_storozh_sajta.py -q  →  14 passed
```
Механизм ВСТАЛ, а не «коммит виден»: грепом по живому файлу в главной папке —
`ops/storozh_sajta.py:79 SERTIFIKAT`, `:111 sertifikat_krasnyj`, `:220-221` ветка диагноза.
🔴 **Но грепа по живой ТОЧКЕ ВЫЗОВА нет, и это не оговорка, а находка:** сторожа не зовёт
никто — ни юнит, ни таймер, ни строка в `ustanovka.sh`. Пункт очереди 2. Инструмент без
точки вызова зелен ровно потому, что его никто не звал.

**4 · Гашение.** `git branch --no-merged main` → **0**. Рабочая папка снята (см. НЕОБРАТИМОЕ).
Ветка `https-i-domen` оставлена жить: она влита, а удаление веток — решение владельца, так
прямо и говорит `git_zona.py poteri` («потерь нет, проверено 1 из 1»).

**5 · Вывоз.** `vyvezti --yes` → ✅, `main` ушёл на origin (3 коммита). Невывезенных в `main`
теперь **0**. У ветки `https-i-domen` upstream не заводился, и это не потеря: её содержимое
целиком внутри `origin/main` через слияние `a852b8e` — проверено
`git branch -r --contains eedf1b2` → `origin/main`. Работы, существующей только на этом диске,
не осталось.

**6 · Сводка числами:** вне git своей зоны — **0** · невлитых — **0** · невывезенных
в `main` — **0** · пост-проверка — **зелёная, откатывать нечего**.

### ОТКРЫТОЕ «ВОЗВРАЩАТЬСЯ»

Пять пунктов очереди в `## ВОПРОСЫ`, все дома достижимы (`--proverit-doma` → rc=0, недостижимых 0).
Самые весомые: **заявка на пересмотр в Search Console** (только владелец) и **юнит+таймер для
сторожа** — без него вся сегодняшняя работа лежит в ящике.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/ops/storozh_sajta.py` — открывать любым редактором; прогонять `python3 ops/storozh_sajta.py` из корня репозитория
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**РОД АРТЕФАКТА:** `исходник`
*(`собранный` — колода, PDF, картинка, любой файл, ПОРОЖДЁННЫЙ этим заходом: он обязан быть моложе файла-захода, и Г3 приёмки сверяет ВРЕМЯ. `исходник` — заход, чей продукт есть КОД: он коммитится РАНЬШЕ отчёта, потому что отчёт цитирует хэш коммита, и сверка по времени дала бы вечное ложное красное — тогда Г3 сверяет не время, а «доехал ли артефакт в названный §4 коммит». Не заполнено — Г3 работает по времени, как раньше.)*
**КОММИТ:** `61be90d` — «the watchdog watches both schemes, and «сайт лёг» stops looking like «443 отфильтрован»»; `eedf1b2` — «a certificate we cannot trust proves 443 ARRIVES, so it stops being called filtering» · `git_zona.py check --zone deploy/` → ✅ · `--zone ops/storozh_sajta.py` → ✅ · `--zone tests/ops/` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*

## ПРАВКИ ПОСЛЕ ВЫДАЧИ — (заполняет АНАЛИТИК; исполнитель ЧИТАЕТ)

### ПРАВКА 1 · 2026-09-07 19:20 · ЗАДАЧА ДРУГАЯ: СЕРТИФИКАТ УЖЕ ЕСТЬ, А САЙТ ПОМЕЧЕН ОПАСНЫМ

🔴 **Всё, что выше про «поставить сертификат», ОТМЕНЕНО.** Он уже стоит: Let's Encrypt,
`CN=math-kluychiki.ru`, выпущен 06.09 19:27 GMT, годен до 05.12.2026; `https://math-kluychiki.ru/`
отдаёт 200. Измерено дважды — утренней сессией в 13:05 и аналитиком в 13:47.

**НАСТОЯЩАЯ БОЛЕЗНЬ.** Владелец 07.09 показал телефон: Chrome пишет **«Опасный сайт. На этом
сайте злоумышленники могут обманом заставить вас установить ПО или раскрыть личную информацию,
например пароль»**. Это НЕ предупреждение о сертификате — это дословный текст **Google Safe
Browsing**, раздел «обманный сайт». Сертификат тут ни при чём, и заменой сертификата это не
лечится.

**ЧТО ИЗВЕСТНО ТОЧНО, А ЧТО НЕТ.** Точно: HTTP не переадресуется на HTTPS (утренняя сессия:
«HTTP по-прежнему отдаёт страницу сам, без редиректа»), и страница, открытая ПО HTTPS, несёт
внутри ссылки `http://math-kluychiki.ru/vhod` — аналитик видел это в отдаче живого сайта.
То есть форма ввода пароля доступна по голому HTTP на новом домене — типовой повод для
такой пометки. **Не точно: почему именно Safe Browsing сработал.** Причину назови ЗАМЕРОМ,
а не догадкой, и если замера нет — скажи «не удалось установить», это законный исход.

**ЗАДАЧА — ЧЕТЫРЕ ПУНКТА.**

**1. HTTP → HTTPS, 301, с сохранением пути.** `http://159.194.254.52/` не убивать: по нему
розданы ссылки, он обязан переадресовывать на домен, а не отдавать ошибку.

**2. Внутренние ссылки — по той же схеме, по которой пришёл посетитель.** Сейчас страница,
отданная по HTTPS, зовёт обратно на HTTP; браузер видит форму пароля на HTTP. Схему брать
из запроса (`X-Forwarded-Proto` за nginx), а не зашивать константой.

**3. HSTS — только ПОСЛЕ того, как первые два пункта проверены живьём, и с маленьким
`max-age` сначала.** 🔴 HSTS необратим на срок своего действия: ошибёшься — сайт станет
недоступен у всех, кто успел его открыть, и откатить это с сервера нельзя. Начни с часа,
не с года; `includeSubDomains` и `preload` НЕ ставить.

**4. Сторож `ops/storozh_sajta.py` — на `https://math-kluychiki.ru/`.**

**ЧТО ДЕЛАЕТ ВЛАДЕЛЕЦ, А НЕ ТЫ.** Снятие пометки Safe Browsing — заявка на пересмотр в
Google Search Console после подтверждения владения доменом. Инструментом это не делается,
и без неё пометка не уйдёт, даже когда всё выше починено. Напиши в `## ОТЧЁТ` отдельной
строкой, что от владельца требуется, и приложи ссылку на раздел «Проблемы безопасности».

🔴 **НОЧЬЮ САЙТ УЖЕ ПАДАЛ НА ЭТОЙ ТЕРРИТОРИИ И НЕ ПОДНИМАЛСЯ НЕСКОЛЬКО ПОПЫТОК.** Отсюда
порядок, от которого не отступать: `nginx -t` перед КАЖДЫМ `reload` · копия рабочего конфига
рядом ДО первой правки · после каждого шага `curl` по домену и по IP · сайт не ответил —
возвращаешь конфиг из копии немедленно, а разбираешься потом. Сайт важнее фичи; это правило
владельца от 07.09.

**КРИТЕРИЙ — ЗАМЕНЯЕТ ТОТ, ЧТО ВЫШЕ:**
1. `curl -s -o /dev/null -w '%{http_code} %{redirect_url}\n' http://math-kluychiki.ru/vhod` → `301 https://math-kluychiki.ru/vhod`.
2. `curl -s https://math-kluychiki.ru/ | grep -c 'http://math-kluychiki'` → **0**: ни одной ссылки обратно на HTTP.
3. `curl -s -o /dev/null -w '%{http_code}\n' http://159.194.254.52/` → `200` или `301`, не `4xx/5xx`.
4. `curl -sI https://math-kluychiki.ru/ | grep -i strict-transport` → есть, `max-age` маленький, без `preload`.
5. `ssh … 'systemctl is-active spetsmat-veb spetsmat-bot'` → оба `active`.
6. Причина пометки Safe Browsing — названа замером либо честно «не установлено, чего не хватило».

**ОХВАТ:** «проверено 6 из 6 клауз».

---

### ПРАВКА 2 · 2026-09-07 20:15 · ОТМЕНЯЮ ПОЛОВИНУ ПРАВКИ 1: РЕДИРЕКТ И HSTS ЗАПРЕЩЕНЫ

Правка 1 велела поставить `301` и HSTS. **Это ровно то, чем ночью положили сайт.** Аналитик
написал её, не подняв ночные записи; поднял через час, субагентом, и вот что там стоит
дословно — читать как приговор пунктам 1 и 3 правки 1:

> «🔴 РЕДИРЕКТ НА HTTPS ПОЛОЖИЛ САЙТ ЦЕЛИКОМ. `certbot` по умолчанию ставит 301 HTTP→HTTPS.
> TLS до сети владельца тогда не доходил — и сайт исчез полностью, а 301 браузеры кэшируют.
> Снято, позже возвращено как 302, снято окончательно. **HSTS не ставился ни разу — это
> единственное, что спасло от необратимости.**»
> — `disciplina/_studio/zhurnal/2026-09-06_pervyj-server/OTCHET-2026-09-07-sajt-dizajn-i-zaprety.md:222`

> «❌ Не ставить редирект с HTTP на HTTPS. Один раз это уже положило сайт целиком. HTTPS
> сейчас работает, но HTTP обязан продолжать отдавать страницу сам, без 301 и 302.»
> — там же, `:74`. И решение владельца, помеченное «не открывать заново»: «HSTS не включать.
> Редирект на https снят.» — `OCHERED-veb.md:204`.

**ПОЧЕМУ ЭТО НЕ «ПРОСТО НЕУДАЧНАЯ НОЧЬ». Порт 443 режется НА ПУТИ, перемежающимся образом.**
Замеры оттуда же: «замер 1 дал 30 из 30 успешных, замер 2 через час — 3 таймаута из 3»
(`OCHERED-veb.md:44`); порт 8443 с тем же сертификатом отвечал за 0.10 с, а 443 уходил в
таймаут; TCP соединялся; изнутри дата-центра 443 работал; SNI исключён тремя пробами
(`OTCHET-…:157`). То есть фильтрация СНАРУЖИ сервера, и она приходит и уходит. Редирект на
HTTPS в такой сети означает: в час, когда фильтр вернулся, сайта нет ни у кого — а `301`
браузер запомнит и после снятия фильтра.

**И ГЛАВНОЕ, ЧТО НИКТО НЕ ЗНАЕТ:** «Что именно разблокировало 443 — я не знаю и не приписываю
себе: могла ответить служба Beget, могла отработать параллельная сессия. Проверить это первым
делом, потому что если фильтр вернётся, сайт по HTTPS исчезнет молча» (`OTCHET-…:163`).

## ЧТО ТЕПЕРЬ ЗАДАЧА

**🔴 ЗАПРЕЩЕНО, И ЭТО НЕ ОБСУЖДАЕТСЯ В ЭТОМ ЗАХОДЕ:** любой редирект HTTP→HTTPS (`301` и `302`
одинаково) · `Strict-Transport-Security` в любом виде · выключение или ослабление HTTP.
Хочешь доказать, что теперь можно, — это отдельная развилка в `## ВОПРОСЫ` с замером, а не
действие.

**A. ЗАМЕР НАДЁЖНОСТИ 443 — ГЛАВНЫЙ ПРОДУКТ ЗАХОДА.** Не одна проба, а серия: не меньше
30 проб с интервалом, растянутая не меньше чем на час, `curl -sS -o /dev/null -w '%{http_code}
%{time_total} %{ssl_verify_result}\n' --max-time 5 https://math-kluychiki.ru/`. Печатать
таблицу: сколько успехов, сколько таймаутов, в какие минуты. Для контроля — тот же замер по
`http://` и по `https://` с портом 8443, если он ещё слушает. 🔴 Мерить с сервера НЕЛЬЗЯ:
изнутри дата-центра 443 работал и тогда. Меряй оттуда, откуда ходит владелец, — то есть
из своей среды, и назови, откуда именно.

**B. ССЫЛКИ — ПО СХЕМЕ ЗАПРОСА.** Страница, отданная по HTTPS, сейчас зовёт обратно на
`http://math-kluychiki.ru/vhod`. Это не редирект и не ослабление HTTP: пришёл по HTTPS —
остаёшься на HTTPS, пришёл по HTTP — всё как было. Схему брать из `X-Forwarded-Proto`.
⚠ Файлы `veb/` — ЧУЖАЯ ЗОНА: там работает заход `dve-stranicy-raspredelenia`. Если он к
твоему приходу не влит — **правку ссылок НЕ делай**, опиши её в `## ОТЧЁТ` одним абзацем
и отдай следующему. Проверить: `git --no-optional-locks branch --no-merged main`.

**C. ПРИЧИНА ПОМЕТКИ «ОПАСНЫЙ САЙТ» — НАЗВАТЬ ЗАМЕРОМ.** Что известно: текст на телефоне
владельца дословно совпадает с Google Safe Browsing, раздел «обманный сайт». Чего НЕ известно:
за что именно. Проверяемые следы: отдаётся ли форма пароля по HTTP (да, отдаётся), какие
заголовки безопасности есть, нет ли на сервере чужого содержимого. **Причина, названная без
замера, — брак; «не установил, не хватило доступа к Search Console» — законный исход.**

**D. СТОРОЖ.** `ops/storozh_sajta.py` сейчас стережёт один адрес. Научи его стеречь ОБА —
`http://` и `https://` — и различать «сайт лёг» от «443 отфильтрован»: это разные беды с
разными действиями, а сейчас обе выглядят одинаково. Вот это и есть настоящая защита от
«HTTPS исчезнет молча».

**КРИТЕРИЙ ГОТОВНОСТИ — ЗАМЕНЯЕТ ОБА ПРЕДЫДУЩИХ:**
1. Таблица замера A: не меньше 30 проб, растянуты не меньше чем на час, с временем каждой; напечатана в `## ОТЧЁТ` целиком.
2. Вердикт по 443 одной строкой: «стабилен» / «перемежается» / «недоступен» — со ссылкой на числа замера, а не на ощущение.
3. `curl -s -o /dev/null -w '%{http_code}\n' http://math-kluychiki.ru/` → `200` (НЕ `301`): HTTP как отдавал страницу, так и отдаёт.
4. `curl -sI https://math-kluychiki.ru/ | grep -ci strict-transport` → `0`.
5. Сторож различает две беды: показать его вывод в обоих случаях, второй — подделав недоступность 443 в тесте.
6. `ssh … 'systemctl is-active spetsmat-veb spetsmat-bot'` → оба `active`; сайт по обоим адресам жив.

**ОХВАТ:** «проверено 6 из 6 клауз; проб 443 — N, из них таймаутов M».

🔴 **Порядок работы с nginx остаётся из правки 1** (копия конфига до правки, `nginx -t` перед
каждым `reload`, `curl` после каждого шага, не ответил — возвращаешь копию немедленно).


> 🔴 **Пусто — значит заход не правился с момента выдачи.** Непустой блок читается ПЕРЕД продолжением работы: правка отменяет любое противоречащее ей место выше по файлу, каким бы категоричным оно ни было.
> **Форма строки — жёсткая, по ней судит приёмка:** `### ПРАВКА N · ГГГГ-ММ-ДД ЧЧ:ММ · <что изменилось, одной фразой>`, дальше — что именно перечитать и что откатить, если уже сделано по старой редакции.
> **Аналитик:** внёс правку — обязан ОТДЕЛЬНО послать владельцу короткое сообщение для пересылки исполнителю. Правка, лежащая только в файле, до работающего исполнителя не доезжает: он файл не перечитывает сам.
> **Исполнитель:** прочитал правку — назови её номер в `## ОТЧЁТ` строкой `ПРАВКИ ПРОЧИТАНЫ: 1, 2`. Нет строки при непустом блоке = отчёт не принимается: неизвестно, по какой редакции работали.

<правок нет>

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `принято` — заход сделал то, ради чего задумывался: дал ЗАМЕР вместо мнения (36 проб из сети владельца, 36 успехов, таймаутов 0, окно 73 минуты) и сам же назвал границу этого замера — «стабилен В ОКНЕ» не равно «стабилен», потому что ночной фильтр держался периодами длиннее часа и первый ночной замер тоже был зелёным целиком.

**ЧТО ПРИЁМКА ПРОВЕРИЛА СВОИМИ РУКАМИ:** зона доехала в git, два коммита существуют, ветка влита · `https://math-kluychiki.ru/` и `http://math-kluychiki.ru/` отвечают страницей из третьей сети (не из сети владельца и не с сервера) — то есть 443 доходит и с ещё одной точки · 17 машинных гейтов из 18.
**ЧЕГО ПРИЁМКА ПРОВЕРИТЬ НЕ СМОГЛА, И ЭТО НАЗВАНО, А НЕ ЗАМОЛЧАНО:** пункт B («абсолютных ссылок со схемой нет») — мой инструмент отдаёт markdown и САМ достраивает относительные ссылки до абсолютных, поэтому на его выводе они выглядят как `http://…` даже когда в HTML их нет. Инструмент отвечает на другой вопрос, чем задан, — ровно та ошибка, которой сегодня уже заплатили с доменом и IP. Верю сырому `curl … | grep -c` исполнителя, потому что он мерит именно то, что заявлено.

🔴 **ДВЕ НАХОДКИ ЗАХОДА ВАЖНЕЕ ЕГО ЗАДАЧИ, обе — в очередь:**
1. **Сервер отдаёт наш сайт на ЛЮБОЙ `Host`** (`curl -H 'Host: evil.example' http://159.194.254.52/` → 200): блок голого IP объявлен `default_server` с `server_name _`. Любой чужой домен, направленный на этот IP, покажет наш сайт под своим именем — и это само по себе годится в повод для пометки «обманный сайт».
2. **Порт 8443 больше не слушает.** Именно им в ночь инцидента снималось решающее различение «фильтрация на пути ↔ поломка сервера»: тот же сертификат отвечал за 0.10 с, пока 443 уходил в таймаут. Сегодня повторить этот замер нечем.

**ЧТО ЗА АНАЛИТИКОМ.** Оба урока исполнителя приняты без возражений и оба — про меня: правка 1 велела `301` и HSTS по участку, где в журнале лежал инцидент, и не подняла его; и я вписал в заход утверждение о живом сайте без команды и времени съёма — к прогону оно устарело. От повторения ночной аварии отделяли 55 минут и то, что владелец придержал запуск. Правило, которое из этого следует, записано в `UROKI-FABRIKE.md` дважды — исполнителем и мной.

**ВЕТКА РАБОТЫ:** `https-i-domen`
*(проверяется фактом, не словом: ветка обязана существовать и быть либо ВЛИТА в основную, либо названа в открытой заявке на влитие. Ни того, ни другого — Г14 краснеет. Снять состояние: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py poteri --branch <ветка>`)*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**
> Адрес списка: `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/_INFRA-git/zayavki`
> Читается командой (из любой папки, в том числе из worktree): `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki`
> Ставится командой: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavka --rod <git-operaciya|pravka-koda> "<текст>"`
> 🔴 Вопрос здесь НЕ «что ты хочешь сделать», а «что ты УЖЕ положил в очередь». Дубль сверяется с очередью по id машинно; намерение сверить не с чем.

- заявок эта приёмка не ставила: обе находки (`default_server` на любой Host и погасший 8443) — правка кода в чужой зоне `deploy/`, а не git-операция; они уходят пунктами очереди захода и в план владельца, а не в очередь git-гигиены.

*(Заявок эта приёмка не ставила — так и напиши строкой «заявок нет: <почему ни одна из пяти операций не понадобилась>». Пустая строка и прочерк не принимаются: молчание неотличимо от «забыл».)*
