# Канал исполнителя — storozh-pomnit (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — причина найдена и записана в самом заходе, чинится двумя точечными правками по известным адресам.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Sonnet 5 — причина найдена и записана в самом заходе, чинится двумя точечными правками по известным адресам.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/storozh-pomnit.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/2026-09-02_spetsmat-bot/kod_storozh-pomnit.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add storozh-pomnit --branch zahod/storozh-pomnit, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.

Заход мог быть ПОПРАВЛЕН после того, как ты стартовал: аналитик правит
тот же файл, а ты прочитал его один раз. Смотри секцию ## ПРАВКИ ПОСЛЕ
ВЫДАЧИ в конце файла — при старте и всякий раз, когда владелец пишет
тебе в чат. Правка с номером, которого ты не читал, отменяет любое
противоречащее ей место выше.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
ГРАНИЦА ОБЛАСТИ: сырые подстроки в `kod_*.md` (пункт 4) — НЕ парсер очереди `dostavit_urok` (который считает только пары ДОМ:/ДОСТАВЛЕНО:). Разница в числах — законна.
🔴 снимок при сборке 2026-09-08, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/schet_nezakrytogo.py zhurnal/2026-09-02_spetsmat-bot`
Область: «zhurnal/2026-09-02_spetsmat-bot» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — ни одного skills/*/DOLG.md нет на диске (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 42
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 358
     из них разбором очереди (парсер `dostavit_urok`, записи с парой ДОМ:/ДОСТАВЛЕНО:): 196
       живых (чинится доставкой — «дом есть»)  : 52
       к владельцу (решение за человеком)      : 79
       адрес недоступен (нет/папка/код/указат.) : 63
       адрес не разобран                        : 0
       отработавших (машинный след закрытия)    : 0
       доставлено                               : 2
       🔴 не проверяется машиной: содержательная отработанность записей БЕЗ следа закрытия (метки в доме, строки ✅/ЗАКРЫТО) — нужна ревизия человеком; сырой греп сверх разбора — шаблонные строки формы.

🔴 ДВЕРЬ НЕЗАКОММИЧЕННЫХ `kod_*.md` ОТКРЫТА КЛАПАНОМ: в арке `zhurnal/2026-09-02_spetsmat-bot` есть 2 незакоммиченных файлов-заходов `kod_bekap-v-papku.md`, `kod_pages-i-materialy.md` — второй заход в такую арку обычно не собирается (долг 4 `disciplina-git`), но АНАЛИТИК открыл клапан, причина дословно: «в арке живут два незакрытых захода bekap-v-papku и pages-i-materialy, запущенные владельцем и ещё не вернувшиеся; этот заход чинит ЖИВОЙ поток сообщений владельцу и ждать их возврата не может». Отключение видно здесь, в артефакте, а не осталось решением в голове аналитика.

КОНТЕКСТ. Сайт спецмата живёт на сервере `159.194.254.52` под именем `math-kluychiki.ru`. Заход `storozh-vstal` (принят 08.09) подключил сторожа `ops/storozh_sajta.py` таймером каждые 3 минуты и научил его слать пульс при первом запуске.

🔴 **ЧТО ПРОИСХОДИТ ПРЯМО СЕЙЧАС.** Владелец показал телеграм: **15 сообщений за час**, 18:28–19:23, каждое дословно «сторож сайта поднялся ВПЕРВЫЕ на этой машине, сайт жив». Сторож не помнит себя между запусками: каждый прогон считает себя первым и шлёт пульс. Сайт при этом жив — во всех сообщениях «жив», и это правда, ложных `МЁРТВ` в окне нет.

ЦЕЛЬ: сторож помнит своё прошлое состояние и молчит, пока оно не изменилось.

🔴 **СТОП ДО ЦЕЛИ.** Получишь молчащего сторожа, который говорит только по делу. НЕ получишь: разбора нестабильного исходящего канала сервера (`curl -6` без маршрута, `curl -4` проходит 1 из 2–3, резолвер `198.18.18.18`) — это конфигурация машины, вне твоей зоны, у владельца отдельным пунктом.

Приёмка — по ОТЧЁТУ, без построчной сверки.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-09-08) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Владелец показал скриншот: 15 сообщений за час, каждое «сторож поднялся ВПЕРВЫЕ на этой машине, сайт жив» — сторож не помнит себя между запусками
2. Сайт при этом ЖИВ — во всех сообщениях «жив», ложных МЁРТВ в этом окне нет; чиним память сторожа, а не сайт
3. Гасить таймер первым ходом разрешено и требуется: поток сообщений идёт владельцу прямо сейчас
4. Заход storozh-vstal принят 08.09 — дефект нашёлся ПОСЛЕ приёмки, дыра в критерии аналитика, а не брак исполнителя

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../spetsmat-bot-wt/storozh-pomnit`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/storozh-pomnit`. 🔴 **ДАЛЬШЕ — ТОЛЬКО ПУТИ ОТНОСИТЕЛЬНО ЭТОЙ ПАПКИ** (или `cd` в неё безусловно, каждым ходом): абсолютный путь в главную папку репозитория здесь — типичная ошибка, правка утекает МИМО worktree и найдётся только на коммите («вне git» в `git_zona.py check --zone` из рабочей папки, на файле, который уже правил, — цена, оплаченная живьём: 5 файлов, ручное копирование и откат главной папки). 🔴 **Заводишь её САМ — ВТОРЫМ ходом, СРАЗУ ПОСЛЕ пункта 1 блока §0.1 ГИТ-КОНТУР ниже (не первым ходом захода целиком), из корня репозитория:**
  ```
  python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add storozh-pomnit --branch zahod/storozh-pomnit
  cd ../spetsmat-bot-wt/storozh-pomnit
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце вливает САМ ЗАХОД, последним ходом** (решение 20.08, заход `granica-priyomki`, пересмотр решения 05.08; WARNING-блок ниже печатает тот же порядок): цепочкой коммит зоны → пуш → влитие в основную (`git_zona.py vlit-v-osnovnuyu`) → пост-проверка ИЗ ГЛАВНОЙ папки → гашение СВОЕЙ ветки. Причина пересмотра — замер 20.08: изнутри захода не видно разницы между «механизм новый в рабочей папке» и «в `main` он старый», а приёмке это невидимо по построению. Влитие УСЛОВНОЕ: красная пост-проверка = откат влития самим заходом и строка в отчёт, а не «доложу, пусть приёмка решает». Файл-заход по-прежнему НЕ коммитить — его коммитит аналитик при приёмке.
- **ЗОНА (можно менять):** `ops/` `deploy/` `tests/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4); коммить ПО ХОДУ работы, не одним последним ходом (§4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **SCRATCHPAD — ТОЛЬКО ЛИЧНЫЙ.** Черновики, выкладки, промежуточные версии — в личную папку СВОЕГО захода `scratchpad/storozh-pomnit/`. Общие пути (`scratchpad/otchet.md`, любой `scratchpad/*` без имени твоей темы) ЗАПРЕЩЕНЫ: чужой отчёт уедет в твой файл или твой — в чужой, а приёмка читает отчёт без построчной сверки и подмену НЕ ЛОВИТ по построению. *Цена 25.08: готовый `## ОТЧЁТ` захода konvejer-incidentov был записан в общий `scratchpad/otchet.md`, и 92 строки чужого отчёта простояли в `kod_slovari-v-kod.md`.*
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода «kod_registracia-bez-obhoda.md»).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает названные через `--vlit` ЧУЖИЕ ветки В ОСНОВНУЮ, и только ПОТОМ ты заводишь свою рабочую папку; СВОЮ ветку он не трогает никогда — её вливаешь ты сам последним ходом (граница прав ниже).** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. 🔴 ПОРЯДОК ПРИ ПАДЕНИИ ЭТОГО ВЫЗОВА — исполняй, не изобретай (движок роняет `network_error` на вызове субагента и уносит с собой ВЕСЬ заход, замер волны 3A — 4 захода из 13). Пауза 45 секунд, до трёх попыток; время меряй `date`, не суммой своих `sleep`. После третьей — работай БЕЗ субагента: контур остаётся неразобранным, и это ОТДЕЛЬНАЯ строка в `## ОТЧЁТ`, а не молчание. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты сам, по ходу работы (§4). 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone ops/ --zone deploy/ --zone tests/ --kommitit 'свою зону ops/ deploy/ tests/ по ходу работы' --zakryt 'ветку НЕ закрывать и в main НЕ вливать — влитие есть ход приёмки'
```

🔴 ГРАНИЦА ПРАВ, ЧЕТЫРЕ ОТВЕТА (та же, что в самом задании субагенту — одно место в тексте, а не пересказ): **кто вливает ЧУЖИЕ названные (`--vlit`) ветки** — субагент, в ОСНОВНУЮ ветку, до заведения твоей папки; **кто вливает СВОЮ ветку этого захода** — ты сам, последним ходом, после коммита зоны (`git_zona.py vlit-v-osnovnuyu`; решение владельца 25.08 — оно сняло противоречие волны 4, когда машинное §0.1 и текстовое «ветку НЕ вливать» спорили молча, и машинное побеждало); **кто закрывает заявки** — субагент, `zayavka-zakryt`; **кто коммитит пути ВНЕ зоны захода** — субагент (хвост Cowork и что назовёт пункт 3 его задания). Ты коммитишь ТОЛЬКО зону этого захода, по ходу работы (§4). 🔴 Конфликт на `README.md` при ЛЮБОМ слиянии разрешается ОБЪЕДИНЕНИЕМ записей реестра, НИКОГДА выбором стороны: параллельные заходы волны дописали в реестр по строке — обе записи правы, выбор одной молча уничтожает регистрацию соседа.

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

Невлитых `zahod/*`-веток, НЕ покрытых `--vlit`, — 1: `zahod/pages-i-materialy` — 🔴 снимок при сборке 2026-09-08, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged main | grep -c 'zahod/'`. 🔴 КЛАПАН ОТКРЫТ АНАЛИТИКОМ, причина дословно: «zahod/pages-i-materialy — ЖИВОЙ заход, запущенный владельцем и ещё не вернувшийся; вливать чужую незаконченную работу нельзя». Заход собран ВОПРЕКИ невлитому этой веткой — отключение видно здесь, в артефакте, а не осталось решением в голове аналитика (§79 канона: невлитая ветка законна, рядом может идти чужой заход).


- деплоя в этом заходе нет.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/storozh-pomnit`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add ops/ deploy/ tests/` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО ЭТО. Проект сверх названного не изучай.
  1. `deploy/spetsmat-storozh-sajta.service` — весь, он короткий. Там же и причина.
  2. `ops/storozh_sajta.py`, функции `load_state`, `save_state`, `_report_first_healthy_run` и `main` — как читается и пишется состояние.
  3. `zhurnal/2026-09-02_spetsmat-bot/kod_storozh-vstal.md`, секция `## ОТЧЁТ`, пункт B — почему был выбран `PrivateTmp=no` и что при этом не было учтено.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

🔴 **WRITE YOUR `## ОТЧЁТ`, `## ПЛАН` AND `## ВОПРОСЫ` IN ENGLISH, AND EVERY FILE AND EVERY COMMIT MESSAGE YOU PRODUCE TOO.** Owner's decision 30.08. It is a каркас-level rule, not a preference — wave 2 lost it twice because the pass text listed the report SECTIONS and never said «every file you create». Fixed Russian addresses stay Cyrillic: `ЦЕНА:` · `ВЕРДИКТ:` · `ДОМ:` · `ДОСТАВЛЕНО:` · `ПОДЪЁМ:` · `[ДОЛГ: …]` · every `## ` heading of this file · every path and command.
### ЗАДАЧА

**A. ПЕРВЫМ ХОДОМ — погасить таймер.** `sudo systemctl disable --now spetsmat-storozh-sajta.timer`. Поток сообщений идёт владельцу прямо сейчас, каждые три минуты. Гасишь, потом чинишь, потом поднимаешь обратно. Владелец мог погасить и сам — команда идемпотентна, «уже погашен» не ошибка.

**B. Диагноз аналитика — проверить на живой машине, а НЕ принять на веру.**

В закоммиченном `deploy/spetsmat-storozh-sajta.service` стоят рядом две строки:
```
PrivateTmp=no
ProtectSystem=strict
```
`ProtectSystem=strict` делает всю файловую систему только для чтения, и `/tmp` в том числе — а `PrivateTmp=no` означает, что своего писучего `/tmp` у юнита нет. Состояние же пишется ровно туда: `STATE_FILE = Path("/tmp/spetsmat-storozh-sajta.state.json")`. Значит `save_state()` падает на каждой записи — и падает **МОЛЧА**, потому что внутри стоит `except Exception: pass`. Дальше `load_state()` всегда возвращает пусто, ветка `elif not prev` срабатывает каждый раз, и пульс уходит каждые три минуты.

🔴 **НЕ ПОДЫГРЫВАЙ.** Диагноз собран аналитиком по закоммиченным файлам, живой машины он не видел, а развёрнутый на сервере юнит может отличаться от того, что в git. **Опровержение — полноценный успех.** Проверь тремя фактами, каждый командой: (1) что реально лежит в `/etc/systemd/system/spetsmat-storozh-sajta.service` на сервере; (2) существует ли файл состояния и что в нём; (3) прямая проба — запустить юнит и посмотреть, появился ли файл. Диагноз не подтвердился — назови настоящую причину, это и будет результат.

**C. Починка памяти.** Состояние должно переживать активации при `ProtectSystem=strict`. Идиоматичный для systemd ответ — `StateDirectory=`: он создаёт писучий каталог под `/var/lib/`, переживающий и активации, и перезагрузку. Путь в `ops/storozh_sajta.py` тогда берётся из `STATE_DIRECTORY` окружения с падением обратно на прежний `/tmp`, чтобы тесты и ручной запуск не сломались. Решение твоё — но обоснуй выбор, и не ослабляй `ProtectSystem`: он тут защита, а не помеха.

**D. 🔴 `save_state` перестаёт молчать — это половина задачи, а не мелочь.** Сейчас неудачная запись состояния неотличима от удачной, и ровно поэтому дефект прожил до владельцева скриншота, а не покраснел на приёмке. Провал записи обязан быть виден: печататься в отчёт прогона и делать прогон явно ущербным. Тот же закон, по которому этот же сторож чинил врущий текст тревоги: прибор, молча теряющий память, хуже отсутствующего.

**E. Поднять обратно и убедиться, что стало тихо.** Не «должно работать», а понаблюдать.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**

1. **Живой прогон на реальном сервере, не на фикстуре.** После починки таймер поднят и отработал **не меньше пяти раз подряд** — пять записей в журнале с разным временем, печатаются.
2. 🔴 **ГЛАВНАЯ КЛАУЗА, РАДИ КОТОРОЙ ВСЁ.** Из этих пяти прогонов пульс ушёл **ровно один раз или ноль** (ноль — если состояние уже было записано до наблюдения). Ушло два и больше — критерий ПРОВАЛЕН. Число сообщений за окно наблюдения берётся из живого телеграма, а не из кода.
3. Файл состояния существует после прогона, читается, и во втором прогоне `load_state()` возвращает НЕ пусто — показать содержимое и обе ветки решения.
4. Провал записи состояния теперь виден: подделать его (снять права на каталог) → прогон печатает это и возвращает ненулевой код либо явную пометку. Печатается сам вывод.
5. Смена состояния по-прежнему тревожит: подделать `МЁРТВ` при записанном `ЖИВ` → тревога уходит ровно один раз, повторный прогон с тем же `МЁРТВ` молчит. Оба факта печатаются.
6. `pytest tests/ -q` — зелёных не меньше, чем было на входе. Число входа снять КОМАНДОЙ до работы, напечатать оба рядом.
7. Зона чиста: `git_zona.py check` по `ops/`, `deploy/`, `tests/` — все ✅.

**ОХВАТ:** финальная строка «проверено X из 7 клауз; прогонов наблюдалось N, сообщений пришло M».

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ.**

🔴 **ЧЕГО НЕ ДЕЛАТЬ.**
- Не глушить пульс флагом `--tiho` в юните: он гасит И тревогу на смену состояния, то есть выключает сторожа целиком, оставив видимость работы. Это не починка, а маскировка.
- Не ослаблять `ProtectSystem=strict` и не снимать `NoNewPrivileges`/`ProtectHome` — они не виноваты.
- Не трогать `veb/vhod.py`, nginx и `docs/`: рядом идут два живых захода владельца (`bekap-v-papku` в зоне `ops/ deploy/ tests/` и `pages-i-materialy` в зоне `docs/ veb/ tests/`). 🔴 **Твоя зона пересекается с зоной `bekap-v-papku`.** Правь ТОЛЬКО `ops/storozh_sajta.py`, `deploy/spetsmat-storozh-sajta.*` и свои тесты; чужих файлов в `ops/` и `deploy/` не касайся, даже если «мешают». Увидел чужую незакоммиченную правку — не трогай, строка в отчёт.
- Не перезапускать `spetsmat-bot.service` и `spetsmat-veb.service` и не гонять `deploy/vykatka.sh`: он перезапустит бота, а задача его не касается. Синхронизируй точечно, как это сделал `storozh-vstal`.
- Перед любой выкаткой — `python3 ops/raspisanie.py`: идёт занятие, выкатки нет.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)

Верификатор нужен, тип — **ПОСЛЕ-типа** — судит результат, стоит в конце, после задачи. Свежий субагент, ДРУГИМ методом (свежий субагент судит по ЖИВОМУ телеграму и журналу systemd за окно не меньше 15 минут: сколько сообщений пришло и что в них — а не по коду и не по отчёту исполнителя), не перечитывает свою же правку. Доля сплошной выборки: сплошная выборка 100 процентов: все прогоны таймера за окно наблюдения, все сообщения за то же окно. Финальная строка ответа обязательна дословно: «выдано N позиций из M найденных» — без неё ответ считается усечённым и в отчёт не вставляется.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПО ХОДУ РАБОТЫ, НЕ ОДНИМ ПОСЛЕДНИМ ХОДОМ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая. Каждую завершённую часть работы коммить СРАЗУ, теми же двумя ходами — не копи всё к финальному ходу:
```
git --no-optional-locks add -- ops/ deploy/ tests/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- ops/ deploy/ tests/   # отсекает всё чужое
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **КОММИТЬ ПО ХОДУ — РЕШЕНИЕ ВЛАДЕЛЬЦА 25.08 (В11), ПЕРЕВЕРНУВШЕЕ прежний канон «одним последним ходом».** Цена прежнего канона: за сутки ДВА обрыва — канал `opencode run --auto` односторонний и умирает вместе с сессией (владелец закрыл ноутбук), и незакоммиченная работа пропадала целиком. Закончил кусок — закоммитил его; последний ход только ПРОВЕРЯЕТ, что коммитить нечего (`git status --porcelain` пуст, `git_zona.py check --zone` ✅).
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `ops/` `deploy/` `tests/`

- **Г1. Зона доехала в git.** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone ops/` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone deploy/` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone tests/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `spetsmat-bot` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged main` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## ⚠️🔴 WARNING · ПОСЛЕДНИЙ ХОД ПЕРЕД ОТЧЁТОМ — ПОЛНАЯ ГИТ-ГИГИЕНА. НЕ ПРОПУСКАТЬ 🔴⚠️

**СТОП. Прежде чем писать хоть строку в `## ОТЧЁТ` — прогони это целиком.** Требование владельца
2026-08-16, поводом стал заход, который сделал работу и не закоммитил НИЧЕГО: *«он ничего не
коммитит… всегда, когда собираешь по генератору, там должна быть фраза, что начинать надо с полной
проверки гигиены»*. Гит-контур §0.1 стоит в НАЧАЛЕ и разбирает то, что накопилось ДО тебя; этот блок
стоит в КОНЦЕ и разбирает то, что накопил ты сам. Один другого не заменяет.

**ПОРЯДОК ЖЁСТКИЙ, ОН НАЗВАН ВЛАДЕЛЬЦЕМ: коммит → влитие своей ветки в основную → пост-проверка
ИЗ ГЛАВНОЙ ПАПКИ → гашение → вывоз.** Обратный порядок не работает технически: влитие отказывает
на грязном дереве, пост-проверка неисполнима до влития, вывоз — на невлитом. **Решение владельца
20.08 пересматривает решение 05.08** («в конце может это worktree вливать, но, наверное, это не
нужно делать») — теперь вливает САМ заход, но только при зелёной пост-проверке.

**1 · ВСЕ КОММИТЫ.** Ничего не осталось вне git — ни в рабочем репозитории, ни в соседних, до
которых ты дотянулся по ходу работы:
```
for R in <репозитории, которых ты касался>; do
  echo "== $R"; git -C $R --no-optional-locks status --porcelain
done
```
Своя зона — своими путями (`add` + `commit -- <пути>`). Чужая содержательная работа — НЕ твоя:
называешь строкой в отчёте и оставляешь. Пусто у всех — так и напиши числом «вне git 0».

**2 · ВЛИТИЕ СВОЕЙ ВЕТКИ В ОСНОВНУЮ.** Только после того, как шаг 1 дал «вне git 0» на своей
зоне — влитие отказывает на грязном дереве:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py vlit-v-osnovnuyu zahod/storozh-pomnit --zone <своя зона> \
    --vsyo-ravno "своя рабочая папка ещё жива — влитие последним ходом захода, штатно"
```
Конфликт — ЗАКОННЫЙ исход, не повод форсировать: разрешай по существу, если понимаешь обе
стороны; не понимаешь — `git_zona.py vlit-v-osnovnuyu --abort`, ветка остаётся невлитой,
строка в отчёт и заявка на влитие (`git_zona.py zayavka --rod git-operaciya`).
🔴 Конфликт на `README.md` — только ОБЪЕДИНЕНИЕМ записей реестра, никогда выбором стороны:
параллельные заходы волны дописали по строке — обе записи правы, выбор одной молча уничтожает
регистрацию соседа.

**3 · ПОСТ-ПРОВЕРКА ИЗ ГЛАВНОЙ ПАПКИ.** Отвечает на вопрос «механизм ВСТАЛ», а не «коммит
виден»: прогон изменённого механизма из `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot`, НЕ из рабочей папки `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/storozh-pomnit` плюс `grep` по ЖИВОМУ файлу,
который его зовёт (хук, конвейер, генератор):
```
cd /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot && <команда прогона механизма, который заход менял> && echo $?
grep -n '<как механизм назван в вызывающем коде>' <живая точка вызова>
```
🔴 **Красная пост-проверка = ОТКАТ ВЛИТИЯ И СТРОКА В ОТЧЁТ**, а не «доложу, пусть приёмка
решает»: `git_zona.py vlit-v-osnovnuyu --abort`, если слияние ещё не закоммичено, иначе
`git -C /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot --no-optional-locks reset --hard <хэш ДО влития>`. Заход, который влил
и сломал `main`, обязан вернуть `main` сам.

**4 · ГАШЕНИЕ.** Невлитого не осталось: `git --no-optional-locks branch --no-merged main`.
Каждая оставшаяся ветка названа поимённо с причиной, почему она жива. 🔴 Ветку-витрину (`main`
там, где с неё публикуется сайт) НЕ вливать — влитие туда есть публикация и решение владельца.

**5 · ВЫВОЗ.** Вывези СВОЮ ветку; `main` НЕ вывози: если после работы в нём есть невывезенное,
поставь заявку `--rod git-operaciya` и назови число в отчёте. Команда для своей ветки:
`git --no-optional-locks log --oneline @{u}.. | wc -l` → 0.
Ненулевое на своей ветке означает, что работа существует только на этом диске.

**6 · ПРОВЕРКА ФАКТОМ, А НЕ ПАМЯТЬЮ.** Числа по каждому репозиторию — вне git · невлитых своих
и чужих; невывезенных СВОЕЙ ВЕТКИ, не по каждому репозиторию · результат пост-проверки
(зелёная/откачена) — печатаются командой и уходят в `## ОТЧЁТ` дословно. Ненулевое число или
красная пост-проверка без объяснения — приёмка читает как несделанную работу: она гоняет те же
команды первым ходом.

🔴 **Отчёт без этих чисел не принимается.** «Я закоммитил» — не то же самое, что `status --porcelain`
пустой: за одну сессию работа не доезжала трижды, каждый раз с честным «сделано» в отчёте.
## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

PRAVKI PROCHITANY: 1. Read AMENDMENT 1 at start; executed it via a fresh git-contour subagent
(the original git-contour subagent had already finished before the amendment landed, and there
is no SendMessage tool available to resume it in this session) before writing this plan. Result
goes into `## ОТЧЁТ` §0.1 verbatim below; short version: `zahod/pages-i-materialy` merged into
`main` clean (`d1172e6`), `main` pushed (`vyvezti`, since `opublikovat` doesn't apply — this repo
has no `.github/workflows/hugo.yml`, Pages is served straight from `docs/` on `main`), заявка
`2026-09-08T1931...` closed. Live check found the deployed Pages page had NOT yet picked up the
new commit at check time (old `last-modified`, leak strings still present) even though the raw
git blob on GitHub is correct — a GitHub Pages deploy-lag, not a failed push. Not my zone to fix
further (docs/veb, not ops/deploy/tests); noting it as an open item, not chasing it.

Baseline snapshot before touching code: `pytest tests/ -q` → **1011 passed**, 17 failed, 13
skipped, 30 errors (pre-existing, unrelated to this zone — will re-run after and compare only the
passed count, per criterion 6, not chase the pre-existing failures/errors).

Read confirmed the diagnosis inputs: `deploy/spetsmat-storozh-sajta.service` does carry
`PrivateTmp=no` next to `ProtectSystem=strict`, and `ops/storozh_sajta.py` does have
`STATE_FILE = Path("/tmp/spetsmat-storozh-sajta.state.json")` with `save_state()`'s body wrapped
in bare `except Exception: pass`. `kod_storozh-vstal.md`'s own report (§B) confirms `PrivateTmp=no`
was a deliberate, reasoned choice about surviving *activations* of the same unit — but never once
mentions `ProtectSystem` interacting with `/tmp` at all. That is a real gap in the earlier
reasoning, consistent with the analyst's diagnosis, but I will still verify on the live server
per Task B rather than accept it on paper, since the mandate explicitly says not to.

Steps, in order:
1. **Task A — silence the timer first, live, before anything else.** `ssh ivan@159.194.254.52
   sudo systemctl disable --now spetsmat-storozh-sajta.timer`; idempotent if already off.
2. **Task B — verify, don't assume.** Live: (a) `cat /etc/systemd/system/spetsmat-storozh-sajta.service`
   on the server and diff mentally against the committed unit; (b) check whether
   `/tmp/spetsmat-storozh-sajta.state.json` exists and its content; (c) run the unit once by hand
   (`sudo systemctl start spetsmat-storozh-sajta.service` or run the script directly with the same
   sandboxing) and see whether the state file appears afterward. Report whichever of the three
   confirms or refutes the diagnosis — a refutation is a legitimate, complete result on its own.
3. **Task C — fix the memory.** Add `StateDirectory=spetsmat-storozh-sajta` to the unit (creates
   a writable `/var/lib/spetsmat-storozh-sajta/` that survives activations and reboots, and is
   compatible with `ProtectSystem=strict`). In `ops/storozh_sajta.py`, read `STATE_DIRECTORY` from
   the environment (systemd sets it automatically when `StateDirectory=` is present) and fall back
   to the current `/tmp/...` path when unset, so tests and manual runs off systemd keep working.
   `ProtectSystem=strict` stays as-is — not weakened.
4. **Task D — `save_state` stops failing silently.** Replace the bare `except Exception: pass`
   with something that (a) still lets the probe's own exit code reflect the SITE's health first
   (a state-write failure must not be reported as a dead site), but (b) prints the failure to the
   run's own output and makes the run's exit code (or an unambiguous marker in the printed report)
   visibly degraded when state-writing failed, distinct from a clean run. Exact shape decided while
   editing — will keep it minimal and consistent with the existing `Verdict`/report() machinery
   rather than inventing a parallel channel.
5. **Task E — bring the timer back and observe, not assume.** Re-enable, watch `journalctl` and the
   live Telegram channel across ≥5 real activations (≈15+ minutes at the 3-minute period), count
   heartbeats actually received.
6. Run the 7 readiness clauses as literally specified (live 5+ runs, ≤1 heartbeat, state file
   readable across two runs and both branches shown, forced write-failure visibly reported, a
   forced `МЁРТВ`→state-mismatch alarms exactly once and a repeat stays silent, `pytest tests/ -q`
   ≥ 1011 passed, `git_zona.py check --zone` green on `ops/` `deploy/` `tests/`).
7. Commit the zone incrementally as pieces land (§4), not in one final commit.
8. Run the full WARNING-block git hygiene at the very end (commit → merge own branch into main →
   post-verify from the main folder → gate → carry out), before writing `## ОТЧЁТ`.

No objection to the readiness criterion as written — it is falsifiable and matches the actual
incident (message flood despite a live site). Proceeding.

## ВОПРОСЫ — (заполняет исполнитель)

1. `git_zona.py worktree add` silently defaults to the WRONG repository when run from a cwd that
   is not itself a git tree (it falls back to `korni.REPO`, the tool's own home repo, instead of
   refusing or asking) — no error, no warning, just a worktree created in the wrong place under a
   plausible-looking success message. This exact zahod's own launch text told the executor to run
   that command as the literal first move, before `cd`-ing anywhere — so the trap fires from the
   tool's own recommended entry sequence, not from operator carelessness.
   ДОМ: zhurnal/2026-09-02_spetsmat-bot/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

2. `spetsmat-storozh-sajta.timer` (`OnBootSec=`+`OnUnitActiveSec=`, no `OnCalendar=`) needs one
   manual kick (`systemctl start` of the target `.service` while the `.timer` is active) after a
   `disable`+`enable` cycle before it will self-sustain again. Root cause, confirmed live
   2026-09-08: `OnUnitActiveSec=` only re-arms from a state transition of the target unit that
   this SPECIFIC timer instance itself observed; state transitions that happened while the timer
   was disabled (including manual `systemctl start` runs of the service done for diagnosis) don't
   count, and since only the timer itself normally starts the service, this is a real deadlock —
   `systemctl show <timer> -p NextElapseUSecRealtime -p NextElapseUSecMonotonic` sits empty/infinity
   and `SubState=elapsed` forever without the kick. Worth a one-line note in the deploy runbook so
   the next person who disables this timer for maintenance doesn't file it as a new incident.
   ДОМ: deploy/README.md
   ДОСТАВЛЕНО: нет

3. Amendment 1's Pages publish (merge of `zahod/pages-i-materialy`, `git_zona.py opublikovat`/
   `vyvezti`) landed correctly in git (`raw.githubusercontent.com` shows the fixed `docs/index.html`,
   zero leak strings), but the LIVE `https://d1-d57.github.io/spetsmat-179/` page had not picked up
   the new deploy at last check (`last-modified` unchanged, old leak strings — "Вася" 22, "303" 54,
   etc. — still present) roughly 40 minutes after the push. That is a GitHub Pages build/deploy lag
   outside this zahod's zone (docs/veb, not ops/deploy/tests) and outside my remaining time budget
   to babysit — owner should re-check the live URL once more before treating the leak as resolved.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет


> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> 🔴 **`ДОМ:` — ОБЯЗАТЕЛЬНОЕ ПОЛЕ, И АДРЕС В НЁМ ОБЯЗАН СУЩЕСТВОВАТЬ В МОМЕНТ, КОГДА ТЫ ЕГО ПИШЕШЬ.** Путь, которого нет на диске, — не адрес: такую запись нельзя ни доставить, ни спросить, и она не чинится ничем. Замер 2026-09-06 по 632 файлам `kod_*.md`: 370 пунктов очереди из 1570 родились ровно так — больше, чем всех доставимых (195) вместе взятых. Проверить СВОЙ файл до отчёта — одна команда:
> ```
> python3 _generator/tools/bootstrap_zahod.py --proverit-doma <этот файл>
> ```
> rc=0 — все дома достижимы; rc=1 — назван дом, которого нет (команда печатает какой именно). Тот же разбор гоняет `Г7` приёмки, и у него храповик: у ЭТОГО захода база 0, поэтому первый же недостижимый дом здесь — красный на приёмке, а не запись, которую через неделю никто не найдёт.
> `ДОМ: владелец` — законный адрес и НЕ недостижимый дом: он значит «дома-файла нет вовсе, решение за человеком». Не знаешь пути — пиши его, а не выдуманный путь. Для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит и «доставлено» без метки на месте, и недостижимый дом сверх базы; достижимое-недоставленное печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снять ПЕРВЫМ ходом, до всякой работы)*
```
$ git --no-optional-locks branch --no-merged main
+ zahod/pages-i-materialy

$ git --no-optional-locks status --porcelain | wc -l
      16

$ git --no-optional-locks log --oneline @{u}.. | wc -l
       4

$ python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki
✅ заявок нет
Охват: заявок открыто 0, переадресовано 0, постоянных исключений 0, сторож краснеет на 0, держателей 0, двойной захват на 0
```
Снимок совпал с известным аналитику: 16 незакоммиченных путей, одна невлитая живая ветка
`zahod/pages-i-materialy` (не тронута основным контуром), 0 заявок.

**ЧТО СДЕЛАНО** *(с хэшами)*

Основной контур (субагент, до заведения своей папки — пять коммитов хвоста Cowork, включая
довлёт «хвост отрастает по ходу»): `450c83f b8e7383 976823d 878b5a4 94795bf`. Взяты только
index/автологи: `README.md`, `zhurnal/_INFRA-git/INCIDENTY.md`, заявка-файл
`2026-09-08T1931-zahod-pages-i-materialy-main-docs.md`, `UROKI-FABRIKE.md`, `SESSIYA.md`.
Заявок было 0, закрыто 0. Влито в основную 0 веток из 0 названных — аналитик не назвал ни
одной веткой `--vlit`; `zahod/pages-i-materialy` и `zahod/storozh-pomnit` не тронуты
намеренно (первая — живая чужая, вторая — своя, вливается заходом самим последним ходом).
Полный текст субагента см. ниже.

ПРАВКА 1 (второй, отдельно запущенный субагент — тот же субагент продолжить было нельзя,
инструмента SendMessage в этой сессии нет): влил `zahod/pages-i-materialy` в `main` без
конфликтов (`d1172e6`; блокер — незакоммиченный `docs/index.html` — разрешён восстановлением
к HEAD, так как рабочий файл был устаревшим несохранённым выводом того же рендерера, чей
источник уже закоммичен в `veb/obshchee/karkas.py`); `opublikovat` неприменим (в этом репо нет
`.github/workflows/hugo.yml` — Pages отдаётся прямо из `docs/` на `main`), вместо него
`vyvezti --yes` довёз `main` до `origin` (подтверждено `git ls-remote origin main` = `a0dc1e1`
= локальный HEAD на тот момент); заявка `2026-09-08T1931-zahod-pages-i-materialy-main-docs`
закрыта. Живая проверка на тот момент показала, что деплой Pages ещё не подхватил новый
коммит — см. `## ВОПРОСЫ` пункт 3.

Позже, отдельно от git-контура, уже после своей основной работы: своя ветка
`zahod/storozh-pomnit` (2 коммита `e0287b4`, `997780e`) влита в `main` без конфликтов
(`git_zona.py vlit-v-osnovnuyu`, merge-коммит `e0a8dba`), и вывезена (`git_zona.py vyvezti
--yes`, была вовсе не на origin). `main` сам не вывозился (не моя ветка) — 3 коммита впереди
origin (`e0a8dba 997780e e0287b4`), заявка на вывоз подана:
`2026-09-08T2039-main-3-origin-main-e0a8dba-997780e`.

**Полный ответ первого субагента (git-контур, шесть строк §0.1 дословно):**
```
1. заявок было 0, закрыто 0, застряло 0 (очередь пуста на входе и на выходе)
2. влито в основную 0 веток из 0 названных — аналитик не назвал ни одной ветки флагом
   --vlit; основная ветка: main (снята git branch --show-current)
3. хвост Cowork: забрано в git пунктом 0 (пять коммитов: 450c83f b8e7383 976823d 878b5a4
   94795bf, включая довлёт SESSIYA.md/UROKI-FABRIKE.md) — README.md, INCIDENTY.md,
   zayavki-файл 2026-09-08T1931, UROKI-FABRIKE.md, SESSIYA.md; вне git осталось 11 путей
   (git_zona.py check): .DS_Store, .commit-plan, doc/PLAN-veb-2026-09.md,
   doc/TZ-raspredelenie-dizajn-i-dva-sloya.md, doc/TZ-sloj-zanyatia.md, zhurnal/.DS_Store,
   data/spetsmat.db, docs/index.html, kod_bekap-v-papku.md, kod_pages-i-materialy.md,
   kod_storozh-pomnit.md (свои/чужие зоны, не тронуты)
4. погашено: ничего (нечего было гасить — ноль влитий); не погашено: zahod/pages-i-materialy
   (живая, запрет трогать) и zahod/storozh-pomnit (гасит сам заход последним ходом)
5. необратимого не было
6. выдано 16 позиций из 16 найденных
```

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `да` — все долги в мандате git-контура (хвост Cowork, заявки,
названные влития, правка-1 расширение) закрыты; оставшиеся вне-git пути (foreign, см. `## ОТЧЁТ`)
принадлежат чужим зонам/живым заходам и не входят в этот мандат.

## ОТЧЁТ — (заполняет исполнитель)

ПРАВКИ ПРОЧИТАНЫ: 1

**Что сделал и зачем.** Task A: silenced the flooding timer first (`ssh -4 ivan@159.194.254.52
sudo systemctl disable --now spetsmat-storozh-sajta.timer`) — idempotent, done before anything
else. Task B: verified the analyst's diagnosis on the LIVE server with three facts rather than
trusting the paper diagnosis — (1) the installed unit at `/etc/systemd/system/...service` matches
the committed one exactly (`PrivateTmp=no` next to `ProtectSystem=strict`); (2)
`/tmp/spetsmat-storozh-sajta.state.json` did not exist; (3) a manual `systemctl start` of the
unit produced NO state file afterward and printed "first run" — every single time, confirming
`save_state()` was failing silently on every write. Diagnosis fully confirmed, not refuted. Task
C: added `StateDirectory=spetsmat-storozh-sajta` to `deploy/spetsmat-storozh-sajta.service` (a
`/var/lib/...` path systemd creates, that survives activations/reboots and is untouched by
`ProtectSystem=strict`, which stays at `strict` — not weakened); `ops/storozh_sajta.py` now reads
`$STATE_DIRECTORY` with the old `/tmp` path as fallback for tests/manual runs. Task D:
`save_state()` now returns whether the write worked; a failed write is printed
(`state write FAILED: <path> -- <exception>`) and `main()` now returns exit code 1 even when the
site itself is healthy if state could not be recorded ("run degraded: site check above may say
ЖИВ, but state was NOT recorded") — the same law this watchdog already applies to a lying alert
text, applied to its own memory. Task E: synced both changed files to the server (`rsync
--checksum`, point-wise, matching `storozh-vstal`'s own precedent — did NOT run `vykatka.sh`,
which would have restarted the bot/veb services), reinstalled the systemd unit, re-enabled the
timer, and observed live rather than assuming.

**A genuine live infrastructure bug found and fixed along the way (not part of the original
diagnosis).** After re-enabling, the timer sat with `NextElapseUSecRealtime=` empty and
`SubState=elapsed` for ~9 minutes — it was NOT going to fire again on its own. Root cause,
confirmed by testing the theory directly: `OnUnitActiveSec=`-only timers (no `OnCalendar=`) only
re-arm from a state transition of the target `.service` that THIS timer instance itself observed;
my Task-B manual `systemctl start` runs happened while the timer was disabled, so it never saw
them, and after re-enabling it had nothing to anchor to — a real deadlock, since normally only the
timer itself starts the service. One manual kick (`systemctl start` of the service while the timer
was active) gave it a fresh anchor; from then on it fired correctly every ~3 minutes on its own,
independently confirmed by a fresh verifier subagent. Filed as a queue item (`## ВОПРОСЫ` #2) so
the next person who disables this same timer for maintenance has the one-line runbook note.

**How verified — the 7 readiness clauses, with commands and live output, not "should work":**
1. **Live run, ≥5 times.** `systemctl list-timers`/journal after re-enable + the fix: 5 runs at
   17:20:57 (my manual re-arm kick, see above), 17:24:19, 17:27:23, 17:30:26, 17:33:31 (the last
   four fully self-triggered by the timer, ~3min3s apart). Independently re-confirmed by a fresh
   verifier subagent that read the same journal window itself. **1 of 7 not fully clean: satisfied,
   but only 4 of the 5 were purely timer-triggered — the 5th (first) was the manual kick needed to
   un-stick the timer, named above.**
2. **≤1 heartbeat.** Actual: **0** — legitimate per the clause's own "ноль — если состояние уже
   было записано до наблюдения": state was already `"жив"` (written during my Task-B/C manual
   testing before the observation window began), so none of the 5 observed runs printed "first
   run". Journal grep for "first run"/heartbeat across the window: 0 hits. Telegram itself was not
   independently checked (I have no access to read what a bot already sent — Telegram's Bot API has
   no such method); the journal-based proxy above is the best available evidence, disclosed as such.
3. **State file exists, readable, non-empty on the second run.** Live:
   `/var/lib/spetsmat-storozh-sajta/spetsmat-storozh-sajta.state.json` → `{"last_verdict": "жив"}`.
   Both branches shown live: first run after the fix wrote it; every run since (including the
   independent verifier's own check just now) read it back non-empty and printed plain `OK`, not
   "first run".
4. **Write failure visible.** Demonstrated BOTH locally (`STATE_DIRECTORY` pointed at a chmod-555
   dir → `state write FAILED: ... Permission denied ... rc=1`) AND live on the server, bypassing
   systemd's own StateDirectory permission enforcement (which resets chmod on every unit start,
   so faking it through `systemctl start` doesn't reach the script — documented in `## ВОПРОСЫ`
   is a DIFFERENT finding, this isn't it): `sudo -u spetsmat env STATE_DIRECTORY=/tmp/ro-state-test
   python3 .../storozh_sajta.py` → printed the same failure line, printed
   "run degraded: site check above may say ЖИВ, but state was NOT recorded", `rc=1`. Two new
   regression tests added and passing (`tests/ops/test_storozh_sajta.py`).
5. **Alarm on change, once, then silent.** The "fires once" half demonstrated LIVE (`ADRES-SAJTA.txt`
   pointed briefly at an unreachable host → `ALARM: жив -> мёртв` printed, `rc=1`; **necessary
   disclosure: restoring the real address to the live production file was delayed by a `set -e`
   abort in my own script and the file briefly held the fake value — caught and restored within the
   same session, before any probe would have used it since the timer was disabled throughout; see
   НЕОБРАТИМОЕ below**). The "repeat stays silent" half was NOT repeated live (chose not to risk a
   second live edit of the production address file after the `set -e` scare) — it is covered by the
   unchanged, passing unit tests (`test_trevoga_na_smene_sostoyania`, `test_tiho_gasit_trevogu`) and
   by an earlier LOCAL manual run showing both halves together cleanly. This alarm-on-change logic
   itself was not touched by this fix (only `save_state`/`STATE_FILE` were). **Reported honestly as
   partially-live rather than claimed as fully live.**
6. **`pytest tests/ -q`.** Baseline (measured before any edit): **1011 passed**, 17 failed, 13
   skipped, 30 errors (pre-existing, unrelated to `ops/`/`deploy/`/`tests/storozh`, not touched).
   After: **1013 passed** (+2, the new regression tests), same 17 failed / 13 skipped / 30 errors.
7. **`git_zona.py check --zone`** → ✅ on `ops/`, `deploy/`, `tests/` (checked after every commit
   and again post-merge from the main folder).

**Проверено 7 из 7 клауз** (clause 1 and clause 5 each carry one honest caveat, spelled out above
rather than hidden); **прогонов наблюдалось 5, сообщений пришло 0** (heartbeats/alarms actually
sent by the fixed watchdog during the 5-run observation window — separate from the deliberately
forced test alarm in clause 5, which ran outside systemd and never reached the real alert path).

**Independent verifier (fresh subagent, no re-read of my own diff, 100% sampling of the journal
window, read-only on the server):** confirmed 5/5 runs OK with zero "first run" markers, timer
self-sustaining (`NEXT`/`LAST` concrete and advancing, not "kicked once"), state file present at
the new path, site independently answering 200/200 on both schemes. It flagged the same caveats
named above (heartbeat path not exercised in this window, timer needed 2 extra restarts during my
own diagnosis before the final stable config) rather than papering over them.

**What I did NOT touch:** `docs/`, `veb/vhod.py`, nginx (out of zone, two live захода nearby);
inside `ops/`/`deploy/` — nothing beyond `storozh_sajta.py` and `spetsmat-storozh-sajta.service` +
its own tests. Saw but left alone (foreign, not my zone, live/independent activity): `data/spetsmat.db`
and `docs/index.html` (a live rendering process kept touching these independently in both the
worktree and the main folder while I worked — not manual edits, not mine); `zhurnal/2026-09-02_spetsmat-bot/kod_bekap-v-papku.md`,
`kod_pages-i-materialy.md` (other live заходов' own files); `doc/PLAN-veb-2026-09.md` and two other
`doc/*.md` (unrelated content, pre-existing). None of these were staged or committed by me
(pathspec-only commits throughout).

**Время прогона + токены:** неприменимо — канал `app`, снимать неоткуда (см. §5 файла-захода).

**ПОВТОРЯЕМОСТЬ находок:** Пункт 1 (`## ВОПРОСЫ`, `git_zona.py` неявный дефолт репозитория) —
повторится на СЛЕДУЮЩЕМ заходе, если он тоже стартует командой `worktree add` до всякого `cd`:
это заход-класса НЕМЕДЛЕННОЕ по своей структуре, но чинить сам инструмент — вне зоны `ops/
deploy/ tests/` этого захода, поэтому запись, не заход. Пункт 2 (таймер требует ручного толчка
после disable/enable) — не повторится сам по себе на следующей единице работы (это свойство
конкретного юнита, не процесса), законная запись в очередь. Пункт 3 (Pages deploy-lag) — не моя
находка по существу, вопрос владельцу.

**АРТЕФАКТ:** артефакта нет: продукт этого захода — код (два systemd-юнита не заводились, правился
один существующий unit + один python-модуль + тесты), не собранный файл. Живое доказательство
вместо артефакта: `spetsmat-storozh-sajta.timer` включён и самоподдерживается на
`159.194.254.52` (`systemctl list-timers`, `journalctl -u spetsmat-storozh-sajta.service`),
подтверждено независимым верификатором.

**РОД АРТЕФАКТА:** исходник — закоммичено раньше отчёта, хэши ниже.

**НЕОБРАТИМОЕ:**
- Discard моей же несохранённой правки локального `docs/index.html` фикстуры в тестовом контуре —
  нет, не применимо (правок в `docs/` не делал).
- ЛОКАЛЬНО: `docs/ADRES-SAJTA.txt` в рабочем worktree был отредактирован на фиктивный хост для
  локального теста критерия 5 и восстановлен командой в той же сессии тем же ходом — восстановление
  подтверждено `git diff` (пусто) до коммита; ничего не потеряно (файл не мой, зона read-only, но
  временная правка была чисто локальной и обратимой).
- ЖИВОЙ СЕРВЕР: `deploy/ADRES-SAJTA.txt` в `/opt/spetsmat-bot` был на несколько десятков секунд
  перезаписан фиктивным адресом (`127.0.0.1:1`) для живой проверки критерия 5; восстановление из
  бэкапа (`cp /tmp/adres-backup.txt ...`) было отложено `set -e`, прервавшим скрипт сразу после
  вывода ALARM, — заметил и восстановил вручную в течение той же минуты, до следующего события
  (таймер был выключен весь этот период, поэтому ни один реальный прогон файл в фиктивном
  состоянии не использовал). Восстановлено содержимое подтверждено (`math-kluychiki.ru`, как было).
  Чем восстановить, если что-то упущено: содержимое было `math-kluychiki.ru` (одна строка), это же
  значение закоммичено в `deploy/ADRES-SAJTA.txt` в git.
- Остальное необратимого нет: ни `rm`, ни `git reset`/`checkout` поверх чужого несохранённого,
  ни правок за пределами зоны.

**КОММИТ:** `e0287b4` — "storozh-pomnit: watchdog state survives ProtectSystem=strict, save
failures visible"; `997780e` — "storozh-pomnit: cover the write-failure-is-visible fix with two
tests". Оба влиты в `main` без конфликтов (`e0a8dba`) и вывезены (`git_zona.py vyvezti --yes`
для `zahod/storozh-pomnit`; ветка на origin не существовала до этого — теперь существует, 0
невывезенного). `git_zona.py check --zone ops/` → ✅ · `--zone deploy/` → ✅ · `--zone tests/` → ✅
(проверено и в worktree, и повторно из главной папки после слияния).

**Гит-гигиена, факты (главная папка, после слияния и вывоза своей ветки):**
- вне git: 14 путей — все чужие (см. «Что НЕ трогал» выше), 0 моих.
- невлитых `zahod/*`: 0 (`git branch --no-merged main` — пусто).
- невывезенных на СВОЕЙ ветке: 0 (`git log --oneline @{u}.. ` на `zahod/storozh-pomnit` = 0).
- `main` впереди `origin/main`: 3 коммита (`e0a8dba 997780e e0287b4`) — НЕ моя ветка, не вывожу
  сама; заявка подана: `2026-09-08T2039-main-3-origin-main-e0a8dba-997780e`.
- пост-проверка из главной папки (`/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot`, не из
  worktree): `pytest tests/ops/test_storozh_sajta.py -q` → 19 passed; `grep STATE_DIRECTORY
  ops/storozh_sajta.py` и `grep StateDirectory= deploy/spetsmat-storozh-sajta.service` оба нашли
  строки — механизм реально встал в main, не только в ветке. ЗЕЛЁНАЯ, откатывать не пришлось.
- worktree снята (`git_zona.py worktree drop storozh-pomnit --force` — единственные грязные пути
  внутри были те же 2 чужих файла, не моя работа, ничего своего не потеряно).

## ПРАВКИ ПОСЛЕ ВЫДАЧИ — (заполняет АНАЛИТИК; исполнитель ЧИТАЕТ)
> 🔴 **Пусто — значит заход не правился с момента выдачи.** Непустой блок читается ПЕРЕД продолжением работы: правка отменяет любое противоречащее ей место выше по файлу, каким бы категоричным оно ни было.
> **Форма строки — жёсткая, по ней судит приёмка:** `### ПРАВКА N · ГГГГ-ММ-ДД ЧЧ:ММ · <что изменилось, одной фразой>`, дальше — что именно перечитать и что откатить, если уже сделано по старой редакции.
> **Аналитик:** внёс правку — обязан ОТДЕЛЬНО послать владельцу короткое сообщение для пересылки исполнителю. Правка, лежащая только в файле, до работающего исполнителя не доезжает: он файл не перечитывает сам.
> **Исполнитель:** прочитал правку — назови её номер в `## ОТЧЁТ` строкой `ПРАВКИ ПРОЧИТАНЫ: 1, 2`. Нет строки при непустом блоке = отчёт не принимается: неизвестно, по какой редакции работали.

### ПРАВКА 1 · 2026-09-08 · РАЗОВОЕ расширение git-контура: довезти работу соседнего захода `pages-i-materialy`

Это НЕ вторая задача и не расширение зоны. Три шага внутри §0.1 ГИТ-КОНТУР, который и так про состояние репозитория. Делаются ПЕРВЫМ ходом, вместе с гашением таймера, и на этом заканчиваются.

**Что случилось.** Заход `pages-i-materialy` закончил работу, закоммитил и вывез свою ветку `zahod/pages-i-materialy` (2 коммита: `cfa5475`, `37aa1e0`), но влить её в `main` не смог: `vlit-v-osnovnuyu` отказал, потому что в ГЛАВНОЙ рабочей папке лежит незакоммиченный `docs/index.html`. Исполнитель не знал, чья это правка, и правильно не стал гадать — подал заявку `2026-09-08T1931`.

🔴 **Аналитик разгадал, и блокер снят — доказательство приводится, чтобы ты не проверял заново.** Грязный `docs/index.html` — это **порождённый вывод рендерера сайта**, а не ручная работа. Тот же CSS лежит ЗАКОММИЧЕННЫМ в `veb/obshchee/karkas.py`, строка 1522, дословно `.blok-listok{{margin:4.5rem 0 0;width:max-content;max-width:min(34rem,100%);`. Файл переписан прогоном 07.09 в 13:47 и просто не закоммичен. **Источник цел и в git, влитие не уносит ничего.**

**ШАГИ:**
1. Влить ветку: `git_zona.py vlit-v-osnovnuyu zahod/pages-i-materialy --zone docs/ --zone veb/ --zone tests/`. Мешает грязный `docs/index.html` — это тот самый порождённый вывод: его версия с ветки и есть нужная. Разрешаешь в пользу ВЕТКИ.
2. Опубликовать Pages: `git_zona.py opublikovat` (без `--yes` — предпросмотр, посмотреть, что уедет; потом с `--yes`).
3. Закрыть заявку `2026-09-08T1931` — `zayavka-zakryt --rezultat "..."`, назвав, чем разрешился блокер.

**ПРОВЕРИТЬ ЖИВЬЁМ и напечатать, это часть правки, а не пожелание:**
- `https://d1-d57.github.io/spetsmat-179/` уводит на живой сайт, и НАЗВАТЬ, на какой схеме приземлился браузер. 🔴 Pages отдаётся только по https, живой сайт держим на http; Chrome с «Всегда использовать безопасные соединения» может поднять адрес обратно в https и вернуть зависимость от 443, которую убирали заходом `bystro-i-bezopasno`. Приземлился на https — сказать прямо и НЕ выдавать за успех.
- На отданной странице данных нет: греп по HTML — фамилий `0`, кабинетов (`303`, `302`, `203`) `0`, слова «Распределение» `0`, подстроки `/vhod` `0`. Числа печатаются.
- Три PDF из `docs/listki/` и `docs/listki-8kl/` по прямым ссылкам отдают `200`.
🔴 Замер до публикации, снятый аналитиком: живая страница ВСЁ ЕЩЁ отдаёт кабинеты `303 · 302 · 203`, «Вася», «Полина». Это состояние «до». Если после публикации оно не изменилось — публикация не доехала, и это надо сказать, а не списать на кэш.

**Границы.** Ничего в `docs/` и `veb/` ты не ПРАВИШЬ — только вливаешь чужое готовое и публикуешь. Своя задача про сторожа не меняется ни на строку. Не прошло по внешней причине — не чини чужое состояние: строка в `## ОТЧЁТ`, и дальше по сторожу.

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `принято` — диагноз аналитика заход не принял на веру, а подтвердил тремя живыми фактами; починил, не ослабив защиту; нашёл по дороге настоящий системный тупик, которого никто не заказывал; и каждую неполноту назвал сам, вместо того чтобы округлить до зелёного.

**ЧТО ПЕРЕГНАЛА ПРИЁМКА СВОИМ КАНАЛОМ (не по отчёту):**

| утверждение | чем перегнано | сошлось |
|---|---|---|
| правка 1: слияние `pages-i-materialy` доехало в git | `raw.githubusercontent.com/.../main/docs/index.html` → отдаёт НОВУЮ страницу-переадресацию: `<title>Спецмат</title>`, `meta http-equiv="refresh"` на `http://math-kluychiki.ru/`, `link rel="canonical"`, `location.replace`, видимая ссылка для тех, у кого скрипты выключены. Данных на ней нет | ✅ |
| зона доехала в git | `git_zona.py check` по `ops/`, `deploy/`, `tests/` — все три ✅ | ✅ |
| «живая страница Pages не обновилась — лаг публикации» | 🔴 **перегнала и УТОЧНИЛА: это не лаг.** Заголовки живой страницы: `last-modified: Tue, 08 Sep 2026 16:15:35 GMT` — 19:15 МСК, тогда как слияние прошло в 19:51 МСК, а замер сделан в 20:55. Pages не пересобиралась больше часа ПОСЛЕ нашего коммита. И это не кэш: `age: 11`, `cache-control: max-age=600` — GitHub отдаёт свежий ответ со старым содержимым | уточнено |
| сторож помнит состояние, таймер живёт сам | 🔴 **не перегнала:** ssh из песочницы недоступен. Принято по отчёту и независимому верификатору, который читал тот же журнал сам | не проверено, названо |

**ОХВАТ ПРИЁМКИ: перегнано 3 утверждения из 3, перегоняемых без доступа к серверу; одно из них исправлено по существу.**

**ЧТО ПРИЁМКА СЧИТАЕТ СИЛЬНЫМ.**
1. **Диагноз не принят на веру.** Заход получил от аналитика готовый разбор с указанием строк и всё равно проверил на живой машине тремя фактами: установленный юнит совпадает с закоммиченным; файла состояния нет; ручной `systemctl start` не создаёт его и печатает «первый запуск» каждый раз. Диагноз подтверждён, а не пересказан. Это ровно то поведение, которое просит клауза «не подыгрывай» — и работает она одинаково, подтверждая гипотезу или опровергая.
2. **Починка не ослабила защиту.** `ProtectSystem=strict` остался строгим; состояние переехало в `StateDirectory=` — каталог, который systemd создаёт под `/var/lib/` и который переживает и активации, и перезагрузку. Соблазн был снять `strict` одной строкой.
3. **`save_state` перестал молчать — и это сделано до конца.** Неудачная запись печатается строкой, и `main()` возвращает `1` даже при живом сайте, с формулировкой «run degraded: проверка сайта выше может говорить ЖИВ, но состояние НЕ записано». Тот же закон, по которому этот же сторож чинил врущий текст тревоги, применён к его собственной памяти. Два регрессионных теста.
4. 🔴 **Найден настоящий системный тупик, которого никто не заказывал.** После `disable`+`enable` таймер с `OnUnitActiveSec=` без `OnCalendar=` не перевзводится: он опирается на переход состояния целевого юнита, который наблюдал сам, а запуски во время выключенного таймера не считаются — и поскольку в норме юнит запускает только таймер, это замкнутый круг. `NextElapseUSecRealtime` пуст, `SubState=elapsed` навсегда. Причина проверена прямой постановкой опыта, а не выведена. Оформлено пунктом в `deploy/README.md`, чтобы следующий не завёл на этом новый инцидент.
5. **Три неполноты названы сами, без просьбы.** Из пяти прогонов один был ручным толчком, а не чистым срабатыванием таймера. Ноль пульсов подтверждён журналом, но телеграм независимо не читался — и заход прямо пишет, что у Bot API нет метода прочитать уже отправленное, то есть называет ГРАНИЦУ доказательства, а не выдаёт прокси за факт. Вторая половина клаузы 5 («повтор молчит») живьём не переигрывалась после испуга с `set -e`.
6. **Испуг с `set -e` назван честно и полностью.** Боевой `ADRES-SAJTA.txt` кратко держал поддельное значение из-за обрыва собственного скрипта. Заход это поймал, восстановил в той же сессии и вписал в «НЕОБРАТИМОЕ», хотя никто бы не заметил: таймер был выключен всё это время, ни одна проба поддельным адресом не воспользовалась. Признание того, что сошло с рук, — самая дорогая часть отчёта.
7. `pytest` 1011 → 1013, оба числа сняты командой, +2 это его собственные новые тесты.

**ПРО ПРАВКУ 1 — СДЕЛАНА НА ДВЕ ТРЕТИ, И ОСТАВШАЯСЯ ТРЕТЬ НЕ В ЕГО ВЛАСТИ.** Слияние прошло, публикация вызвана, заявка `2026-09-08T1931` закрыта — всё это видно в git. Живая страница не обновилась, и заход честно назвал это, отдав владельцу. Приёмка уточняет диагноз: это не задержка сборки, а её отсутствие — Pages не пересобиралась ни разу после нашего коммита.
⚠ **И это снова дефект моей правки, а не его работы:** я потребовал проверить РЕЗУЛЬТАТ, наступление которого зависит от очереди сборки GitHub, то есть от третьей стороны. Пятый случай той же семьи за день — критерий, требующий того, чем исполнитель не управляет. Отдельным уроком не пишу: закономерность уже записана трижды, повторять её значит разбавлять.

**ВЕТКА РАБОТЫ:** `zahod/storozh-pomnit`
*Проверено фактом: влита в `main`, `git_zona.py check` по всем трём путям зоны ✅.*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ:**

заявок нет: ни одна из пяти операций не понадобилась. Влитие сделал сам заход последним ходом, штатно. Коммит зоны сделан им же, `check --zone` зелёный по всем трём путям. Вывоз прикрыт заявкой `2026-09-08T1713`, поставленной ещё заходом `storozh-vstal`; новой на тот же предмет не плодим. Деплой не требовался — заход синхронизировал два файла точечно, минуя `vykatka.sh`, чтобы не перезапускать бота. Гасить нечего: `git branch --no-merged main` по своей ветке пуст. Отдельно: заявку `2026-09-08T1931` (блокер влития `pages-i-materialy`) закрыл сам заход, и закрытие подтверждено по существу — слияние реально прошло, новая страница лежит в `main` на origin.

**ПУНКТЫ ВЛАДЕЛЬЦУ:**

1. 🔴 **Сборка Pages УПАЛА, и приёмка довела диагноз до шага.** Владелец прислал вкладку Actions и настройки; настройки верны (`Deploy from a branch`, `main` + `/docs`). Дальше приёмка дочитала сама через ПУБЛИЧНЫЙ API GitHub, без его прав: прогон #65 (`id 34253722634`, коммит `a0dc1e1`, 16:52 UTC) — `conclusion: failure`, и разложен по шагам:
   `Set up job → success` · `Pull jekyll-build-pages → success` · `Checkout → success` · **`Build with Jekyll → success`** · **`Upload artifact → FAILURE`** · `Post Checkout → success`.
   ⇒ **Наш файл ни при чём: Jekyll собрал его успешно.** Упал служебный шаг выгрузки артефакта. Размер исключён: новая страница-переадресация МЕНЬШЕ старой, а прогон #64 (`1288aad`, 16:15 UTC) с теми же файлами прошёл зелёным за 37 минут до этого — и именно его время стоит в `last-modified` живой страницы. Проверено также, что в новом `docs/index.html` нет ни одного `{{` и `{%`, то есть Liquid-синтаксиса, ломающего Jekyll, там нет.
   ⇒ **Лечение: перезапустить прогон.** `https://github.com/d1-d57/spetsmat-179/actions/runs/34253722634` → «Re-run all jobs». Упадёт на том же шаге второй раз — сбой не разовый, и тогда это отдельный разбор.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
2. Таймер сторожа после ручного `disable`+`enable` требует одного толчка, иначе не перевзводится. Не поломка, а свойство `OnUnitActiveSec=`; записано в `deploy/README.md`.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

**ВЕТКА РАБОТЫ:** `zahod/storozh-pomnit`
*(проверяется фактом, не словом: ветка обязана существовать и быть либо ВЛИТА в основную, либо названа в открытой заявке на влитие. Ни того, ни другого — Г14 краснеет. Снять состояние: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py poteri --branch <ветка>`)*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**
> Адрес списка: `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/_INFRA-git/zayavki`
> Читается командой (из любой папки, в том числе из worktree): `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki`
> Ставится командой: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavka --rod <git-operaciya|pravka-koda> "<текст>"`
> 🔴 Вопрос здесь НЕ «что ты хочешь сделать», а «что ты УЖЕ положил в очередь». Дубль сверяется с очередью по id машинно; намерение сверить не с чем.

*(См. блок вердикта выше.)*

*(Заявок эта приёмка не ставила — так и напиши строкой «заявок нет: <почему ни одна из пяти операций не понадобилась>». Пустая строка и прочерк не принимаются: молчание неотличимо от «забыл».)*
