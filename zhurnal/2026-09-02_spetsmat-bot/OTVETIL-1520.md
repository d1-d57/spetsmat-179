# 🟢 ОТВЕТИЛ — ВОРОТА ФАЗЫ 1 ПРОЙДЕНЫ. ВЛАДЕЛЕЦ, 02.09 15:20

**Полосу B разблокирую. Начинай.**

Прогон сделан мной в 15:16, все четыре шага, живьём:

1. голый `/start` → «Этот бот работает только по ссылке из чата класса.» ✅
2. `?start=register-student` → «Введите вашу фамилию.» ✅
3. «Яковлев» → «Теперь имя.» → «Иван» → «Заявка отправлена владельцу.» ✅
4. `/pending` → «Заявки (1): id=1: Яковлев Иван» + кнопки принять / переименовать /
   отклонить ✅

Разговор целиком прошёл без единой заминки. Это первый настоящий диалог бота с
человеком за всю волну.

---

## ТРИ НАХОДКИ ЖИВОГО ПРОГОНА — ПОЧИНИ ВМЕСТЕ С ПОЛОСОЙ B

### 1 · 🔴 `id=1` В ЛИЦЕ У ЧЕЛОВЕКА

    Заявки (1):
    - id=1: Яковлев Иван

Внутренний номер записи показан владельцу. Кнопка ниже подписана «Яковлев Иван» —
номер не добавляет ничего, кроме привкуса отладочной печати. **Это ровно тот
дефект, который ты чинил час назад** в `middleware.py` («you are a owner»), и он
живёт в другом месте: значит, дело не в одной строке, а в привычке печатать
человеку внутреннее состояние.

Прошу не просто убрать эту строку, а **пройти все тексты, которые видит человек,
одним заходом**: ищи внутренние имена ролей, английский, `id=`, `None`, имена
полей и таблиц. Судить должна модель, которая эти тексты не писала.

### 2 · «вы сможете ОТКРЫТЬ БОТ»

> Заявка отправлена владельцу. Он подтвердит её — и тогда вы сможете открыть бот.

По-русски так не говорят. «Бот откроется» либо «сможете им пользоваться».
Мелочь, но её прочитают 56 детей и 18 преподавателей.

### 3 · 🔴 О ЗАЯВКЕ МНЕ НИКТО НЕ СООБЩИЛ

Я узнал о ней только потому, что сам набрал `/pending`. Пуша не было.

На занятии это значит: ребёнок зарегистрировался, преподаватель об этом не знает
и узнает, только если догадается проверить. **Ответь прямо: это закрывает P14 из
полосы B, или уведомление о заявке — отдельная дыра, которой в мандате нет
вовсе?** Если второе — заводи и говори, чьей зоной.

### ⚠ И ВОПРОС, НА КОТОРЫЙ Я НЕ ЗНАЮ ОТВЕТА

Я зарегистрировался как ученик «Яковлев Иван», и такого ученика в каталоге из 56
нет. Кнопку «принять» я НЕ нажимал. **Что произойдёт, если нажать?** Заявка
привяжется к несуществующему ученику, создаст нового, или откажет? Проверь по
коду и скажи; если создаёт — скажи, как мне потом убрать эту запись, чтобы она не
уехала в боевую базу.

---

## ПО ФАЗАМ

**Фаза 1 закрыта целиком:** `P17`, `P10`, `P5` в `main`, бот отвечает живому
человеку. Время 15:20 — уложились в план (было «до 15:30»).

**Фаза 2 идёт, ворота прежние:** сквозной прогон завтрашнего занятия. Из трёх
входов кнопки и текст можешь изобразить сам; **фото и голос — только я**, и для
них мне нужны от тебя две вещи, обе из `KLYUCHI-VLADELCA-1500.md`:

- проведённый в код `vision` (иначе фото отвечает «не настроен»);
- окончательные имена переменных ASR, чтобы я вписал ключ SpeechKit один раз.

**Точка решения 16:00 в силе.** Сейчас я склоняюсь идти полным объёмом: полоса A
закрылась быстрее плана.

---

## ОТВЕТ ОРКЕСТРАТОРА

Отвечено 15:35. **Полоса B пущена** — `P14` и `P16` работают, `P9` продолжает.

---

## ⚠ СНАЧАЛА ВАШ ВОПРОС ПРО «ПРИНЯТЬ», ПОТОМУ ЧТО ОТВЕТ ХУЖЕ, ЧЕМ ВЫ ДУМАЕТЕ

**Не нажимайте.** Проверено по коду и по живой базе.

`on_accept` → `roster.confirm_student` → `create_confirmed_student` — то есть
**СОЗДАЁТ НОВОГО УЧЕНИКА**. Сопоставления с каталогом из 56 нет НИГДЕ: грепом по
`core/services/roster.py` и `infra/roster_repo.py` на `match|find_student|by_surname`
— ноль совпадений.

Для «Яковлев Иван» это было бы просто мусорная 57-я строка. Но вот что это значит
на самом деле, и это уже не мелочь:

> В базе есть **Пирогов Константин, 201 отметка**. Если Пирогов завтра
> зарегистрируется и вы нажмёте «принять» — бот создаст **второго** Пирогова с
> нулём отметок и привяжет телеграм к нему. Мальчик откроет бота и увидит пустой
> год. Настоящие 201 отметка останутся висеть на строке без телеграма.

То есть регистрация в нынешнем виде **отменяет весь смысл импорта прошлого года** —
пункт F6 мандата, ради которого P2 грузила 15 847 отметок. Это дыра, которой в
мандате нет: ни одна позиция не описывала привязку заявки к существующему ученику,
и потому ни одна её не сделала.

**Заведена отдельной позицией `P19` — она в полосе B, платная.** Правило простое:
регистрация ученика ПРИВЯЗЫВАЕТ телеграм к строке каталога, найденной по фамилии и
имени, и СОЗДАЁТ только если совпадений нет вовсе; при двух совпадениях — кнопки,
а не догадка. Это тот же закон, что у распознавания: заставлять машину угадывать
человека нельзя, она угадает.

**Убирать вам ничего не надо: вы не нажали.** В базе 56 учеников, Яковлевых 0 —
проверено. Заявка висит в `roster.db` как `pending` и в боевые данные не попала.

---

## ТРИ ВАШИ НАХОДКИ

### 1 · `id=1` в лице у человека — принято, и вы правы про привычку

Вы поставили диагноз точнее, чем я: дело не в строке, а в том, что человеку
печатают внутреннее состояние. Заведена позиция **`P18` — сплошной проход по всем
текстам, которые видит человек**: внутренние имена ролей, английский, `id=`,
`None`, имена полей и таблиц. Судит её **модель, которая этих текстов не писала** —
как вы и просили; я вписал это в задание отдельным требованием, а не понадеялся.

### 2 · «сможете открыть бот» — туда же, в `P18`

Согласен дословно. Это читают 56 детей и 18 преподавателей, и «открыть бот» —
не по-русски.

### 3 · О заявке никто не сообщил — 🔴 ОТВЕЧАЮ ПРЯМО: ЭТО НЕ P14

`P14` — уведомления **после занятия**: преподавателю, не отметившему ничего,
вопрос «были ли вы»; старшему — сводка. Регистрация там не упоминается ни словом.

**Это отдельная дыра, и в мандате её нет вовсе.** Чья зона: по смыслу владельца
(`bot/handlers/owner.py`), но по механизму — общая с `P14`, потому что и то и другое
есть «бот пишет первым». Отдал её `P18`-й позиции? Нет: положил в **`P19`** вместе
с привязкой, потому что обе про один и тот же путь регистрации и трогают один файл.
Иначе два захода полезут в `owner.py` одновременно — ровно та ошибка, от которой я
вас предостерегал час назад про `bot/app.py`.

---

## ПО ФАЗЕ 2

**Фото и голос сейчас НЕ РАБОТАЮТ, и вы это нашли раньше меня.** Подтверждаю оба
ваших замера числом — подробности в ответе на `KLYUCHI-VLADELCA-1500.md`, коротко:
`vision` действительно не кладётся в `workflow_data` (в `app.py` его нет), а имена
переменных ASR в коде и в шаблоне разные. Обе починки — в `P20`, тоже полоса B.

Значит для ваших ворот фазы 2 порядок такой: **сначала `P20`, только потом ваш
прогон фото и голоса.** Раньше не пробуйте — получите «не настроен» и потеряете
время.

**Список команд для `/setcommands`** — ниже, по факту того, что реально
зарегистрировано в коде (проверено грепом, не по памяти):

```
me - кто я и что мне доступно
god - мои плюсы за год
dolgi - мои ближайшие долги
setka - отметить сдачу задач
spiski - кто молчит и какие задачи не взяты
auditoria - экран аудитории для старшего
pending - заявки на регистрацию
```

⚠ `/peek` в список не кладите: это отладочная команда ученика, её саму надо
пересмотреть в `P18`.

**Точка 16:00.** Поддерживаю полный объём: полоса A закрылась в 15:20 против плана
15:30, на `main` 645 тестов. Но честно предупреждаю: три новые позиции (`P18`,
`P19`, `P20`) — это не «сверх плана», это дыры, найденные вашим живым прогоном, и
без `P19` завтрашнее занятие проводить нельзя: первая же регистрация настоящего
ученика раздвоит его.
