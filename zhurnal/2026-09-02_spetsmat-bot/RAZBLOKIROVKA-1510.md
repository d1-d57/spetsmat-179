# РАЗБЛОКИРОВКА — ВЛАДЕЛЕЦ, 02.09 15:10

Полоса A закрыта, вижу сам: `P17` (`3620e0a`), `P10` (`e210297`), `P5` (`66c1a8f`)
в `main`. Хорошая работа, особенно P5 — 168 подделок, 0 пробоев.

## 1 · 🔴 ТЫ НЕ ПРОЧИТАЛ ФАЙЛ, КОТОРЫЙ Я ОСТАВИЛ В 15:00

`zhurnal/2026-09-02_spetsmat-bot/KLYUCHI-VLADELCA-1500.md` — секция ответа пуста.
Прочитай его сейчас, там находка, которая важнее всей полосы B.

Коротко, чтобы ты понял срочность до чтения: **`vision` потребляется в
`bot/routers/photo.py:346` и не кладётся в `workflow_data` ни в `app.py`, ни где
бы то ни было в боевом коде — только в `tests/photo/conftest.py`.** Если я прав,
фото прямо сейчас отвечает «Разбор фото не настроен», при принятой P7.

## 2 · ЧТО ДЕЛАТЬ ПРЯМО СЕЙЧАС, НЕ ДОЖИДАЯСЬ МЕНЯ

Запрет на полосу B остаётся: ворота — мой живой прогон, я иду его делать сейчас,
ответ будет в течение получаса.

**Но починка `vision` — это НЕ полоса B.** Это дефект на уже принятой позиции,
вскрытый после приёмки, и он не ждёт ворот. Начинай его немедленно:

1. подтверди или опровергни находку замером;
2. проведи ключ модели из окружения в `workflow_data`;
3. сведи имена переменных ASR к одному варианту — в коде (`infra/asr.py:245-246`)
   и в шаблоне `bot.env.example`. Скажи, какие имена побеждают: я по ним буду
   вписывать ключи, и вписывать дважды не хочу;
4. пройди списком все зависимости роутеров: сколько потребляется, сколько
   подставляется в бою. Ответ числом.

## 3 · ЧТО У МЕНЯ ЕСТЬ ИЗ КЛЮЧЕЙ — ЧТОБЫ ТЫ ПЛАНИРОВАЛ ПО ФАКТУ

- **OpenRouter — десять долларов на счету УЖЕ**, аккаунт не нулевой. Потолок
  тысяча запросов в сутки, а не пятьдесят. Ключ в `secrets/bot.env` лежит.
- **Gemini** — беру бесплатный ключ сейчас. У него бесплатный тир порядка 15
  запросов в минуту и полутора тысяч в сутки на Flash-Lite, то есть для наших
  двадцати фото за занятие денег не нужно вовсе. Скажи, какой `LLM_PROVIDER` и
  `LLM_MODEL` вписывать, когда ключ будет.
- **Yandex SpeechKit** — завожу. Платится рублями, 0,16 ₽ за 15 секунд, пробный
  месяц. Это единственный путь к голосу, который у меня вообще есть: зарубежный
  счёт мне пополнять нечем, и это ограничение, а не предпочтение.
- **ALERT_TOKEN** — завожу второго бота у BotFather сейчас.

## 4 · СЕРВЕР — РЕШЕНО, БЕРУ РОССИЙСКИЙ

Не только из-за рублей: на машине будут фамилии 56 детей. Ubuntu LTS, 2 vCPU,
2 ГБ, 20 ГБ. Пиши выкатку под неё и назови прямо, что сломается при переезде с
моего Mac. Долг «боевая база не промигрирована» — туда же, ты его уже вписал.

---

## ОТВЕТ ОРКЕСТРАТОРА

<здесь>
