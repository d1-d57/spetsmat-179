# ЖУРНАЛ ОРКЕСТРАТОРА — волна НОЧЬ-2, 11.09.2026

Форма строки круга (пункт 8 мандата):
ЧЧ:ММ · круг N · запущено · принято · без вердикта · main зелёный (число) · выкачено до · не в git · молчит сердце (мин) · ЗАКРЫТО X из 8

## СОСТАВ, КАК ОН ЛЕЖИТ НА ДИСКЕ

Восемь рабочих позиций — восемь файлов-заходов, собранных 11.09 00:01–00:12.
Модель каждой взята ИЗ ШАПКИ ЕЁ ЗАХОДА и здесь НЕ дублируется (пункт 4).

V1 gejt-vse-ekrany · V2 zhurnaly-i-kabinet · V3 konduit-i-raspredelenie · V4 offlajn-ochered
V5 melochi-i-dovoz · V6 nadzor-zvonit · V7 dannye-07-09 · V8 shirina-prep-vdn
плюс ZK1 zakon-zakrytiya-volna-noch2 — закрывающая, последним ходом.

🔴 НАХОДКА КРУГА 1, ЦЕНА — ПОЗИЦИЯ ЦЕЛИКОМ. Файл VOLNA-NOCH2-SOSTAV.txt перечисляет
СЕМЬ позиций, V1–V7. Заход `kod_shirina-prep-vdn.md` собран (00:12, бесплатная модель
в шапке), а строки в составе у него нет. Мандат в разделе SCALE говорит «eight positions,
V1–V4 on Opus, V5–V8 on free models» — то есть восьмая предусмотрена, и это ровно она:
ширина поля принимающего, единственное место дизайна по F6. Описание V3 в составе
дотягивает «плюс ширина поля принимающего» — но правит её отдельная позиция в отдельной
рабочей папке. Оркестратор ведёт волну по ВОСЬМИ и дописывает строку V8 в состав.

## КРУГИ

00:23 · круг 1 · запущено 1 (V1) · принято 0 · без вердикта 1 · main зелёный (не мерян, гейт ещё не встал) · выкачено до — · не в git: 8 заходов + состав + этот журнал (коммичу этим же кругом) · сердце волны ещё не поднято, часовой не запущен · ЗАКРЫТО 0 из 8
    V1 gejt-vse-ekrany поднят 00:22:42, рабочая папка spetsmat-bot-wt/gejt-vse-ekrany,
    ветка zahod/gejt-vse-ekrany. Один и первым: остальные семь трогают то, что владелец
    судит глазами, принимать это должно чем-то, что не молчит (пункт 1).
    Веток zahod/ не влитых в main на входе: 0 (`git branch --no-merged main | grep -c zahod/`).

00:39 · круг 2 · запущено 1 (V1) · принято 0 · без вердикта 1 · main зелёный (гейт ещё строится, числа нет) · выкачено до — (push копится до конца волны, решение владельца) · не в git: правки заходов, коммичу этим кругом · сердце било в 00:32:59, молчит 6м · ЗАКРЫТО 0 из 8
    ОТВЕТ ВЛАДЕЛЬЦА, три уточнения — разнесены по домам, каждое в ПРАВКИ ПОСЛЕ ВЫДАЧИ своего захода:
    1. V7 dannye-07-09 локально НЕВОЗМОЖНА. Замер оркестратора одним ходом 00:36:
       `ls -d /srv/spetsmat/data` → No such file or directory · `SPETSMAT_BAZA` не выставлена ·
       в ~/.ssh/config ни одного Host кроме `Host *` · туннеля в процессах нет ·
       `python3 core/istochnik.py` → «ИСТОЧНИК НЕ НАЗВАН».
       СЛУЧАЙ ВТОРОЙ ИЗ ДВУХ, названных владельцем: боевого доступа НЕТ, значит V7 не вносит
       ничего и говорит об этом строкой. Заходу вписано: `--da` не запускать вовсе, прогнать
       ПРОБУ и вставить вывод дословно, а ночь потратить на то, что от боевой базы не зависит —
       черновик кондуита 07.09 с источником у строки, готовые команды внесения строка-в-строку
       для утреннего прогона на сервере, и F8 «дверь понятна человеку».
    2. V1 и V8: мерить локально, принимать глазами на боевом. Гейту вписано требование к
       устройству: базовый адрес принимается СНАРУЖИ (переменная или аргумент), localhost не
       вшит, — иначе последнего хода волны (боевой прогон тем же гейтом, оба набора чисел рядом)
       физически не существует. У обеих позиций затребован ОХВАТ рядом с числом.
    3. Пароль владельца не понадобится ни одной позиции. Общая правка дописана во ВСЕ восемь
       заходов: попросит пароль — это ошибка понимания, останов и строка в отчёт.
    🔴 ЧЕСТНО О ЦЕНЕ ПРАВКИ ДЛЯ V1: она дописана в 00:38, а V1 поднят в 00:22 и файл уже прочитал.
    Канала в запущенный `claude -p` у оркестратора нет; перезапуск с --dobor стёр бы 16 минут
    работы (коммитов у неё пока нет, в 00:38 правит tools/gejt_verstki.py). Решение: НЕ трогаю,
    требование проверяю на приёмке. Окажется адрес вшитым — это добивка на одну правку, дешевле
    перезапуска. Правка остаётся в файле свидетельством, что требование было названо, а не забыто.

## ДОЛГ НА УТРО — ВНЕ ВОЛНЫ, НЕ ПОТЕРЯТЬ

🔴 ПАРОЛИ ШКОЛЬНИКОВ НЕ ВЫПУЩЕНЫ НИ ОДНОМУ, И ПУТИ НА СЕРВЕР У НИХ НЕТ.
Названо владельцем 11.09, ПЕРЕГНАНО ОРКЕСТРАТОРОМ ОДНИМ ХОДОМ 00:44 (оба конца сверки сняты вместе):
  * `ls -l secrets/veb-lichnye-paroli.json` → файл от **07.09 04:03**, 3749 байт, права 600;
  * структура: ключ `lyudi` — список из **14** записей; это преподаватели;
  * `grep -c shkolnik secrets/veb-lichnye-paroli.json` → **0**. Ни одного школьника в файле нет;
  * `deploy/vykatka.sh:97-98` → в массиве `NE_VYKATYVAEM` стоит `--exclude 'secrets/'`.
ЗНАЧИТ ДВЕ ДЫРЫ, А НЕ ОДНА: механизм влит 10.09, но (а) выпуск по школьникам не прогонялся ни разу,
(б) даже выпущенные пароли на боевой не попадут — выкатка исключает `secrets/` целиком, а как они
туда должны попадать, не решено НИГДЕ. Вторая дыра дороже первой: она делает первую бессмысленной.
Владелец в волну не добавляет — восьми хватает. Разбирать утром, отдельным заходом.

00:46 · круг 3 · запущено 1 (V1) · принято 0 · без вердикта 1 · main зелёный (тесты пошли в фоне, число будет кругом 4) · выкачено до origin/main = HEAD (2 коммита вывезены 00:45:36) · не в git: пусто · сердце било 00:39, молчит 6м · ЗАКРЫТО 0 из 8
    РЕШЕНИЕ ВЛАДЕЛЬЦА ИЗМЕНЕНО: push не копится, вывозим кусочками. Довод владельца дословно:
    коммит на одной машине переживает смерть модели, но не смерть машины. Ветки позиций вывожу
    свободно по первому их коммиту; main — только зелёным, иначе ломается `worktree add` следующей.
    ВЫВОЗ ГЛАВНОЙ ПОТРЕБОВАЛ ОБХОДА, и это находка: дверь отказалась вывозить main из-за ОТКРЫТОЙ
    заявки. Прочитала не первую, а ВСЕ (пункт 11): открытых 24, все от 10.09, ни одной от 11.09;
    трое из них (0532, 0622, 1236) сами ПРОСЯТ вывезти main — то есть дверь загораживала ровно то,
    о чём её же почта просила. Обход `--vsyo-ravno` с письменной причиной; замер зелёности честный
    и узкий: `git diff --name-only origin/main..main | grep -c '.py$'` → 0, сегодняшние коммиты
    трогают только markdown волны. Заявки о вывозе закрою, когда волна перестанет их плодить.
    V1 без коммита 23 минуты: процесс жив (`ps` → 1 живой `claude -p`), в рабочей папке правится
    tools/gejt_verstki.py. Пачку держу: правило 1 говорит ждать ПЕРВОГО КОММИТА, а не признаков жизни.

00:56 · круг 4 · запущено 8 из 8 · принято 0 · без вердикта 8 · main зелёный (прогон пошёл заново, первый упал не тестами: у pytest нет плагина --timeout) · выкачено до origin/main = HEAD + ветка zahod/gejt-vse-ekrany на origin · не в git: пусто · сердце noch2 било 00:53, молчит 3м · ЗАКРЫТО 0 из 8
    🔴 ПОПРАВКА ВЛАДЕЛЬЦА В МАНДАТ, ПРИНЯТА: правило 1 читалось как «остальные ждут коммита V1»,
    а смысл его другой — ГЕЙТ СТОИТ НА ПРИЁМКЕ, НЕ НА СТАРТЕ. Порядок старта свободный;
    ПРИНИМАТЬ любую из V2-V8 можно только после того, как принята V1. Волна стояла в очереди за
    самой большой позицией 32 минуты — цена моего буквального чтения, не владельца.
    ПОДНЯТЫ 00:54:12-00:54:19 семь позиций одной пачкой, ветки заведены, процессы живы:
    claude -p 6 шт (V1-V4), opencode 4 шт (V5-V8). Модели брала ИЗ ШАПОК заходов грепом,
    а не из головы и не из своих записей — пункт 4.
    ОСНАСТКА ВОЛНЫ ПЕРЕЕХАЛА (аналитик, 00:47). Я била в SERDCE-VOLNY-noch.md — сердце МЁРТВОЙ
    волны УТРО; часовой мерил по нему же, поэтому его «молчит 0м» относилось не к нам. Теперь:
    сердце SERDCE-VOLNY-noch2.md, часовой CHASOVOJ-VOLNY-noch2.sh поднят 00:53 (PID 38173),
    ДВА старых часовых погашены (17424, 37736) — они караулили покойника и четыре часа звали
    владельца вхолостую. V6 дописана ПРАВКА 2: чинить файлы С ДВОЙКОЙ, старые не трогать вовсе.
    🔴 ЛОЖНО-ЗЕЛЁНОЕ, ПОЙМАНО ЖИВЬЁМ И СТОИЛО БЫ ВСЕЙ РАБОТЫ V1. Коммитя чужую рабочую папку с
    `GIT_ZONA_REPO`, указывающей на ГЛАВНЫЙ репозиторий, получила «✅ Зона чиста — коммитить
    нечего» при двух изменённых файлах в worktree: инструмент судил главную папку, а не ту, где
    работа. Лечится тем, что `GIT_ZONA_REPO` = САМА рабочая папка. Тот же класс, что зона-префикс:
    зелёное, снятое не с того объекта. Правило волны: коммитя за позицию, ставь переменную на её папку.
    V1: 31 минута без коммита → сохранена оркестратором (2f44cf1, tools/gejt_verstki.py +
    tests/gejt/test_gejt_verstki.py), ветка вывезена на origin. Дальше держу правило владельца:
    двадцать минут без коммита — сохраняю за позицию.
    Фоновый сторож первого коммита V1 отработал и поймал МОЙ коммит, а не её — как свидетельство
    работы позиции он бесполезен, снимаю его с учёта и сужу по содержимому рабочих папок.

01:16 · круг 5 · запущено 8 из 8 · принято 0 · без вердикта 8 (две отработали, ждут приёмки за V1) · main зелёный: НЕТ, число ниже · выкачено до: пять веток позиций на origin, main держу до замера · не в git: пусто (нашла и сохранила две пропажи, см. ниже) · сердце noch2 било 00:59, молчит 17м — бью этим же ходом · ЗАКРЫТО 0 из 8
    🔴 MAIN НЕ ЗЕЛЁНЫЙ, И ПЕРВЫЙ ЗАМЕР НЕВАЛИДЕН — ОБА ФАКТА НАЗЫВАЮ СРАЗУ.
    Прогон 00:57→01:07:11 в ГЛАВНОЙ папке дал `29 failed, 1266 passed, 62 skipped, 1 xfailed,
    43 errors in 617.93s`. Верить этому числу нельзя: пока он шёл, V5 и V7 влили свои ветки
    ПРЯМО В ЭТО ДЕРЕВО (мержи c841f95 и 01391ae). Замер снят с движущегося объекта — ровно тот
    класс, за который 10.09 объявили дефект, которого не было. Перезамер идёт на ФИКСИРОВАННОМ
    дереве: отдельная рабочая папка `zamer-noch2`, ветка `zamer/noch2-01391ae`, старт 01:15:38.
    Пока число не получено, main на origin НЕ вывожу — правило владельца «main только зелёным».
    ДВЕ ПОЗИЦИИ ОТРАБОТАЛИ САМИ, ВЛИЛИСЬ И ЖДУТ ВЕРДИКТА (принимаю после V1, как велел владелец):
      V7 dannye-07-09 — merge c841f95, поверх 9a43f89 «F8 fix: human-readable errors and hints in
        vnesti_s_bumagi.py». Рабочую папку снесла за собой (worktree drop), отчёт дописала в свой
        kod_-файл. То есть по боевой базе она послушалась запрета и ушла в то, что от неё не зависит.
      V5 melochi-i-dovoz — merge 01391ae, поверх 540b39c (довоз трёх doc/ от 06.09) и b54c457
        («Елена» → «Лена» в seed/teachers.csv, karkas.py, shkolniki.py). Движок ещё жив — вероятно
        доделывает слово ПРИХОДИТ.
    🔴 ДВЕ ПРОПАЖИ ИЗ GIT, НАЙДЕНЫ И ЗАКРЫТЫ ЭТИМ КРУГОМ, ОБЕ — СВИДЕТЕЛЬСТВА, А НЕ КОД:
      1. ВЕСЬ МАНДАТ ВОЛНЫ. `mandate_volna-noch2.md` собран в 00:16, волна идёт с 00:22 — закон,
         по которому она живёт, полтора часа существовал только на диске одной машины. Сохранён.
      2. ОСНАСТКА noch2 целиком: часовой, будильник, сердце, подъёмный лист, лог пульса.
         Заведены аналитиком в 00:47, часовой работает с 00:53 — и ничего из этого в git не было.
    ПОПРАВКА К СВОЕМУ ЖЕ ХОДУ, ЧЕСТНО: сохраняя V6 nadzor-zvonit «29 минут без коммита, три файла
    изменены», я приняла за её работу три НЕОТСЛЕЖЕННЫХ файла оснастки noch2, видных в её папке
    потому, что их не было в main. Её работы там не было вовсе. Коммит безвреден (оснастку всё
    равно надо было сохранить), но счёт «позиция работает» по нему ставить нельзя: у V6 на
    01:16 коммитов своих 0. Урок мой, не её: грязное в папке ≠ работа позиции, смотреть ЧТО именно.
    Вывезены на origin ветки: gejt-vse-ekrany (4 коммита), zhurnaly-i-kabinet (1),
    konduit-i-raspredelenie (2), offlajn-ochered (1, мой за неё), melochi-i-dovoz (влита).

01:34 · круг 6 · запущено 8 из 8 · принято 0 · без вердикта 8 (три отработали: V5, V7 сами; V6 идёт) · main зелёный: замер на фиксированном дереве ещё идёт (19-я минута, первый занял 10) · выкачено до: все шесть живых веток на origin, main держу · не в git: пусто · сердце noch2 било 01:17, молчит 17м — бью этим ходом · ЗАКРЫТО 0 из 8
    🔴 ТРЕТИЙ СЛУЧАЙ ОДНОГО КЛАССА ЗА НОЧЬ, И ТЕПЕРЬ ЭТО ПРАВИЛО, А НЕ СЛУЧАЙНОСТЬ:
    `git_zona.py commit --zone .` ответил «✅ Зона `.` чиста — коммитить нечего» при ТРЁХ
    изменённых файлах в той же папке (V1, 01:32). До этого: `--zone` с префиксом `.../kod_`
    не совпал ни с чем; `GIT_ZONA_REPO` на главный репозиторий судил не ту папку. Общее у всех
    трёх — ЗЕЛЁНОЕ, СНЯТОЕ НЕ С ТОГО ОБЪЕКТА, и каждое выглядит как успех. Правило волны:
    зону разворачивать в ПОЛНЫЕ ПУТИ из `git status --short`, никаких `.` и префиксов.
    Оформлено скриптом /tmp/sohrani.sh, чтобы правило исполнялось, а не помнилось.
    СОСТОЯНИЕ ПОЗИЦИЙ НА 01:34 (коммиты свои, без моих сохранений — где отличаются, сказано):
      V1 gejt-vse-ekrany   5 коммитов, последний свой «an empty screen on non-empty data is red» —
                           гейт уже ловит пустой экран на непустых данных; жива, сохранена мной в 01:33
      V2 zhurnaly-i-kabinet 2 коммита, «the tabs were nephews, not siblings — both tables were
                           invisible always»: вкладки журналов были племянниками, а не братьями,
                           и обе таблицы не показывались НИКОГДА. Жива, сохранена мной в 01:33
      V3 konduit-i-raspredelenie 5 коммитов, идёт по правкам О1-О4 канона «нельзя смешивать», жива
      V4 offlajn-ochered   3 коммита, «a refused edit survives instead of being lost» — отказ
                           переживает перезагрузку, ровно цель позиции; жива
      V5 melochi-i-dovoz   ОТРАБОТАЛА, влита (01391ae), папку снесла
      V6 nadzor-zvonit     жива, своих коммитов 0 за 40 минут — на бесплатной это тревожный знак,
                           слежу отдельно
      V7 dannye-07-09      ОТРАБОТАЛА, влита (c841f95), папку снесла
      V8 shirina-prep-vdn  жива, коммитов 0, но работает ПО СУЩЕСТВУ: в логе виден замер живого
                           правила `.org.pr-sel { max-width: 9.6rem }` и computedMax 121.6px —
                           то есть меряет ширину на поднятом сервере, а не рассуждает о коде

01:44 · круг 7 · запущено 8 из 8 · принято 0 · без вердикта 8 (V5, V7 влиты и погашены) · main зелёный: 🔴 НЕТ по замеру, но замер был снят НЕ ТЕМ СПОСОБОМ, см. ниже · выкачено до: шесть веток на origin; main 13 коммитов держу до честного числа · не в git: пусто · сердце noch2 било 01:34, молчит 10м · ЗАКРЫТО 0 из 8
    ЗАБРАНА В GIT РАБОТА АНАЛИТИКА — 22 пути одним коммитом: PLAN.md (переписан), SESSIYA.md
    с отсечкой разнесения, SOSTOYANIE.md, TZ-DOBOR-10-09.md (L-R), REFLEKSIA-ORKESTRATOROV.md
    (классы 22-35), UROKI-FABRIKE.md (уроки 50-60), правки после выдачи в восьми kod_*.md,
    следы часового и сердец, INCIDENTY.md. `.DS_Store` (два) и `scratchpad/verifikator/` НЕ забраны.
    ВЫГРУЗКА ТРАНСКРИПТА `VYGRUZKA-2026-09-11.md`, 138 783 байта — репозиторий ПУБЛИЧНЫЙ, поэтому
    проверена мной отдельно от проверки аналитика, восемь паттернов, все по нулям: телеграм-токен ·
    sk-/ghp_/AKIA/Bearer · BEGIN PRIVATE KEY · password= со значением · sk-or-/sk-ant- · содержимое
    secrets/veb-lichnye-paroli · хеши bcrypt/pbkdf2/sha256. Слово «парол» встречается 4 раза —
    все четыре ВОПРОСЫ владельца («Пароль для личных кабинетов школьников — сделали или нет?»),
    ни одного значения. Забрана.
    БЭКЛОГ ВЕБ-ЧАСТИ ПЕРЕЕХАЛ В НАШ РЕПОЗИТОРИЙ: `doc/OCHERED-veb.md`, отдельным коммитом.
    Перенос побайтовый и это ПРОВЕРЕНО, а не заявлено: md5 исходника d9a324f88519ab84044a868026bb9994
    совпал с md5 первых 36 899 байт копии; дописан только комментарий о происхождении. Требование
    владельца от 08.09 («список дел должен быть в НАШЕМ артефакте») закрыто на третьи сутки.
    ⚠ Расхождение в счёте, называю, а не заминаю: в задании 225 позиций, сам файл в шапке говорит
    «Найдено 220 позиций (116 + 104)». Содержание не трогала, разбирать потом — цифра не моя.
    🔴 ГЛАВНОЕ ЗА КРУГ: КРАСНОЕ MAIN ОКАЗАЛОСЬ МОИМ, А НЕ КОДА. Замер на фиксированном дереве дал
    те же `29 failed, 1266 passed, 43 errors` (1136 с). Пошла смотреть ПРИЧИНУ ошибки, а не число:
    `ERROR at setup ... config.IstochnikNeNazvan: 🔴 ИСТОЧНИК НЕ НАЗВАН: переменная среды
    SPETSMAT_BAZA не выставлена`. То есть 43 ошибки — это отказ ДВЕРИ ИСТОЧНИКА, честный и
    правильный, вызванный тем, КАК Я ЗАПУСКАЛА ТЕСТЫ. Объяви я «main красный, 43 ошибки» — это
    был бы пятый за ночь случай замера не с того объекта, и на этот раз мой собственный.
    Перезамер идёт с базой, заведённой штатно: 12 миграций в `.zamer/spetsmat.db`, переменная
    выставлена. До его числа main на origin не вывожу.
    ПОПРАВКА ВЛАДЕЛЬЦА ПРИНЯТА И ВШИТА В ИНСТРУМЕНТ: считать влитость `merge-base --is-ancestor`,
    а «папки нет» читать как «влита и погашена», а не «умерла». У влитой позиции `main..zahod/X`
    равен нулю ПО ПОСТРОЕНИЮ — именно так V5, V7 и V8 выглядели бы бездельницами.
    V8 shirina-prep-vdn: 56 минут без коммита, но в логе виден живой замер — она поднимает сервер
    и читает настоящий `<select class="org pr-sel" data-org=... data-slot=...>`. Работа идёт,
    следа в git нет. Если к 02:05 правки не появится — перезапуск с --dobor, не смена модели.

02:01 · круг 8 · запущено 8 из 8 · принято 0 · без вердикта 8 · ВЛИЛИСЬ САМИ: V3, V5, V6, V7 (четыре из восьми) · main зелёный: число есть, но без базовой линии оно НИЧЕГО НЕ ЗНАЧИТ, см. ниже · выкачено до: семь веток на origin, main 18 коммитов держу · не в git: пусто · сердце noch2 било 01:49, молчит 12м · ЗАКРЫТО 0 из 8
    ВЛИЛИСЬ ЗА КРУГ, СВЕРЕНО ПО main, А НЕ ПО ОТЧЁТАМ:
      V3 konduit-i-raspredelenie — merge ddb7af7, поверх шести правок канона:
         Q4 «рычаг для нельзя смешивать — одна клетка, один смысл», O3 (инициалы принимающего
         выехали из клетки фамилии в свою колонку; колонка получила разделительную черту),
         O4 (пустой гробарий пуст, без прозы), O5 (не строить подсказку для галочки, которой
         не будет). То есть пять правок канона из пяти названных плюс ширина — у неё же.
      V6 nadzor-zvonit — d457504 «add death alarm to CHASOVOJ-VOLNY-noch2.sh — skazat_vladelcu
         call on heart silence >= MERTVA_MIN (45m), flag to dedupe alerts». Часовой ЗВОНИТ.
         Позиция, которую я час считала бездельницей, чинила ровно ту дыру, из-за которой
         владельца четыре часа звали вхолостую. Правку в файл С ДВОЙКОЙ она приняла.
    🔴 ЧЕСТНЫЙ ЗАМЕР MAIN ПОЛУЧЕН И ОКАЗАЛСЯ ХУЖЕ ПРЕЖНЕГО, А НЕ ЛУЧШЕ:
      без SPETSMAT_BAZA: 29 failed · 1266 passed · 62 skipped · 43 errors (1136 с)
      с SPETSMAT_BAZA:   73 failed · 1269 passed · 15 skipped · 43 errors (572 с)
    Число упавших выросло ВТРОЕ именно потому, что я «починила» запуск: без переменной 47 тестов
    молча пропускались, с ней они побежали — и упали на моей ПУСТОЙ базе из двенадцати миграций.
    Сорок три ошибки не ушли вовсе. Вывод, который я обязана сделать вслух: у меня до сих пор НЕТ
    права сказать «main красный по вине волны» — я не знаю, сколько красного было ДО неё. Поэтому
    запущен третий замер, и он единственный осмысленный: ТА ЖЕ команда, ТА ЖЕ пустая база, но
    дерево на коммите 457342e — состояние main ДО первого коммита волны. Сравнение будет
    яблок с яблоками: два числа, снятые одним способом, разница между ними и есть вклад волны.
    Пока базовой линии нет, main на origin не вывожу и «зелёный/красный» не объявляю. Это не
    осторожность, это шестой за ночь случай одного класса — число, снятое не с того объекта.
    V8 shirina-prep-vdn: 73 минуты без коммита. Сохранила за неё (b06033f): в karkas.py ОДНА
    строка правки — ровно тот масштаб, который заказан (одно правило CSS, левый край на месте).
    В логе видно, что она читает живой `<select class="org pr-sel" data-slot=...>` и цитирует
    жалобу владельца про обрезанные имена. Работа настоящая, темп бесплатной модели — медленный.
    Перезапуск с --dobor НЕ делаю: он стоил бы часа работы ради строки в git, которую я только что
    сохранила сама. Правило «двадцать минут — сохраняю» и есть ответ на медленную модель.

02:17 · круг 9 · запущено 8 из 8 · принято 0 · без вердикта 8 · ВЛИЛИСЬ САМИ: V2, V3, V5, V6, V7 (пять из восьми) · работают: V1, V4, V8 · main: базовая линия ЕСТЬ, число ниже · выкачено до: семь веток на origin, main 27 коммитов держу до замера 3 · не в git: пусто · сердце noch2 било 02:01, молчит 16м · ЗАКРЫТО 0 из 8
    🔴 БАЗОВАЯ ЛИНИЯ ПОЛУЧЕНА, И ОНА ПЕРЕВОРАЧИВАЕТ ВЧЕРАШНИЙ ВЫВОД. Одна команда, одна пустая
    база из двенадцати миграций, два дерева:
      ДО ВОЛНЫ   (457342e): 79 failed · 1271 passed · 15 skipped · 43 errors (327 с)
      В ВОЛНЕ    (01391ae): 73 failed · 1269 passed · 15 skipped · 43 errors (572 с)
    Волна не сломала main — она УМЕНЬШИЛА красное на ШЕСТЬ упавших тестов. Сорок три ошибки
    одинаковы в обоих деревьях: это ДОЛГ, доставшийся от прошлых волн (отказ двери источника при
    отсутствии боевой базы), а не вклад ночи. Если бы я объявила «main красный, 73 падения» без
    базовой линии — это была бы ровно та ложь, за которую 10.09 заплатили четырьмя ложными
    докладами владельцу. Число без второго конца сверки не значит НИЧЕГО.
    Замер 3 запущен на АКТУАЛЬНОМ main 249ffee (в нём уже V2, V3, V6) той же командой и той же
    базой: только он даёт право вывезти main. Порог «зелёного» на эту ночь называю заранее, чтобы
    не подгонять его под результат: НЕ ХУЖЕ 79 упавших и НЕ БОЛЬШЕ 43 ошибок.
    ВЛИЛАСЬ ЗА КРУГ V2 zhurnaly-i-kabinet — merge 249ffee, и в ней видна редкая честность:
      d40ea04 «journal, cabinet: two defects the §3 verifier found in this заход's own work» —
      позиция позвала независимого верификатора, ПОЛУЧИЛА от него два дефекта в собственной
      работе и починила их, а не спрятала;
      f7fbbf0 «revert docs/index.html to what main has: this заход never meant to touch it» —
      сама заметила, что залезла за границу зоны, и откатилась.
    Осталось трое: V1 gejt (9 своих коммитов, жива), V4 offlajn (9 своих, жива), V8 (1, жива).
    Приёмку не начинаю: правило владельца — сначала принять V1, гейт стоит на приёмке.
