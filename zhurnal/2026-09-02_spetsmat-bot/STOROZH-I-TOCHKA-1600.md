# СТОРОЖ ЗАВЕДЁН · ТОЧКА 16:00 · ОДНА ПРОЦЕДУРА В КАНОН — ВЛАДЕЛЕЦ, 02.09 15:40

⚠ `RAZBLOKIROVKA-1510.md` не читай, он устарел — всё, что там было, ты уже сделал.

---

## 1 · 🟢 СТОРОЖ ЗАВЕДЁН И ПРОВЕРЕН ЖИВЬЁМ

`@watchman57_bot`. `ALERT_TOKEN` вписан в `secrets/bot.env`, права `600`.

Проверено не «файл заполнен», а тремя ответами Telegram подряд:

    getMe        → ok:true · username watchman57_bot
    sendMessage  → ok:true · message_id 3
    и сообщение ДОШЛО ко мне в Telegram — я его прочитал глазами

Третья строка тут главная: она разом доказывает, что токен верный, что я нажал
боту `/start` и что он умеет писать мне первым. Ни один из первых двух ответов
этого по отдельности не доказывает.

**Что это меняет для тебя:** долг `OnFailure` из `P10` больше не висит в воздухе —
адресат уведомления существует и отвечает. Проверь, что `ustanovka.sh` читает
именно `ALERT_TOKEN` из `secrets/bot.env`, и сними долг, если он закрыт.

## 2 · ТОЧКА 16:00 — РЕШЕНИЕ: ИДЁМ ПОЛНЫМ ОБЪЁМОМ

Подтверждаю. Основание — факт, а не прогноз: полоса A закрылась в 15:20 против
плана 15:30, `P17` и `P10` приняты 18 из 18, на `main` 645 тестов, и я своими
руками провёл живой диалог с ботом.

Твоё предупреждение про `P18`, `P19`, `P20` принято дословно: это не расширение
плана, а дыры, вскрытые живым прогоном. **`P19` не режется.** Резерв на рез
прежний и в прежнем порядке: `P16`, потом `P9`.

## 3 · 🔴 В КАНОН: ПРОЦЕДУРА «ОДНА КОМАНДА, КОТОРУЮ ВЛАДЕЛЕЦ ВСТАВЛЯЕТ ЦЕЛИКОМ»

Сегодня мы нащупали форму, которая работает, и я хочу её закрепить, а не
вспоминать в следующий раз. Дом — `disciplina/skills/kak-zavesti-telegram-bota`,
у него уже есть половина про long polling и урок про токен в чате.

**Форма.** Владельцу даётся ОДНА команда, копируемая целиком, без предисловий, и
она обязана делать все шесть вещей сама:

1. спросить секрет приглашением (не заставлять открывать редактор);
2. обрезать пробелы — вставка из мессенджера тащит хвост;
3. показать, что именно принято, и проверить формат регулярным выражением;
   при непопадании — НЕ ПИСАТЬ НИЧЕГО и сказать это вслух;
4. вписать в `secrets/bot.env` и поставить права `600`;
5. **доказать, что секрет рабочий, вызовом наружу** — `getMe`;
6. **доказать доставку человеку** — `sendMessage` себе, и человек читает её
   глазами в Telegram.

Шаги 5 и 6 — суть формы. Без них «секрет записан» это утверждение о файле, а не
о работе, и оно зелёное ровно так же, как пустой `BOT_TOKEN` был зелёным.

**Три ловушки с ценой, все три сегодняшние:**

- 🔴 **`read -r -p` — это bash. У владельца zsh, где `-p` означает корутину**, и
  команда падает с `read: -p: no coprocess`. Цена: один потерянный круг. Верная
  форма — `printf 'приглашение: ' && read -r T`.
- 🔴 **Видимый вывод секрета + привычка присылать скриншот терминала = секрет в
  переписке.** Это ВТОРАЯ форма вчерашнего урока «токен в чат»: вчера его прислали
  текстом, сегодня он уехал картинкой, и спусковым крючком был мой же видимый
  вывод, сделанный по просьбе владельца — чтобы ловить опечатки. Владелец,
  оценив ущерб (сторож умеет только писать ему), **решил не перевыпускать
  токен — решение зафиксировано как его, а не как недосмотр.** Лечение формы, а
  не человека: печатать МАСКУ — первые 14 символов, хвост и длину. Опечатку она
  ловит, целиком секрет на экран не выводит.
- **`git check-ignore -v secrets/` в конце той же команды.** Молчание
  `git status` не доказывает ничего: оно молчит и когда файл просто не добавлен.

Заведи это позицией или впиши в скилл сам — как чище по зонам, решай ты;
`disciplina` твоя чужая зона на эту волну, так что если трогать её сейчас нельзя,
положи долгом с точной формулировкой, чтобы не пересобирать по памяти.

## 4 · ЧТО У МЕНЯ В РАБОТЕ

- SpeechKit — завожу, жду от `P20` окончательных имён переменных;
- `/setcommands` — оформлю твоим списком;
- Gemini не беру, довод про OpenAI-совместимый эндпоинт принят.

---

## ОТВЕТ ОРКЕСТРАТОРА

Отвечено 15:55. `RAZBLOKIROVKA-1510.md` не открывал.

## 1 · СТОРОЖ: ДОЛГ ЗАКРЫТ, ПРОВЕРЕНО НЕ ФАЙЛОМ

`getMe` вашим `ALERT_TOKEN` → `ok: True · @watchman57_bot`. Отвечает.

**Долг `OnFailure` закрыт по построению, а не доработкой:** юнит читает файл
целиком — `deploy/spetsmat-bot.service:40` → `EnvironmentFile=@CHECKOUT@/secrets/bot.env`,
а `ustanovka.sh:139` прямо называет `ALERT_TOKEN` в списке обязательных. То есть
`P10` была написана так, что появление токена её чинит само. Снимаю долг.

**И ваша третья строка — про то, что сообщение ДОШЛО и вы прочитали его глазами —
сильнее моей проверки.** `getMe` доказывает токен. `sendMessage → ok:true`
доказывает, что API принял. Только прочитанное глазами доказывает, что вы нажали
боту `/start` и он имеет право писать вам первым — а без этого сторож молчит именно
в тот момент, когда нужен. Забираю это себе как форму.

## 2 · ТОЧКА 16:00 — ПРИНЯТО, ИДЁМ ПОЛНЫМ ОБЪЁМОМ

**Все три новые позиции заведены и пущены**, зоны сверены посегментно с живыми:
**0 пересечений из 101 пары.**

| | что | почему сейчас |
|---|---|---|
| **`P19`** | регистрация ПРИВЯЗЫВАЕТ к каталогу, а не создаёт дубль; владелец узнаёт о заявке сразу | без неё завтра нельзя: первая же регистрация раздвоит ребёнка |
| **`P20`** | ключ распознавания доходит до бота; имена ASR сводятся к одному дому | блокирует ваш прогон фото и голоса |
| **`P18`** | сплошной проход по текстам, судит модель, которая их не писала | собираю следующей, чтобы не было трёх писателей в один файл |

Сейчас в работе шесть окон: `P9` · `P14` · `P16` · `P19` · `P20` и ваш бот.

В `P19` вписал именованными тестами ровно ваши случаи: «Пирогов Константин»
привязывается к строке с 201 отметкой, два совпадения дают КНОПКИ, а не догадку, и
охват печатается числом с обязательным «дублей 0».

В `P20` главным сделал не починку `vision`, а **гейт на класс**: тест сам обходит
`bot/**`, собирает всё, что берётся через `data.get()` мимо сигнатуры, вычитает
положенное в `app.py` и падает, если остаток непуст. Починить один `vision` мало —
дыра вернётся со следующим таким доступом, и снова будет невидима тестам.

## 3 · ПРОЦЕДУРА В КАНОН — КЛАДУ ДОЛГОМ, И ВОТ ПОЧЕМУ ИМЕННО ТАК

`disciplina` мне сейчас чужая зона: там прямо в эту минуту работают заходы соседних
волн, а `skills/kak-zavesti-telegram-bota/SKILL.md` — файл, который они уже правили
сегодня (коммит `94b0ceb`). Два писателя в один файл — то, чего я не позволяю
позициям своей волны и не позволю себе.

**Заявка заведена с ПОЛНЫМ текстом процедуры, а не с напоминанием «дописать».** Всё
шесть шагов, обе ловушки с ценой и готовая рабочая форма — чтобы тот, кто это
внесёт, не пересобирал по памяти. Три вещи оттуда я забираю и себе:

🔴 **Первая, и она шире телеграма.** «Секрет записан» — утверждение о ФАЙЛЕ. Оно
зелёное ровно так же, как был зелёным пустой `BOT_TOKEN` при 486 тестах. Доказывает
работу только вызов наружу, а доставку человеку — только прочитанное человеком.
Это тот же закон, что вы поставили воротами фазы 1, и сегодня он окупился дважды.

🔴 **Вторая — про `read -r -p`.** Записал с вашей ценой: у вас zsh, где `-p` это
корутина, и команда падает `read: -p: no coprocess`. Рабочая форма —
`printf 'приглашение: ' && read -r T`. Это ровно тот класс, что уже дважды бил
волну: **среда проверки не равна среде прогона** (у меня локаль `C`, у окон
`ru_RU.UTF-8`; у меня bash, у вас zsh).

🔴 **Третья — про маску, и она мне неприятна, потому что спусковой крючок был мой.**
Видимый вывод секрета я предложил сам, чтобы вы ловили опечатки, — и он уехал
картинкой. Вчера токен утёк текстом, сегодня скриншотом; лечится форма, а не
человек: печатать первые 14 символов, хвост и длину. Опечатку маска ловит, секрет
на экран не выводит. Ваше решение не перевыпускать сторожа зафиксировано как ваше.

## 4 · ЧТО ОТ МЕНЯ ЖДЁТЕ

- **имена переменных ASR** — назову в отчёте `P20`, одним явным списком, чтобы вы
  вписали SpeechKit один раз;
- **`/setcommands`** — список отдан в `OTVETIL-1520.md`, собран грепом по живому коду;
- **фото и голос** — пробуйте ТОЛЬКО после того, как я скажу, что `P20` принята.
  Раньше получите «не настроен» и потеряете время.
