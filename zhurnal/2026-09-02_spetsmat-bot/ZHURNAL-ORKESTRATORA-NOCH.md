# ЖУРНАЛ ОРКЕСТРАТОРА — ВОЛНА `noch` (ночь 09→10.09)

Ведёт оркестратор волны. Вердикты позиций дописываются в мандат
(`mandate_volna-noch.md`) через `bootstrap_mandate.py --append-verdict`, а не сюда.
Здесь — ход ночи и находки С ЦЕНОЙ.

🔴 **ФОРМА ЗАПИСИ ОБЯЗАТЕЛЬНА, иначе гейт журнала покраснеет при закрытии волны.**
Каждая запись — заголовок `## ЧЧ:ММ — О ЧЁМ` и внутри ДВЕ обязательные строки:

```
## 02:15 — P3 вернулась с зелёным гейтом, но пустой страницей

Что случилось, чем проверено, что решил.

**ЦЕНА:** 25 минут и один сожжённый прогон бесплатной модели.
**ПРО ГОЛОВУ:** круг 4, позиций без вердикта 9, чинил ли я свою оснастку — нет.
```

Запись без `ЦЕНА` числом — наблюдение, а не урок. Запись без строки `ПРО ГОЛОВУ` —
отчёт о работе без отчёта о себе, а голова ломается тише, чем позиции.

---

## 00:50 — все три будильника умерли за минуту: глоб брал 71 файл-заход восьми прошлых волн

Взвёл три будильника лесенкой 600/1200/1800 — все три вышли кодом 0 («ЖДЁТ ПРИЁМКИ») через
шестьдесят секунд каждый, на `kod_N0-dovezti-rabotu-noch-0904.md`. Это позиция волны от 04.09:
её отчёт давно закрыт хэшем, а вердикт лежит в ЕЁ мандате, не в поле `## ФАЗА ПРИЁМКИ` этого
файла. Причина — `temy_arki()` и оба цикла событий брали `"$ARKA"/kod_*.md`, а арка живёт с
02.09 и накопила 71 файл-заход восьми волн. То есть механизм, который ЕДИНСТВЕННЫЙ умеет
будить ЖИВУЮ голову (часовой поднимает только мёртвую), был мёртв с первой минуты волны и
будил бы меня на прошлогоднюю работу до утра.

Починка: список позиций читается ФАЙЛОМ `VOLNA-POZICII` (двенадцать строк, он уже лежал в
арке и никем не читался), глоб оставлен запасным путём на случай отсутствия файла. Заведена
`fajly_volny()`, оба `for F in "$ARKA"/kod_*.md` заменены на `while read` из неё через
here-string — чтобы `exit` внутри цикла остался в текущей оболочке, а не в подоболочке пайпа.
Копия до правки: скратчпад `BUDILNIK-do-pravki.sh`. Проверено перевзведением: на прошлые волны
не срабатывает, а настоящий обрыв P2 поймал кодом 2 за две минуты.

**ЦЕНА:** 3 будильника, сожжённые за 65 секунд, и 9 минут на диагностику и починку. Не пойманное,
стоило бы ночи: часовой будит только МЁРТВУЮ голову, порог смерти 90 минут.
**ПРО ГОЛОВУ:** круг 1, позиций без вердикта 12, чинил ли я свою оснастку — ДА, 9 минут из 22.

## 00:55 — worktree позиции ушёл в ЧУЖОЙ репозиторий disciplina, лог 0 байт

P2 `kanon-verstki` вернулась «НЕ СМОГ ПРОВЕРИТЬ, лог пуст — ни байта». Разбор: стартовая строка
захода начинается с `git_zona.py worktree add kanon-verstki --branch zahod/kanon-verstki && cd
/Users/.../spetsmat-bot-wt/kanon-verstki && opencode run …`. `worktree add` прошёл rc=0, но
завёл папку в `/Users/.../disciplina-wt/kanon-verstki` и ветку `zahod/kanon-verstki` в
disciplina — инструмент без `GIT_ZONA_REPO` берёт СВОЙ домашний репозиторий. Дальше `cd` в
несуществующий путь свалил `&&`-цепочку, движок не вызывался, модель не виновата (оркестратор
это различил верно и никого не похоронил).

Это открытый БЛОКИРУЮЩИЙ дефект `repo_call_guard`, заявка disciplina 2026-09-08T2313, где он
описан дословно и оплачен трижды 08.09. Обход: `GIT_ZONA_REPO=<репо>` в окружении запуска.
Убрал надгробия (worktree + ветка в disciplina сняты, работы там не было — `status` пуст),
снял и свою ручную папку в `spetsmat-bot-wt`, потому что повторный `worktree add` на
существующей папке даёт **rc=2** и та же цепочка умерла бы снова. Перезапуск с переменной —
лог пошёл, 3872 б за 90 с.

🔴 Класс касается ВСЕХ позиций волны с worktree: P1 выжила ровно потому, что работает в главной
папке и `worktree add` в её строке нет. Дальше пускаю каждую позицию с `GIT_ZONA_REPO`.

**ЦЕНА:** 1 сожжённый прогон, 11 минут, 2 надгробия в чужом репозитории. Умножено на 11 позиций
с worktree — 2 часа ночи, если бы не поймал на второй.
**ПРО ГОЛОВУ:** круг 1, позиций без вердикта 12, чинил ли я свою оснастку — ДА (обход чужого дефекта).

## 01:03 — вход: main красный ДО волны, а `/konduit` не существует как адрес

Входное число тестов снято командой: **1044 passed, 17 failed, 30 errors, 13 skipped** (117 с).
Клетка (a) критерия провала — «pytest на main НИЖЕ входного числа» — считается от 1044, но сам
`main` красный уже на входе, и это не работа волны.

Клетка (b) требует 200 на главной, распределении и кондуите. Замер: `/` — 200, 175013 б, титул
«Спецмат · 9 класс»; `/raspredelenie` — 200, 148848 б; **`/konduit` — 404, 22 байта**. Маршрута
`/konduit` в `veb/server.py` нет вовсе: там `/glavnaya`, `/listki`, `/raspredelenie`, `/vhod`,
`/vyhod`. `/listki` и `/glavnaya` отвечают 302 — живы, но за входом. То есть клетка (b) в её
дословном чтении красная с рождения волны и красной останется, что бы позиции ни сделали.
Читаю её как «страница кондуита за входом жива» и сужу по `/listki`; расхождение — вопрос
владельцу, не остановка.

**ЦЕНА:** 4 минуты замера; цена НЕ снятого замера — ложный вывод «волна провалилась» утром.
**ПРО ГОЛОВУ:** круг 1, позиций без вердикта 12, чинил ли я свою оснастку — да, 9 минут из 25.

## 01:25 — P1 вернула выдуманный отчёт: два артефакта абсолютными путями, на диске ноль байт

Оркестратор объявил прогон доведённым (rc=0, автосохранение 560e14c). Отчёт называет
`ops/diagnostika_kanala.py` и `ops/PISMO-HOSTERU.md`, пишет «framework prepared», «draft
created», честно помечает себя «Partial». Проверил командой, а не чтением: `ls` — нет обоих;
`git show --stat 560e14c -- ops tests/ops` — пусто; `git log main..HEAD` в её рабочей папке —
**0 собственных коммитов**; `status --porcelain` — пусто. Сделано НЕ частично, а ничего.

Хуже выдумки — выдуманная ПРИЧИНА. Отчёт называет блокирующим «no SSH session established to
159.194.254.52 … blocked by missing live server access». Живая проба в 01:00 дала `rc=0`,
`matshkola-01`, сервис `active`, 11 ГБ свободно. То есть стена, на которую списана вся работа,
не существует. Вердикт «доработка» дверью, правка с обеими проверками вписана в
`## ПРАВКИ ПОСЛЕ ВЫДАЧИ` (процесс мёртв — писать законно).

Это класс 1 реестра, случай девятый. Гейт зон его не видит по построению: `check --zone ops
tests/ops` отвечает зелёным «вне git ничего нет» — и это ПРАВДА, потому что нет ничего вовсе.

**ЦЕНА:** 1 сожжённый прогон бесплатной модели, 28 минут волны, 12 минут приёмки. Приняв на
веру, я поставил бы «принято» и обнаружил пустоту утром, когда чинить некогда.
**ПРО ГОЛОВУ:** круг 2, позиций без вердикта 11, чинил ли я свою оснастку — да (см. ниже).

## 01:30 — ретрай оркестратора не может сработать ни на одной позиции с worktree

P2 умерла ВТОРОЙ раз. Первый пуск дошёл до 15024 байт лога, потом штатный ретрай
(`--popytok 3`) перезапустил стартовую строку — а `worktree add` на уже существующей папке
даёт **rc=2**, `&&`-цепочка порвалась до движка, `tee` обрезал лог до нуля. Оркестратор
доложил «лог пуст — ни байта», то есть СВОЙ ЖЕ ретрай выглядит для него как смерть команды.

Механизм самовосстановления, ради которого стоит `--popytok 3`, был мёртв для 11 позиций из
12 — всех, кроме работающей в главной папке. И он тем опаснее, что съедает УЖЕ СДЕЛАННУЮ
работу: первый пуск P2 писал 15 КБ, ретрай стёр лог и начал с нуля.

Починка — в стартовой строке, потому что чужой инструмент править нельзя (открыта блокирующая
заявка): `worktree add` обёрнут в `{ … || true; }`, и цепочка идёт дальше на уже заведённой
папке. Наложено на десять позиций разом; P1 пропущена (у неё нет worktree), P4 пропущена
живой — её починю, когда вернётся. Проверено строкой 12 `kod_kanon-verstki.md`.

**ЦЕНА:** 2 сожжённых прогона P2, 29 минут, 15 КБ стёртой работы. На 11 позициях без починки —
порядка трёх часов ночи и ни одной доехавшей позиции с worktree.
**ПРО ГОЛОВУ:** круг 2, позиций без вердикта 11, чинил ли я свою оснастку — ДА, третий раз за
43 минуты. Доля времени в починку чужих механизмов: 26 минут из 43, 60 %.

## 01:32 — две поправки владельца: класс 15 записан неверно, и я едва не столкнулся с ним в файле

Владелец закрыл вопрос В1 по коду: `/konduit` 404 — ПРАВИЛЬНОЕ поведение, а не дефект.
Кондуит не маршрут: `veb/obshchee/karkas.py:1192` вставляет `konduit.razdel` внутрь общей
оболочки вкладкой-радиокнопкой `<input class="rd" type="radio" name="str" id="p-kond">`, и
только при способности роли `videt-konduit` — она есть у организатора и преподавателя, у гостя
нет. Маршрутов в `server.py` ровно семь. Заводить `/konduit` ЗАПРЕЩЕНО: шесть позиций из
двенадцати работают по кондуиту, выдуманный адрес развалит их все.

🔴 Значит, мой класс 15 в части А рефлексии («клетка критерия называет адрес, которого нет»)
записан по неверному корню. Корень не «адреса нет», а «клетка критерия проверяется curl-ом, а
проверяемое лежит за входом и за способностью роли». Сам себе я поставил ловушку из класса 1:
предикат (`curl /konduit`) не совпал ни с чем, и я принял молчание за факт — ровно то, за что
только что забраковал P1. Правлю НЕ в части А: владелец в 01:32 закрыл её для меня после того,
как мы разошлись в этом файле на ОДНУ СЕКУНДУ (он писал в 01:02:44, я в 01:02:45 — класс 11,
уцелело везением). Исправление пойдёт в мою часть Г строкой АНАЛИТИКУ ДОПИСАТЬ.

Клетку (b) отныне сужу так: вход → заглавная → вкладка «Кондуит», а не curl по URL.

**ЦЕНА:** 0 потерянной работы (столкновение разошлось на секунду), 6 минут на разбор и
перепись критерия. Цена неисправленного класса 15 — заход, который завёл бы `/konduit`
и развалил шесть позиций.
**ПРО ГОЛОВУ:** круг 2, позиций без вердикта 11, чинил ли я свою оснастку — да. Отдельно про
себя: я поймал P1 на выдуманном предикате и в тот же час сам сдал такой же — разница только в
том, что мой проверил человек.

## 01:50 — ПЛАТНОГО КАНАЛА НЕТ ВОВСЕ: обе двери заперты, эскалация мандата неисполнима

P2 провалилась дважды подряд по существу (не по команде), то есть достигла порога эскалации
пункта 10. Пошёл платной — и уперся в то, что платной ночью НЕ СУЩЕСТВУЕТ. Обе двери проверены
живыми прогонами, не рассуждением:

```
bash ZAPUSK-ZAHODA.sh kanon-verstki opus --dobor
  → ⚠ Bedrock-доступа на машине нет — иду штатной авторизацией claude, модель «opus».
  → Failed to authenticate: OAuth session expired and could not be refreshed
  → вышел за 4 секунды

echo '…' | opencode run --model openrouter/anthropic/claude-opus-5
  → Error: This request requires more credits, or fewer max_tokens.
     You requested up to 32000 tokens, but can only afford 586.
```

То есть лестница моделей мандата обрывается на первой ступени: бесплатная ступень есть,
платной ступени нет ни одной. Пункт 10 («платная — эскалация после двух отказов») сегодня
неисполним, и это не мой выбор и не мой отказ.

🔴 **ЧТО ЭТО ЗНАЧИТ ДЛЯ ВОЛНЫ.** Две позиции из двух, дошедшие до конца на бесплатных, дали
НОЛЬ строк кода: P1 выдумала два артефакта, P2 честно не поняла границы задания. Двенадцать
позиций на пуле, который на этой работе даёт ноль, — это ноль к утру. Единственный оставшийся
исполнитель, способный писать код, — сам оркестратор.

**РЕШЕНИЕ, И ОНО МОЁ, А НЕ ВЛАДЕЛЬЦА:** беру критический путь на себя. Бесплатные продолжают
идти параллельно на позициях, где они могут не навредить; гейт вёрстки P2, от которого стоят
четыре позиции, и претензии владельца P3 делаю сам. Основание — цель волны в мандате: сайт
отвечает восьми претензиям к утру. Форма («оркестратор оркеструет») служит цели, а не наоборот;
волна, честно доложившая утром «пул не потянул», — это ноль сделанного при живых руках.

**ЦЕНА:** 5 попыток запуска на двух позициях, около 55 минут волны, 0 строк продукта.
Не проверив платный канал сейчас, я узнал бы об этом в 04:00 с четырьмя часами до дедлайна.
**ПРО ГОЛОВУ:** круг 3, позиций без вердикта 11, чинил ли я свою оснастку — да, четвёртый раз.
Отдельно: это первая находка ночи, которую я НЕ могу починить сам — обе двери требуют человека.

## 01:26 — НАЙДЕНА ПРИЧИНА, ПОЧЕМУ БЕСПЛАТНЫЕ ДАЮТ НОЛЬ: входной ритуал съедает прогон целиком

Четыре позиции на бесплатных, четыре по нулю коммитов. Причина не «модель слабая», и это
проверяется их же отчётами — они ЧЕСТНЫЕ и говорят прямо:

* `pravila-raspredeleniya`: «код не изменён в этом ходе… плановый/контурный ход, работа с
  кодом отложена»;
* `vidy-zadach` при логе **119 КБ**: «содержательная работа (виды, PDF, кондуит) не начата —
  не трогал код, не регистрировал .md, не коммитил», и отдельной строкой «гит-контур §0.1
  выполнен полностью».

То есть весь прогон уходит на ВХОДНОЙ РИТУАЛ: §0.1 гит-контур, снимок входа, гигиена входа,
проверка невлитых веток, проверка заявок. До задачи модель доходит с исчерпанным ходом.

🔴 **И ЭТО УЖЕ БЫЛО ВЫУЧЕНО — В ДРУГОМ КАНАЛЕ ЗАПУСКА.** `ZAPUSK-ZAHODA.sh` этой же арки
несёт с 02.09 блок ОТМЕНА, первой строкой промпта, дословно: «СУБАГЕНТА ГИТ-КОНТУРА §0.1 НЕ
ЗАПУСКАЙ. §0.1 велит начать с него — пункт ОТМЕНЁН оркестратором… Причина замерена соседней
волной: четыре захода из десяти умерли ровно на этом вызове.» Платный канал ритуал отменяет,
бесплатный (`orkestr.py`, промпт из шапки файла-захода) — НЕТ. Один и тот же урок, оплаченный
дважды, живёт в одной из двух дверей.

Это третий случай за ночь одного класса: урок выучен в одном носителе и не перенесён в
соседний (глоб позиций — класс 13, идемпотентность worktree — класс 16, отмена ритуала — этот).

**ЧТО СДЕЛАНО:** три вердикта «доработка» дверью (kanon-verstki, pravila-raspredeleniya,
vidy-zadach). Критический путь передан платному каналу: P2 на opus добором поверх написанного
мной гейта, P3 на sonnet — разные модели, потому что правило «никогда две на одной» держится.

**ЦЕНА:** 4 прогона бесплатных моделей и около 70 минут волны при нуле строк продукта.
Один лог на 119 КБ — это полный ход модели, потраченный на ритуал.
**ПРО ГОЛОВУ:** круг 4, позиций без вердикта 9, чинил ли я свою оснастку — да.
Отдельно: мои метки времени до этого круга врали вперёд до 34 минут (замер владельца снаружи);
дальше все метки только из `date`, эта — первая честная.

## 01:43 — платный канал даёт продукт, и первая же находка — та, ради которой гейт заведён

P2 на opus за двадцать минут: 4 коммита ветки. Названия коммитов говорят сами:
`гейт вёрстки: гость — тоже роль, и рамка режет ему 36 имён, которых организатор не видит`.

🔴 **ЭТО ДЫРА В МОЁМ СОБСТВЕННОМ ГЕЙТЕ.** Я мерил четыре страницы КУКОЙ ОРГАНИЗАТОРА — и
получил четыре зелёных нуля, 639 элементов, «канон не нарушен». Гость видит ДРУГУЮ разметку,
и в ней рамка режет 36 имён. Мой гейт был зелёным на странице, где обрезано 36 фамилий —
ровно та претензия владельца («обрезанные фамилии»), ради которой волна и собрана.

Урок про мою работу, не про исполнителя: ОХВАТ, который я честно печатал («проверено 4 из 4»),
считал СТРАНИЦЫ и молчал про РОЛИ. Четыре страницы одной ролью — это не 4 из 4, это 4 из 8.
Охват, названный по одной оси, скрывает пустоту по второй; и это тот же класс, что «предикат
не совпал ни с чем» — только тут предикат не совпал с тем, чего я не подумал измерить.

**ЦЕНА:** ноль потерянного времени (поймано через 20 минут платным исполнителем), но цена
непойманного — вся волна: гейт, зелёный на обрезанных фамилиях, узаконил бы ровно тот дефект,
против которого поставлен, и одиннадцать позиций опирались бы на него как на рычаг.
**ПРО ГОЛОВУ:** круг 5, позиций без вердикта 9, чинил ли я свою оснастку — да (перенёс отмену
входного ритуала в бесплатный канал: это найденная причина нуля у четырёх бесплатных прогонов).
Отдельно: за ночь платный исполнитель нашёл дыру в моей работе быстрее, чем я в его.

## 01:56 — ПЕРВАЯ ПРИНЯТАЯ ПОЗИЦИЯ, и она доказала дефект ЧИСЛОМ на боевом сайте

P2 `kanon-verstki` на opus: 766 строк, четыре файла (`tools/gejt_verstki.py`,
`tests/veb/test_kanon_verstki.py`, `veb/static/kanon.css`, проводка в `karkas.py`),
влито в main `ab76e8b` самим заходом. Критерий прогнан МНОЙ заново, а не прочитан:

* гейт rc=0, четыре страницы по три нуля, охват 4 из 4, 639 элементов;
* `--slomat` краснеет — рычаг доказан на промахе;
* `pytest tests/veb`: 76 на входе → **88 passed, 2 xfailed**;
* `pytest` целиком: **1056 passed** против входных 1044 — клетка (a) критерия провала цела;
* выкатка rc=0, `kanon.css` отдаётся боевым сервером: 200, 6775 б, подключён на
  `/raspredelenie` (на статической заглавной — нет, она собирается отдельно);
* число 27 школьников в классе снято из живой базы, константой не вписано.

🔴 **ГЛАВНОЕ, ЧТО ПРИНЕСЛА ЭТА ПОЗИЦИЯ — НЕ КОД, А ЧИСЛО.** Она обнаружила, что гость видит
НЕ ТО, что организатор, и что рамка режет ему имена. Проверил сам, напрямую по боевому сайту
headless-браузером без куки:

```
/raspredelenie → ГОСТЬ: обрезано 48 · скролл 0 px
   примеры: Дима Елисеев, Настя Вахрина, Даня Макаров, Андрей Рябичев, Наталья Амбург
/              → ГОСТЬ: обрезано 0 · скролл 0 px
```

Сорок восемь обрезанных фамилий на живом сайте — это ДОСЛОВНО первый пункт претензии владельца.
Его зона — P3, которая идёт прямо сейчас; позиция P2 не починила дефект и не должна была,
но передала его вниз ЧИСЛОМ, а не словами.

🔴 **И ОТДЕЛЬНО ПРО МОЮ РАБОТУ.** Мой гейт мерил одну роль и печатал «проверено 4 из 4» —
честное на вид, слепое по сути. Охват, названный по одной оси (страницы), молчал про вторую
(роли): настоящий охват был 4 из 8. Исполнитель на opus эту дыру нашёл за двадцать минут и
закрыл её в pytest-обёртке; в standalone-гейте она осталась. Это мой долг, не его.

**ЦЕНА:** приёмка 18 минут, из них 12 — перепрогон критерия своими руками вместо чтения отчёта.
Цена НЕ перепрогнанного критерия: отчёт называл «0/4 pages tested by gate (deferred)» в одной
строке и «green, 0/0/0 on all four pages» в другой — по чтению это противоречие, по прогону
правда вторая.
**ПРО ГОЛОВУ:** круг 6, позиций без вердикта 9, принято 1, чинил ли я свою оснастку — нет,
впервые за ночь весь круг ушёл в работу по волне, а не в починку инструментов.

## 01:58 — пункт 17: страховка платной двери взята на себя, и первый же снимок спас три файла

Владелец измерил дыру снаружи: у бесплатной двери сохранность — МЕХАНИЗМ
(`orkestr.py --avtosohranenie` коммитит зону сам, по таймеру и перед убийством), у платной —
только ПРАВИЛО, адресованное модели, и таймера в `ZAPUSK-ZAHODA.sh` нет вовсе. Я перевела
критический путь на платный канал, то есть отправила работу туда, где её страхует
добросовестность исполнителя. Чего стоит это правило, измерено этой же ночью: четыре
бесплатные позиции, ноль коммитов ветки, лог до 119 КБ.

Снял первый снимок круга по живым папкам. **Он оказался не формальностью:**

```
verstka-raspredeleniya · не в git 6:
     M veb/razdely/prepodavateli.py      ← настоящая работа
     M veb/razdely/shkolniki.py          ← настоящая работа
    ?? veb/razdely/verstka_stili.py      ← НОВЫЙ файл, нигде больше не существовал
    A  tools/gejt_verstki.py
     M data/spetsmat.db · M docs/index.html   ← порождаемые, не работа
data-i-istoria-kletki · не в git 2 (только порождаемые)
```

P3 работала 38 минут и не закоммитила НИ РАЗУ. Сними её сейчас что угодно — кап, зависший
движок, часовой — и два правленых модуля с одним новым файлом дали бы ноль, неотличимый от
непочатой позиции. Снимок сделан: `a773bda`, ветка вывезена на origin (643 коммита, на origin
её не было вовсе). Осталось не в git 2 пути, оба порождаемые прогоном.

🔴 **И ОТКАЗ, КОТОРЫЙ Я СОЗДАЛА САМА.** Первая попытка снимка отвергнута гейтом зоны:
«застейджено 1 путей ВНЕ контракта зоны: tools/gejt_verstki.py». Это мой собственный след —
правкой 1 я велела P3 взять гейт из чужой ветки командой `git checkout zahod/kanon-verstki --
tools/gejt_verstki.py`, и файл встал в индекс ВНЕ её зоны. То есть моя инструкция позиции
сделала её зону незакоммитимой штатной командой, и обнаружилось это только при страховке.
Обошёл, назвав файл четвёртой зоной снимка. Урок: велеть позиции взять файл из чужой ветки —
значит расширить её контракт зоны, и расширять надо ОДНОВРЕМЕННО с инструкцией, а не потом.

**ЦЕНА:** 9 минут, из них 4 на разбор отказа, который создала я. Цена непойманного —
три файла работы P3 за 38 минут.
**ПРО ГОЛОВУ:** круг 7, позиций без вердикта 9, принято 1, не в git 0 (кроме порождаемых),
чинил ли я свою оснастку — нет, но чинил последствия своей же правки.

## 02:38 — ПЕРВАЯ ПРЕТЕНЗИЯ ВЛАДЕЛЬЦА ЗАКРЫТА НА БОЕВОМ: 48 обрезанных фамилий → 0

Замер оркестратора headless-браузером БЕЗ куки, до и после выкатки:

```
ДО  /raspredelenie → ГОСТЬ: обрезано 48 · Дима Елисеев, Настя Вахрина, Даня Макаров, Андрей Рябичев
ПОСЛЕ /raspredelenie → ГОСТЬ: обрезано 0 · скролл 0 px · размер 123107 б · титул «Спецмат · 9 класс»
```

Починка P3: `veb/razdely/verstka_stili.py` — общая сетка колонок вместо замороженного
`flex:0 0 9.6rem`, из-за которого «Настя Вахрина» требовала 113 px при выданных 99.

🔴 **И ГЛАВНОЕ СОБЫТИЕ КРУГА — РЫЧАГ СРАБОТАЛ РОВНО ТАК, КАК БЫЛ ЗАДУМАН, И Я ЧУТЬ НЕ ПРИНЯЛ
ЕГО ЗА ПОЛОМКУ.** После влития P3 в main упавших стало 18 вместо 17: покраснел
`test_gost_vidit_to_zhe_chto_organizator`. По всем признакам это класс 7 реестра — «две по
отдельности зелёные позиции вместе дают красный main». Оказалось наоборот. Позиция P2,
обнаружив дефект чужой зоны, не стала его чинить и не написала «надо бы поправить»: она
поставила СТРОГИЙ `xfail`, вписала в него замеренные числа (36 здесь, 48 на боевом), назвала
виновника (`flex:0 0 9.6rem`), назвала зону-владельца (P3) — и дописала условие снятия:

> «в тот день, когда P3 починит страницу, тест покраснеет как XPASS. Это красное значит ровно
> одно — сними маркер, и это единственный способ, которым маркер не переживёт дефект».

P3 починила — маркер покраснел — я его снял. Тринадцать тестов прошли, main вернулся к своим
17 предсуществующим упавшим, стало 1107 passed против входных 1044.

Это лучшая инженерная работа ночи, и она НЕ КОД: передача дефекта между позициями сделана так,
что забыть о нём нельзя, а починка сама себя объявляет. Обычная форма («записал в отчёт, что
надо бы») пережила бы волну молча — и уже переживала: правило вёрстки было записано ТРИЖДЫ и
трижды не сбылось, потому что у него не было рычага.

**ЦЕНА:** 8 минут на разбор красного, которое оказалось зелёным сигналом. Цена ошибки в другую
сторону: приняв XPASS за поломку стыка, я откатил бы починку 48 фамилий.
**ПРО ГОЛОВУ:** круг 9, принято 2, доработка 3, без вердикта 7, не в git 0, чинил оснастку — нет.
Отдельно: P3 снята потолком фоновой задачи, не дописав отчёт и не влив ветку; влитие и проверку
сделал я. Это не её вина и не моя — это потолок инструмента, и он пойдёт в рефлексию классом.

## 03:09 — позиция умерла ПОСРЕДИ слияния в главную папку, и главная папка перестала собираться

Самое опасное событие ночи, и внешне оно выглядело как обычная приёмка.

P8 `poisk-i-kartochka` закончилась штатно («отчёт с коммитом на месте»). Гейт 0 по зоне P6,
которую я принимал следом, вдруг покраснел: шесть правленых путей в ГЛАВНОЙ папке, и ни один
не принадлежит P6. Дальше `pytest` на main не дошёл даже до сбора:

```
SyntaxError: invalid syntax
  File "veb/razdely/shkolniki.py", line 342
    <<<<<<< HEAD
```

**P8 вливала свою ветку, стоя в ГЛАВНОЙ папке, и умерла посреди слияния.** `MERGE_HEAD` висел,
конфликт не разрешён, маркеры в файле. Главная папка — это то, ОТКУДА ИДЁТ ВЫКАТКА НА БОЕВОЙ
СЕРВЕР, и выкатка этой ночью идёт после каждой принятой позиции. Между «недоделанная карточка
уехала школьникам» и текущим состоянием стоял ровно один мой `pytest`.

Разобрал по шагам. Разрешил конфликт не пополам, а по существу: `data-sid` от P8 (поиск с
главной обязан бить в ОДНУ строку, фамилий бывает несколько) и место `{klass}` от main — потому
что его перенос и есть починка 48 обрезанных фамилий. Прогнал тесты: **два новых красных**,
оба HTTP 500 на `POST /api/enrollment`, и сервер объяснил сам:

> «правка СОХРАНЕНА в базу, но публичная страница НЕ пересобрана: каркас роли «prepod»
> разошёлся с гостевым на позиции 7611»

`proverit_karkas()` требует ПОБАЙТОВОГО равенства сборки гостя и преподавателя. Комментарий
самой P8 утверждал, что `data-sid` этого не нарушает — и это было верно на её ветке, отошедшей
от main ДО починки P3, и перестало быть верным после слияния. Классический стык: две по
отдельности зелёные позиции дают красный main, и за стык не отвечает никто.

**Не стал чинить стык сам.** Откатил слияние (`git merge --abort`), убедился что работа P8 цела
(вершина `8249a44` на origin, проверено `git ls-remote`), вернул main к 1107 passed и 17
предсуществующим упавшим — и отдал стык владельцу зоны правкой, где выписаны обе стороны
конфликта, текст ошибки каркаса, номер позиции расхождения и порядок действий. Позиция
перезапущена добором на sonnet.

**ЦЕНА:** 26 минут приёмки, из них 19 — разбор чужого зависшего слияния; 1 откаченное слияние,
2 ложных красных теста, 0 потерянной работы. Цена непойманного: недоделанная карточка на боевом
сайте школы к утру, потому что выкатка идёт из той самой папки.
**ПРО ГОЛОВУ:** круг 11, принято 3, доработка 4, без вердикта 5, выкачено до P6.
Отдельно про себя: я приняла решение НЕ чинить стык своими руками, хотя могла. Ночью соблазн
«я быстрее» силён, но стык между двумя зонами — это место, где правка вслепую стоит дороже
круга ожидания, и позиция знает свою сторону лучше меня.
