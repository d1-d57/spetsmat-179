# Канал исполнителя — P8-golos (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — нормализация числительных своим кодом и словарь фамилий: у распознавания речи русского нормализатора нет, а мультимодальная модель на диктовках отвечает НА аудио вместо транскрибирования.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
bash /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/2026-09-02_spetsmat-bot/ZAPUSK-ZAHODA.sh P8-golos opus
```
🔴 **ЧЕМ ЭТА СТРОКА ОТЛИЧАЕТСЯ ОТ ТОЙ, ЧТО ПЕЧАТАЛ ГЕНЕРАТОР.**
Генератор вписал `claude -p --model arn:aws:bedrock:…` — ARN application inference
profile. На ЭТОЙ машине Bedrock-доступа НЕТ вовсе: ни `~/.aws/`, ни переменных `AWS_*`
(замер 2026-09-02 08:22). Цена оплачена соседней волной 2026-09-02 07:07: пять платных
позиций из пяти оборвались за девять минут с «Could not load credentials from any
providers», и снаружи это неотличимо от «заход думает». `ZAPUSK-ZAHODA.sh` берёт модель
вторым аргументом и сам выбирает маршрут; добор — тем же вызовом с `--dobor`.

<!-- прежняя строка генератора, сохранена дословно, НЕ исполнять:

python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add P8-golos --branch zahod/P8-golos && cd /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/P8-golos && claude -p --verbose --output-format stream-json --model arn:aws:bedrock:us-east-1:811345154057:application-inference-profile/d78ovu0ye0t4 --dangerously-skip-permissions 'Твой заход — файл /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/2026-09-02_spetsmat-bot/kod_P8-golos.md. Прочитай ТОЛЬКО его и то, что он называет; остальной проект не изучай. План/вопросы/отчёт пиши в этот же файл внизу (## ПЛАН / ## ВОПРОСЫ / ## ОТЧЁТ). Ничего сверх задачи не трогай — «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.' < /dev/null 2>&1 | tee /tmp/zahod-P8-golos.jsonl | python3 -u -c 'import sys,json
for l in sys.stdin:
 try:
  d=json.loads(l); t=d.get("type")
  if t=="assistant":
   for b in d.get("message",{}).get("content",[]):
    k=b.get("type")
    if k=="text" and (b.get("text") or "").strip(): print(b.get("text"))
    elif k=="tool_use": print("  . "+str(b.get("name")))
  elif t=="result":
   u=d.get("usage") or {}
   print("== ПРОГОН: %s c . USD %s . токены вх %s / вых %s / кэш-чтение %s" % ((d.get("duration_ms") or 0)//1000, d.get("total_cost_usd"), u.get("input_tokens"), u.get("output_tokens"), u.get("cache_read_input_tokens")))
 except Exception:
  sys.stdout.write(l)'

-->

Приёмке — время и токены печатает команда, руками их не вписывают (`KONSTITUCIYA §10`; лог остаётся на диске после прогона):
```
python3 -c 'import sys,json
for l in open(sys.argv[1], encoding="utf-8"):
 try:
  d=json.loads(l)
 except Exception:
  continue
 if d.get("type")=="result":
  u=d.get("usage") or {}
  print("время: %s c" % ((d.get("duration_ms") or 0)//1000))
  print("стоимость USD: %s" % d.get("total_cost_usd"))
  print("токены: вход %s, выход %s, кэш-чтение %s" % (u.get("input_tokens"), u.get("output_tokens"), u.get("cache_read_input_tokens")))
  for m,v in (d.get("modelUsage") or {}).items():
   print("  %s: вх %s вых %s USD %s" % (m, v.get("inputTokens"), v.get("outputTokens"), v.get("costUSD")))' /tmp/zahod-P8-golos.jsonl
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
ГРАНИЦА ОБЛАСТИ: сырые подстроки в `kod_*.md` (пункт 4) — НЕ парсер очереди `dostavit_urok` (который считает только пары ДОМ:/ДОСТАВЛЕНО:). Разница в числах — законна.
🔴 снимок при сборке 2026-09-02, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/schet_nezakrytogo.py zhurnal/2026-09-02_spetsmat-bot`
Область: «zhurnal/2026-09-02_spetsmat-bot» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : н/д — VERDIKTY.md/INCIDENTY.md не найдены
  2. долгов СТАТУС: ЖИВ                  : н/д — /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 8
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 56
     из них разбором очереди (парсер `dostavit_urok`, записи с парой ДОМ:/ДОСТАВЛЕНО:): 29
       живых (чинится доставкой — «дом есть»)  : 11
       к владельцу (решение за человеком)      : 12
       адрес недоступен (нет/папка/код/указат.) : 4
       адрес не разобран                        : 0
       отработавших (машинный след закрытия)    : 0
       доставлено                               : 2
       🔴 не проверяется машиной: содержательная отработанность записей БЕЗ следа закрытия (метки в доме, строки ✅/ЗАКРЫТО) — нужна ревизия человеком; сырой греп сверх разбора — шаблонные строки формы.

КОНТЕКСТ. `spetsmat-bot` — телеграм-бот кондуита спецмата 179-й школы: 56 учеников,
18 преподавателей, три аудитории. Прошлый этап: приняты и влиты P1 (ядро), P2 (импорт),
P3 (роли), P4 (сетка приёма) и P12 (закрепление); P7 делает приём по фотографии и
строит ТУ ЖЕ таблицу подтверждения, которую переиспользуешь ты. ЦЕЛЬ: третий
равноправный способ ввода — преподаватель наговаривает «Петров три пять семь бэ», бот
разбирает, показывает таблицу, преподаватель поправляет тапами и подтверждает.
Приёмка — по ОТЧЁТУ, без построчной сверки. Если стоп до цели: получишь разбор голоса
до черновика, но НЕ быстрый текст (P15) — он идёт через тот же путь отдельной позицией.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-09-02) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. три способа ввода равноправны и работают параллельно: кнопки, фотография, голосовое
2. дословный режим распознавания: умная нормализация превращает надиктованные цифры в лотерею
3. отметка НИКОГДА не пишется в базу без подтверждения человеком

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/P8-golos`** — ТОЛЬКО ДЛЯ КОДА (worktree захода, ветка `zahod/P8-golos` в ней уже стоит). 🔴 **ДАЛЬШЕ — ТОЛЬКО ПУТИ ОТНОСИТЕЛЬНО ЭТОЙ ПАПКИ** (или `cd` в неё безусловно, каждым ходом): абсолютный путь в главную папку репозитория здесь — типичная ошибка, правка утекает МИМО worktree и найдётся только на коммите («вне git» в `git_zona.py check --zone` из рабочей папки, на файле, который уже правил, — цена, оплаченная живьём: 5 файлов, ручное копирование и откат главной папки). 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) при этом остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия в рабочей папке: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути, названному в стартовой строке, а сам файл НЕ коммитишь — это делает аналитик при приёмке (цена обратного правила — полсуток 03.08: отчёт писали в рабочую папку, владелец и приёмка её не видели, приёмка трижды объявила отчёт пустым). 🔴 **Ветку в конце вливаешь САМ, последним ходом, после коммита зоны** (решение владельца 25.08; полный порядок печатает WARNING-блок ниже).
- **ЗОНА (можно менять):** `core/services/golos.py` `infra/asr.py` `bot/routers/voice.py` `tests/voice/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4); коммить ПО ХОДУ работы, не одним последним ходом (§4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **SCRATCHPAD — ТОЛЬКО ЛИЧНЫЙ.** Черновики, выкладки, промежуточные версии — в личную папку СВОЕГО захода `scratchpad/P8-golos/`. Общие пути (`scratchpad/otchet.md`, любой `scratchpad/*` без имени твоей темы) ЗАПРЕЩЕНЫ: чужой отчёт уедет в твой файл или твой — в чужой, а приёмка читает отчёт без построчной сверки и подмену НЕ ЛОВИТ по построению. *Цена 25.08: готовый `## ОТЧЁТ` захода konvejer-incidentov был записан в общий `scratchpad/otchet.md`, и 92 строки чужого отчёта простояли в `kod_slovari-v-kod.md`.*
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода «kod_registracia-bez-obhoda.md»).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает названные через `--vlit` ЧУЖИЕ ветки В ОСНОВНУЮ, и только ПОТОМ ты заводишь свою рабочую папку; СВОЮ ветку он не трогает никогда — её вливаешь ты сам последним ходом (граница прав ниже).** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. 🔴 ПОРЯДОК ПРИ ПАДЕНИИ ЭТОГО ВЫЗОВА — исполняй, не изобретай (движок роняет `network_error` на вызове субагента и уносит с собой ВЕСЬ заход, замер волны 3A — 4 захода из 13). Пауза 45 секунд, до трёх попыток; время меряй `date`, не суммой своих `sleep`. После третьей — работай БЕЗ субагента: контур остаётся неразобранным, и это ОТДЕЛЬНАЯ строка в `## ОТЧЁТ`, а не молчание. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты сам, по ходу работы (§4). 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone core/services/golos.py --zone infra/asr.py --zone bot/routers/voice.py --zone tests/voice/ --kommitit 'core/services/golos.py, infra/asr.py, bot/routers/voice.py, tests/voice/' --zakryt 'влить ветку zahod/P8-golos в main последним ходом, после коммита зоны'
```

🔴 ГРАНИЦА ПРАВ, ЧЕТЫРЕ ОТВЕТА (та же, что в самом задании субагенту — одно место в тексте, а не пересказ): **кто вливает ЧУЖИЕ названные (`--vlit`) ветки** — субагент, в ОСНОВНУЮ ветку, до заведения твоей папки; **кто вливает СВОЮ ветку этого захода** — ты сам, последним ходом, после коммита зоны (`git_zona.py vlit-v-osnovnuyu`; решение владельца 25.08 — оно сняло противоречие волны 4, когда машинное §0.1 и текстовое «ветку НЕ вливать» спорили молча, и машинное побеждало); **кто закрывает заявки** — субагент, `zayavka-zakryt`; **кто коммитит пути ВНЕ зоны захода** — субагент (хвост Cowork и что назовёт пункт 3 его задания). Ты коммитишь ТОЛЬКО зону этого захода, по ходу работы (§4). 🔴 Конфликт на `README.md` при ЛЮБОМ слиянии разрешается ОБЪЕДИНЕНИЕМ записей реестра, НИКОГДА выбором стороны: параллельные заходы волны дописали в реестр по строке — обе записи правы, выбор одной молча уничтожает регистрацию соседа.

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-09-02, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged main | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.


- деплоя в этом заходе нет.

- `cd /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/P8-golos` — рабочая папка ДЛЯ КОДА. Ветку НЕ переключай: `zahod/P8-golos` в ней уже стоит.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/P8-golos`.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add core/services/golos.py infra/asr.py bot/routers/voice.py tests/voice/` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: `названные файлы-якоря`. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

🔴 **WRITE YOUR `## ОТЧЁТ`, `## ПЛАН` AND `## ВОПРОСЫ` IN ENGLISH, AND EVERY FILE AND EVERY COMMIT MESSAGE YOU PRODUCE TOO.** Owner's decision 30.08. It is a каркас-level rule, not a preference — wave 2 lost it twice because the pass text listed the report SECTIONS and never said «every file you create». Fixed Russian addresses stay Cyrillic: `ЦЕНА:` · `ВЕРДИКТ:` · `ДОМ:` · `ДОСТАВЛЕНО:` · `ПОДЪЁМ:` · `[ДОЛГ: …]` · every `## ` heading of this file · every path and command.
Voice → transcript → the SAME schema call as photo → draft → the SAME confirmation table → journal.
**There is never a direct write.**

Голос устроен так же, как фото, только вход текстовый. Всё, что ниже, — измерено, а не выбрано
по вкусу.

### 1 · Intake

`ogg/opus`, mono. 🔴 **Do NOT hardcode the sample rate:** Android switched from 16 to 48 kHz only
in March 2024, and older cached messages still exist in the wild. `getFile` gives at most 20 MB —
never a constraint for a voice note, but the same door as photo.

### 2 · 🔴 SPEECH RECOGNITION: THREE TRAPS, ALL MEASURED

1. **Do NOT push the audio into a multimodal model in one call.** On twenty-to-thirty-second
   dictations a quarter of the calls ANSWER the audio instead of transcribing it. Worse and aimed
   straight at our design: putting 56 surnames into the prompt of an audio model measurably biases
   it toward the text and away from what it actually heard.
2. **Whisper's `initial_prompt` is NOT a vocabulary hint mechanism.** It is the context of the
   previous fragment, limited to 224 tokens, with documented looping hallucinations.
3. **OpenAI does not accept ogg/opus** — that means dragging in ffmpeg. Know it before you design
   around it.

**What to use:** a model with a USER DICTIONARY — up to 1000 terms, optimum under 100, and we have
exactly 101 (56 surnames + 45 task labels). 🔴 **Verbatim mode, not "smart":** smart normalisation
turns dictated digits into a lottery, and digits are the entire payload here.

⚠ A local alternative worth naming in the report: GigaAM-v3 gives 7,19% WER against Whisper
large-v3's 15,44% on Russian, and runs on four cores in 5–8 seconds where Whisper takes minutes.
If no ASR key is available at build time, **build against a Protocol and a fake** — do not block.

### 3 · 🔴 NUMERAL NORMALISATION IS OUR OWN CODE — no ASR has a Russian one

«Петров три пять семь бэ» → student `Петров`, problems `3`, `5`, `7б`.

- Spelled-out numerals → digits: `три` → 3, `семнадцать` → 17, `первая` → 1.
- Letter suffixes: `бэ` → `б`, `а` → `а`, `вэ` → `в`, `дэ` → `д`. This is where dictation actually
  lives: `7б` is said «семь бэ».
- Ordinals and cases: «третью и пятую» is the same as «три пять».
- Ranges: «с третьей по шестую» → 3, 4, 5, 6. Decide whether to support it, and say so either way.
- **Write the vocabulary as DATA, not as `if`s**, and put it where it can be read and extended;
  name the location in the report (it is a constant of the project — see the note on `config.py` below).

Every rule above gets a test with a real dictated line, not a synthetic one.

### 4 · Matching — the same two independent channels as photo

The transcript goes to the SAME schema call P7 built (`enum` over student IDs and task IDs, never
surnames; `raw_text` first and required; **strip the markdown fence before parsing** — measured on
this project 02.09: a valid `{"rows": []}` came back inside a fence and the naive parser rejected it).

Then the second, independent channel: `rapidfuzz.fuzz.ratio` of `raw_text` against the 56 surnames
expanded across cases. **Not `token_set_ratio`** — it returns 100 on containment. Threshold ~0,7.
Disagreement between the model's pick and the fuzzy match IS the signal «doubtful».
On ambiguity the model returns `UNKNOWN` and fills `alternatives`; the bot shows buttons. Never
make it guess.

🔴 **Numeric `confidence` from the model does not exist** — it collapses to 0,9/1,0 and stays high
while accuracy falls. Do not use it for anything.

### 5 · Confirmation and idempotency — reuse, do not rebuild

The parsed table is shown WHOLE, a tap toggles a cell, and only a confirmed draft reaches the
journal through the marking path P4 already built. Idempotency key = SHA-256 of the downloaded
bytes plus the confirmed-drafts table, exactly as in P7.

⚠ **P7 owns `core/services/raspoznavanie.py` and `infra/llm.py`.** They are NOT in your zone:
import them, do not fork them. If something you need is missing there, say so in `## ВОПРОСЫ` —
a forked copy of the schema call is two homes for one truth and they will diverge silently.

### 6 · Constants

Every constant of the project lives in `config.py`. ⚠ `config.py` is not in your zone: name the
constants you need in the report and put them in your own module for now. *(Two positions of this
wave had to edit it out of zone because the задание asked for a constant there while the zone
forbade it — that was the orchestrator's error and it is not repeated here.)*

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ), каждая команда печатает ЧИСЛО:**

    cd /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/P8-golos
    make check
        # rc=0; печатает «N passed», N больше того, что было до тебя
    python3 -m pytest tests/voice -q
        # rc=0; печатает охват: не менее 20 надиктованных строк разобрано в пары
        # «ученик — задачи», ошибок 0
    python3 -m pytest tests/voice -q -k "petrov or normal"
        # rc=0; ИМЕНОВАННЫЙ тест: «Петров три пять семь бэ» → Петров, 3, 5, 7б
    grep -c "token_set_ratio" core/services/golos.py ; echo "должно быть 0 — он даёт 100 на вложенности"

🔴 Ноль разобранных строк при непустом наборе — КРАСНЫЙ, а не зелёный. Отрицательный вердикт
несёт охват В СЕБЕ: «ошибок 0, разобрано 20 из 20».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)

Верификатор нужен, тип — **ПОСЛЕ-типа** — судит результат, стоит в конце, после задачи. Свежий субагент, ДРУГИМ методом («Петров три пять семь бэ» разрешается в ученика Петров и задачи 3, 5, 7б; нормализация числительных и «бэ»→«б» своим кодом), не перечитывает свою же правку. Доля сплошной выборки: 20 надиктованных строк × разбор в пары ученик-задачи, ошибок 0. Финальная строка ответа обязательна дословно: «выдано N позиций из M найденных» — без неё ответ считается усечённым и в отчёт не вставляется.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПО ХОДУ РАБОТЫ, НЕ ОДНИМ ПОСЛЕДНИМ ХОДОМ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая. Каждую завершённую часть работы коммить СРАЗУ, теми же двумя ходами — не копи всё к финальному ходу:
```
git --no-optional-locks add -- core/services/golos.py infra/asr.py bot/routers/voice.py tests/voice/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<что сделано>" -- core/services/golos.py infra/asr.py bot/routers/voice.py tests/voice/   # отсекает всё чужое
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone "core/services/golos.py" && \
    git_zona.py check --zone "infra/asr.py" && \
    git_zona.py check --zone "bot/routers/voice.py" && \
    git_zona.py check --zone "tests/voice/"   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **КОММИТЬ ПО ХОДУ — РЕШЕНИЕ ВЛАДЕЛЬЦА 25.08 (В11), ПЕРЕВЕРНУВШЕЕ прежний канон «одним последним ходом».** Цена прежнего канона: за сутки ДВА обрыва — канал `opencode run --auto` односторонний и умирает вместе с сессией (владелец закрыл ноутбук), и незакоммиченная работа пропадала целиком. Закончил кусок — закоммитил его; последний ход только ПРОВЕРЯЕТ, что коммитить нечего (`git status --porcelain` пуст, `git_zona.py check --zone` ✅).
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `core/services/golos.py` `infra/asr.py` `bot/routers/voice.py` `tests/voice/`

- **Г1. Зона доехала в git.** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone core/services/golos.py` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone infra/asr.py` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone bot/routers/voice.py` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone tests/voice/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `spetsmat-bot` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged main` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## ⚠️🔴 WARNING · ПОСЛЕДНИЙ ХОД ПЕРЕД ОТЧЁТОМ — ПОЛНАЯ ГИТ-ГИГИЕНА. НЕ ПРОПУСКАТЬ 🔴⚠️

**СТОП. Прежде чем писать хоть строку в `## ОТЧЁТ` — прогони это целиком.** Требование владельца
2026-08-16, поводом стал заход, который сделал работу и не закоммитил НИЧЕГО: *«он ничего не
коммитит… всегда, когда собираешь по генератору, там должна быть фраза, что начинать надо с полной
проверки гигиены»*. Гит-контур §0.1 стоит в НАЧАЛЕ и разбирает то, что накопилось ДО тебя; этот блок
стоит в КОНЦЕ и разбирает то, что накопил ты сам. Один другого не заменяет.

**ПОРЯДОК ЖЁСТКИЙ, ОН НАЗВАН ВЛАДЕЛЬЦЕМ: коммит → влитие своей ветки в основную → пост-проверка
ИЗ ГЛАВНОЙ ПАПКИ → гашение → вывоз.** Обратный порядок не работает технически: влитие отказывает
на грязном дереве, пост-проверка неисполнима до влития, вывоз — на невлитом. **Решение владельца
20.08 пересматривает решение 05.08** («в конце может это worktree вливать, но, наверное, это не
нужно делать») — теперь вливает САМ заход, но только при зелёной пост-проверке.

**1 · ВСЕ КОММИТЫ.** Ничего не осталось вне git — ни в рабочем репозитории, ни в соседних, до
которых ты дотянулся по ходу работы:
```
for R in /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot; do
  echo "== $R"; git -C $R --no-optional-locks status --porcelain
done
```
Своя зона — своими путями (`add` + `commit -- <пути>`). Чужая содержательная работа — НЕ твоя:
называешь строкой в отчёте и оставляешь. Пусто у всех — так и напиши числом «вне git 0».

**2 · ВЛИТИЕ СВОЕЙ ВЕТКИ В ОСНОВНУЮ.** Только после того, как шаг 1 дал «вне git 0» на своей
зоне — влитие отказывает на грязном дереве:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py vlit-v-osnovnuyu zahod/P8-golos --zone "core/services/golos.py" --zone "infra/asr.py" --zone "bot/routers/voice.py" --zone "tests/voice/" \
    --vsyo-ravno "своя рабочая папка ещё жива — влитие последним ходом захода, штатно"
```
Конфликт — ЗАКОННЫЙ исход, не повод форсировать: разрешай по существу, если понимаешь обе
стороны; не понимаешь — `git_zona.py vlit-v-osnovnuyu --abort`, ветка остаётся невлитой,
строка в отчёт и заявка на влитие (`git_zona.py zayavka --rod git-operaciya`).
🔴 Конфликт на `README.md` — только ОБЪЕДИНЕНИЕМ записей реестра, никогда выбором стороны:
параллельные заходы волны дописали по строке — обе записи правы, выбор одной молча уничтожает
регистрацию соседа.

**3 · ПОСТ-ПРОВЕРКА ИЗ ГЛАВНОЙ ПАПКИ.** Отвечает на вопрос «механизм ВСТАЛ», а не «коммит
виден»: прогон изменённого механизма из `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot`, НЕ из рабочей папки `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/P8-golos` плюс `grep` по ЖИВОМУ файлу,
который его зовёт (хук, конвейер, генератор):
```
cd /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot && make check && python3 -m pytest tests/voice -q && echo $?
grep -c 'raspoznavanie\|from infra.llm' core/services/golos.py   # схему P7 ИМПОРТИРУЕМ, а не форкаем
grep -c 'sha256' core/services/golos.py bot/routers/voice.py   # идемпотентность по байтам
grep -rn 'include_router' bot/app.py | tail -2   # роутер голоса реально подключён
```
🔴 **Красная пост-проверка = ОТКАТ ВЛИТИЯ И СТРОКА В ОТЧЁТ**, а не «доложу, пусть приёмка
решает»: `git_zona.py vlit-v-osnovnuyu --abort`, если слияние ещё не закоммичено, иначе
`git -C /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot --no-optional-locks reset --hard <хэш ДО влития>`. Заход, который влил
и сломал `main`, обязан вернуть `main` сам.

**4 · ГАШЕНИЕ.** Невлитого не осталось: `git --no-optional-locks branch --no-merged main`.
Каждая оставшаяся ветка названа поимённо с причиной, почему она жива. 🔴 Ветку-витрину (`main`
там, где с неё публикуется сайт) НЕ вливать — влитие туда есть публикация и решение владельца.

**5 · ВЫВОЗ.** Вывези СВОЮ ветку; `main` НЕ вывози: если после работы в нём есть невывезенное,
поставь заявку `--rod git-operaciya` и назови число в отчёте. Команда для своей ветки:
`git --no-optional-locks log --oneline @{u}.. | wc -l` → 0.
Ненулевое на своей ветке означает, что работа существует только на этом диске.

**6 · ПРОВЕРКА ФАКТОМ, А НЕ ПАМЯТЬЮ.** Числа по каждому репозиторию — вне git · невлитых своих
и чужих; невывезенных СВОЕЙ ВЕТКИ, не по каждому репозиторию · результат пост-проверки
(зелёная/откачена) — печатаются командой и уходят в `## ОТЧЁТ` дословно. Ненулевое число или
красная пост-проверка без объяснения — приёмка читает как несделанную работу: она гоняет те же
команды первым ходом.

🔴 **Отчёт без этих чисел не принимается.** «Я закоммитил» — не то же самое, что `status --porcelain`
пустой: за одну сессию работа не доезжала трижды, каждый раз с честным «сделано» в отчёте.
## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Заход говорил о зависимости в настоящем времени, а рабочая папка была отпочкована ДО её влития — и проверить это было нечем

`## 2. ЗАДАЧА` §4 и §5 говорят про P7 как про наличное: «the SAME schema call P7 built», «P7 owns `core/services/raspoznavanie.py` and `infra/llm.py` … import them, do not fork them». В момент прогона ни одного из этих файлов на ветке `zahod/P8-golos` НЕ БЫЛО: папка была отпочкована от `main` до того, как P7 влилась. Заход при этом не содержал ни одной команды, которой это проверяется. §0.1 велит проверять невлитые ЧУЖИЕ ветки (`git branch --no-merged main`) — и эта команда честно отвечает «невлитых нет», потому что чужие ветки УЖЕ влиты в `main`; вопрос «а не ушла ли вперёд сама основная относительно МОЕЙ папки» она не задаёт и задать не может. Я прочитал §5 буквально («чего нет — в `## ВОПРОСЫ`»), построил второй канал за протоколом с честной заглушкой, написал под это тесты и закоммитил четыре части.

ЦЕНА: три из четырёх коммитов позиции содержали форк того, что уже лежало в `main` — своя таблица падежных окончаний на 16 правил, своя обёртка над `rapidfuzz.fuzz.ratio` с собственным порогом 0,7, свой съёмник markdown-забора. Пятый коммит (`e55d7aa`) их удаляет и зовёт P7-шные: 161 строка вписана, 108 удалено, плюс правка семи мест документации, которые к тому моменту утверждали про `main` неправду. Это примерно четверть работы позиции, потраченная на уже сделанное соседом, и она была бы нулевой от ОДНОЙ команды первым ходом: `git --no-optional-locks log --oneline HEAD..main | wc -l` → «основная ушла вперёд на N коммитов, слей её ДО работы». Правило, закрывающее ровно этот класс, в §0.1 уже НАПИСАНО — «заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит» — но носитель у него защищает только момент СОЗДАНИЯ папки и молчит про папку, созданную раньше и прогнанную позже. Волна, где позиции стартуют не одновременно, ломает это правило по построению.

### Пост-проверка захода требует грепнуть то, что КОНТРАКТ ЗОНЫ запрещает написать

Блок WARNING, шаг 3, печатает `grep -rn 'include_router' bot/app.py | tail -2   # роутер голоса реально подключён`. Зона позиции — `core/services/golos.py` `infra/asr.py` `bot/routers/voice.py` `tests/voice/`; `bot/app.py` в неё не входит, а `ЗАПРЕТ` сформулирован без исключений («ничего за пределами зоны, даже если «мешает» или «чинится в одну строку»»). Критерий проверяет ровно то, что исполнителю запрещено сделать, и зелёным быть не может ни при каком его поведении.

ЦЕНА: оплачена дважды и уже была предсказана. P7 упёрлась первой (её `## ВОПРОСЫ` пункт 1: `bot/routers/photo.py` влит и не подключён) и написала прямым текстом, что «P8 (голос) и P15 (быстрый текст) упрутся в то же самое». P8 упёрлась. Замер на `main` сейчас: `grep -c photo bot/app.py` → 0, `grep -c voice bot/app.py` → 0 — два экрана влиты, оба зелены по своим тестам, оба мертвы в продакшене. Третьей идёт P15, чей заход уже несёт это предупреждение в своём тексте. Цена одной итерации — полный прогон позиции, заканчивающийся работающим кодом и неработающим ботом, плюс отдельный разбор на приёмке. Класс НЕМЕДЛЕННОЕ: это не пункт очереди, а одна правка до следующего прогона — либо `bot/app.py` входит в зону экранной позиции (тогда конфликт двух писателей решается порядком включения, который у каждой позиции свой и не пересекается), либо в волне есть позиция-интегратор, чья зона — только он.

## ПЛАН — (заполняет исполнитель)

**Read first, then measured.** Baseline `make check` -> rc=0, `220 passed`. `rapidfuzz` 3.13.0 is
importable. Seed carries exactly **56 surnames** and **544 problems over 18 sheets** (243 distinct
labels). `config.MARK_SOURCES` already contains `голос`, so no constant of mine has to reach
`config.py`.

**Two facts about the entry that change the shape of the work (both named again in `## ВОПРОСЫ`):**

* `core/services/raspoznavanie.py` and `infra/llm.py` **do not exist on this branch.** P7 is a
  parallel position of the same wave and has not landed. §5 of the задание says import them, do
  not fork them — so the schema call becomes an OPTIONAL second channel behind a Protocol, imported
  lazily and reported as absent, and the fuzzy channel carries the load alone until P7 lands.
  Nothing of P7's is copied.
* `bot/app.py` and `bot/callbacks.py` are **outside my zone**, and both are where a voice router
  would normally be wired and where its payloads would normally live. The ЗАПРЕТ wins: the router
  is built by a factory `build_router()` ready to be included, its payloads live in
  `bot/routers/voice.py`, and the missing `include_router` line is a reported item, not an edit.

**Order of parts, each committed on its own:**

1. **Numerals are our own code** — `core/services/golos.py`: the vocabulary as DATA (word forms ->
   value, tens+units composition, letter suffixes, ranges), `parse_dictation` splitting a line into
   `(surname phrase, problem labels)`. Tests: every rule with a real dictated line, and the named
   `test_petrov_...`.
2. **ASR behind a Protocol** — `infra/asr.py`: `Transcriber` Protocol declared in `core`, a fake, a
   verbatim-mode user-dictionary client, and `build_transcriber()` that returns the fake when no key
   is set. The 101-term dictionary (56 surnames + task labels) is built from the seed, not typed.
3. **Two independent channels** — matching in `core/services/golos.py`: `rapidfuzz.fuzz.ratio`
   (never `token_set_ratio`) over surnames expanded across cases, threshold 0,7; the optional model
   channel with the markdown fence stripped before parsing; disagreement -> `doubtful`; ambiguity ->
   `UNKNOWN` + `alternatives`. Label -> problem id resolution tolerant of `°`, `*`, `:)`.
4. **The screen** — `bot/routers/voice.py`: voice in, transcript, draft, the confirmation table
   shown WHOLE, a tap toggles one cell, «Подтвердить» writes through P4's `MarkingService` with
   `source="голос"` and an idempotency key of SHA-256(bytes)+cell. There is never a direct write.
5. Gates, hygiene, verifier, report, merge.

**Decisions I am taking rather than asking about** (§3 says say so either way):

* **Ranges ARE supported.** «с третьей по шестой» -> 3, 4, 5, 6. Bare numbers only: a range whose
  end carries a letter suffix («с седьмой а по седьмую в») has no defined enumeration and is left
  as two separate labels.
* **«и» is always a conjunction, never the suffix letter `и`.** «три и пять» must not become `3и`.
  The letters a teacher actually dictates as a suffix are а · бэ · вэ · гэ · дэ · е · жэ · зэ, and
  `и`/`й` labels (`10и*`) are reached by tapping, not by voice. Named in the report as a known limit.
* **«же» is NOT a letter.** It is a particle and would eat «три же» into `3ж`. `жэ` alone stands.
* **One dictation may carry several students.** A non-numeral word after a run of numbers opens a
  new row: a teacher dictates a session, not a line.
* **`ё` is folded to `е` on BOTH sides of every comparison** (`Фёдоров` is in the roster and no ASR
  is reliable about the diaeresis), and the original spelling is what the table shows.


## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. `bot/routers/voice.py` влит в `main` и НЕ ПОДКЛЮЧЁН: `bot/app.build` его не включает, поэтому экран в продакшене мёртв. `bot/app.py` вне зоны этой позиции (КОНТРАКТ ЗОНЫ), поэтому три строки отсюда не применялись. 🔴 ЭТО ТА ЖЕ САМАЯ НАХОДКА, ЧТО ПУНКТ 1 У P7 — там она про `bot/routers/photo.py`, здесь про голос, и P7 в своём отчёте прямо предсказала, что P8 упрётся в то же. Замер сейчас: `grep -c voice bot/app.py` → 0, `grep -c photo bot/app.py` → 0. Две позиции влиты, оба экрана мертвы. 🔴 ПОРЯДОК НЕСУЩИЙ: роутер включается ПОСЛЕ `grid_router` и ДО `stale_router`, который `build` включает ПОСЛЕДНИМ и который забирает любой callback, не разобранный выше — включённый после него экран отвечает «экран устарел» на каждую свою кнопку, будучи при этом полностью исправным. Точный патч:

       from bot.routers import marking, room, voice                    # <-- ДОБАВИТЬ voice
       ...
       dp.include_router(room.build_router())
       dp.include_router(voice.build_router())                         # <-- ДОБАВИТЬ, здесь
       dp.workflow_data.update(voice.voice_dependencies(catalogue))    # <-- ДОБАВИТЬ
       dp.include_router(stale_router)                                 # без изменений, остаётся ПОСЛЕДНИМ

   `voice_dependencies(catalogue)` возвращает ровно те ключи, которые ждут обработчики (`transcriber`, `download`, `asr_note`, `schema_extractor`), и строит распознаватель со словарём этого кондуита. `tests/voice/conftest.py` исполняет именно этот порядок над настоящим диспетчером, так что патч доказан до применения.
   ДОМ: bot/app.py
   ДОСТАВЛЕНО: нет

2. Протокол `Transcriber` объявлен в `core/services/golos.py`, а его дом — `core/ports.py`, рядом с `Clock`, `MarkJournal` и `Catalogue`. `core/ports.py` вне зоны этой позиции. Перенос — четыре строки, шов при этом не меняется.
   ДОМ: core/ports.py
   ДОСТАВЛЕНО: нет

3. Четыре класса `callback_data` голоса (`VoiceCell` `VoicePick` `VoiceConfirm` `VoiceCancel`) живут в `bot/routers/voice.py`, а дом всех payload-ов этого бота — `bot/callbacks.py`, вне зоны. Тот же долг несёт P7 со своими `Foto*`. Пока не перенесены, закон «префиксы не сталкиваются» держит тест `test_the_prefixes_do_not_collide_with_any_other_screen_of_this_bot` — а он сверяет только пять P4-шных префиксов и не видит P7-шных.
   ДОМ: bot/callbacks.py
   ДОСТАВЛЕНО: нет

4. `rapidfuzz` по-прежнему не в цели `deps` файла `Makefile`. Заявка на это УЖЕ СТОИТ — `2026-09-02T1236-rapidfuzz-deps-makefile-p7-rapidfuzz-fuzz`, поставлена оркестратором по находке P7; новой я не ставил, чтобы не плодить дубль. Голос добавляет к ней одно уточнение: голосовой путь ходит через `raspoznavanie.ratio`, у которого ЕСТЬ запасной путь без rapidfuzz (та же формула через LCS), поэтому на чистой машине голос не падает, а тихо считает по запасной ветке. Это лучше падения и хуже зелёной цели `deps`.
   ДОМ: Makefile
   ДОСТАВЛЕНО: нет

5. Схемного разбора ТЕКСТА у P7 нет: `infra.llm.VisionModel` принимает изображение, `raspoznavanie.rows_from_answer` читает коды, НАПЕЧАТАННЫЕ на листке. Расшифровка — ни то, ни другое. Второй независимый канал голоса поэтому объявлен отсутствующим (`load_schema_extractor` называет модуль и фабрику, которых ждал), и строка `channels` черновика говорит это вслух, чтобы «один канал» никогда не читался как «два канала сошлись». Чтобы канал включился, в `core/services/raspoznavanie.py` нужна фабрика `build_extractor()` над текстовой схемой — файл P7, не мой.
   ДОМ: core/services/raspoznavanie.py
   ДОСТАВЛЕНО: нет

6. Задачи с буквой `и` (`10и*` и соседи) голосом НЕДОСТИЖИМЫ — по решению, а не по недосмотру: «и» оставлена союзом, иначе «семь и восемь» схлопывается в несуществующую `7и`. Кнопками достижимы. Если владелец захочет диктовать и их, нужен разделитель в речи («семь буква и») — это его решение, а не моё.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

7. `bot/routers/photo.py` и `bot/routers/voice.py` держат по своей копии `_redraw`, `_editable`, `_teacher_id` и `_current_sheet` — одинаковые тела, потому что первая копия у P4 приватная (`bot/routers/marking.py`), а P4 вне зоны обеих позиций. Подстрока `NOT_MODIFIED` при этом уже импортируется из P4 и живёт в одном доме — то есть отделить общее от копий можно, до этого просто дошли не все.
   ДОМ: bot/routers/marking.py
   ДОСТАВЛЕНО: нет

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки.
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо.

🔴 **СУБАГЕНТ ГИТ-КОНТУРА §0.1 НЕ ЗАПУСКАЛСЯ — ПУНКТ ОТМЕНЁН ОРКЕСТРАТОРОМ** в стартовом
сообщении этого прогона, и то указание объявлено сильнее текста захода. Причина, названная
там же: замер соседней волны — четыре захода из десяти умирали ровно на этом вызове.
Вместо всего блока §0.1 оркестратор велел исполнителю выполнить САМОМУ одну команду и
вставить её вывод сюда. Секцию поэтому заполнил исполнитель, а не субагент, и это НЕ
нарушение Г12, а его причина.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снят ПЕРВЫМ ходом, до всякой работы)*
```
$ git --no-optional-locks branch --no-merged main | grep -c zahod/
1
```
Единица — это СВОЯ ветка `zahod/P8-golos`, отпочкованная и ещё не влитая; чужих невлитых
не было. Команда выполнена первым ходом, до чтения кода.

**ЧТО СДЕЛАНО** *(с хэшами)*
- Влито ЧУЖОГО: ничего — чужих невлитых веток на входе не было (снимок выше: 1, и это своя).
- Закоммичено СВОЕЙ зоны по ходу работы, шестью коммитами: `01aab37` · `c2d6195` · `cb4b85f` · `e42b811` · `e55d7aa` · `889740b`.
- Влито В СВОЮ ветку по ходу работы: `9d36808 merge main into zahod/P8-golos` — основная ушла вперёд на 3 коммита за время прогона и принесла P7; без этого влития позиция форкала бы чужой код (см. `## УРОКИ ФАБРИКЕ`, урок 1).
- Влито В ОСНОВНУЮ (свой последний ход, после коммита зоны): `afa5dbf Merge branch 'zahod/P8-golos'`, без конфликтов.
- Погашено: невлитых веток после влития — 0.
- Заявки закрыты: ни одной. Эта позиция ни одной не ставила и ни одной чужой не закрывала — закрытие чужих заявок было правом субагента §0.1, который отменён.
- Вывезено: **неприменимо, потому что** у репозитория `spetsmat-bot` нет ни одного удалённого репозитория (`git remote -v` → пусто, `git remote | wc -l` → 0). Вывозить некуда, и заявку на вывоз ставить не на что.

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `да`
*(на входе долгов не было: единственная невлитая ветка была своя собственная, и она влита
последним ходом — `afa5dbf`. Вывоз неприменим за отсутствием удалённого репозитория.)*

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/core/services/golos.py` — открыть редактором; продукт этой позиции есть КОД, и он лежит в главной папке, влитый в `main`. Рядом, тем же коммитом: `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/infra/asr.py`, `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/bot/routers/voice.py`, `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/tests/voice/` (пять файлов). Собранного файла нет — колоды, PDF и картинок эта позиция не производит.
**РОД АРТЕФАКТА:** `исходник`
**КОММИТ:** `889740b` — `voice: «что запишется» — тоже P7's function, and the draft shape is what makes that legal` · `git_zona.py check --zone "core/services/golos.py" && git_zona.py check --zone "infra/asr.py" && git_zona.py check --zone "bot/routers/voice.py" && git_zona.py check --zone "tests/voice/"` → ✅ (все четыре, каждая rc=0)
**ВЛИТИЕ В ОСНОВНУЮ:** `afa5dbf Merge branch 'zahod/P8-golos'`, без конфликтов, последним ходом после коммита зоны.
**ПРАВКИ ПРОЧИТАНЫ:** блок `## ПРАВКИ ПОСЛЕ ВЫДАЧИ` пуст — правок с момента выдачи не было.

---

### ЧТО СДЕЛАНО И ЗАЧЕМ

Третий равноправный способ ввода собран целиком: голосовое → расшифровка → черновик → таблица
подтверждения → тап → запись через путь отметок P4. **Прямой записи нет ни на одном шаге** — это
третий финализированный пункт интервью, и он держится тестом, который смотрит в журнал ПОСЛЕ
прохода диктовки, а не в код.

**1 · Нормализация числительных — свой код, и её словарь есть ДАННЫЕ** (`core/services/golos.py`).
Русского нормализатора нет ни у одного распознавателя, а цифры здесь — вся полезная нагрузка.
Наверху модуля лежат таблицы: словоформа → значение (0–19 и десятки, которые складываются с
единицами), надиктованное имя буквы → буква ярлыка, слова-открыватели и закрыватели диапазона,
слова-шум. Добавить «двадцать седьмую» — это дописать строку в кортеж, а не править разборщик.
Два ОТСУТСТВИЯ в таблице букв — решения с ценой, и у каждого свой тест: **«и» осталась союзом**
(иначе «семь и восемь» схлопывается в несуществующую `7и`), **«же» осталось частицей** (иначе
«три же» становится `3ж`). Диапазоны поддержаны и включают оба конца; диапазон, конец которого
несёт букву, оставлен двумя ярлыками, потому что буквы листка — не непрерывный алфавит и
перечислить его нечем.

**2 · Распознавание — за протоколом, с заглушкой, которая называет себя заглушкой**
(`infra/asr.py`). Аудио НЕ уходит в мультимодальную модель одним вызовом: на диктовках в 20–30
секунд четверть таких вызовов ОТВЕЧАЕТ на аудио вместо расшифровки, а 56 фамилий в промпте
аудиомодели смещают её к тексту и прочь от услышанного. Запрос строится ЧИСТЫМ методом
`build_request`, поэтому три его несущих свойства проверяются без ключа и без сети: дословный
режим включён, контейнер назван `oggopus` и **частота дискретизации не передаётся вовсе**
(Android перешёл с 16 на 48 кГц в марте 2024, старые кэшированные сообщения живы, а ogg несёт
свою частоту сам), ключ едет в заголовке, а не в query-строке. Ответ без слов — отказ, никогда
пустой черновик: пустой черновик неотличим от диктовки, которую преподаватель и хотел оставить
пустой. Ключа на машине нет, поэтому дверь `build_transcriber` отдаёт заглушку И предложение,
которым это надо залогировать: молча установившаяся заглушка отвечает одной и той же
консервированной строкой и выглядит ровно как работающий бот.

**3 · Два независимых канала.** Нечёткое сопоставление `raw_text` с ростером, развёрнутым по
падежам; порог 0,7; расхождение между каналами И ЕСТЬ сигнал «сомнительно». Числовой уверенности
модели нет нигде и никто её не читает — она схлопывается в 0,9/1,0 и держится высокой, пока
точность падает. Неоднозначность внутри поля 6 пунктов — это `UNKNOWN` и кнопки, а не догадка.

**4 · Экран** (`bot/routers/voice.py`). Таблица показывается ЦЕЛИКОМ, тап переключает клетку,
«Записать» — единственное место на пути, которое касается журнала, и оно ходит через
`MarkingService` с `source="голос"`. Ключ идемпотентности — SHA-256 скачанных БАЙТОВ плюс клетка,
как у фото: то же голосовое, доставленное дважды, даёт тот же дайджест, ту же таблицу, те же
ключи и одно событие — проверено отправкой одной записи дважды. Распознавание уходит с event
loop: встроенное в него, оно заморозило бы тапы всех остальных преподавателей на длину одной
диктовки.

**5 · P7 приехала посреди позиции, и форк был удалён.** Рабочая папка отпочкована от `main` ДО
влития P7; на входе `core/services/raspoznavanie.py` и `infra/llm.py` физически отсутствовали, и
первые четыре коммита построены против их отсутствия — за протоколом, с честной заглушкой.
Обнаружилось это на шаге «гашение» финального блока: `main` ушёл вперёд на 3 коммита и принёс
P7 целиком. `main` влит в ветку (`9d36808`), после чего **всё, что оказалось форком, удалено, а
P7-шное вызвано** (`e55d7aa`): таблица падежных окончаний — P7-шная (`case_forms`
переэкспортирована, а не переписана), метрика — `raspoznavanie.ratio` (это `fuzz.ratio` при
установленном `rapidfuzz` и та же формула через LCS без него — запасной путь P7, и он
наследуется одним импортом), порог — `raspoznavanie.CONFIDENCE_THRESHOLD`, а не второй 0,7;
съёмник markdown-забора — `infra.llm.strip_fence`, дотянутый из `infra/asr.py`, потому что
`core/` не имеет права импортировать `infra/`; разборщик в `core` теперь ОТКАЗЫВАЕТ на
забранном теле, вместо того чтобы тихо научиться его открывать. Шестым коммитом (`889740b`)
переиспользована и таблица подтверждения: черновик голоса хранит клетки в форме P7
(`checked`/`shown`), и «что запишется» считает `photo.checked_cells` — правило «строка без
ученика не пишет ничего, сколько бы её клеток ни было отмечено» получило один дом на два экрана.
Личные имена вошли в оценку тоже по измерению P7: в этой школе преподаватель говорит «Ирина» так
же охотно, как «Агаркова».

Чего у P7 НЕ взято и почему: её схемный вызов — зрительный конвейер (изображение на входе,
напечатанные коды на выходе). Расшифровка — ни то, ни другое, текстовой фабрики у неё нет, и шов
называет модуль и имя, которых ждал, вместо того чтобы форкать. Пункт 5 в `## ВОПРОСЫ`.

### КАК ПРОВЕРЕНО — КРИТЕРИЙ ГОТОВНОСТИ, ЧЕТЫРЕ КОМАНДЫ, КОД ВОЗВРАТА ПЕРВЫМ

Все четыре прогнаны из главной папки `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot` ПОСЛЕ
влития, то есть отвечают на «механизм встал», а не на «коммит виден».

```
1) make check                                  rc=0   486 passed in 29,87 s
2) python3 -m pytest tests/voice -q            rc=0   139 passed
     [голос] надиктованных строк разобрано 28 из 28 · пар «ученик — задачи» 31 · ошибок 0
     [голос] фамилий 56 · падежных форм разрешено 254 из 254 · ошибок 0
     [голос] задач текущего листка разрешено 26 из 26
3) python3 -m pytest tests/voice -q -k "petrov or normal"
                                               rc=0   40 passed, 96 deselected
     именованный тест: test_petrov_tri_pyat_sem_be_resolves_to_petrov_3_5_7b
4) grep -c "token_set_ratio" core/services/golos.py    → 0   (требовалось 0)
```

`486 passed` больше того, что было до меня: на момент отпочкования в `main` было **220**, на
момент влития `main` в ветку — **480**. Мои — 139 в `tests/voice/`; разница 486−480 = 6 приходится
на то, что шестой коммит добавил три теста, а интеграция P7 — ещё три, при этом ни один чужой
тест не покраснел ни разу.

Пункт 2 несёт охват В СЕБЕ и он не синтетический: 28 надиктованных строк — предложения, которые
преподаватель ЭТОГО кондуита сказал бы о детях ЭТОГО кондуита; 254 падежные формы — это все 56
фамилий семени, каждая во всех формах, которые генерирует таблица P7; 26 задач — весь текущий
листок, каждая сказанная так, как её печатает листок, минус непроизносимые украшения (`°`, `*`,
`:)`). Ноль разобранных строк при непустом наборе был бы КРАСНЫМ: соответствующие assert-ы стоят.

**ПОСТ-ПРОВЕРКА ИЗ ГЛАВНОЙ ПАПКИ — зелёная, откат не понадобился.**
```
grep -c 'raspoznavanie\|from infra.llm' core/services/golos.py   → 8    (схему P7 ИМПОРТИРУЕМ)
grep -c 'sha256' core/services/golos.py bot/routers/voice.py      → 4 и 4 (идемпотентность по байтам)
grep -rn 'include_router' bot/app.py                              → 7 строк, голоса среди них НЕТ
```
🔴 Третий греп — ЕДИНСТВЕННОЕ КРАСНОЕ МЕСТО ЭТОЙ ПОЗИЦИИ, и оно красное по устройству задания, а
не по моему поведению: подпись к команде — «роутер голоса реально подключён», а подключается он
в `bot/app.py`, который КОНТРАКТ ЗОНЫ объявляет READ-ONLY без исключений. Я исполнил ЗАПРЕТ и не
тронул файл. Точный патч из трёх строк, с обязательным порядком включения, — пункт 1 в
`## ВОПРОСЫ`; `tests/voice/conftest.py` исполняет ровно этот порядок над настоящим диспетчером,
так что патч доказан до применения. Замер: `grep -c voice bot/app.py` → 0 и `grep -c photo
bot/app.py` → 0 — **два экрана волны влиты и оба мертвы в продакшене**.

### РЕЗУЛЬТАТ ВЕРИФИКАТОРА §3

Свежий субагент, ПОСЛЕ-типа, другим методом: он не читал ни `git diff`, ни `tests/voice/`, а
написал СВОИ пробные скрипты в песочнице, сам загрузил 56 фамилий из `seed/students.csv` и сам
сочинил корпус диктовок, после чего сверял «что имеет в виду русскоговорящий преподаватель» с
тем, что вернул код. В репозиторий он ничего не писал.

- Именованная строка: `parse_dictation("Петров три пять семь бэ")` → `[('петров', ['3','5','7б'])]` — Петров · 3 · 5 · 7б, верно.
- Корпус: **35 строк из 35, ошибок 0.** Покрыты голые числительные, подростковые числа 11–19, составные десятки (24, 45, 52, 67, 83, 99), порядковые и косвенные падежи, **каждая буква таблицы `LETTER_FORMS`** — а, бэ, вэ, гэ, дэ, е, жэ, зэ, диапазоны с обоими закрывателями («по» и «до») включая составной и вырожденный, отрицательные («минус один»), слова-шум и **три** строки с двумя учениками. Все фамилии разрешились в того ребёнка, которого он имел в виду, со счётом 100, включая косвенные (Фёдорову → Фёдоров, Могилевскому → Могилевский) и дефисную Тухватулин-Йалчын.
- **Дефектов не найдено, проверено 35 строк из 35 сочинённых, ошибок 0.**
- Отдельно он прогнал пять внеконтрактных проб, и все выродились аккуратно: «сто» отсутствует в таблице (листков всего 18) и осталось словом, а не стало неверным числом; обратный диапазон «с шестой по вторую» не развернулся назад и не упал, а сохранил оба конца; фамилия не из ростера вернула `unknown`; «Пономарёв три же» дало `3`, а не `3ж`.
- Его финальная строка пришла дословно: `выдано 35 позиций из 35 найденных`.

Одну его находку стоит назвать: `match_surname("петров", ростер)` возвращает `None`/`unknown`/67.
Это ВЕРНО, а не дефект — Петрова в кондуите нет (есть Пирогов, Пономарев, Погорельцев), 67 ниже
порога 70, и отказ угадывать — заданное поведение. Именованная строка задания проверяет РАЗБОР, а
не разрешение в ростер, и тесты разведены соответственно.

### ЧТО НЕ ТРОГАЛ

- `bot/app.py` — вне зоны. Отсюда единственный содержательный долг позиции: экран собран, влит, проверен и в продакшене не подключён. Пункт 1 в `## ВОПРОСЫ`.
- `config.py` — вне зоны, и **не понадобился**: `MARK_SOURCES` уже содержит `голос`, а `GRID_COLUMNS` импортируется. Свои константы (`FUZZY_THRESHOLD`, `AMBIGUITY_MARGIN`, `MAX_ALTERNATIVES`, `MAX_RANGE_WIDTH`, `USER_DICTIONARY_MAX`, `USER_DICTIONARY_OPTIMUM`, `TELEGRAM_GETFILE_MAX_BYTES`, `SHORT_AUDIO_MAX_BYTES`, `ASR_TIMEOUT_SECONDS`, `SOURCE`, `DRAFT_SLOT`) лежат в своих модулях и названы здесь, как велит §6. Из них ОДНА — не моя вовсе: `FUZZY_THRESHOLD` вычисляется из `raspoznavanie.CONFIDENCE_THRESHOLD`, чтобы два экрана не спорили о том, насколько близко — достаточно близко.
- `core/ports.py`, `bot/callbacks.py`, `bot/routers/marking.py`, `bot/routers/photo.py`, `core/services/raspoznavanie.py`, `infra/llm.py`, `Makefile` — вне зоны, только читал и импортировал. Пункты 2, 3, 4, 5, 7 в `## ВОПРОСЫ`.
- `tests/bot/conftest.py` и `tests/grid/conftest.py` — чужие фикстурные файлы. Записывающая сессия в `tests/voice/conftest.py` — их родственник, а не импорт: два писателя в одном файле — единственное, чего волна сделать не может. Дублирование намеренное, ровно как его объявила P4 у себя.
- В главной папке на момент сдачи лежат пять незакоммиченных путей, и ни один из них не есть моя недоехавшая работа — разобраны поимённо, потому что молчание читается как пустота:
  - ` M zhurnal/2026-09-02_spetsmat-bot/kod_P8-golos.md` — **этот файл-заход**. Не коммитится исполнителем по КОНТРАКТУ ЗОНЫ — его коммитит аналитик при приёмке.
  - ` M zhurnal/_INFRA-git/INCIDENTY.md` — **автолог инструмента**: `git_zona.py` сам вписал туда причину моего `--vsyo-ravno` при влитии. Не моя правка руками, но последствие моего хода — называю его явно.
  - ` M README.md`, ` M zhurnal/2026-09-02_spetsmat-bot/PULS-CHASOVOGO-sborka-bota.log`, `?? .commit-plan` — чужие (реестр волны, лог часового, план коммитов оркестратора). Не трогал и не коммитил.

### НЕОБРАТИМОЕ

**Необратимого нет.** Ни одного удаления, переименования, перемещения, `git reset` или `checkout`
поверх несохранённого. Единственное, что вышло за пределы моих новых файлов, — влитие своей ветки
в `main` (`afa5dbf`), и оно предписано блоком WARNING как последний ход; откатывается
`git -C /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot --no-optional-locks reset --hard 5f2e385^^`
(хэш ДО влития — `654c1d6`), но пост-проверка зелёная и откатывать нечего. Удаления кода внутри
СВОЕЙ зоны (форк, снесённый коммитом `e55d7aa`) обратимы через историю ветки: `git show e55d7aa^`.

### ПОВТОРЯЕМОСТЬ НАХОДОК

**Повторятся на следующей единице работы — класс НЕМЕДЛЕННОЕ, чинить ДО следующего прогона:**

1. **Роутер, влитый и не подключённый, потому что `bot/app.py` вне зоны.** Уже повторилось: P7 (фото) и P8 (голос), оба экрана мертвы в `main` прямо сейчас. Следующая — P15 (быстрый текст), чей заход НЕСЁТ это предупреждение в своём тексте, то есть находка повторится в третий раз, будучи известной заранее. Это не пункт очереди, а одна правка `bot/app.py`.
2. **Рабочая папка, отпочкованная до влития зависимости, и нечем это заметить.** Повторится у любой позиции волны, стартовавшей не первой: §0.1 проверяет чужие НЕВЛИТЫЕ ветки и по построению молчит про основную, ушедшую вперёд. Цена уже измерена здесь — четверть работы позиции на форк чужого. Закрывается одной строкой в §0.1: `git --no-optional-locks log --oneline HEAD..main | wc -l` первым ходом.

**Не повторятся — законно уходят пунктами очереди** (`## ВОПРОСЫ` 2, 3, 6, 7): дом протокола
`Transcriber`, дом payload-ов голоса, недостижимость буквы «и» голосом, четыре пары копий
приватных помощников P4. Каждое — свойство этого конкретного экрана, а не следующего.

Пункт 4 (`rapidfuzz` в `deps`) повторился бы, но заявка на него УЖЕ СТОИТ по находке P7
(`2026-09-02T1236-…`); новой я не ставил, чтобы не плодить дубль, и добавил к ней уточнение.

### СЧЁТ НЕЗАКРЫТОГО — ПРОВЕРЕН ПЕРВЫМ ХОДОМ, КАК ВЕЛЕЛ ЗАХОД

Снимок сборки не проверялся командой `schet_nezakrytogo.py`: `## 0. ПЕРВЫЙ ХОД` §0.1 был отменён
оркестратором целиком, и вместо всего блока предписана ОДНА команда — счёт невлитых веток. Её
вывод (`1`) стоит в `## ГИГИЕНА ВХОДА`. Это отклонение от текста захода названо здесь явно, а не
умолчано: отменил его оркестратор, а не я.

### ЧИСЛА ФАКТОМ, НЕ ПАМЯТЬЮ (шаг 6 блока WARNING)

```
вне git, моя рабочая папка  /Users/…/spetsmat-bot-wt/P8-golos : 0
вне git, главная папка      /Users/…/spetsmat-bot             : 5   ← ни одного моего кода; разбор выше в «ЧТО НЕ ТРОГАЛ»
невлитых веток после влития (git branch --no-merged main)     : 0
невывезенных СВОЕЙ ветки                                      : неприменимо — у репозитория 0 удалённых
пост-проверка из главной папки                                : ЗЕЛЁНАЯ (rc=0, 486 passed), откат не понадобился
Г1 зона доехала в git, все четыре пути                        : ✅ ✅ ✅ ✅
Г2 второй репозиторий                                         : неприменимо — вся зона внутри spetsmat-bot
Г3 невлитых не прибавилось                                    : на входе 1 (своя), на выходе 0
Г4 новый .py в _generator/**                                  : неприменимо — папки _generator в этом репозитории нет
Г5 новый .md зарегистрирован                                  : неприменимо — новых .md позиция не заводила (0)
Г6 чужих путей в коммитах                                     : 0 — только core/services/golos.py, infra/asr.py,
                                                                bot/routers/voice.py и пять файлов tests/voice/
```

**Время прогона и токены** снимает ПРИЁМКА из лога прогона — исполнителю счётчик недоступен, он
снаружи сессии.

### НЕСДЕЛАННОГО НЕТ — все части задания закрыты по порядку

§1 приём · §2 распознавание за протоколом · §3 нормализация числительных · §4 два канала ·
§5 подтверждение и идемпотентность через переиспользование · §6 константы названы. Единственное,
что осталось за границей, — три строки в `bot/app.py`, и они остались там потому, что зона
запрещает их написать, а не потому, что до них не дошли.

## ПРАВКИ ПОСЛЕ ВЫДАЧИ — (заполняет АНАЛИТИК; исполнитель ЧИТАЕТ)
> 🔴 **Пусто — значит заход не правился с момента выдачи.** Непустой блок читается ПЕРЕД продолжением работы: правка отменяет любое противоречащее ей место выше по файлу, каким бы категоричным оно ни было.
> **Форма строки — жёсткая, по ней судит приёмка:** `### ПРАВКА N · ГГГГ-ММ-ДД ЧЧ:ММ · <что изменилось, одной фразой>`, дальше — что именно перечитать и что откатить, если уже сделано по старой редакции.
> **Аналитик:** внёс правку — обязан ОТДЕЛЬНО послать владельцу короткое сообщение для пересылки исполнителю. Правка, лежащая только в файле, до работающего исполнителя не доезжает: он файл не перечитывает сам.
> **Исполнитель:** прочитал правку — назови её номер в `## ОТЧЁТ` строкой `ПРАВКИ ПРОЧИТАНЫ: 1, 2`. Нет строки при непустом блоке = отчёт не принимается: неизвестно, по какой редакции работали.

<правок нет>

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `<принято | доработка | отклонено>` — `<почему именно так, одной фразой: что проверено и чем>`

**ВЕТКА РАБОТЫ:** `zahod/P8-golos`
*(проверяется фактом, не словом: ветка обязана существовать и быть либо ВЛИТА в основную, либо названа в открытой заявке на влитие. Ни того, ни другого — Г14 краснеет. Снять состояние: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py poteri --branch <ветка>`)*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**
> Адрес списка: `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/_INFRA-git/zayavki`
> Читается командой (из любой папки, в том числе из worktree): `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki`
> Ставится командой: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavka --rod <git-operaciya|pravka-koda> "<текст>"`
> 🔴 Вопрос здесь НЕ «что ты хочешь сделать», а «что ты УЖЕ положил в очередь». Дубль сверяется с очередью по id машинно; намерение сверить не с чем.

- `<id заявки>` — `<род>` — `<суть одной строкой: влитие / коммит / вывоз / деплой / гашение>`

*(Заявок эта приёмка не ставила — так и напиши строкой «заявок нет: <почему ни одна из пяти операций не понадобилась>». Пустая строка и прочерк не принимаются: молчание неотличимо от «забыл».)*
