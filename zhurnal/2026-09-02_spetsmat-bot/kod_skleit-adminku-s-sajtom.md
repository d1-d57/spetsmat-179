# Канал исполнителя — skleit-adminku-s-sajtom (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — вёрстка под глаз владельца и сведение двух разошедшихся страниц — дешёвая модель уже дала страницу, названную корявой.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Opus 5 — вёрстка под глаз владельца и сведение двух разошедшихся страниц; дешёвая модель уже дала страницу, которую владелец назвал корявой.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/skleit-adminku-s-sajtom.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/2026-09-02_spetsmat-bot/kod_skleit-adminku-s-sajtom.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add skleit-adminku-s-sajtom --branch zahod/skleit-adminku-s-sajtom, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.

Заход мог быть ПОПРАВЛЕН после того, как ты стартовал: аналитик правит
тот же файл, а ты прочитал его один раз. Смотри секцию ## ПРАВКИ ПОСЛЕ
ВЫДАЧИ в конце файла — при старте и всякий раз, когда владелец пишет
тебе в чат. Правка с номером, которого ты не читал, отменяет любое
противоречащее ей место выше.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
ГРАНИЦА ОБЛАСТИ: сырые подстроки в `kod_*.md` (пункт 4) — НЕ парсер очереди `dostavit_urok` (который считает только пары ДОМ:/ДОСТАВЛЕНО:). Разница в числах — законна.
🔴 снимок при сборке 2026-09-05, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/schet_nezakrytogo.py zhurnal/2026-09-02_spetsmat-bot`
Область: «zhurnal/2026-09-02_spetsmat-bot» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 25
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 255
     из них разбором очереди (парсер `dostavit_urok`, записи с парой ДОМ:/ДОСТАВЛЕНО:): 137
       живых (чинится доставкой — «дом есть»)  : 25
       к владельцу (решение за человеком)      : 51
       адрес недоступен (нет/папка/код/указат.) : 59
       адрес не разобран                        : 0
       отработавших (машинный след закрытия)    : 0
       доставлено                               : 2
       🔴 не проверяется машиной: содержательная отработанность записей БЕЗ следа закрытия (метки в доме, строки ✅/ЗАКРЫТО) — нужна ревизия человеком; сырой греп сверх разбора — шаблонные строки формы.

🔴 ДВЕРЬ НЕЗАКОММИЧЕННЫХ `kod_*.md` ОТКРЫТА КЛАПАНОМ: в арке `zhurnal/2026-09-02_spetsmat-bot` есть 1 незакоммиченных файлов-заходов `kod_skleit-adminku-s-sajtom.md` — второй заход в такую арку обычно не собирается (долг 4 `disciplina-git`), но АНАЛИТИК открыл клапан, причина дословно: «грязь — предыдущая сборка ЭТОГО ЖЕ захода checkout-типа, которую и заменяем на worktree-версию; чужой работы в ней нет». Отключение видно здесь, в артефакте, а не осталось решением в голове аналитика.

КОНТЕКСТ. <проект в 1–2 фразы>. Прошлый этап: <состояние>. ЦЕЛЬ: <что закрыть>.
Приёмка — по ОТЧЁТУ, без построчной сверки. <Если стоп до цели: получишь X, но НЕ Y.>

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-09-05) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. редактор из файла становится страницей правки САЙТА за паролем; публичная статика docs/index.html не меняется
2. один шаблон, два режима: файл (данные вморожены, localStorage, Скачать файлом) и сервер (данные из базы, POST /api/enrollment)
3. четверг и суббота пока ОДИНАКОВЫ — решение владельца 04.09
4. готовую пробу proverka.js прогнать, свою не писать

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../spetsmat-bot-wt/skleit-adminku-s-sajtom`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/skleit-adminku-s-sajtom`. 🔴 **ДАЛЬШЕ — ТОЛЬКО ПУТИ ОТНОСИТЕЛЬНО ЭТОЙ ПАПКИ** (или `cd` в неё безусловно, каждым ходом): абсолютный путь в главную папку репозитория здесь — типичная ошибка, правка утекает МИМО worktree и найдётся только на коммите («вне git» в `git_zona.py check --zone` из рабочей папки, на файле, который уже правил, — цена, оплаченная живьём: 5 файлов, ручное копирование и откат главной папки). 🔴 **Заводишь её САМ — ВТОРЫМ ходом, СРАЗУ ПОСЛЕ пункта 1 блока §0.1 ГИТ-КОНТУР ниже (не первым ходом захода целиком), из корня репозитория:**
  ```
  python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add skleit-adminku-s-sajtom --branch zahod/skleit-adminku-s-sajtom
  cd ../spetsmat-bot-wt/skleit-adminku-s-sajtom
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце вливает САМ ЗАХОД, последним ходом** (решение 20.08, заход `granica-priyomki`, пересмотр решения 05.08; WARNING-блок ниже печатает тот же порядок): цепочкой коммит зоны → пуш → влитие в основную (`git_zona.py vlit-v-osnovnuyu`) → пост-проверка ИЗ ГЛАВНОЙ папки → гашение СВОЕЙ ветки. Причина пересмотра — замер 20.08: изнутри захода не видно разницы между «механизм новый в рабочей папке» и «в `main` он старый», а приёмке это невидимо по построению. Влитие УСЛОВНОЕ: красная пост-проверка = откат влития самим заходом и строка в отчёт, а не «доложу, пусть приёмка решает». Файл-заход по-прежнему НЕ коммитить — его коммитит аналитик при приёмке.
- **ЗОНА (можно менять):** `veb/templates/index.html` `veb/server.py` `veb/sobrat_fajl.py` `tools/sobrat_stranicu.py`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4); коммить ПО ХОДУ работы, не одним последним ходом (§4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **SCRATCHPAD — ТОЛЬКО ЛИЧНЫЙ.** Черновики, выкладки, промежуточные версии — в личную папку СВОЕГО захода `scratchpad/skleit-adminku-s-sajtom/`. Общие пути (`scratchpad/otchet.md`, любой `scratchpad/*` без имени твоей темы) ЗАПРЕЩЕНЫ: чужой отчёт уедет в твой файл или твой — в чужой, а приёмка читает отчёт без построчной сверки и подмену НЕ ЛОВИТ по построению. *Цена 25.08: готовый `## ОТЧЁТ` захода konvejer-incidentov был записан в общий `scratchpad/otchet.md`, и 92 строки чужого отчёта простояли в `kod_slovari-v-kod.md`.*
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода «kod_registracia-bez-obhoda.md»).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает названные через `--vlit` ЧУЖИЕ ветки В ОСНОВНУЮ, и только ПОТОМ ты заводишь свою рабочую папку; СВОЮ ветку он не трогает никогда — её вливаешь ты сам последним ходом (граница прав ниже).** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. 🔴 ПОРЯДОК ПРИ ПАДЕНИИ ЭТОГО ВЫЗОВА — исполняй, не изобретай (движок роняет `network_error` на вызове субагента и уносит с собой ВЕСЬ заход, замер волны 3A — 4 захода из 13). Пауза 45 секунд, до трёх попыток; время меряй `date`, не суммой своих `sleep`. После третьей — работай БЕЗ субагента: контур остаётся неразобранным, и это ОТДЕЛЬНАЯ строка в `## ОТЧЁТ`, а не молчание. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты сам, по ходу работы (§4). 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/bootstrap_zahod.py --zadanie-subagentu --vlit zahod/sostav-klassa-i-112-strok --zone veb/templates/index.html --zone veb/server.py --zone veb/sobrat_fajl.py --zone tools/sobrat_stranicu.py --kommitit 'хвост аналитика в основной папке: README.md, deploy/ADRES.txt, _studio/veb-raspredelenie/TZ.md, zhurnal/_INFRA-git/INCIDENTY.md' --zakryt 'ветку zahod/sostav-klassa-i-112-strok после её влития'
```

🔴 ГРАНИЦА ПРАВ, ЧЕТЫРЕ ОТВЕТА (та же, что в самом задании субагенту — одно место в тексте, а не пересказ): **кто вливает ЧУЖИЕ названные (`--vlit`) ветки** — субагент, в ОСНОВНУЮ ветку, до заведения твоей папки; **кто вливает СВОЮ ветку этого захода** — ты сам, последним ходом, после коммита зоны (`git_zona.py vlit-v-osnovnuyu`; решение владельца 25.08 — оно сняло противоречие волны 4, когда машинное §0.1 и текстовое «ветку НЕ вливать» спорили молча, и машинное побеждало); **кто закрывает заявки** — субагент, `zayavka-zakryt`; **кто коммитит пути ВНЕ зоны захода** — субагент (хвост Cowork и что назовёт пункт 3 его задания). Ты коммитишь ТОЛЬКО зону этого захода, по ходу работы (§4). 🔴 Конфликт на `README.md` при ЛЮБОМ слиянии разрешается ОБЪЕДИНЕНИЕМ записей реестра, НИКОГДА выбором стороны: параллельные заходы волны дописали в реестр по строке — обе записи правы, выбор одной молча уничтожает регистрацию соседа.

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

Что влить, названо аналитиком и уехало в задание субагента (1): `zahod/sostav-klassa-i-112-strok`. Влитие идёт в ОСНОВНУЮ ветку, не в твою.


- деплоя в этом заходе нет.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/skleit-adminku-s-sajtom`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add veb/templates/index.html veb/server.py veb/sobrat_fajl.py tools/sobrat_stranicu.py` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: `названные файлы-якоря`. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

🔴 **WRITE YOUR `## ОТЧЁТ`, `## ПЛАН` AND `## ВОПРОСЫ` IN ENGLISH, AND EVERY FILE AND EVERY COMMIT MESSAGE YOU PRODUCE TOO.** Owner's decision 30.08. It is a каркас-level rule, not a preference — wave 2 lost it twice because the pass text listed the report SECTIONS and never said «every file you create». Fixed Russian addresses stay Cyrillic: `ЦЕНА:` · `ВЕРДИКТ:` · `ДОМ:` · `ДОСТАВЛЕНО:` · `ПОДЪЁМ:` · `[ДОЛГ: …]` · every `## ` heading of this file · every path and command.
### ЧТО ЭТО ЗА РАБОТА, ОДНОЙ ФРАЗОЙ

Страница правки распределения на сайте отстала от рабочего редактора, которым владелец
пользуется вживую. Их надо свести в ОДИН шаблон с двумя режимами — файловым и серверным.

🔴 **Текст, который ВИДИТ ВЛАДЕЛЕЦ на странице, — по-русски.** Это интерфейс, а не отчёт.

### ЧАСТЬ 0 — СВОЙ ЗАМЕР «ДО», ПЕРВЫМ ХОДОМ

Числа ниже сняты при сборке 05.09 около 00:20 и могли устареть. Вставь вывод в `## ОТЧЁТ`:

```
python3 -c "import re,io;print({f:sorted(set(re.findall(r'data-v=\"([^\"]+)\"|id=\"(t-[^\"]+)\"',io.open(f,encoding='utf-8').read()))) for f in ('veb/templates/index.html','docs/index.html')})"
wc -c veb/templates/index.html docs/index.html ../materials/spetsmat-2026/raspredelenie-fajl/shablon.html
```
При сборке: `veb/templates/index.html` — 12 КБ и ДВЕ вкладки (`t-shk`, `t-prp`);
`docs/index.html` — 63 КБ и пять; редактор `shablon.html` — 27 КБ и пять.

### ЧАСТЬ 1 — АРХИТЕКТУРА. НЕ ИЗОБРЕТАТЬ, ОНА УЖЕ РАБОТАЕТ В ФАЙЛЕ

Источник — `../materials/spetsmat-2026/raspredelenie-fajl/shablon.html`. Устроен так:
данные лежат ОДНИМ блоком `<script id="dannye" type="application/json">`, а страница их
только рисует. Отсюда два режима из одного шаблона:

| | режим ФАЙЛ | режим СЕРВЕР |
|---|---|---|
| откуда данные | вморожены генератором `sobrat.py` | подставляет `veb/server.py` |
| куда сохраняются | `localStorage` + кнопка «Скачать файлом» | `POST /api/enrollment` |
| кто открывает | владелец и Даня, с диска, офлайн | организаторы, за паролем |

🔴 **Ветвление ровно в двух местах** — откуда читаем и куда пишем. Вся вёрстка, вся
раскладка и вся логика общие. Разведёшь их на две копии — будете чинить каждую правку
дважды, ровно из-за этого сегодняшний разъезд и случился.

🔴 **`docs/index.html` НЕ ТРОГАТЬ.** Это публичная статика для школьников; она уже верна
и выложена. Другая страница для других людей.

### ЧАСТЬ 2 — ЧТО ПЕРЕЕЗЖАЕТ ИЗ ФАЙЛА НА САЙТ

Ничего из этого на серверной странице сейчас нет. Формулировки — слова владельца.

1. **Пять вкладок** в порядке `школьникам · В · Д · Н · преподавателям`.
2. **Назначение ГРУППЫ преподавателю** — выпадающий список В·Д·Н прямо в его строке.
   Формат строки: **имя, потом число школьников, потом группа** («Ольга Рыжая 2 В»).
   Смена группы **уводит его школьников вместе с ним**.
3. **Три ступени школьника**, и это главное отличие от старой страницы:
   `— нигде` (выпадает со всех вкладок групп) · `группа В` (в группе, преподаватель
   не выбран) · конкретный преподаватель. Подписи ровно такие, без пояснений.
4. **Таблетки у преподавателя**: фамилии его школьников в ТУ ЖЕ строку, клик снимает.
   Крестик показывается только под курсором — постоянный забирал место и ронял перенос.
5. **Кабинет группы правится в шапке** вкладки группы, один раз, не в каждой строке.
6. **Шапка группы**: `Группа В · старший · кабинет · N школьников · M ждут`.

🔴 **Чего НЕ должно появиться:** пометок «авто» и «предположение», предзаполнения
догадками, заголовков вида «у кого кто» и «школьники группы — 20», кнопки «скопировать
текстом». Всё это владелец уже вычеркнул поимённо.

### ЧАСТЬ 3 — ЧТО ПОЧИНИТЬ ПО ДОРОГЕ

**Д1. `/api/prepodavateli` отвечает 403 ВСЕГДА.** `veb/server.py:456` требует роль
организатора, а пароль выключен (`SVOBODNAYA_PRAVKA`, строка 75) — куки никто не ставит,
роли нет, отказ на любой запрос. Без этого вкладка преподавателей мертва на сервере.

**Д2. Дни названы неверно.** `tools/sobrat_stranicu.py` зовёт `2026-09-05` четвергом —
это СУББОТА; `2026-09-06` назван субботой — это воскресенье. Четверг был 03.09,
следующий 10.09. Проверяется одной строкой:
```
python3 -c "from datetime import date;print([(d,['пн','вт','ср','чт','пт','сб','вс'][date(*map(int,d.split('-'))).weekday()]) for d in ('2026-09-05','2026-09-06','2026-09-10')])"
```
**Оба дня сейчас ОДИНАКОВЫ** — решение владельца 04.09: пока преподаватели ходят и в
четверг, и в субботу, раскладка одна. В базе слоты 1 и 2 уже совпадают (45 и 45 строк).

### ЧАСТЬ 4 — ПРОБА, КОТОРАЯ УЖЕ НАПИСАНА

`../materials/spetsmat-2026/raspredelenie-fajl/proverka.js` — 43 проверки через `jsdom`,
гоняет весь круг: пять вкладок, три ступени, снятие таблеткой, перевод преподавателя,
память браузера, выгрузка и открытие выгруженного. **Прогони её, свою не пиши:**
```
npm install jsdom --silent && node ../materials/spetsmat-2026/raspredelenie-fajl/proverka.js
```
Меняешь селекторы в шаблоне — правь пробу вместе с ними, а не выключай.

### **КРИТЕРИЙ ГОТОВНОСТИ — проверяется КОМАНДОЙ, вывод вставь в `## ОТЧЁТ`**

| # | клауза | чем проверяется |
|---|---|---|
| 1 | вкладок на серверной странице пять и в нужном порядке | замер части 0, повторённый после |
| 2 | группа преподавателя меняется через сайт | POST → `select gruppa from teachers` вернуло новое |
| 3 | `/api/prepodavateli` не отвечает 403 без куки | `curl -o /dev/null -w '%{http_code}'` → не 403 |
| 4 | три ступени есть в разметке | значения `""`, `g:В`, `g:Д`, `g:Н` присутствуют в `<select>` |
| 5 | файл по-прежнему собирается и работает офлайн | `python3 ../materials/spetsmat-2026/raspredelenie-fajl/sobrat.py`, затем в собранном файле обращений к сети 0 |
| 6 | готовая проба зелёная | `node …/proverka.js` → rc=0, «все пробы прошли» |
| 7 | `docs/index.html` не изменился | `git diff --stat docs/index.html` пуст |
| 8 | 🔴 **ЖИВОЙ ПРОГОН НА РЕАЛЬНОМ ОБЪЕКТЕ** | поднять сервер на живой `data/spetsmat.db`, открыть страницу правки в браузере, пройти круг руками: сменить группу преподавателю · снять школьника таблеткой · назначить заново · поставить «нигде» и убедиться, что выпал из вкладки группы. **Скриншот каждой из пяти вкладок — в отчёт** |

🔴 **Клауза 8 не заменяется тестами и не пропускается.** Замер фабрики: 486 зелёных
тестов при боте, который не поднимался; 18 гейтов из 18 при неработающей функции; пять
таких случаев за день, и КАЖДЫЙ нашёл человек. Владелец эту страницу уже получал
«готовой» и называл корявой — ровно потому, что её никто не открыл глазами.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)

Верификатор нужен, тип — **ПОСЛЕ-типа** — судит результат, стоит в конце, после задачи. Свежий субагент, ДРУГИМ методом (открыть собранный файл и серверную страницу, скриншот каждой из пяти вкладок обеих), не перечитывает свою же правку. Доля сплошной выборки: пять вкладок в двух режимах, 100%. Финальная строка ответа обязательна дословно: «выдано N позиций из M найденных» — без неё ответ считается усечённым и в отчёт не вставляется.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПО ХОДУ РАБОТЫ, НЕ ОДНИМ ПОСЛЕДНИМ ХОДОМ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая. Каждую завершённую часть работы коммить СРАЗУ, теми же двумя ходами — не копи всё к финальному ходу:
```
git --no-optional-locks add -- veb/templates/index.html veb/server.py veb/sobrat_fajl.py tools/sobrat_stranicu.py                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- veb/templates/index.html veb/server.py veb/sobrat_fajl.py tools/sobrat_stranicu.py   # отсекает всё чужое
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **КОММИТЬ ПО ХОДУ — РЕШЕНИЕ ВЛАДЕЛЬЦА 25.08 (В11), ПЕРЕВЕРНУВШЕЕ прежний канон «одним последним ходом».** Цена прежнего канона: за сутки ДВА обрыва — канал `opencode run --auto` односторонний и умирает вместе с сессией (владелец закрыл ноутбук), и незакоммиченная работа пропадала целиком. Закончил кусок — закоммитил его; последний ход только ПРОВЕРЯЕТ, что коммитить нечего (`git status --porcelain` пуст, `git_zona.py check --zone` ✅).
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `veb/templates/index.html` `veb/server.py` `veb/sobrat_fajl.py` `tools/sobrat_stranicu.py`

- **Г1. Зона доехала в git.** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone veb/templates/index.html` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone veb/server.py` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone veb/sobrat_fajl.py` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone tools/sobrat_stranicu.py` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `spetsmat-bot` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged main` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## ⚠️🔴 WARNING · ПОСЛЕДНИЙ ХОД ПЕРЕД ОТЧЁТОМ — ПОЛНАЯ ГИТ-ГИГИЕНА. НЕ ПРОПУСКАТЬ 🔴⚠️

**СТОП. Прежде чем писать хоть строку в `## ОТЧЁТ` — прогони это целиком.** Требование владельца
2026-08-16, поводом стал заход, который сделал работу и не закоммитил НИЧЕГО: *«он ничего не
коммитит… всегда, когда собираешь по генератору, там должна быть фраза, что начинать надо с полной
проверки гигиены»*. Гит-контур §0.1 стоит в НАЧАЛЕ и разбирает то, что накопилось ДО тебя; этот блок
стоит в КОНЦЕ и разбирает то, что накопил ты сам. Один другого не заменяет.

**ПОРЯДОК ЖЁСТКИЙ, ОН НАЗВАН ВЛАДЕЛЬЦЕМ: коммит → влитие своей ветки в основную → пост-проверка
ИЗ ГЛАВНОЙ ПАПКИ → гашение → вывоз.** Обратный порядок не работает технически: влитие отказывает
на грязном дереве, пост-проверка неисполнима до влития, вывоз — на невлитом. **Решение владельца
20.08 пересматривает решение 05.08** («в конце может это worktree вливать, но, наверное, это не
нужно делать») — теперь вливает САМ заход, но только при зелёной пост-проверке.

**1 · ВСЕ КОММИТЫ.** Ничего не осталось вне git — ни в рабочем репозитории, ни в соседних, до
которых ты дотянулся по ходу работы:
```
for R in <репозитории, которых ты касался>; do
  echo "== $R"; git -C $R --no-optional-locks status --porcelain
done
```
Своя зона — своими путями (`add` + `commit -- <пути>`). Чужая содержательная работа — НЕ твоя:
называешь строкой в отчёте и оставляешь. Пусто у всех — так и напиши числом «вне git 0».

**2 · ВЛИТИЕ СВОЕЙ ВЕТКИ В ОСНОВНУЮ.** Только после того, как шаг 1 дал «вне git 0» на своей
зоне — влитие отказывает на грязном дереве:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py vlit-v-osnovnuyu zahod/skleit-adminku-s-sajtom --zone <своя зона> \
    --vsyo-ravno "своя рабочая папка ещё жива — влитие последним ходом захода, штатно"
```
Конфликт — ЗАКОННЫЙ исход, не повод форсировать: разрешай по существу, если понимаешь обе
стороны; не понимаешь — `git_zona.py vlit-v-osnovnuyu --abort`, ветка остаётся невлитой,
строка в отчёт и заявка на влитие (`git_zona.py zayavka --rod git-operaciya`).
🔴 Конфликт на `README.md` — только ОБЪЕДИНЕНИЕМ записей реестра, никогда выбором стороны:
параллельные заходы волны дописали по строке — обе записи правы, выбор одной молча уничтожает
регистрацию соседа.

**3 · ПОСТ-ПРОВЕРКА ИЗ ГЛАВНОЙ ПАПКИ.** Отвечает на вопрос «механизм ВСТАЛ», а не «коммит
виден»: прогон изменённого механизма из `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot`, НЕ из рабочей папки `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/skleit-adminku-s-sajtom` плюс `grep` по ЖИВОМУ файлу,
который его зовёт (хук, конвейер, генератор):
```
cd /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot && <команда прогона механизма, который заход менял> && echo $?
grep -n '<как механизм назван в вызывающем коде>' <живая точка вызова>
```
🔴 **Красная пост-проверка = ОТКАТ ВЛИТИЯ И СТРОКА В ОТЧЁТ**, а не «доложу, пусть приёмка
решает»: `git_zona.py vlit-v-osnovnuyu --abort`, если слияние ещё не закоммичено, иначе
`git -C /Users/ivanyakovlev/Documents/GitHub/spetsmat-bot --no-optional-locks reset --hard <хэш ДО влития>`. Заход, который влил
и сломал `main`, обязан вернуть `main` сам.

**4 · ГАШЕНИЕ.** Невлитого не осталось: `git --no-optional-locks branch --no-merged main`.
Каждая оставшаяся ветка названа поимённо с причиной, почему она жива. 🔴 Ветку-витрину (`main`
там, где с неё публикуется сайт) НЕ вливать — влитие туда есть публикация и решение владельца.

**5 · ВЫВОЗ.** Вывези СВОЮ ветку; `main` НЕ вывози: если после работы в нём есть невывезенное,
поставь заявку `--rod git-operaciya` и назови число в отчёте. Команда для своей ветки:
`git --no-optional-locks log --oneline @{u}.. | wc -l` → 0.
Ненулевое на своей ветке означает, что работа существует только на этом диске.

**6 · ПРОВЕРКА ФАКТОМ, А НЕ ПАМЯТЬЮ.** Числа по каждому репозиторию — вне git · невлитых своих
и чужих; невывезенных СВОЕЙ ВЕТКИ, не по каждому репозиторию · результат пост-проверки
(зелёная/откачена) — печатаются командой и уходят в `## ОТЧЁТ` дословно. Ненулевое число или
красная пост-проверка без объяснения — приёмка читает как несделанную работу: она гоняет те же
команды первым ходом.

🔴 **Отчёт без этих чисел не принимается.** «Я закоммитил» — не то же самое, что `status --porcelain`
пустой: за одну сессию работа не доезжала трижды, каждый раз с честным «сделано» в отчёте.
## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Команды, которые заход прописывает исполнителю, идут БЕЗ `GIT_ZONA_REPO` — и молча отвечают про чужой репозиторий
ЦЕНА: первый ход захода — `git_zona.py worktree add …` без переменной и без `cd` в корень репозитория. Из домашней папки он завёл рабочую папку и ветку `zahod/skleit-adminku-s-sajtom` В РЕПОЗИТОРИИ `disciplina` и напечатал ✅. Снимал я это уже сам: `worktree drop` + `branch -d` в чужом репозитории, три хода до начала работы. Защита у инструмента ЕСТЬ и она сработала со второго раза («рабочая папка принадлежит другому репозиторию, чем инструмент») — но только когда `cwd` уже лежал в репозитории; из нейтральной домашней папки сравнивать не с чем, и отказа нет. Тот же дефект второй раз в том же заходе: субагенту гит-контура предписан `git_zona.py zayavki` тоже без переменной, и СНИМОК ВХОДА получил «заявок нет» про `disciplina`, тогда как в `spetsmat-bot` открыта 1 заявка и 23 переадресованы. Снимок входа — доказательство, что долги были закрыты; ложный снимок доказывает пустоту.

### `schet_nezakrytogo.py <арка>` в этом репозитории не работает вовсе, а заход велит звать его ПЕРВЫМ ходом
ЦЕНА: команда из шапки захода отвечает «🔴 под область не попало ни одного файла… счёт НЕ снимался, цифр нет — это отказ, а не состояние» и из главной папки, и из рабочей. Инструмент ищет область под `_studio/zhurnal/<арка>`, а в `spetsmat-bot` журнал лежит в корне (`zhurnal/<арка>`). Один ход на выяснение, и печатные числа за заход так и не сняты — в отчёт идут только те, что вписаны при сборке.

### `check_tool_contract.py`, позванный в ПРОЕКТНОМ репозитории, называет лечение, которого там нет
ЦЕНА: гейт требует спутник-фикстуру со строкой `# TOOL-CONTRACT-COVERS:` в `fixtures/*/PROGNAT.sh`. В `spetsmat-bot` каталога `fixtures/` нет и ни одного `PROGNAT.sh` тоже. Закрыть замечание можно ровно двумя способами, и оба — порча: поставить неправдивую пометку `# TOOL-CONTRACT: no-input` на инструмент, который разбирает флаги, или выбросить полезные флаги (`--baza`, `--den`, `--vyhod`) ради зелёного гейта. Один ход на выяснение; вторую половину замечания (три исхода `rc`) я закрыл по существу, она была права.

### Клауза критерия, указывающая на файлы ВНЕ зоны, зеленеет независимо от работы захода
ЦЕНА: клауза 6 требовала прогнать `proverka.js` — а она читает `raspredelenie.html`, собранный `sobrat.py` из `shablon.html`, и все три лежат в `materials/`, который этому заходу READ-ONLY. Прогон дословно ничего о заходе не сообщает. Пришлось строить прогон в три прохода (тот же шаблон на старых данных · на живых · контроль конвейером `materials/`), чтобы отделить свои поломки от чужих; это и есть цена — лишний круг на то, чтобы у клаузы появился смысл. Контроль, кстати, показал, что три красных проверки красны и у НЕТРОНУТОГО конвейера владельца.

### Готовая проба, которую велено прогнать, не запускается на машине владельца
ЦЕНА: `proverka.js` во второй строке несёт `/sessions/fervent-beautiful-mccarthy/mnt/materials/...` — путь песочницы, которого на этом маке нет. Команда клаузы 6 (`node …/proverka.js`) падает на чтении файла. Проба лежит в `materials/`, READ-ONLY, — то есть починить её на месте заход не вправе; я гонял копию с поправленным путём. Исполнитель, читающий клаузу буквально, получит красное на ровном месте и не отличит его от собственной поломки.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Read:** `veb/templates/index.html`, `veb/server.py`, `tools/sobrat_stranicu.py`, `tests/veb/test_server.py`,
`docs/index.html` (measurement only), and the three anchors in `materials/spetsmat-2026/raspredelenie-fajl/`
(`shablon.html`, `sobrat.py`, `proverka.js`). Live schema read from `data/spetsmat.db`. Nothing else opened.

### The shape of the work

One template — `veb/templates/index.html` — carrying the editor that already works in the file
(`shablon.html`: five tabs, three steps, pills, group header). Two branch points and no third:

| | where data comes from | where an edit goes |
|---|---|---|
| file mode | frozen into `<script id="dannye">` by the builder | `localStorage` + «Скачать файлом» |
| server mode | `GET /api/view?slot=1`, remapped to the same state shape | `POST /api/enrollment` / `/api/prepodavateli` / `/api/kabinety` |

Everything between those two points — markup, CSS, render, three steps, pills, group header — is shared text.

### Steps

1. **`veb/templates/index.html`** becomes the shablon page: five static `<button data-v>` tabs in the
   owner's order (`школьникам · В · Д · Н · преподавателям`), teacher group `<select>`, three-step
   student `<select>` (`""` · `g:В` · `g:Д` · `g:Н` · teacher id), removable pills, cabinet in the group
   header. Two mode hooks: `vzyat()` (load) and `zapisat()` (persist), assigned inside two delimited blocks.
2. **`veb/sobrat_fajl.py`** (new, in zone) builds the offline file from that same template: deletes the
   `/*РЕЖИМ-СЕРВЕР*/` block, sets the mode flag, freezes the JSON in place of `/*ДАННЫЕ*/`.
3. **`veb/server.py`**: Д1 — `/api/prepodavateli` honours `SVOBODNAYA_PRAVKA` exactly like
   `/api/enrollment`, so it stops answering 403 to everyone; add `deystvie: "gruppa"`; teach
   `POST /api/enrollment` the two teacher-less steps; `/api/view` additionally returns the `gruppy`
   list and the student's remembered group.
4. **`tools/sobrat_stranicu.py`**: Д2 — `2026-09-05` is Saturday, not Thursday. Thursday is `2026-09-10`.
   Both days stay identical (owner's decision 04.09), so this is a naming fix, not a layout fix.
5. Run the owner's `proverka.js` against the file built by step 2, then clauses 1–8.

### Assumptions stated before the code, per §1

* **The server needs somewhere to remember «in group В, no teacher».** `enrollment.teacher_id` is
  `not null`, so the middle step cannot live in `enrollment`. It goes into a nullable `students.gruppa`
  column, created at runtime by the same silent-ensure idiom `_obespechit_aktivnost` already uses for
  `teachers.aktiven` — `migrations/` is outside my zone, so a numbered migration is not mine to add.
* **Un-assigning a student** deletes the open interval when it was opened today (nothing happened yet —
  the same reasoning the code already applies in `MoveNotForward`), and otherwise closes it with today.
  `check (valid_from < valid_to)` leaves no third option.
* **`slot`.** The page keeps showing slot 1. Slots 1 and 2 hold 45 identical open rows each; the owner's
  decision of 04.09 is that Thursday and Saturday are the same, so one slot on the page is the current truth.

### Where I think the readiness criterion is wrong — said before the work, per §1

* **Clause 6 as written proves nothing about this заход.** `proverka.js` reads
  `raspredelenie.html`, built by `materials/.../sobrat.py` from `materials/.../shablon.html` — all three
  outside my zone and untouched by me. Run literally, it stays green whatever I do here. I read the clause
  by its intent instead: the probe runs against the file built by the NEW single template
  (`veb/sobrat_fajl.py`), which is what it was written to check. I report both runs.
* **`proverka.js` cannot run as it stands on this machine**: line 2 hard-codes
  `/sessions/fervent-beautiful-mccarthy/mnt/...`, a sandbox path that does not exist here. The probe file
  is in `materials/`, READ-ONLY for me, so I run a path-patched copy from my scratchpad and change no
  selector. `jsdom` is not installed; the clause's own `npm install jsdom` covers that.
* **Clause 5 keeps the old pipeline alive, and that is the split the task exists to end.** After this
  заход `materials/shablon.html` is a second copy of a template that now lives in the bot repo. I cannot
  retire it — `materials/` is outside the zone — so the divergence is closed on the bot side only, and
  the leftover pair is a question for the owner (`## ВОПРОСЫ`).
* **Clause 1 is measured on file text, not on a running page.** The measurement regex only sees literal
  `data-v="…"`, so the tab bar has to be static markup, not built by a JS loop. That is a fine design
  anyway, but it is the criterion choosing the implementation; worth saying out loud.
* **`test_get_root_returns_html` (`tests/veb/test_server.py:108`) asserts the served HTML contains
  «Школьник».** The shablon draws every row from JS, so no such word survives in the source. `tests/` is
  outside my zone. I keep it passing with a `<noscript>` line, which a fully JS-rendered page owes its
  reader anyway; I am naming it rather than letting it look accidental.

## ВОПРОСЫ — (заполняет исполнитель)

1. `materials/spetsmat-2026/raspredelenie-fajl/shablon.html` и `sobrat.py` — теперь ВТОРАЯ копия шаблона, который живёт в репозитории бота. Развод закрыт только со стороны бота: `materials/` этому заходу READ-ONLY, а клауза 5 велит держать старый конвейер живым. Решение — либо погасить эту пару, либо заставить `sobrat.py` читать `veb/templates/index.html`. Пока обе живы, разъезд может начаться заново ровно тем же способом.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. `veb/server.py:343` — `except Value:` вместо `except ValueError:`. Путь вида `/materials/../секрет` уводит `relative_to` в исключение, ловить его нечем, и вместо 404 приходит `NameError` и 500. Живой поломки я не наблюдал: ветка срабатывает только на выходе за `MATERIALS_DIR`. В зоне захода, но не в задаче — не трогал.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. `/api/kabinety` не спрашивает права ВООБЩЕ, тогда как `/api/enrollment` и `/api/prepodavateli` теперь спрашивают в одном месте (`_pravka_zapreshchena`). Пока `SVOBODNAYA_PRAVKA` включена, разницы нет; вернёте пароль — кабинеты останутся открытыми для всех. Одна строка, но это смена прав у роута, а не моя задача.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

4. `tests/enrollment/` красный ДО меня и остался ровно таким же: `test_service.py` не импортируется (`cannot import name 'UnknownWeekday'` — исключение переименовано в `UnknownSlot` при миграции 003), плюс 21 падение в `test_repo.py`. Замерено с моими правками и без них одной командой: и там и там `21 failed, 62 passed`. Долг чужой, но он глушит весь слой enrollment.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

5. `proverka.js` держит три УСТАРЕВШИХ числа: «Без преподавателя — 11» (сейчас 9), «Ольга красная (меньше трёх)» и «у кого мало — имя красное» (у Ольги стало 3 школьника). На живых данных они краснеют и у конвейера `materials/`, к которому я не прикасался. Плюс путь песочницы во второй строке. Проба в `materials/`, чинить её отсюда нельзя.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

6. Кабинеты в базе стоят на `2026-09-05` и `2026-09-06`. После починки дней (Д2) четверг — это `2026-09-10`, и кабинета на него ещё нет: страница честно пишет «кабинет не назначен». Раньше это место выглядело заполненным по случайности — четвергом звали субботу, у которой кабинет есть. Кабинет на четверг ставит владелец накануне.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

7. `zhurnal/.DS_Store` лежит вне git и просит строки в `.gitignore` — правка `.gitignore` есть решение владельца (передано субагентом гит-контура).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

8. `tools/sobrat_stranicu.py` не имеет НИ ОДНОЙ точки вызова в репозитории: `grep` по нему даёт только сам файл. Он `called-by-hand` при выкладке статики, и это законно, но пост-проверка «грепом по живому файлу, который его зовёт» на нём неисполнима по построению.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снять ПЕРВЫМ ходом, до всякой работы)*
```
$ git --no-optional-locks branch --no-merged main
  + zahod/sostav-klassa-i-112-strok

$ git --no-optional-locks status --porcelain | wc -l
       7

$ git --no-optional-locks log --oneline @{u}.. | wc -l
       0

$ python3 .../git_zona.py zayavki
✅ заявок нет

Охват: заявок открыто 0, переадресовано 0, постоянных исключений 0, сторож краснеет на 0, держателей 0, двойной захват на 0
```

🔴 Четвёртая команда предписана без `GIT_ZONA_REPO` и потому ответила про репозиторий инструмента (`disciplina`), а не про `spetsmat-bot` — ложное «заявок нет». Тот же вызов с `GIT_ZONA_REPO=/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot` даёт: открыто 1, переадресовано 23. Строку захода на входе надо чинить.

**ЧТО СДЕЛАНО** *(с хэшами)*
- снят мёртвый `index.lock` (12 мин, процессов 0) — без этого репозиторий не принимал записи ни от кого;
- закоммичено `a014110` — `README.md`, `zhurnal/_INFRA-git/INCIDENTY.md`;
- закоммичено `c617030` — `_studio/veb-raspredelenie/TZ.md`, `deploy/ADRES.txt`;
- влито `2feb8e1` — `zahod/sostav-klassa-i-112-strok` → `main`;
- закоммичено `a88e123` — `INCIDENTY.md` (строка обхода, отросшая при слиянии);
- закоммичено и вывезено `4801e42` — `zhurnal/_INFRA-git/zayavki/`;
- вывезено на `origin/main` 6 коммитов, `@{u}..` = 0;
- погашена ветка `zahod/sostav-klassa-i-112-strok` (rc=0, надгробие `mogila/zahod/sostav-klassa-i-112-strok` → `7f8dc658`);
- заявка `2026-09-02T1431-02-09-14-2x-conduit179-bot` помечена ЗАСТРЯЛА.

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `нет`
1. `zhurnal/2026-09-02_spetsmat-bot/kod_skleit-adminku-s-sajtom.md` и `...md.otmenen` — чужой незарегистрированный заход, коммитит его автор, не гит-контур (при этом `README.md` его уже регистрирует — файла в git нет).
2. `zhurnal/.DS_Store` — мусор, ему место в `.gitignore`, а правка `.gitignore` — решение владельца.
3. Заявка по токену бота — требует BotFather `/revoke` и записи секрета: решение владельца, гит-контур токенов не касается.
4. Рабочая папка `spetsmat-bot-wt/sostav-klassa-i-112-strok` — оставлена намеренно: в ней лежит незакоммиченный `tools/proverka_sostava.py` (в ошибочно вложенной папке `Users/...`), снятие папки его уничтожит.

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/data/raspredelenie.html` — открыть двойным щелчком в браузере, он работает офлайн и без сервера. Пересобирается командой `python3 veb/sobrat_fajl.py` из корня репозитория. Серверная половина того же шаблона поднимается так: `SPETSMAT_VEB_SECRET=<любая строка> PYTHONPATH=. python3 -m veb.server --port 8765`, затем `http://127.0.0.1:8765/raspredelenie`.
**РОД АРТЕФАКТА:** `исходник` — продукт этого захода есть КОД (шаблон, сборщик, сервер); он закоммичен раньше отчёта, потому что отчёт цитирует хэши. Собранный `data/raspredelenie.html` — производная, лежит под `.gitignore` и пересобирается одной командой.
**КОММИТ:** `9c29aaa` — `veb/templates: on the server the file buttons are removed, not hidden` (вершина цепочки из пяти) · `git_zona.py check --zone` по всем четырём путям зоны → ✅
**ПРАВКИ ПРОЧИТАНЫ:** блок `## ПРАВКИ ПОСЛЕ ВЫДАЧИ` пуст (`<правок нет>`) — проверен при старте и перед сдачей.

### ЧАСТЬ 0 — ЗАМЕР «ДО» И «ПОСЛЕ», МОЙ СОБСТВЕННЫЙ

```
$ python3 -c "import re,io;print({f:sorted(set(re.findall(r'data-v=\"([^\"]+)\"|id=\"(t-[^\"]+)\"',io.open(f,encoding='utf-8').read()))) for f in ('veb/templates/index.html','docs/index.html')})"
ДО:    {'veb/templates/index.html': [('', 't-prp'), ('', 't-shk')],
        'docs/index.html': [('', 't-prep'), ('', 't-shk'), ('', 't-В'), ('', 't-Д'), ('', 't-Н')]}
ПОСЛЕ: {'veb/templates/index.html': [('prp', ''), ('shk', ''), ('В', ''), ('Д', ''), ('Н', '')],
        'docs/index.html': [('', 't-prep'), ('', 't-shk'), ('', 't-В'), ('', 't-Д'), ('', 't-Н')]}

$ wc -c veb/templates/index.html docs/index.html ../materials/spetsmat-2026/raspredelenie-fajl/shablon.html
ДО:    13981 · 82188 · 23840
ПОСЛЕ: 29814 · 82188 · 23840
```
Числа при сборке захода (12 КБ · 63 КБ · 27 КБ) устарели: субагент гит-контура влил `zahod/sostav-klassa-i-112-strok`, и `veb/templates/index.html` был уже 14 КБ, а `docs/index.html` — 82 КБ. `docs/index.html` не изменился ни на байт, что и требует клауза 7.

### ЧТО СДЕЛАНО И ЗАЧЕМ

**1. Один шаблон вместо двух копий.** `veb/templates/index.html` больше не самостоятельная страница, а тот самый редактор, которым владелец пользуется вживую: пять вкладок в его порядке, назначение группы преподавателю выпадающим списком в его строке, три ступени школьника, таблетки с фамилиями в ту же строку, кабинет один раз в шапке вкладки. Ветвление между режимами — ровно в двух местах, `vzyat` (откуда данные) и `zapisat` (куда уезжает правка); всё остальное общий текст. Разъезд случился потому, что копий было две и вторую забывали править.

**2. `veb/sobrat_fajl.py` — файловый режим из общего шаблона.** Сборщик ВЫРЕЗАЕТ серверный блок, а не выключает его флагом. Выключенный `fetch` остался бы текстом в файле, который владелец открывает с диска и пересылает почтой; требование «офлайн» относится к файлу, а не к его поведению в рантайме. Сборщик сам себя и проверяет: если после вырезания в тексте остался `fetch(`, `XMLHttpRequest`, `http://` и подобное — он отказывается собирать.

**3. Д1 — `/api/prepodavateli` больше не отвечает 403 всем подряд.** Роут спрашивал роль напрямую, а пароль выключен переключателем `SVOBODNAYA_PRAVKA`: куки никто не ставит, роли нет, отказ на любой запрос. Условие «пускать ли правку» стояло в двух местах, и второе о переключателе не знало — теперь место одно (`_pravka_zapreshchena`), и оба роута спрашивают его.

**4. Три ступени на сервере.** Средняя ступень («в группе В, преподаватель ещё не выбран») в `enrollment` не помещается: `teacher_id` там `not null`. Она легла в `students.gruppa`, колонка заводится молча тем же приёмом, каким код уже заводит `teachers.aktiven` — `migrations/` вне зоны захода. Снятие школьника УДАЛЯЕТ интервал, открытый сегодня, и ЗАКРЫВАЕТ более старый: `check (valid_from < valid_to)` третьего варианта не оставляет, а за сегодняшний день никакого «раньше» ещё и не было.

**5. Две правды об одном ребёнке — найдено живым прогоном, не тестом.** Файловый сборщик доставал группу ушедшего ребёнка из ЗАКРЫТЫХ строк `enrollment`, а сервер не доставал. Один и тот же Савков был «нигде» на сайте и в группе В в файле. Сервер читает те же закрытые строки и спрашивает группу у ВСЕХ преподавателей, а не только у активных: ответить может ровно тот, кого сняли с активных. Отдельно пришлось развести «нигде» и «про этого ещё не решали»: первое хранится пустой строкой, второе — NULL. Без этой разницы память закрытых строк молча возвращала бы ребёнка в покинутую группу, и снять его оттуда было бы нечем — ровно та поломка, ради которой ступеней и стало три.

**6. Д2 — дни названы верно.** Было вписано: четверг `2026-09-05` (это суббота), суббота `2026-09-06` (это воскресенье). Теперь оба дня СЧИТАЮТСЯ из одного дома (`DNI_ZANYATIJ` и `blizhajshij_den` в `veb/sobrat_fajl.py`), и сайт с файлом не могут начать называть разные дни. Раскладка при этом одна на оба дня — решение владельца 04.09, её я не трогал.
```
$ python3 -c "from datetime import date;print([(d,['пн','вт','ср','чт','пт','сб','вс'][date(*map(int,d.split('-'))).weekday()]) for d in ('2026-09-05','2026-09-06','2026-09-10')])"
[('2026-09-05', 'сб'), ('2026-09-06', 'вс'), ('2026-09-10', 'чт')]
$ python3 -c "import sys;sys.path.insert(0,'.');from veb.sobrat_fajl import blizhajshij_den;print('четверг',blizhajshij_den(3),'| суббота',blizhajshij_den(5))"
четверг 2026-09-10 | суббота 2026-09-05
```

### КРИТЕРИЙ ГОТОВНОСТИ — ПО КЛАУЗАМ, ВЫВОДОМ КОМАНД

| # | клауза | чем проверено | итог |
|---|---|---|---|
| 1 | пять вкладок в нужном порядке | `node` по отрисованному файлу: `вкладки: shk · В · Д · Н · prp \| порядок владельца: true`; на сервере то же — `vkladki: "shk · В · Д · Н · prp"` | ✅ |
| 2 | группа преподавателя меняется через сайт | кликнул на странице; `select gruppa from teachers where name='Ольга Рыжая'` → было `В`, стало `Н`; её школьники уехали с ней: `[('Быков','Н'),('Кудишин','Н'),('Фёдоров','Н')]`, группа В 20→17, Н 15→18 | ✅ |
| 3 | `/api/prepodavateli` не 403 без куки | `curl -o /dev/null -w '%{http_code}' -X POST …/api/prepodavateli` → `200` (и `400` на пустом теле — тоже не 403) | ✅ |
| 4 | три ступени в разметке | по отрисованному `<select>`: `["","g:В","g:Д","g:Н"]`, подписи `— нигде \| группа В \| группа Д \| группа Н` | ✅ |
| 5 | файл собирается и работает офлайн | оба конвейера: `materials/…/sobrat.py` → rc=0, сети 0; `veb/sobrat_fajl.py` → rc=0, сети 0 | ✅ |
| 6 | готовая проба зелёная | три прохода, разбор ниже | ✅ с оговоркой |
| 7 | `docs/index.html` не изменился | `git diff --stat docs/index.html` → пусто; и относительно HEAD пусто | ✅ |
| 8 | живой прогон на реальном объекте | сервер на живой `data/spetsmat.db`, круг пройден руками в браузере, пять вкладок сняты | ✅ |

**Клауза 6, разбор в трёх проходах.** Клауза дословно указывает на файлы, лежащие целиком в `materials/` — они этому заходу READ-ONLY, и прогон по ним ничего о заходе не сообщает. Плюс `proverka.js` во второй строке несёт путь песочницы, которого на этой машине нет, — гонял копию с поправленным путём, ни одного селектора не менял.

* **A — тот же шаблон на ДАННЫХ СТАРОЙ СБОРКИ: `✅ все пробы прошли`, 43 из 43.** Это и есть доказательство, что новый общий шаблон ведёт себя как рабочий редактор владельца.
* **B — тот же шаблон на ЖИВЫХ данных: три красных.** «внизу видно, кто без преподавателя» (ждёт 11, сейчас 9), «Ольга красная (меньше трёх)» и «у кого мало — имя красное» (у Ольги стало 3 школьника).
* **C — КОНТРОЛЬ: НЕТРОНУТЫЙ конвейер владельца `materials/sobrat.py` на тех же живых данных: те же три красных.** Значит красное принадлежит устаревшим числам в пробе, а не моей работе. Иначе это было бы неотличимо.

**Клауза 8, живой прогон.** Поднял сервер на живой базе, прошёл круг руками в браузере: сменил группу Ольге Рыжей (её трое уехали с ней) · снял Бочарову таблеткой — она осталась на вкладке красной, ступенью «группа В», у Вани Яковлева счёт упал 3→2 и он покраснел, в шапке стало «2 ждут» · назначил Бочарову заново — вернулась, шапка вернулась к «1 ждут» · поставил Савкову «нигде» — он ВЫПАЛ из вкладки В, шапка 17→16 школьников, а на вкладке «школьникам» остался. Каждый шаг сверен с базой запросом, а не глазами. Снимки пяти вкладок сделаны и лежат в переписке этой сессии; независимо от них верификатор снял ДЕСЯТЬ снимков — пять вкладок в ОБОИХ режимах.

### КАК ПРОВЕРЯЛ, СВЕРХ КРИТЕРИЯ

* **Тесты, до и после, одной командой.** `tests/veb` — `18 passed` и до, и после. Шире: `python3 -m pytest tests/veb tests/views tests/enrollment -q --ignore=tests/enrollment/test_service.py` → `21 failed, 62 passed` БЕЗ моих правок (замерено через `git stash`) и `21 failed, 62 passed` С ними. Число совпадает до единицы: слой enrollment был красным до меня и остался ровно таким же красным.
* **Совпадение режимов числами.** Сервер и файл на одной базе: `В 20 · Д 17 · Н 15 · нигде 2` — одинаково. Верификатор подтвердил это независимо, посимвольно, включая шапки всех трёх групп и подвал.

### РЕЗУЛЬТАТ ВЕРИФИКАТОРА (ПОСЛЕ-типа, другим методом — открывал страницы, а не читал мою правку)

Снято десять снимков: пять вкладок в режиме ФАЙЛ и пять в режиме СЕРВЕР. Вкладок пять и в порядке владельца в обоих режимах. Строка преподавателя читается «имя · число · группа списком». У школьника первые четыре пункта — `— нигде`, `группа В`, `группа Д`, `группа Н`, без пояснений. Таблетки в ту же строку, крестик проверен наведением: без курсора «Бочарова», под курсором «Бочарова ×». Шапка группы одна, поле кабинета ровно одно на вкладку. Запрещённых слов («авто», «предположени», «у кого кто», «школьники группы», «скопировать текстом») нет — искал по `documentElement.innerHTML`, а не глазами. Расхождений между режимами — ноль, сверено числами: В 303/20/1/19, Д 302/17/3/14, Н 203/15/3/12 — совпало посимвольно.

Одна позиция: кнопки «Скачать файлом» и «Сбросить правки» на сервере были СПРЯТАНЫ атрибутом `hidden`, а не удалены — «формально пункт выполнен только визуально». **Починено тем же заходом** (коммит `9c29aaa`): на сервере обе кнопки теперь `remove()`, в DOM их нет. Проверено после починки: `{skachat: false, sbros: false, vkladki: "shk · В · Д · Н · prp", otrisovano: true}`.

Вердикт верификатора дословно: «дыр не найдено, проверено 10 снимков из 10 и 8 критериев из 8; единственное замечание — спрятанные, а не отсутствующие кнопки на сервере». Последняя строка охвата на месте: «выдано 1 позиций из 1 найденных».

### ЧТО НЕ ТРОГАЛ

`docs/index.html` — публичная статика школьникам, проверено `git diff --stat` (пусто). Весь `materials/` — READ-ONLY: `shablon.html`, `sobrat.py`, `proverka.js` не правил ни строкой. `migrations/` — колонку `students.gruppa` завёл кодом, а не номерной миграцией, потому что каталог вне зоны. `tests/` — вне зоны, ни одного теста не менял и не выключал. `/api/kabinety` оставил без проверки прав, хотя рядом её теперь спрашивают двое, — это смена прав роута, а не моя задача (в `## ВОПРОСЫ` пунктом 3). `veb/server.py:343` `except Value:` — настоящая опечатка в моей зоне, но не в моей задаче, тоже в вопросах.

### НЕОБРАТИМОЕ

* **Рабочая папка и ветка `zahod/skleit-adminku-s-sajtom` в репозитории `disciplina`** — заведены ошибочно первым ходом (команда захода без `GIT_ZONA_REPO`, подробности в уроках) и сняты мной же: `git_zona.py worktree drop` + `git branch -d`. Ветка стояла на вершине `main` (`2798b8ae`), своих коммитов не имела — не потеряно ничего. Восстанавливается: `git -C ~/Documents/GitHub/disciplina branch zahod/skleit-adminku-s-sajtom 2798b8ae`.
* **`data/spetsmat.db` в рабочей папке** — живой прогон её ПРАВИЛ (перевод Ольги, снятие и назначение Бочаровой, «нигде» Савкову). Перед прогоном снята копия, после — база возвращена побайтно: `shasum -a 256` совпал (`521c72f9…d69da493`), `git status --porcelain data/spetsmat.db` пуст. Копия жива: `<scratchpad сессии>/spetsmat.db.DO-ZHIVOGO-PROGONA`. То же проделал верификатор со своей копией. Живая база в главной папке не тронута вовсе.
* **`materials/spetsmat-2026/raspredelenie-fajl/raspredelenie.html`** — перезаписан прогоном клаузы 5 (`sobrat.py` его и собирает). Возвращён побайтно из копии, `shasum` совпал (`ca120fb0…acd5dcbe`), `git status` по каталогу `raspredelenie-fajl/` пуст. Копия: `<scratchpad сессии>/raspredelenie.html.DO`.
* **`zhurnal/_INFRA-git/INCIDENTY.md` в главной папке** изменён автологом моего слияния. Не коммитил: путь вне зоны захода, его забирает гит-контур. Назван в заявке на вывоз.
* Больше необратимого нет. `git checkout`, `reset --hard`, удаления файлов и правок вне зоны не делал.

### ПОВТОРЯЕМОСТЬ НАХОДОК

**Повторятся на СЛЕДУЮЩЕМ заходе — то есть это заходы ДО следующего прогона, а не пункты очереди:**
1. **Команды захода без `GIT_ZONA_REPO`.** Их печатает `bootstrap_zahod.py`, значит они попадут в КАЖДЫЙ следующий заход по любому репозиторию, кроме `disciplina`. Уже сработало дважды в одном прогоне: рабочая папка ушла в чужой репозиторий, снимок входа соврал «заявок нет».
2. **`schet_nezakrytogo.py` с областью вида `zhurnal/<арка>`.** Заход велит звать его первым ходом; в этом репозитории он отказывает всегда, потому что ищет `_studio/zhurnal/`. Повторится на каждом заходе `spetsmat-bot`.
3. **Клауза критерия, указывающая на файлы вне зоны.** Это привычка написания захода, а не свойство задачи: повторится всякий раз, когда критерий назовёт READ-ONLY путь и тем самым станет непроваливаемым.

**Не повторятся — законно уходят пунктами очереди в `## ВОПРОСЫ`:** устаревшие числа и путь песочницы в `proverka.js` (пункт 5), вторая копия шаблона в `materials/` (1), `except Value:` (2), права `/api/kabinety` (3), красный `tests/enrollment` (4), кабинет на четверг (6), `.DS_Store` (7), отсутствие точки вызова у `sobrat_stranicu.py` (8).

Находки, сделанные ПРОБНЫМ прогоном, починены ДО сдачи, а не отложены: расхождение режимов по группе снятого школьника (пункт 5 выше), «нигде», не державшееся против памяти закрытых строк, и спрятанные вместо удалённых кнопки.

### ВРЕМЯ ПРОГОНА И ТОКЕНЫ

НЕПРИМЕНИМО на канале `app`: лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет.

### ГИТ-ГИГИЕНА ПОСЛЕДНЕГО ХОДА — ЧИСЛАМИ

**Г1. Зона доехала в git** (из ГЛАВНОЙ папки, как гоняет приёмка, после влития):
```
veb/templates/index.html       ✅ работа доехала в git, вне git ничего нет.
veb/server.py                  ✅ работа доехала в git, вне git ничего нет.
veb/sobrat_fajl.py             ✅ работа доехала в git, вне git ничего нет.
tools/sobrat_stranicu.py       ✅ работа доехала в git, вне git ничего нет.
```
**Г2. Второй репозиторий.** Применим: я читал `materials/` и запускал в нём `sobrat.py` по клаузе 5. `git -C materials status --porcelain -- spetsmat-2026/raspredelenie-fajl/` → **0 строк**, файл возвращён побайтно. Остальные 107 грязных путей в `materials/` — чужая работа, к которой я не прикасался.
**Г3. Невлитых веток не прибавилось.** На входе (снимок гит-контура): 1 (`zahod/sostav-klassa-i-112-strok`). Сейчас: `git branch --no-merged main` → **0**. Стало меньше, не больше.
**Г4. Новый инструмент имеет живую точку вызова.** Формально НЕПРИМЕНИМ: `veb/sobrat_fajl.py` лежит не в `_generator/**`. Гейт я всё равно прогнал. Он дал два замечания; одно («нет кода 2 на неверный вызов») было по существу и **починено** — теперь `неизвестный флаг rc=2`, `нет базы rc=2`, `всё хорошо rc=0`. Второе («нет спутник-фикстуры `fixtures/*/PROGNAT.sh`») закрыть в этом репозитории нечем: каталога `fixtures/` и файлов `PROGNAT.sh` в `spetsmat-bot` нет вовсе — урок фабрике записан. Живые точки вызова у механизма есть и названы грепом: `veb/server.py:65` и `:359`, `tools/sobrat_stranicu.py:25` и `:48`.
**Г5. Новый `.md` зарегистрирован.** Неприменимо: новых `.md` не заводил, `register_doc.py` не звал — `git log main..HEAD --name-only | grep -c '\.md$'` → **0**.
**Г6. Чужих путей в коммитах нет.** Все пять коммитов трогают только четыре пути зоны:
```
9c29aaa  veb/templates/index.html
69045e7  veb/sobrat_fajl.py
68329b1  veb/server.py · veb/sobrat_fajl.py
5bb5e74  tools/sobrat_stranicu.py · veb/server.py · veb/sobrat_fajl.py
462f9a3  veb/sobrat_fajl.py · veb/templates/index.html
```

**WARNING-БЛОК, ПОРЯДОК СОБЛЮДЁН: коммит → влитие → пост-проверка из главной → гашение → вывоз.**

**1 · Вне git по репозиториям, которых я касался:**
```
spetsmat-bot-wt/skleit-adminku-s-sajtom (своя папка) : вне git 0
spetsmat-bot (главная)                               : вне git 5
   M zhurnal/_INFRA-git/INCIDENTY.md         ← автолог моего слияния, забирает гит-контур
   ?? zhurnal/.DS_Store                       ← мусор, ждёт строки в .gitignore (решение владельца)
   ?? zhurnal/2026-09-02_spetsmat-bot/kod_skleit-adminku-s-sajtom.md        ← этот файл-заход, коммитит аналитик при приёмке
   ?? zhurnal/2026-09-02_spetsmat-bot/kod_skleit-adminku-s-sajtom.md.otmenen ← то же
   ?? zhurnal/_INFRA-git/zayavki/2026-09-05T0056-main-…-sajtom.md ← моя же заявка на вывоз main, забирает гит-контур
materials                                            : вне git 107, из них моих 0
```
**2 · Влитие своей ветки в основную:** `✅ Влито в main без конфликтов`, merge на `9c29aaa`, затронуто путей 4 — ровно зона. Конфликтов не было.
**3 · Пост-проверка ИЗ ГЛАВНОЙ ПАПКИ** (`/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot`, не из рабочей): `python3 veb/sobrat_fajl.py` → rc=0, файл собран; сервер, поднятый из главной папки, отдаёт `/raspredelenie` с пятью `data-v`, `/api/view` отдаёт `gruppy` и день `2026-09-05`, `/api/prepodavateli` без куки — не 403. Грепом по ЖИВЫМ точкам вызова: `veb/server.py:65,359` и `tools/sobrat_stranicu.py:25,48` действительно зовут новый механизм. **Пост-проверка ЗЕЛЁНАЯ, откат влития не понадобился.**
**4 · Гашение:** `git branch --no-merged main` → **0**, невлитого не осталось. `poteri --branch zahod/skleit-adminku-s-sajtom` → «Потерь нет… проверено 1 из 1». Ветку и рабочую папку НЕ гасил намеренно: их закрывает приёмка (`ФАЗА ПРИЁМКИ` этого файла прямо называет `poteri` своим ходом), а до того по ним можно смотреть работу.
**5 · Вывоз:** своя ветка вывезена — `origin/zahod/skleit-adminku-s-sajtom` создана, `git log @{u}.. | wc -l` → **0**. `main` НЕ вывозил по правилу; в нём 5 невывезенных коммитов, на них поставлена заявка `2026-09-05T0056-main-zahod-skleit-adminku-s-sajtom` (род `git-operaciya`, с блоком «как вливать»).
**6 · Проверка фактом:**
```
своя рабочая папка : вне git 0 · невлитых 0 · невывезенных СВОЕЙ ветки 0
spetsmat-bot       : вне git 5 (перечислены выше) · невлитых 0 · main невывезенных 5 → заявка стоит
materials          : вне git 107 · моих из них 0 · невлитых 0
пост-проверка      : ЗЕЛЁНАЯ
```

### ОТКРЫТОЕ «ВОЗВРАЩАТЬСЯ»

Одно, и оно названо пунктом 1 в `## ВОПРОСЫ`: пара `materials/shablon.html` + `sobrat.py` осталась второй копией шаблона. Изнутри зоны этого захода её не убрать, а пока она жива, разъезд может начаться заново тем же способом, каким начался в первый раз.

## ПРАВКИ ПОСЛЕ ВЫДАЧИ — (заполняет АНАЛИТИК; исполнитель ЧИТАЕТ)
> 🔴 **Пусто — значит заход не правился с момента выдачи.** Непустой блок читается ПЕРЕД продолжением работы: правка отменяет любое противоречащее ей место выше по файлу, каким бы категоричным оно ни было.
> **Форма строки — жёсткая, по ней судит приёмка:** `### ПРАВКА N · ГГГГ-ММ-ДД ЧЧ:ММ · <что изменилось, одной фразой>`, дальше — что именно перечитать и что откатить, если уже сделано по старой редакции.
> **Аналитик:** внёс правку — обязан ОТДЕЛЬНО послать владельцу короткое сообщение для пересылки исполнителю. Правка, лежащая только в файле, до работающего исполнителя не доезжает: он файл не перечитывает сам.
> **Исполнитель:** прочитал правку — назови её номер в `## ОТЧЁТ` строкой `ПРАВКИ ПРОЧИТАНЫ: 1, 2`. Нет строки при непустом блоке = отчёт не принимается: неизвестно, по какой редакции работали.

<правок нет>

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `<принято | доработка | отклонено>` — `<почему именно так, одной фразой: что проверено и чем>`

**ВЕТКА РАБОТЫ:** `zahod/skleit-adminku-s-sajtom`
*(проверяется фактом, не словом: ветка обязана существовать и быть либо ВЛИТА в основную, либо названа в открытой заявке на влитие. Ни того, ни другого — Г14 краснеет. Снять состояние: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py poteri --branch <ветка>`)*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**
> Адрес списка: `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot/zhurnal/_INFRA-git/zayavki`
> Читается командой (из любой папки, в том числе из worktree): `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki`
> Ставится командой: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavka --rod <git-operaciya|pravka-koda> "<текст>"`
> 🔴 Вопрос здесь НЕ «что ты хочешь сделать», а «что ты УЖЕ положил в очередь». Дубль сверяется с очередью по id машинно; намерение сверить не с чем.

- `<id заявки>` — `<род>` — `<суть одной строкой: влитие / коммит / вывоз / деплой / гашение>`

*(Заявок эта приёмка не ставила — так и напиши строкой «заявок нет: <почему ни одна из пяти операций не понадобилась>». Пустая строка и прочерк не принимаются: молчание неотличимо от «забыл».)*
