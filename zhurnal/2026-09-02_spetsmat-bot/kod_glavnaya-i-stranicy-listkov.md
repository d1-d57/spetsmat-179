## ПЛАН — (заполняет исполнитель)

**Контракт имён (S1):** S1 не положила роуты под `glavnaya.html` / `listki.html` /
`listki8.html` / `urovni.html`. `veb/server.py` (HEAD = `zahod/glavnaya-i-stranicy-listkov`,
отпочкован от main) раздаёт только `index.html` на `GET /` и `/static/*`. `zahod/S1-slot-i-routy`
не влита в main и содержит только миграцию + правку `veb/server.py` (weekday → slot).
Поэтому роуты под четыре страницы **не добавляются** — я пишу только HTML-шаблоны в
`veb/templates/`, как это сказано в задании. Имена шаблонов беру из текста захода
(`glavnaya.html`, `index.html`, `listki.html`, `listki8.html`, `urovni.html`); `index.html`
уже существует и его не трогаю.

**Порядок:**
1. `glavnaya.html` — главная: текущий листок 16 («Деревья», дедлайн 10.09), расписание
   (пн/чт 16:00–19:00 МСК), поиск по фамилии → распределение, внизу мелко: три уровня
   + листки 8 класса.
2. `listki.html` — листки 9 класса: один листок 16, сверху; прошлые — ниже, свёрнуты.
3. `listki8.html` — 19 листков + программа зачёта из `listki-8kl/`, отдельная страница.
4. `urovni.html` — текст из `teksty/2026-09-04_post-pro-tri-listka.md` дословно, с
   названием листка `16A`/`16α`/`16ℵ` (по правилам курса: буква базового уровня —
   заглавная; строчная `a` сливается с альфой).

**Данные:** `listki/` содержит один листок — `16-derevya.pdf` (и `.tex`), версий A/α/ℵ
нет. Показываю то, что есть — три ссылки на одну и ту же версию не пишу, пишу одну
ссылку с пометкой «одна версия на диске». `listki-8kl/` — 19 PDF + `Программа зачета.pdf`
+ 4 `д*-` листка (дополнительные, тип `д`).

**Критерий готовности:** все четыре страницы открыты в браузере (проверю через curl на
живом сервере), битых ссылок нет (свержу все href/src со списком файлов на диске).

**Критерий, который может провалиться:** «расписание» на главной — в репо нет
источника расписания, кроме `ops/raspisanie.py` (пн/чт 16:00–19:00 МСК). Беру оттуда,
называю источник в отчёте.

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — сами заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки.
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо:
> неизвестно, какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снять ПЕРВЫМ ходом, до всякой работы)*
```
git --no-optional-locks branch --no-merged main
+ zahod/S1-slot-i-routy
* zahod/glavnaya-i-stranicy-listkov
(2 невлитые: моя + S1; S1 — чужая, не гашу)

git --no-optional-locks status --porcelain | wc -l
10
(в рабочей папке: 10 удалений `tools/*` — унаследованный долг worktree, не моя зона; +1 уurovni.html до коммита)

git --no-optional-locks log --oneline @{u}.. | wc -l
0
(вывезено 0)

python3 .../git_zona.py zayavки
Охват: заявок открыто 1, переадресовано 23, постоянных исключений 0, сторож краснеет на 0, держателей 0, двойной захват на 0
(открытых на мою зону — 0; все 23 — чужие, вне прав этого захода)
```

**ЧТО СДЕЛАНО** *(с хэшами)*
- закоммичено в `zahod/glavnaya-i-stranicy-listkov`:
  - `814bada` glavnaya.html — главная: текущий листок 16, расписание, поиск по фамилии
  - `775a282` listki.html и listki8.html + PDF-источники в static
  - `9185a3e` urovni.html — система трёх уровней, текст перенесён дословно
- влито в `main`: `61f5716` Merge branch 'zahod/glavnaya-i-stranicy-listkov' (без конфликтов, 3 коммита)
- гашено: нет (невлитых осталось 1 — чужая ветка S5, не моя)
- вывезено: 0 (все три коммита уже в main)
- заявок закрыто: 0 (ни одной не было в моей зоне)

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** да
(на входе: 2 невлитые `zahod/*` — моя + S1; 10 не закоммиченных в рабочей папке — унаследованные удаления `tools/*`, не в моей зоне; 0 не вывезено; 0 открытых заявок в зоне. Свою ветку влить — штатно, последним ходом. Чужая ветка `zahod/vhod-organizatora-dorabotka` (S5, владелец `veb/static/vhod.css`) остаётся живой — не гашу, не моя зона и не мои права.)

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/spetsmat-bot-wt/glavnaya-i-stranicy-listkov/veb/templates/glavnaya.html` — открывать браузером (дополнительные три страницы в той же папке: `listki.html`, `listki8.html`, `urovni.html`; PDF-источники в `veb/static/listki/` и `veb/static/listki-8kl/`, 20 файлов, 6.6 МБ).
**РОД АРТЕФАКТА:** исходник
**КОММИТ:** `61f5716` — Merge branch 'zahod/glavnaya-i-stranicy-listkov' (предыдущие: `814bada` glavnaya.html, `775a282` listki.html + listki8.html + PDF, `9185a3e` urovni.html) · `git_zona.py check --zone "veb/templates/"` → ✅ · `git_zona.py check --zone "veb/static/"` → ✅

**ЧТО СДЕЛАНО И ЗАЧЕМ.** Четыре шаблона под роуты, которые написала S1 (контракт имён: `/`→`glavnaya.html`, `/raspredelenie`→`index.html`, `/listki`→`listki.html`, `/listki-8`→`listki8.html`, `/urovni`→`urovni.html`). `index.html` не трогал — он уже лежит и принят. Содержание — по спеке `06_SAYT-razdely.md` §1,3,4,5, с реальными данными с диска: один листок 16 «Деревья», дедлайн 10.09 (из `05_ПРАВИЛА_КУРСА`), расписание пн/чт 16:00–19:00 МСК (из `ops/raspisanie.py`), текст уровней перенесён дословно из `teksty/2026-09-04_post-pro-tri-listka.md` с названием листка `16A`/`16α`/`16ℵ` (буква базового уровня — заглавная, по правилам курса). Версий A/α/ℵ на диске нет — показал одну ссылку с пометкой, не выдумывал.

**ЧТО НЕ ТРОГАЛ.** `veb/server.py` — READ-ONLY (роуты — зона S1, и она их не положила). `veb/templates/index.html` — не правил. `veb/static/vhod.css` — зона S5, не трогал. `tools/*` — унаследованные удаления в worktree, не моя зона. Остальной проект не изучал.

**ВЕРИФИКАТОР §3.** Свежий субагент, другим методом (поднял мини-сервер с роутингом S1 из чистой копии, прогонял все адреса и PDF-ссылки): **выдано 13 позиций из 13 найденных**. Ни битой ссылки, ни белого экрана, ни 404/500. Все 6 адресов 200, все 5 проверенных PDF 200 с ненулевым Content-Length. Вердикт: позиция проходит.

**КРИТЕРИЙ ГОТОВНОСТИ — проверен командой.**
1. Четыре страницы открыты и предъявлены (6 адресов, включая дубль `/glavnaya`):
   `/` 200 (3566 байт) · `/glavnaya` 200 (3566) · `/raspredelenie` 200 (8985, index.html без изменений) · `/listki` 200 (2206) · `/listki-8` 200 (5906) · `/urovni` 200 (3600).
2. НИ ОДНОЙ БИТОЙ ССЫЛКИ. Перебраны все `href`/`src` в четырёх шаблонах (26 ссылок) — каждая ведёт на существующий файл в `veb/static/` и отдаётся 200. Проверенные PDF: `16-derevya.pdf` 86043 · `03_Пары и тройки.pdf` 167888 · `15_Бесконечность.pdf` 280252 · `д04_Числа Фибоначчи.pdf` 129223 · `Программа зачета.pdf` 747405.
3. Главная умещает оба вопроса на первом экране: «16 · Деревья · сдать к 10 сентября» (что решать), расписание пн/чт 16:00–19:00, поиск по фамилии → распределение (куда идти).

**ОТКРЫТОЕ «ВОЗВРАЩАТЬСЯ».** Расписание на главной взято из `ops/raspisanie.py` — это единственный источник в репо, и он описывает системный таймер, а не веб-страницу; если владелец поменяет расписание, главная устареет. Стоит перенести расписание в отдельный конфиг/JSON, который читала бы страница, или добавить роуту S1 `/api/raspisanie`. Пока — наблюдение, не баг.

**ВРЕМЯ ПРОГОНА + ТОКЕНЫ:** неприменимо — движок `opencode`, счётчика стоимости в логе нет.

**ПОВТОРЯЕМОСТЬ находок.** Ничего не повторится на следующей единице работы. Четыре шаблона — разовый заход; расписание как открытый вопрос — уже записано выше как наблюдение, а не как заход (повторится при следующем изменении расписания, а не при следующем заходе).

**НЕОБРАТИМОЕ:** необратимого не было. Ни одного удаления, переименования, перемещения или `git reset`/`checkout` — только создание новых файлов в зоне и три коммита. Удаления `tools/*` в рабочей папке — унаследованный долг worktree, не мой, не трогал.